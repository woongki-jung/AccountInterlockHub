# IA: API-04 — 연동 규약 자가진단 API

> 본 IA 노드와 연관된 작업의 시계열 이력(최신순). 정책: ai/strategies/ia-history.md

| 일시 (KST) | 단계 | 산출물·결과 | 관련 일감 | 상태 |
|---|---|---|---|---|
| 2026-07-26 21:37 | build | `apps/backend/src/common/logging/redacting-console-logger.ts` — **P12 `#489` 회귀 2회차 [I-1] 시정**: 독립 재리뷰 지적("마스킹이 문자열 메시지만 검사해 프레임워크의 객체 로깅 경로가 우회") 해소. 재실측: `@nestjs/core/errors/exception-handler.js` `ExceptionHandler.handle()` → `this.logger.error(exception)`(`nest-factory.js` `handleInitializationError` 가 호출)가 **Error 객체를 인자 하나로만** 넘겨, `ConsoleLogger.getContextAndStackAndMessagesToPrint` 가 `errorStack` 을 분리하지 못해(`messages.length<=1` 분기) `printStackTrace()` 도 비켜 간다 — 문자열 전용 검사·스택 전용 검사 둘 다 우회. `isPathFormat`(`interlock-config.validators.ts`)이 `:`·`*`·`(` 같은 path-to-regexp 메타문자를 막지 않아, 그런 값이 오면 라우트 등록 예외의 message·stack 에 경로 원문이 실려 이 경로로 흐른다. ① `containsSecretValue` 를 비문자열까지 검사하도록 확장 — base(`ConsoleLogger.stringifyMessage`, `console-logger.service.js` 실측)와 **동일한** `this.inspectOptions`(protected 상속 필드, depth 5)로 `util.inspect` 해 검사(Error 는 `isPlainObject` 가 false 라 접두어 없이 그 결과를 그대로 쓰므로 base 와 1:1 일치 — `shared.utils.js` 실측). 순환 참조·거대 객체는 `util.inspect` 자체 방어(circular 표기·depth/길이 상한)로 안전, `inspect` 실패 시 안전을 낙관하지 않고 비밀 값 포함으로 판정. 함수 값은 base 처럼 호출하지 않음(검사가 부작용 있는 함수를 실행시키는 위험 회피 — `@nestjs/{core,common,platform-express,schedule}`·자체 `src` 전체 재grep 결과 함수 타입 로그 메시지 호출 자체가 없음을 확인). ② `printMessages` 의 `context`·`errorStack` 슬롯도 같은 판정 — 오염되면 그 슬롯만 `undefined` 로 치환하고 이미 안전 판정을 통과한 messages 는 유지(과잉 차단 금지). ③ 기존 76행의 "객체·함수 등은 FN-015 의 몫" 단언을 삭제 — 실측 결과 `sanitizeValue`(`common/security/sanitize-value.ts`)는 `error-envelope.ts`·`sanitize-response.interceptor.ts` **응답 경로에만** 배선돼 있고 로그 호출부에는 걸려 있지 않음을 확인해 정정. 회귀시키지 말아야 할 성질(메시지 배열 원소 단위 필터링·6개 레벨 커버·`String.includes` 만 사용·디코이 라우트 로그·빈 문자열 secret 제외·`main.ts` 조기 반환 계약) 전부 무변경. `nest build` 에러 0. 변경 파일은 이 한 파일뿐 | `accountinterlockhub#489`·`#469` | 🚧 |
| 2026-07-26 19:45 | spec ⓒ | (공통 반영) `process_PROC-204.md` `B4` 의 `FN-004` 호출을 **`{ payload }` 만 받도록** 명시(사양 결함 4). 자가진단은 판정만 하고 전달을 하지 않으며 복호화 원문은 응답에도 담지 않는다(`SEC-003-03`) — common.md | `accountinterlockhub#486`·`#469` | ℹ️ |
| 2026-07-26 19:40 | spec ⓒ | (공통 반영) `process_PROC-204.md` `:233` 자기모순 해소(E7-5 — 같은 파일 `:43` 이 이미 *요청 1건으로 경로 존재가 확인된다*를 확정했는데 *유일한 완화 장치가 사라진다*가 남아 있었다) · `tc_API-04.md` §검증 목적 자기모순 해소(같은 파일 신설 `API-04_016` 이 *존재가 드러나는 것은 수용된 성질*로 확정) + `API-04_010` 완화 장치 축 명시(W7-4 — 제품 전체 둘 / 이 접점 하나). **범위 밖 자기모순 동반 마감** — `policy_SEC-crypto.md` `SEC-001-09` 가 *허브 자가진단은 400 `EX-SEC-004`* 라 적었으나 `PROC-204`·`spec-functions-api-server.md`·`API-04_005` 와 build 구현이 전부 **200 부적합**(build 코드리뷰가 독립적으로 **결함 12** 등재). 자가진단은 판정 결과를 알려 주는 접점이라 부적합이 오류 응답일 수 없다 — common.md | `accountinterlockhub#486`·`#493`·`#489`·`#469` | ℹ️ |
| 2026-07-26 19:02 | build | `apps/backend/src/main.ts`(`NestFactory.create()` 3번째 인자로 커스텀 Logger 주입) + `apps/backend/src/common/logging/redacting-console-logger.ts`(신규) — **P12 `#489` 회귀 1회차 [C-1] 시정**: 독립 코드리뷰가 실측한 "Nest 프레임워크의 `RouterExplorer` 가 기동 시 `Mapped {<SELFCHECK_PATH>, POST} route` 를 stdout 에 **자동** 출력"(코드에 로그 호출이 없어 정적 읽기로 불검출 — `process_PROC-204.md` §보안 요구 "경로 값을 응답·오류 메시지·로그에 담지 않는다" `SEC-003-02` · `policy_OPS.md` `OPS-003-03` 위반)을 시정했다. `NestFactory.create()` 의 `logger` 옵션에 `RedactingConsoleLogger`(신규, `ConsoleLogger` 상속 — Nest v9+ 는 `extends Logger` 를 금지한다, `Logger.overrideLogger()` 실측 확인)를 주입 — 이 인스턴스가 전역 `Logger.staticInstanceRef` 로 등록되어(`@nestjs/common/services/logger.service.js` 실측) 애플리케이션 전체의 `new Logger(context)` 호출이 결국 이 인스턴스로 위임된다. `ConsoleLogger` 의 `log`·`warn`·`debug`·`verbose`·`fatal`·`error` 여섯 메서드가 전부 거치는 공통 출력 지점인 `printMessages`/`printStackTrace`(`console-logger.service.js` 실측)를 오버라이드해 **주입된 `selfcheckPath` 값이 문자열로 실제 등장하는 메시지만** 통째로 버린다(부분 마스킹 없음 — `sanitize-value.ts` 와 같은 태도, 같은 로그 호출의 다른 메시지는 보존). 전역 `logger:false` 는 다른 기동 로그까지 함께 잃어 채택하지 않았다. `selfcheck.controller.ts` 상단 주석이 "로그·응답·오류 메시지 어디에도 담지 않는다"고 단언하며 실측과 반대였던 부분을 "로그는 예외였다(프레임워크 자동 기록)"로 정정하고 새 방어 지점을 인용했다. **[S-2]** `protocol-conformance.service.ts` 의 `reasonCode: MappedExCode \| null`(카탈로그 12종 전체)을 실제로 채워질 수 있는 4종(`EX-SEC-001`·`EX-AUTH-002`·`EX-SEC-002`·`EX-SEC-004`, `process_PROC-204.md` §분기 및 예외 흐름과 일치)으로 `Extract<MappedExCode, ...>` 좁혀, `EX-OPS-002` 등 다른 카탈로그 코드의 오대입을 컴파일 타임에 막았다. **실측 검증**(격리 워크트리 `wt-p12-fix`, 지어낸 가짜 `SELFCHECK_PATH`·가짜 DB 접속정보 — `apps/backend/.env` 미열람·`accountinterlockhub` 무접촉): ① `nest build` 에러 0 ② **기동 계약 회귀**(P01 `#478` 소유 `main.ts`) — 필수 상수 8종 전부 미설정 시 기존과 동일하게 `[PROC-901] 기동 중단 — ...` + `exitCode=1`, Nest 로그 자체가 뜨지 않음(내 로거 구성 코드가 그 이전 조기 반환보다 뒤에 있어 무관) — **무변경 확인** ③ 정상 기동 시 `RouterExplorer` 가 등록 라우트 6종(진입·verify·approve·status·completion·callback) 로그는 **전부 정상 출력**(디코이) 되고 자가진단 라우트 로그 1건만 **정확히 부재** — stdout+stderr 전체를 가짜 경로 문자열로 grep = **0건** ④ 자가진단 API 기능 회귀 무변경 — `apps/sender-sdk/protocol-test-vectors.json` `VEC-002-KEY-EXACT` 재사용 실호출로 200 `{isConform:true,reasonCode:null,...}`(적합), 같은 encX·encY 에 틀린 birthDate(`VEC-001` 값)로 200 `{isConform:false,reasonCode:"EX-AUTH-002"}`(부적합, EX 원값 그대로), 빈 입력 400 `EX-SEC-003`, `GET` 메서드 불일치 404(본문 없음) — 전건 사양 기대값과 일치. **[추가 발견 — 동일 세션 중 다른 에이전트가 제보, 직접 재실측으로 독립 확인]** `GlobalExceptionFilter.describeForLog()`(P05 `#482` 소유, **본 커밋에서 손대지 않음**)가 500 응답 시 `${request.method} ${request.path} -> ...` 로 요청 경로를 그대로 로그에 담는 **2번째 유출 지점**을 제보받아, 저장소 소스를 고치지 않고 `--require` 프리로드 훅으로 `RegExp.prototype.test` 를 `birth-date.validator.ts` 의 정규식(`source === '^\\d{6}$'`)·전용 sentinel 값 조합에만 좁게 몽키패치해 `validateBirthDateFormat()` 내부에서 분류되지 않은 예외(500 `EX-OPS-002`)를 유도하는 방식으로 자가진단 경로·디코이 경로(`/verify`) 양쪽에 동일 결함을 재현했다. **실측 결과: 자가진단 경로의 500 로그 2건 전부 완전 부재(가짜 경로 문자열 stdout+stderr 전체에서 0건), 디코이 경로(`/verify`)의 500 로그 2건은 전부 정상 출력**(`ERROR [GlobalExceptionFilter] POST /entry-fake-p12/verify -> 500 (Error)`) — `GlobalExceptionFilter` 도 `new Logger('GlobalExceptionFilter')` 로 같은 전역 `Logger.staticInstanceRef` 를 거치므로 **`main.ts` 의 로거 주입 하나로 이 2번째 지점까지 이미 해소되어 있음을 확인**했다(추가 코드 변경 불요 — `GlobalExceptionFilter.ts` 는 실제로 무변경, `관련 일감` 에 `#482` 를 넣지 않은 이유). 검증 후 워크트리·정션·프로세스·포트 3420 잔존 0 실측 확인 | `accountinterlockhub#489`·`#478`·`#469` | 🚧 |
| 2026-07-26 19:03 | build | `history/API-04.md` **정정(append-only)** — 바로 아래 2026-07-26 18:25 행(P12 최초 구현, `b3128b1`) 말미의 "`app.module.ts` 배선 ... 동시 작업 중이던 spec 레그 doer 의 커밋(`93e5157`)에 함께 실렸다 ... 본 build 커밋에는 그 파일이 없다(이미 반영돼 스테이징할 diff가 없음)"라는 서술이 **둘 다 사실이 아니다**. 실측: ① `git show --stat b3128b1` → `apps/backend/src/app.module.ts \| 4 ++` **포함**(그 파일이 실제로 이 커밋 안에 있다) ② `git log --oneline -- apps/backend/src/app.module.ts` 최신 커밋 = `b3128b1`(이후로도 그 파일을 마지막으로 건드린 커밋) ③ `git merge-base --is-ancestor 93e5157 HEAD` → **아니다**(exit 1) — `93e5157` 은 커밋 객체로는 존재하나(`git cat-file -t` = commit) 현재 브랜치 이력의 조상이 **아니다**(spec 레그가 이후 amend 로 그 SHA 를 브랜치 이력에서 밀어낸 것으로 보인다). **기존 행 원문은 append-only 원칙에 따라 고치지 않고 이 행으로 정정한다** — 정정 목적은 **후행 QA 가 그 행만 보고 "app.module.ts 배선이 이 build 범위 밖(다른 커밋)에서 처리됐다"고 오판해 배선 자체를 재검증 없이 넘기지 않도록 하는 것**이다(실제로는 `b3128b1` 자신이 4행 diff 로 배선을 담고 있어 배선 자체는 정상 반영됐고, 그 반영 지점을 서술한 문장만 틀렸다) | `accountinterlockhub#489`·`#469` | 🚧 |
| 2026-07-26 18:48 | spec | `docs/specs/qa/API/tc_API-04.md` — 신설 `OPS-002-06`(`f4e515b`) 반영 + **`API-04_016` 신설**. 신설 TC 는 **`POST` + 해석 불가 본문**에서 실경로가 400 `EX-SEC-003`(`SEC-003-04` 가 명시 요구), 디코이 경로가 **일반 404** 로 갈려 **요청 1건으로 경로 존재가 확인되는 것**을 고정한다 — 이것은 **결함이 아니라 `OPS-002-06` 상 수용된 성질**이며, 진단 응답을 일반 404 로 바꾸는 선택지는 `EXC-OPS-10` 으로 **명시 기각**됐다(자가진단이 주는 능력은 공개된 본인확인 재입력에서 이미 얻어져 진단성을 버려도 대가로 보호되는 것이 없다). 판정 규약을 뒤집어 적었다 — **"구분된다"가 Pass, "구분되지 않게 바꿔 놓은 것"이 Fail**. 기존 `API-04_010` 판정·`API-04_011` ④ 의 "완화 장치가 경로 비공개 하나뿐"이라는 서술은 **효력 범위 한정**으로 정합화했다(경로 **값의 추측 난이도**에만 기대는 장치이고 **반복 시도를 막는 완화가 아니다** — 경로를 이미 아는 요청 앞에서는 효력 0). TC 15→16 | `accountinterlockhub#469` | 🚧 |
| 2026-07-26 18:48 | spec ⓒ | (공통 반영) `docs/specs/qa/` 카탈로그 3종 정합 — `QA-OPEN-02` 해소 표기·본문 오기 정정 · `CHK-FOCUSEMU`·`CHK-PGKILL`·`SEED-PROOF-STALE` 신설 · `SCEN_024`~`_026` 신설 · 집계 전면 재실측(TC 242→247·정책 17→18건·규칙 88→93). 이 노드분은 `spec-qa.md` §수용 리스크의 취급에 **`API-04_016` 등재 + `OPS-002-06` 취급 규약**(구분된다=Pass / 구분되지 않게 바꿔 놓은 것=Fail)으로, `spec-qa-mapping.md` 에는 `SVC-013` 15→16·`BR-014` 6→7·`EX-SEC-003` 2→3·접점 표 4xx 로 반영 — common.md | `accountinterlockhub#469` | ℹ️ |
| 2026-07-26 18:45 | spec ⓒ | (공통 반영) `f4e515b` 하류 정합 — **`OPS-002-06` 신설로 생긴 자기모순 해소**. "경로 비공개가 유일한 완화 장치다"라고 주장하던 4곳(`policy_SEC.md` §개요·`EXC-SEC-09` · `process_PROC-204.md` §진입 조건 요청 제한 · `spec-functions-api-server.md` 자가진단 API Rate Limiting 행 · `service_SVC-013.md` §예외 사항)을 **"반복 시도 완화가 아니라 노출 범위 축소 장치"**로 정정. `SEC-003-04` 의 400 `EX-SEC-003` 은 불변 · 경로 값 보호를 뜻하는 서술(`SVC-008`·`PROC-403`·`PROC-901`·`PROC-204` `B1` 은닉 요구)은 참이므로 무변경. `OPS-002-06` 을 `PROC-204` §관련 사양 코드 정책 행·`spec-process-policy-map.md`·`spec-process-mapping.md` 에 등재 — common.md | `accountinterlockhub#489`·`#494`·`#469` | ℹ️ |
| 2026-07-26 18:25 | build | `apps/backend/src/interlock-selfcheck/{selfcheck-request.dto.ts,selfcheck-response.model.ts,protocol-conformance.service.ts,selfcheck.controller.ts,selfcheck.module.ts,index.ts}` + `apps/backend/src/crypto/crypto.constants.ts`(`PROTOCOL_VERSION` 신설) — **PROC-204 연동 규약 자가진단 API `POST <SELFCHECK_PATH>` 최초 구현**(P12, `#489`). 컨트롤러 `B2`(입력 존재 검증)~서비스 `B3`(FN-005)~`B6`(응답 구성)을 `ProtocolConformanceService.diagnose()` 로 오케스트레이션. **`B4` 는 `judgeDecryption()`(FN-004, `SVC-002`·`SVC-004` 와 공유 — P09 `#486` 소유, 시그니처·반환형 무변경으로 재사용)을 그대로 호출**하고 세 예외(`ProtocolFormatError`·`IdentityMismatchError`·`ProtocolViolationError`)만 200 부적합으로 흡수, **그 밖의 예외는 다시 던져 전역 필터의 500 `EX-OPS-002` 로 흘려보낸다**(부적합 위장 금지, `tc_API-04.md` `API-04_015` ③). `B5` 크기 상한 검사는 `judgeDecryption()` 이 반환하는 `rawPlaintext`(P09 확장분, 재직렬화 없는 UTF-8 원본 바이트열)의 `Buffer.length` 를 그대로 쓴다. **`B2` 존재 검증은 빈 문자열도 "부재"로 취급**(`SEC-003-04`) — 다른 접점의 `asStringOrUndefined` 관례(빈 문자열 통과)와 다른 이 접점 전용 결정이며 근거는 `tc_API-04.md` `API-04_007` ② "셋 다 빈 문자열 → 400 EX-SEC-003" 실측 요구. `DatabaseModule`·`RecordsModule` 미주입 — 스키마 0개(테이블 0건)인 임시 DB 로 전 테스트가 오류 없이 통과해 **무기록을 실측으로도 확인**(`API-04_012`). **자체 제작 벡터 생성기**(`policy_SEC-crypto.md` §규약 확정값 그대로 구현)를 `apps/sender-sdk/protocol-test-vectors.json` `VEC-002-KEY-EXACT` 재생성 대조로 자체 검증(바이트 동일) 후, 적합·`EX-SEC-001`(구조 위반 2종)·`EX-AUTH-002`(키 불일치)·`EX-SEC-002`(비-JSON·trackingKey 부재·공백)·`EX-SEC-004`(1024/1025바이트 경계)·`EX-SEC-003`(부재 5종+깨진 JSON)·`EX-AUTH-001`(형식 위반 4종+달력無값 통과 확인) 70건 자체 검증 — 전건 사양 기대값과 일치. **은닉(디코이) 대조 — 판정 기준을 build 오케스트레이터가 착수 중 정정**: 메서드 불일치(GET·PUT·DELETE·HEAD·OPTIONS)만 미정의 임의 경로와 상태·헤더(Date 제외)·본문 **바이트 단위 동일**(디코이 6종 대조, 대용량 300KB 본문·깨진 gzip 조합 포함 실측). `POST` + 입력 부재·본문 해석 불가는 400 `EX-SEC-003` 로 **의도적으로 구분**되며 이는 결함이 아니라 `policy_OPS.md` `OPS-002-06`·`EXC-OPS-10`(이번 세션 중 `f4e515b` 로 커밋 확정)이 확정한 설계 — "경로 비공개는 반복 시도 완화가 아니라 노출 범위 축소 장치"·"진단성을 버려도 대가로 보호되는 것이 없다"(자가진단이 주는 능력은 이미 공개 경로의 재입력·오프라인 전수대입으로 얻을 수 있는 수준을 넘지 않는다). 응답 시간 균일화 등 추가 은닉 장치는 사양 요구가 없어 **도입하지 않았다**. **`app.module.ts` 배선 1줄(import)+1줄(등록)은 동시 작업 중이던 spec 레그 doer 의 커밋(`93e5157`)에 함께 실렸다** — 착수 전 클린 상태를 확인하고 편집 직후 diff 를 육안 검증(내 변경만 존재)했으나, 스테이징 후 커밋 전 사이에 그쪽이 인덱스를 통째로 커밋해 발생한 동시성 사고다. 실제 반영 내용은 `93e5157` diff 로 실측 확인(내 의도와 100% 일치, 다른 파일과 섞이지 않음) — 본 build 커밋에는 그 파일이 없다(이미 반영돼 스테이징할 diff가 없음). **`crypto.constants.ts` 는 P03(`#480`) 소유** — 신규 상수 1개 순수 추가(기존 암호 함수 거동 무변경, 값 기반 검증: `X_PLAINTEXT_MAX_LENGTH_BYTES` 등 기존 export 무변경 실측)라 판정 결론에 영향은 없으나, 발송처 라이브러리 `InterlockRequestBuilder.ProtocolVersion` 과의 **새 값 일치 계약**(`BR-017`)을 걸었으므로 애매성을 인정해 관련 일감에 `#480` 을 포함했다. 작성만 수행 — 코드리뷰·기능검증은 별도 doer 대기 | `accountinterlockhub#489`·`#480`·`#469` | 🚧 |
| 2026-07-26 18:13 | spec ⓒ | (공통 반영) `policy_OPS.md` — 자가진단 경로 비공개의 **효력 범위** 명문화(`OPS-002-06`·`EXC-OPS-10`): `POST` 요청 1건으로 경로 존재가 확인되므로 경로 비공개는 **반복 시도 완화가 아니라 노출 범위 축소 장치**다. `SEC-003-04` 의 400 `EX-SEC-003` 은 **일반 404 로 바꾸지 않는다**(진단성 유지 — 기각 근거는 `EXC-OPS-10`). `OPS-002-03` 의 "경로 비공개로만 완화한다" 서술절 정정 — common.md | `accountinterlockhub#489` | ℹ️ |
| 2026-07-26 00:45 | spec ⓒ | (공통 반영) 요구사항 회귀의 연쇄 반영 — 자가진단의 계약·판정은 **무변경**이다(무전달·무기록이라 결과 구분 값 체계와 무관). TC `tc_API-04.md` `API-04_012`(무기록·무전달)의 기대값을 지표 **카운터 5종 → 4종**으로 정정(`user_denied_count` 컬럼 삭제 반영). `service_SVC-013.md`·`process_PROC-204.md` 는 PRD §미결·확인 필요 항번(⑧→⑨) 인용만 갱신(TC 신설·결번 0) — common.md | `accountinterlockhub#469`·`#471`·`#475`·`#476` | ℹ️ |
| 2026-07-25 17:36 | spec ⓒ | (공통 반영) 교차검증 2회차 경고 5건 보완(회귀 2회차 — 🟡 N-1~N-5 전건 종결) — `service_SVC-013.md` F-002·Happy Path 2단계의 "형식" 을 **생년월일 한정**으로 축소(암호값 구조 위반은 판정 결과 — §예외 흐름 경계와 일치) · `tc_API-04.md` `API-04_015` §검증 대상에 `OPS-003-05` 병기(TC 신설 0) — common.md | `accountinterlockhub#471`·`#476` | ℹ️ |
| 2026-07-25 17:01 | spec ⓒ | (공통 반영) 교차검증 1회차 지적 보완(회귀 1회차 — 🟠 2·🟡 7 전건 해소) — `SEC-003-04` 를 **입력 부재 한정**으로 좁히고 형식 위반은 `AUTH-002-02`(생년월일)·`SEC-002-02`(암호값 구조)로 위임 — `EX-SEC-003` 의미 정정 + `SVC-013` 예외 흐름·`MDL-014` 에 경계 문구 · `MDL-005`·`MDL-006` 자가진단 소비 반영 — common.md | `accountinterlockhub#470`·`#471`·`#472` | ℹ️ |
| 2026-07-25 16:19 | spec ⓒ | (공통 반영) 검증 TC 도메인 신규 작성 — `docs/specs/qa/` **TC 232건**(커버리지 SVC 18/18 · PROC 18/18 · BR 24/24 · EX 13/13 · 정책 17/17 · 접점 11/11 — 미검증 0)·**횡단 시나리오 `SCEN_001`~`SCEN_023`**(여정 6 · 규약 대칭 왕복 3 · 기동·상수 검증 5 · 무저장 원칙 2 · 설계된 부재·수용 리스크 7)·**AUTH 치환 12건**·모의 수신처·C# 하네스·시드 카탈로그 37항·미해소 1건(성공률 정의 이월) — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-25 15:59 | spec | `docs/specs/qa/API/tc_API-04.md` 신규 — **연동 규약 자가진단 API TC 15건**(API·서버). 적합 판정·**부적합 4종 전부 HTTP 200**(`EX-SEC-001`·`EX-AUTH-002`·`EX-SEC-002`·`EX-SEC-004`)·**200 ↔ 400 경계 = "판정을 수행할 수 있었는가"**(입력 부재 `EX-SEC-003`·형식 위반 `EX-AUTH-001` 만 400)·X 평문 상한 경계 3종·**비공개 경로에서만 응답**(접두 일치·추측 경로 전건 일반 404, 다른 404 와 바이트 단위 동일)·**메서드 불일치도 405 아닌 404**(다른 API 는 405 — 대조 확인)·**무기록·무전달**(저장 3종·지표 5카운터·모의 수신처 전건 무변화)·응답에 원문·키·중간 값·단계 번호 0·규약 버전 MAJOR/MINOR 경계·내부 실패 500. **AUTH 치환** 3건(비공개 경로 한정 응답·메서드 404·인증 부재+반복 호출 미차단 수용). 검증 BR-013·BR-014·BR-017 · EX 7종 | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec | `process_PROC-204.md` 신규 — **PROC-204 연동 규약 자가진단 API**(RR·동기·`POST <SELFCHECK_PATH>`). `B1` 경로·메서드 불일치는 **본문 없는 일반 404**(405 를 쓰지 않는다 — 경로 존재를 드러내므로) → `B2`·`B3` 입력 검증 → `B4` **연동과 같은 FN-004** → `B5` 평문 크기 상한(FN-004 밖에서 별도 검사) → `B6` **부적합도 200**·EX 코드를 사유 코드로 그대로. **기록·전달·계수 전무**(`SEC-003-03`)·원문/키/단계 번호 미포함. 운영 노출 기본안 적용 명시 | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec ⓒ | (공통 반영) 프로세스 도메인 신규 작성 — `docs/specs/processes/` **PROC 18종 확정**(정책·서비스 예약분을 같은 코드·같은 의미로 — 통합·폐기·추가·재배치 0)·**단계 라벨 체계 확정**(`F` FE·`B` BE·`C` 호출측·`L` 라이브러리 내부, 레이어별 1부터 시간 순, 분기는 접미 문자)·선행 도메인 단계명 전건 라벨 매핑·의존관계 순환 0(호출 깊이 3층)·트랜잭션 경계와 멱등 수단 총괄·정책 규칙 86건 매핑(런타임 80 + 게이트·절차 6) — common.md | `accountinterlockhub#475`·`#469` | ℹ️ |
| 2026-07-25 14:40 | spec ⓒ | (공통 반영) 화면 도메인 신규 작성 — `docs/specs/screens/` **화면 4건**(SCR-001~004 — 서비스 예약 대비 통합·폐기·추가·재배치 0)·**단일 경로 4단계 모델**(리다이렉트 없음·화면별 URL 없음)·**디자인 시스템 1벌**(표면이 하나라 토큰 분기·접두어 없음 · 색 23종 **실측 대비비 표** · WCAG 2.1 AA · 모바일 우선 2 브레이크포인트 · 히트 영역 44x44)·공통 컴포넌트 11종·**화면 없는 IA 노드 12건 근거 기재** — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-25 14:10 | spec | `spec-functions-api-server.md` §연동 규약 자가진단 API — **`POST <SELFCHECK_PATH>` 확정**(본문 전송이라 암호값·생년월일이 URL·로그에 남지 않는다). **사유 코드 체계 확정 = 정책 예외 코드 카탈로그의 EX 코드 값을 그대로 사용**하며 **연동 라이브러리 오류 사유 코드와 같은 값 체계** — 적합 `null` / `EX-SEC-001`(구조) · `EX-AUTH-002`(1·2단계) · `EX-SEC-002`(3·4단계) · `EX-SEC-004`(평문 상한). **부적합도 HTTP 200**(판정이 정상 수행된 결과이며 요청 오류가 아니다), 요청 자체가 성립하지 않는 경우만 400(`EX-SEC-003`·`EX-AUTH-001`). 경로 불일치·**메서드 불일치도 일반 404**(경로 존재 미노출). 복호화 원문·키·중간 값·단계 번호 응답 미포함·무기록·무계수. **X 평문 크기 상한 검사는 자가진단만 수행**(FN-004 에 넣지 않은 근거를 사양에 명시 — `SEC-001-09` 위반 시 처리가 라이브러리·자가진단만 지정). 응답에 허브 규약 버전 포함(호환 판정 근거). 운영 환경 노출은 기본안 적용 | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-25 14:10 | spec ⓒ | (공통 반영) 기능·API 도메인 신규 작성 — `docs/specs/functions/` **공통 기능 15건**(FN-001~015 · POL 5·DAT 8·CRS 2·**EXT 0**)·**인터페이스 11접점**(사용자 진입 표면 3 · 서버 대면 API 4 · 수신처 전달 1 · 명령 진입점 1 · 라이브러리 공개 진입점 2)·오류 응답 엔벨로프 `{code,message,details?}`·인증 0/요청 제한 0·**신규 EX `EX-OPS-002` 1건**을 정책 카탈로그에 등록 — common.md | `accountinterlockhub#473`·`#469` | ℹ️ |
| 2026-07-25 13:35 | spec ⓒ | (공통 반영) 데이터 도메인 신규 작성 — `docs/specs/datas/` **엔터티 3건**(ENT-001~003 = 저장 대상 셋)·**데이터 모델 22종**(MDL-001~022 확정 — 예약 대비 통합·추가·재배치 0)·인덱스 5·물리 FK 0·소프트 삭제 미도입 — common.md | `accountinterlockhub#472`·`#469` | ℹ️ |
| 2026-07-25 13:05 | spec | `service_SVC-013.md` 신규 — **SVC-013 연동 규약 자가진단**(매핑 PROC-204). Happy Path 6단계(진단 요청→입력 검증→판정 수행→응답 구성→**무기록 종료**→발송처 버전 대조), 입력 MDL-014 → 출력 MDL-015. BR-014 = 경로 일치 여부(비경로는 존재를 드러내지 않는 일반 응답), BR-013 = 적합·부적합 판정, BR-017 인용. 예외/사유 코드 6종(`EX-SEC-003`·`EX-AUTH-001`·`EX-SEC-001`·`EX-AUTH-002`·`EX-SEC-002`·`EX-SEC-004`) — **라이브러리 오류 사유 코드와 같은 값**. 응답에 **허브 규약 버전** 포함. **PRD 미결 ⑧ 기본안 적용(운영 환경 노출·비공개 경로 유지) 명시**. 부적합 응답의 HTTP 상태·필드 구성은 기능·API 도메인 위임 | `accountinterlockhub#471`·`#469` | 🚧 |
| 2026-07-25 13:05 | spec ⓒ | (공통 반영) 서비스 도메인 신규 작성 — `docs/specs/services/` **서비스 18건**(SVC-001~018)·사용자 역할 4종·분기 `BR-001`~`BR-024`·MDL 22종/SCR 4종 예약 채번·`PROC-403`·`PROC-404` 추가 채번 — common.md | `accountinterlockhub#471`·`#469` | ℹ️ |
| 2026-07-25 12:28 | spec | `policy_SEC.md` `SEC-003` 자가진단 경로 취급 신설(진입 게이트 🟡 **G-03 자체보완**) — `<SELFCHECK_PATH>` 의 **운영 값은 git 관리 상수표에 적지 않고 배포 시 주입되는 비밀 값**으로 다루고 상수표에는 형식·잠정값만 남긴다. 경로 값은 공개 문서·클라이언트 코드·응답·오류 메시지에 미포함, 비경로 요청은 **경로 존재를 드러내지 않는 일반 404**, **유출 확인 시 경로 교체·재배포**. 자가진단은 연동이 아니므로 전달·기록(추적 레코드·동의 증적·지표) 전량 미수행·복호화 원문 미반환. 입력 3종(encX·encY·생년월일) 형식 위반 400 `EX-SEC-003`. 판정은 `SEC-002-01` 4단계를 **허브와 같은 함수로 공유**하고 응답에 **허브 규약 버전**을 포함(`SEC-001-11` 호환 판정 근거). **운영 환경 노출 = PRD 미결 ⑧ 기본안 적용**(EXC-SEC-08) | `accountinterlockhub#470`·`#469` | 🚧 |
| 2026-07-25 12:28 | spec ⓒ | (공통 반영) 정책 도메인 신규 작성 — `docs/specs/policies/` 정책 17건·규칙 86건·EX 코드 12종(암호화 연동 규약·복호화 판정 4단계·결과 구분 4종·추적 레코드 생성 시점·보관·무저장·동의 증적·인증 부재·수용 리스크) — common.md | `accountinterlockhub#470`·`#469` | ℹ️ |
| 2026-07-25 11:25 | directing ⓒ | (공통 반영) 값 확정 대기 잠정값 설정 — 자가진단 경로 `<SELFCHECK_PATH>` 에 비공개 경로 형식의 잠정값 적용 + **git 기록분은 비공개가 아니므로 운영 값은 배포 시점 재지정** 조건을 상수표·`external-apis.md` §7 에 명시 — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
| 2026-07-25 10:05 | directing ⓒ | (공통 반영) 리셋 방향 담당자 확정분 반영 — 자가진단 API **신설**(담당자 확정 — API 테스트 엔드포인트). 무전달·무기록·원문 미반환·비공개 경로 상수 `<SELFCHECK_PATH>`·운영 노출 포함 — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
