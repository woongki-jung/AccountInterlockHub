# IA: USR-05 — 연동 결과 안내

> 본 IA 노드와 연관된 작업의 시계열 이력(최신순). 정책: ai/strategies/ia-history.md

| 일시 (KST) | 단계 | 산출물·결과 | 관련 일감 | 상태 |
|---|---|---|---|---|
| 2026-07-26 19:40 | spec ⓒ | (공통 반영) `screen_SCR-004.md` — 직전 `af350e6` 이 넣은 **거짓 서술 정정**(E7-1). *"확정 결과 재안내도 진입(첫 화면)으로 온다"* 는 성립하지 않는다 — 진입 응답 `stage: RESULT` 는 **항상 경로 ②** 이고 `PROC-101` 은 복호화 전이라 추적 키를 몰라 레코드 조회 자체를 하지 않는다. 그대로 두면 "진입에는 제목 포커스 없음" 규칙과 결합해 **경로 ①·③ 재안내 도착의 제목 포커스가 잘못 억제**된다(`USR-05_010` ⑤~⑦ 충돌). 재안내 = `PROC-102` `B4` · `PROC-103` `B4`·`B6` 뒤의 **전환**임을 명시하고, **`isReAnnouncement` 를 진입/전환 구별 신호로 쓰지 말 것**(최초 확정도 전환이므로 오탐)을 경고로 추가. 규칙 자체(진입 무포커스·전환 포커스)는 무변경 — common.md | `accountinterlockhub#486`·`#493`·`#489`·`#469` | ℹ️ |
| 2026-07-26 18:37 | spec | `docs/specs/screens/screen_SCR-004.md` — build P16(`#493`)이 상신한 **사양 결함 6** 확정의 화면측 반영. ⑴ §기본 정보 **선행 화면** 행이 이 화면이 첫 화면이 되는 경우를 *"진입 판정 실패 시"* 로만 적어 **확정 결과 재안내(경로 ①·③) 진입이 빠져 있던 것**을 `stage = RESULT` 기준(진입 판정 실패 = 경로 ② · 확정 결과 재안내 = 경로 ①·③)으로 정정했다 — **사각지대의 문서적 뿌리**였다. ⑵ §화면 상태 전이에 불릿 1개 신설 — **진입으로 그려진 결과에는 결과 제목에 프로그램 포커스를 주지 않고, SCR-001·002·003 에서 전환으로 도착했을 때만 준다**(문서 제목은 진입에서도 맞춘다). `resultPath`·`isReAnnouncement`·`returnUrl` 이 진입·전환 양쪽에서 같은 값을 가질 수 있어 **표시 상태만으로는 구별 불가**하므로 화면 상태를 진입 표지로 대신 읽지 않고 **구별 신호를 따로 둔다**고 명시. 규칙 정본·근거는 `design-system.md`(common.md). **build 재작업 발생** — 현재 구현은 수화 진입 시 `activeElement === H1` + 링이 서는 것이 CDP 로 실측돼 있다(`#493` j3641). | `accountinterlockhub#493`·`#469` | 🚧 |
| 2026-07-26 18:37 | spec ⓒ | (공통 반영) 디자인 시스템 §접근성 기준 — **진입(최초 로드) 제목 자동 포커스 금지** 일반 규칙 신설(`design-system.md`·`design-system-components.md`). 이 노드의 **`SCR-004`** 가 그 규칙이 실제로 닿는 유일한 미해소 자리였다(진입 응답 `stage` 가 산출하는 화면은 `SCR-001`·`SCR-004` 둘뿐이고 전자는 `8b94e5a` 로 이미 닫힘). **`SCR-004` 특례로 쓰지 않고** 진입 전반의 일반 규칙으로 확정했다. 본문·근거는 common.md | `accountinterlockhub#493`·`#469` | ℹ️ |
| 2026-07-26 18:16 | spec | `docs/specs/processes/process_PROC-105.md` — build P10(`#487`)이 상신한 **사양 결함 7 회귀**(경미 · **표기 전용이며 동작 정의는 1자도 바뀌지 않았다**). §진입점 표가 `PROC-102 B4b`·`PROC-103 B2 → REASON_CODE` 행을 다른 네 행과 똑같은 "호출 지점"으로 적어 **백엔드가 직접 `PROC-105` 를 부르는 자리처럼 읽혔다** — P10 doer 가 실제로 "미배선 갭"으로 오판할 뻔했고(journal 3615 ⑥), 독립 코드리뷰도 같은 오독 위험을 S-3 으로 확인했다. 표에 **수행 계층** 열을 더해 **BE(직접 호출) 4행**과 **FE `F1` 1행**을 갈라 놓고, 해당 행의 지점 표기를 "…의 **오류 응답을 받은 뒤**"로 다시 적었으며(BE 행 4개를 위로 모아 `source` 순서도 정돈), 각주로 근거를 명문화했다 — 두 실패 지점은 결과를 확정하지 않고 예외를 그대로 올려 `resultPath` 필드 자체가 없는 `FN-014` 오류 응답 엔벨로프로 응답이 끝나므로 서버는 `B1`~`B3` 을 호출하지 않고, 그 출처를 실제로 재현하는 자리는 화면 `F1`(`err.code` → 경로 도출)이며, **"백엔드에 이 호출을 배선하면 반환값을 버리는 죽은 코드가 되므로 미배선을 구현 갭으로 보고하지 않는다."** `B1`~`B3`·`F1`~`F3` 의사코드, 결과 경로 3종(네 번째 경로 없음), `returnUrl` 동봉 판정(BR-025 — 경로 ① 한정·절대 URL·미달 시 속성 자체 부재), `DECRYPT_FAILED` 방어적 분기, `B3` 이 구성 상수를 직접 읽는 규칙은 **전부 무변경**. | `accountinterlockhub#487`·`#469` | 🚧 |
| 2026-07-26 17:25 | build | P10 회귀 1회차 — **감사 결론 정정(append-only 신규 entry) + `[I-1]` 시정**(`accountinterlockhub#487`, 독립 코드리뷰 FAIL journal 3627 → 오케스트레이터 판정 ⓐ journal 3629). **정정 대상**: 바로 아래 2026-07-26 15:53 row(및 journal 3613)의 "중복 구현 없음"·"다른 매핑 로직도 backend 전체에서 이 파일 밖에 없다" 결론은 **위음성**이었다 — 그 대조가 쓴 방법(`class ResultInfoBuilder`·`selectResultPath`·`selectReturnUrl` 등 **클래스·함수명 grep**)은 **리터럴로 인라인된 대응을 원리상 검출할 수 없는 방법**이라 실제 사례를 놓쳤다. **실제로 있었던 것**: `global-exception.filter.ts:221`(진입 경로 한정 폴백, P07 `b058bfe` 산출)가 `ResultInfoBuilder` 를 부르지 않고 `renderEntryDocument({stage:'RESULT', reasonCode:'EX-OPS-002', resultPath:2, isReAnnouncement:false})` 로 `MDL-009` 를 직접 리터럴로 구성하고 있었다 — `process_PROC-101.md:161-168` `B6`·`process_PROC-105.md` §개요("대응이 이 프로세스 한 곳에만")·`MDL-009` §구현 가이드("대응은 한 곳에만") 위반(동작 결함은 0 — 값이 우연히 일치했을 뿐, 표류는 시간 문제였다). **시정(ⓐ 채택)**: `GlobalExceptionFilter` 생성자에 `ResultInfoBuilder` 를 주입하고(`common.module.ts` 가 `EntryModule`·`VerifyModule`·`ApproveModule` 과 같은 결로 독립 provider 등록), 그 리터럴을 `resultInfoBuilder.build({source:'ENTRY_FAILURE', reasonCode:'EX-OPS-002'})` 호출로 교체(`entry.controller.ts` `handleEntry()` B6 과 같은 호출 형태) — 기존 `try/catch`+`HARD_FALLBACK_DOCUMENT` 최후 방어는 무변경으로 유지하며 이제 빌더 호출 실패까지 그 안전망이 흡수한다. **자가검증**: `npm --prefix apps/backend run build` 시정 전/후 에러 0 · Nest 부트스트랩·DB 없이 `GlobalExceptionFilter.catch()` 를 직접 호출하는 하네스로 7개 시나리오 실측 — 진입 경로 폴백 2종·일반 404·마지막방어 500·자가진단/디코이 대칭 2종은 시정 전/후 **바이트 단위 완전 동일**, 빌더가 던지도록 강제한 7번째 시나리오는 `HARD_FALLBACK_DOCUMENT` 로 정확히 물러남을 확인. **값 기반 전수 재탐색**(`resultPath`+`isReAnnouncement` 동시 출현·`stage:'RESULT'`+`reasonCode:` 리터럴 조합·`EX-OPS-002` 리터럴, backend 전체) — 이번 대상 외 인라인 대응 **0건**(`entry-document.ts:70` `HARD_FALLBACK_DOCUMENT` 는 같은 값을 담은 순수 문자열 상수이나 **어떤 함수 호출도 거치지 않는 최후의 최후 방어**라 범주가 달라 수렴 대상이 아니다 — 그 자체가 실패하면 안 되는 자리라 의도적으로 정적이다). **⚠️ 후행 QA 필독** — 바로 아래 15:53 row 의 "중복 구현 없음" 헤드라인은 이 row 가 대신한다. **그 row 만 보고 재확인을 생략하지 말 것.** 독립 코드리뷰·기능검증은 별도 doer 소관(본 entry 는 작성만 수행) | `accountinterlockhub#487`·`#469` | 🚧 |
| 2026-07-26 15:53 | build | P10 — `PROC-105` 커버리지 재확인(**신규 구현이 아니다** — `#487` 착수 전 범위 재조정 노트가 지시한 대조 작업). `apps/backend/src/interlock-entry/result-info.builder.ts` `B1`~`B3` 을 `process_PROC-105.md`·`service_SVC-005.md`·`model_MDL-007-010.md`(`MDL-009`)·`policy_BIZ.md`(`BIZ-001-01`·`02`·`03`·`06`)와 **1:1 대조 — 전 단계 사양대로 완전 구현 확인, 기능 갭 없음**(`selectResultPath`·`selectReturnUrl` 스위치·조건이 의사코드와 정확히 일치, `returnUrl` 은 경로 ① + 절대 URL 일 때만 **속성 자체를 추가**하는 두 리터럴 반환으로 구현돼 "필드 유무" 계약을 지킨다). **호출부 4곳 전수 확인** — `entry.controller.ts`(`ENTRY_FAILURE`, PROC-101 `B6`) · `identity-verification.service.ts`(`RECORD`, PROC-102 `B7`) · `consent-approval.service.ts`(`RECORD` PROC-103 `B4` · `RESULT_CODE` PROC-103 `B8`) — 전부 사양의 (source, payload) 와 일치. **`REASON_CODE`(PROC-102 `B4b`·PROC-103 `B2`) 는 호출부가 없다 — 갭이 아니라 설계**: 두 경로 모두 결과를 확정하지 않고 예외를 그대로 던져 `GlobalExceptionFilter` 가 순수 FN-014 엔벨로프(`{code,message,details?}` — `resultPath` 필드 자체가 없다)로 응답을 끝내고, 화면(PROC-105 `F1`)이 `err.code` 로 직접 경로를 도출한다(실측: `process_PROC-102-logic.md` `B4b` "return 400 FN-014(code) // 화면은 결과 경로 ②로 간다" · `process_PROC-103.md` §의존 프로세스 "PROC-105(동기 — `B4`·`B8`)"— `B2` 제외 · `GlobalExceptionFilter.catch()` 실측 코드 확인). **중복 구현 없음** — `class ResultInfoBuilder` 정의는 1곳뿐(`git grep` 확인), `EntryModule`·`VerifyModule`·`ApproveModule` 3곳의 provider 등록은 상태 없는 클래스의 DI 스코프 등록일 뿐 소유권 수렴 대상이 아니다(`EntryModule.forRoot()` 재호출 시 `GET` 라우트 중복 등록을 피하기 위한 설계, 각 모듈 문서 주석에 이미 근거 기재). **변경 사항 = 헤더 주석 정정 1건**뿐(3 호출 지점이 이미 배선됐음을 반영 + `REASON_CODE` 미배선이 설계임을 명문화 — 실행 로직 무변경). `npm run build`(`nest build`) 에러 0건. **사양 재대조**: `git log b058bfe(P07)..HEAD -- docs/specs/` 변경 0건 — 착수 후 사양 드리프트 없음 | `accountinterlockhub#487`·`#469` | 🚧 |
| 2026-07-26 13:35 | spec ⓒ | (공통 반영) 디자인 시스템 §접근성 기준 **포커스 우선순위 확정(`QA-OPEN-02` 해소)** + 목업 4종 포커스 링 미충족 시정 — 이 노드에 닿는 것은 **`SCR-004`**(연동 결과 안내)다. **[① 결과 도착은 무변경]** 확정된 예외는 **필드에 매인 안내가 함께 뜨는 전환** 한정인데 `SCR-004` 도착에는 그런 안내가 없어 **종전대로 제목으로 포커스**가 간다 — 직전 `e39dd60` 이 `USR-05_010` 에 세운 관측 ⑤⑥⑦(결과 제목의 2px 띠 · 3경로 한 벌 · 탭 순서 밖)은 **기대 결과 그대로 유효**하고 `screen_SCR-004.md` 도 **무접촉**이다. **[② 목업 — 결과 제목까지 포함했다]** `mockup/SCR-004.html` 에 `.result-panel h1:focus` **1줄**(+주석 3줄)을 더했다. 겨냥한 것은 결과 제목 `#result-title`(**StageTitle 결과 변형** · `ResultPanel` 이 합성하는 `<h1>`)이며, 그 전까지는 `:focus-visible` 이 `.return-link` 등 조작 요소에만 걸려 있어 제목이 포커스를 받으면 **브라우저 기본 1px 링**으로 떨어졌다 — **정본이 Fail 로 규정한 케이스**다. 값은 조작 요소와 **같은 한 벌**(바탕색 2px 안쪽 여백 + 2px `--color-focus` `#0B4FA8`)이라 **결과 3경로에 분기가 없다** — 패널 바탕만 경로별 약한 바탕으로 갈리고 링의 두께·색·안쪽 여백은 갈리지 않는다(안쪽 여백에는 그 패널 바탕이 그대로 비친다). `36aeb39` 가 고친 **복귀 안내 기본 노출 거동·카운트다운·replay 는 무접촉**이고 정상 상태 시각도 불변이다(마크업 0줄·새 토큰 0). 이 노드 TC **16건 무접촉** · 총 242건·결번 2건(`USR-05_002` 포함) 불변. 규칙 본문·근거 셋·검증은 common.md 항목 참고 — common.md | `accountinterlockhub#474`·`#476`·`#469`·`#467` | ℹ️ |
| 2026-07-26 13:15 | spec | `docs/specs/qa/USR/tc_USR-05.md` `USR-05_010` **확장**(번호·**제목 모두 무변경** — "결과 3경로 접근성·반응형" 이 이미 이 관측을 품는 제목이라 손대지 않았다) + `docs/specs/qa/spec-qa-mapping.md` `SCR-004` 행. **이력·산출물 동일 commit**(작성 시점 미커밋). **[`SCR-004` 몫]** 직전 `513aca6` 이 세운 **제목의 프로그램 포커스 표시**를 보는 TC 가 전 도메인 0건이던 공백을 이 노드에서 닫았다(공백의 성격·Fail 판별 기준은 USR-02.md 항목). 기존 단계 ③ 이 **"결과 도착 시 제목으로 포커스 이동"까지만** 보고 **그 제목에 무엇이 서는지**는 보지 않던 자리를 이어받아 ⑤⑥⑦ 을 더했다 — ⑤ ③ 의 그 제목(**`StageTitle` 결과 변형** · `ResultPanel` 이 합성한 `<h1>`)에 **바탕색 2px → `--color-focus` `#0B4FA8` 2px** 띠가 **렌더 픽셀에** 보인다(기본 링 1px·여백 0 도, 아무 표시 없음도 **Fail**) ⑥ **결과 3경로 전부에서 같은 한 벌** — 패널 바탕이 경로별 약한 바탕으로 갈려도 링의 두께·색·안쪽 여백이 갈리지 않는다 ⑦ `tabindex="-1"` 확인은 **이동이 없어 화면이 유지되는 경로 ②·③ 에서** 수행한다(두 경로는 조작 요소가 0 이라 `Tab` 이 문서 밖으로 나가고, 경로 ① 에서도 제목은 건너뛰어 **수동 이동 링크**가 첫 목적지다 — §ResultPanel "링크로 포커스를 옮기지 않는다"). **[경로별로 나눠 배치한 이유]** 이 노드만 **한 화면이 세 모습**을 가져, 링의 단일성(⑥)과 탭 순서 밖 확인(⑦)을 **각각 성립하는 경로에 배치**해야 관측이 성립한다. **[확장으로만 닫았다]** 신설 **0** · 재채번 **0** · 총 **242건**·결번 2건(이 파일 `USR-05_002` 포함) **불변** · 이 노드 TC 16건 그대로. ①~④(3경로 색·아이콘·문구 구분 · 대비 실측 · `role="status"` · 반응형)는 **무접촉**이고 사양 문서·`mockup/`·`apps/` 도 무접촉이다. `SCR-001` 만 관측을 세우지 못해 `QA-OPEN-02` 로 인계했다(USR-03.md 항목) | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-26 12:53 | build | P16 — `SCR-004`(이 노드) 정밀 마감. `StageTitle` 배치 축 신설·`ProgressPanel`·정적 서빙 배선 상세는 **USR-02.md 항목 참고**(같은 commit). **[① `ResultPanel` 비균일 간격]** `.module.css` — 균일 `gap` 을 걷어내고 `design-system-components.md` §ResultPanel §간격 확정값을 요소별 `margin-top` 으로 직접 구현: 아이콘→제목 `.panel>h1{margin-top:--space-md}`(StageTitle `placement="panel"` 이 Fragment 라 `h1` 이 `.panel` 직계라 이 선택자가 유효) · 제목→배지 `.reannounceBadge{margin-top:--space-sm}`(아래 ②) · 배지(없으면 제목)→설명 `.description{margin-top:--space-md}`(조건 분기 없이 한 값으로 양쪽을 만족 — 두 경우 다 요구값이 16px 이라서 가능) · 설명→다음 안내·다음 안내→복귀 영역도 각각 `--space-md`. **`padding` 을 `var(--space-lg) var(--space-md)`(비대칭) → `var(--space-lg)`(균일 24px) 로 정정** — 카탈로그 "안쪽 여백 `--space-lg`" 원문이 단일값이라 P14 의 좌우 축소값은 사양과 불일치였다(발견·정정). **[② `Badge` 확장]** `components/Badge.tsx` 에 선택적 `className` prop 추가(기존 호출부 `ConsentList` 는 미전달 — 렌더 바이트 무변경 확인) — 재안내 배지에 wrapper div 없이 여백을 실어 패널의 직계 flex 자식 구조를 유지. **[③ 제목 포커스 링 — 사양 확정 반영]** 착수 당시 사양 공백(`#493` journal 3553)이었으나 진행 중 `design-system.md` §접근성 확정(commit `513aca6`, journal 3562)으로 착수 — `StageTitle.module.css` `.title[tabindex='-1']:focus{outline:2px solid var(--color-focus);outline-offset:2px}`. 속성 선택자로 특이도를 (0,3,0) 까지 올려 전역 `:focus:not(:focus-visible){outline:none}`(0,2,0, 실측상 번들 CSS 에서 이 파일보다 **뒤**에 배치돼 동수였다면 전역이 이겼을 자리)을 소스 순서와 무관하게 이긴다. **[CDP 실측 — 포트 49511, 실제 NestJS 정적 서빙 대상]** 헤드리스 탭은 실제 OS 창 포커스가 없어 `document.activeElement` 는 갱신돼도 `:focus`/`:focus-visible` 이 매치되지 않는 함정을 실측 발견 — `Emulation.setFocusEmulationEnabled({enabled:true})` 로 우회(표준 기법, 신규 CDP 노하우로 기록). 그 상태에서 제목 `outline` 실측 `2px solid rgb(11,79,168)`(`#0B4FA8`=`--color-focus`) + `offset 2px` — `SCR-003`·`SCR-004` 양쪽 확인. 결과 경로 ①(배지 없음) gap `[16,16,16,16]`(아이콘→제목→설명→다음안내→복귀영역) · ①+재안내 gap `[16,**12**,16,16,16]`(제목→배지 12px 확인) · ②(`EX-SEC-001`) gap `[16,16,16]`·설명 카탈로그 문구 일치·배지 없음 · ③(`EX-BIZ-002`/502) gap `[16,16,16]`·복귀영역 없음(returnUrl 미실림, 사양대로). **복귀 이동**: 카운트다운 실감소 확인("3초 후 이동합니다"→"2초"→"1초", 1.15초 간격 재측정 일치) · **이력 치환은 `ResultPanel.tsx` 의 `window.location.replace()` 호출로 코드 확인**(실제 만료·이동은 CDP 세션 관측 단절 위험이 있어 만료 전에 멈춤 — 실측/코드확인 구분) · **민감값 폐기가 이동보다 먼저**도 코드 확인(`useInterlockFlow.ts` `applyNextView()` 가 `SCR-004` 전이 **시점에** encX/encY/birthDate/agreedCodes 를 동기 폐기 — `ResultPanel` 의 3초 뒤 이동보다 항상 먼저, P14 기존 구현·본 Phase 무변경). **[정적 서빙 — 자가진단 은닉 디코이 대조]** 포트 49511 실행 중 백엔드(`DB_*` 등은 접속 불필요한 더미값 — 라우팅 테스트라 쿼리 미발생) 대상 curl: 실제 `<SELFCHECK_PATH>` GET/PUT/DELETE/HEAD/OPTIONS 전부 디코이 경로(`/api/_selfcheck/decoy00000000`)와 **상태·헤더·본문 바이트 단위 동일**(404, `Content-Length:0`) — `SEC-003-02` 무회귀. 정적 자산(`/assets/*.js·css`) 200, `/`·`<INTERLOCK_ENTRY_PATH>` 는 본문 없는 404(교체 전 상태와 동일 — 아래 참고). **[발견 — 수정 범위 밖]** `POST` + **유효한** JSON 본문(`{"probe":true}`)을 실 자가진단 경로로 보내면 `400 EX-SEC-003` 이 나오는데 디코이는 `404` — `body-parse-failure.ts`(P05, 무접촉)의 기존 로직이 "메서드가 맞으면 입력이 사실상 없는 것과 같다" 규칙을 본문 유효성과 무관하게 적용한 결과로 보인다(정상 POST 는 처음부터 다른 응답이어야 하는 접점이라 완전한 은닉은 애초에 불가능하지만, 이 특정 사례는 담당자 판단이 필요해 보고만 하고 손대지 않음). `npm --prefix apps/frontend run build`·`npm --prefix apps/backend run build` 둘 다 에러 0. 작성만 수행 — 리뷰·기능검증 별도 doer 대기 | `accountinterlockhub#493`·`#469` | 🚧 |
| 2026-07-26 12:20 | spec ⓒ | (공통 반영) 디자인 시스템 §접근성 기준의 **제목 프로그램 포커스 표시 확정** — 이 노드에 닿는 것은 **`SCR-004`** 다. 결과 제목은 `StageTitle(결과 변형)` 합성이라 §접근성 기준의 제목 포커스 계약을 그대로 받으며(같은 회차에 `design-system-components.md` §StageTitle §접근성도 그 정본을 가리키도록 1줄 정합됐다), 결과 단계로 전이하며 그 제목이 프로그램 포커스를 받는 자리에서도 **브라우저 기본 링(1px)이 아니라 공통 링이 선다**(예외 없음 — 요구가 조건이 아니라 **결과**로 적혔다). 결과 경로 3종·사유 코드·확정 결과 재안내 배지·복귀 안내 영역 **전부 무변경**이고 `screen_SCR-004.md`·`mockup/` 무접촉이며 **새 토큰·새 색 0** 이다. `spec-qa.md` 각주가 든 결번 `USR-05_002`(사용자 거부 패널)는 **정의·번호 그대로**이며 각주는 헤딩 자동 계수가 244 로 나오는 이유를 밝힌 것뿐이라 이 노드 TC 16건은 손대지 않았다 — common.md | `accountinterlockhub#474`·`#476`·`#469` | ℹ️ |
| 2026-07-26 11:32 | spec ⓒ | (공통 반영) 목업 잔여 접근성 3건 마감 중 **F-6 만 이 노드에 닿는다** — `mockup/SCR-004.html` 의 결과 제목 `h1` 에 **`tabindex="-1"` 을 부여**했다. 이 제목은 `StageTitle(결과 변형)` 합성이라 §StageTitle §접근성의 **단계 전환 시 새 제목으로 포커스 이동** 계약을 그대로 받는다. **목업은 화면당 1파일이라 실제 단계 전환이 없어 동작 영향은 없고 정적 구조 계약 표기**이며, **포커스를 실제로 옮기는 스크립트는 만들지 않았다**. 결과 경로 3종·사유 코드·확정 결과 재안내 배지·복귀 안내 영역(`36aeb39` 시정분 포함) **전부 무변경**이고 `SCR-004` 사양 파일도 손대지 않았다 — common.md | `accountinterlockhub#474`·`#469`·`#467` | ℹ️ |
| 2026-07-26 11:08 | spec | `mockup/SCR-004.html` — 목업 **복귀 안내 영역의 기본 노출 시정(F-4)**. **⚠️ 이력 누락 보정 — 산출물은 commit `36aeb39` 로 이미 커밋됐고(같은 commit 에 이력을 넣지 못했다) 본 entry 가 그 근거 해시로 추적을 잇는다.** `returnUrl` 토글이 **경로와 무관하게 기본 켜짐**이라 경로 **②(링크·복호화 오류)·③(수신처 전달 실패)** 의 **기본 화면에도 복귀 안내 영역(고지·카운트다운·수동 이동 링크)이 함께 서 있었다** — 복귀 주소가 실리는 경로는 **① 하나뿐**이므로(`BIZ-001-06`·`EXC-BIZ-15`) **실재하지 않는 조합**이다. 각주에 "실제로 일어나지 않는 조합"이라는 경고가 이미 있었으나 **글자는 화면보다 약해**, 담당자 승인 검토에서 "오류 결과에도 자동 복귀가 있다"고 읽히면 **잘못된 전제 위에 승인이 난다**. **사양·구현이 맞고 목업이 틀린 건**으로 목업 `hidden` 무력화(`9e6eb90`)·`SCR-001` 재입력 상태 오류 표현(`316bf77`)과 **같은 계열·같은 기준**이다. **[시정]** 토글의 **기본값을 경로에서 파생**시켰다(`applyReturnUrlDefault` — 경로 ① 이면 켜짐 · ②·③ 이면 꺼짐, **최초 로드 상태도 같은 규칙에서** 나온다). **경로가 실제로 바뀔 때만** 재적용하므로 같은 경로 안에서 검토자가 손으로 켠 상태는 다른 조작(사유 코드·재안내 전환)에도 유지된다. 각주도 새 거동에 맞춰 최소 수정. **[하지 않은 것 — 근거]** ① **일괄 기본 끄기 안 함** — 경로 ① 은 값이 실제로 실리는 유일한 경로라 거기서까지 끄면 이번 개정의 핵심 신규 요소인 **진짜 조합을 감춘다** ② **토글을 잠그지 않음** — 검토자가 ②·③ 에서 직접 켜서 시연하는 것은 그대로 가능하다(리뷰 도구의 표현력을 줄이지 않는 것이 이 시정의 전제). **재안내 토글이 경로 ② 에서 `disabled` 되는 것과는 근거가 다르다** — 그쪽은 배지가 붙는 경로가 정해져 있어 잠그는 것이 사양(`EXC-BIZ-14`)이고, 이쪽은 **표시 판정이 여전히 `returnUrl` 존재 여부 하나뿐**이라(§조건부 표시 — 경로 번호로 다시 판정하지 않는다) 잠글 근거가 없다 ③ **3초 뒤 이동·소멸 거동 무접촉** — 사양이다. **[검증]**(격리 프로필 헤드리스 Chrome 150 + CDP · `file://` · 420×1000 dSF1) **거동 4항 ALL PASS** — 최초 로드 경로 ① `checked=true`·`#return-area` `display=block`·높이 **93**·보임 / **①→② 전환** `checked=false`·`display=none`·높이 **0**·숨김(**①→③ 동일**) / **②→① 복귀** `checked=true`·`display=block`·높이 **93** / **수동 토글 유지** — 경로 ② 에서 손으로 켠 뒤 **사유 코드 클릭**에도, 경로 ③ 에서 켠 뒤 **재안내 토글 조작**에도 보임 유지. **전 19개 프로브에서 `toggleDisabled=false`**(잠기지 않음을 실증) · `reannounce-toggle` 은 경로 ② 에서 `disabled=true` 로 기존 사양 그대로. **양성 대조 — 판별력 있음**: 시정 전 블롭(`36aeb39^` 의 `f8bf4fc`)에 같은 검사를 돌려 **4건 FAIL**(②전환 토글 OFF·②전환 복귀영역 숨김·③전환 토글 OFF·③전환 복귀영역 숨김), 그 밖 항목(표현력·F-5·초기 로드)은 전후 모두 PASS — **이번 결함만 정확히 집는다**. 시정 후 파일 **ALL PASS**. **1px 불변 — 제품 카드 영역 0px**: 카운트다운 `setInterval` 을 `addScriptToEvaluateOnNewDocument` 로 동결해 결정적으로 캡처하고 `main.card` 를 카드 자기 좌표계로 clip 비교 — 경로① 기본(진짜 조합) **0px**(388×404) · 경로① + 재안내 배지 **0px**(388×467) · 카드 박스 좌표 전후 동일 · 카드 내부 **19개 요소**의 기하·색·display·텍스트 상대좌표 차이 **0건**. 전체 페이지 차이 **17,828px** 은 bbox `[12,351]~[406,473]` = **이번에 고친 각주 문단 한 곳**이며 카드는 y552 부터라 무관(문단 높이·문서 높이·카드 y좌표 전후 동일 → **레이아웃 밀림 0**). **비결정성 아님** — 같은 파일 자기비교가 카드·전체 페이지 모두 **0px**(이 페이지엔 안티에일리어싱 잡음이 없다)이므로 17,828px 은 **의도한 각주 변경 그 자체**다. **F-5 동반 확인 — 조치 불요**: `replay-return-btn` 실동작 — 경로① 3.6초 대기 → 사양대로 소멸(`moved=true`·notice/link `false`) → replay 클릭 시 notice·link 복구 + 카운트다운 "3초 후 이동합니다" 초기화(수동 링크로 이동한 뒤에도 복구) · 경로②의 토글 OFF 상태 replay 는 계속 숨김(되살릴 복귀 주소가 없어 정상). **목업 원칙 실측** — CDP Network 요청 **문서 자신 1건뿐** · `link`/`script[src]`/`img`/`iframe` **0개** · 조작 요소 인벤토리 **18개 전후 완전 동일**(경로 3·사유 4·체크박스 3·replay 1·복귀 링크 1·내비 5) · CSS 변경 **0줄** · 새 토큰 0 · 오프라인 열람 유지. **[미검증]** 외부 이동 토글을 켠 실제 `location.replace` 이동(페이지 이탈이라 기본 꺼짐 상태로만 검증) · 헤드풀 렌더 · 420px 외 뷰포트 | `accountinterlockhub#474`·`#469`·`#467` | 🚧 |
| 2026-07-26 08:45 | spec ⓒ | (공통 반영) `StageTitle` 합성 간격 규칙 명문화·목업 `hidden` 무력화 시정(6회차 회귀) — 이 노드에 닿는 것은 **`SCR-004`**(`screen_SCR-004.md` · `design-system-components.md` §ResultPanel·§StageTitle · `mockup/SCR-004.html`)이며 **회귀의 발원지가 이 화면**이다 — 합성된 `StageTitle`(결과 변형)의 블록 아래 여백 `--space-lg` 와 패널이 배지에 주려던 `--space-sm` 이 **합산**돼 어느 사양에도 없는 값이 나오던 것을, `StageTitle` 이 패널 안에서 **자기 여백을 하나도 갖지 않고** `ResultPanel` 이 간격을 전적으로 지배하도록 확정했다. `ResultPanel` **간격 모델 신설** — 균일 간격을 쓰지 않고 아이콘→제목 `--space-md` · 제목→배지 **`--space-sm`**(제목 바로 아래 — §확정 결과 재안내의 배지 위치와 양립) · 배지(없으면 제목)→설명 문단 `--space-md` · 설명→다음 안내 `--space-md` · 다음 안내→복귀 안내 영역 `--space-md` + **안쪽 여백 `--space-lg`**, §복귀 안내 영역에 중복돼 있던 "위 여백" 서술은 §간격으로 일원화했다(**새 토큰·새 컴포넌트 0**). `screen_SCR-004.md` 에는 값 복제 없이 정본이 카탈로그임을 가리키는 문장만 더했다. 목업은 `[hidden]{display:none !important}` 로 **재안내 배지가 최초 결과에도 항상 노출**되던 결함이 시정되고 국소 패치 `#reason-row[hidden]` 은 흡수·제거됐다 — 결과 경로 3종·문구·복귀 이동 거동은 무변경. **[이월]** `ResultPanel` 모서리 `--radius-md` 미기재·복귀 안내 영역 내부 2px 하드코딩(토큰 미준수)은 common.md 08:45 row 참고. **⚠️ 소급 보정 entry** — 근거 커밋 **`9e6eb90`**. 이력은 공유 워킹트리 경합으로 직전 build 커밋 `fdc9f76` 에 common.md 08:45 row 만 선행 반영되고 **각 IA 역링크가 누락**돼 지금 채운다 — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-26 06:19 | spec ⓒ | (공통 반영) 횡단 국소 회귀(5회차) — `spec-policies.md` EX 카탈로그 `EX-OPS-002` 행의 **"결과 미확정" 전역 단정 제거** → **"발생 지점이 정의상 분류되지 않아 결과 확정 여부는 접점에 따라 다르다"**. 결과 안내가 이 코드를 만났을 때의 귀결을 **카탈로그가 단정하지 않고 인터페이스·프로세스·화면 사양이 정본**임이 확정됐다(발생 지점이 특정돼 단정이 참인 `EX-BIZ-003` 등 다른 행은 무변경) — common.md | `accountinterlockhub#473`·`#470`·`#472`·`#469` | ℹ️ |
| 2026-07-26 04:30 | spec ⓒ | (공통 반영) build 사양 결함 국소 회귀(🟠 2건) 중 **제목 글꼴 상충 해소**분 — `screen_SCR-004.md` §레이아웃 구성 2 를 **StageTitle(결과 변형)** 으로 **표기만** 다듬었다(`--font-size-2xl` 값·제목 문구·구조 무변경). 카탈로그 §ResultPanel 제목도 **StageTitle(결과 변형) 합성** 표기로 바뀌었으나 **크기 값이 같아 거동 변화 없음 · build 재작업 없음**(재작업이 생긴 쪽은 ProgressPanel 뿐이다). SCR-004 의 그 밖의 절은 손대지 않았다 — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-26 02:46 | build | `apps/frontend/src/` — **디자인 시스템·공통 컴포넌트 11종·FE 셸·단계 상태머신 신규 구현**(정본 `design-system.md`·`design-system-components.md`). 색 토큰 23종(결과 3경로 색 포함)·타이포·간격·모션 값을 그대로 이식(`styles/tokens.css`) — 실측 대비비 27행 재계산 전건 표와 일치. `ResultPanel`(경로별 아이콘·색·기본 제목은 `resultContent.ts`, 정본 값 그대로) + `Badge`(재안내) 신설. **복귀 이동**(경로 ① 한정)을 컴포넌트가 직접 구현 — `returnUrl` 절대 URL 재확인·1초 카운트다운(`aria-hidden`)·이력 치환(`location.replace`) 이동·수동 링크 클릭 시 즉시 이동. **대기 시간(3초)은 하드코딩하지 않고 `returnWaitSeconds` prop 필수화** — 정본(screen_SCR-004.md)을 값으로 복제하지 않기 위함이며, 호출측(`flow/InterlockJourney.tsx`)이 유일한 참조 지점으로 3 을 넘긴다. 결과 도달 시 훅이 암호값·생년월일 메모리를 폐기(`stage/useInterlockFlow.ts`). `npm run build`(`tsc -b && vite build`) 에러 0건 **[회귀 1회차]**(5건 중 결과 안내 영향분) — `ResultPanel.tsx` 의 결과 경로 조회에 **`normalizeResultPath()`**(`api/types.ts` 신설, 1~3 밖·부재 → 경로 ②) 적용 — `screen_SCR-004.md` §구현 가이드 "경로 값이 1~3 밖이거나 없으면 경로 ②로 그린다"(`SVC-005` F-003)를 그대로 구현해 `RESULT_PATH_META[resultPath]` 미가드로 인한 화면 전체 공백화(TypeError)를 차단(같은 함수를 `api/hydration.ts`·`stage/transitions.ts` 도 공유 — 진입 쪽 상세는 USR-01.md). `stage/useInterlockFlow.ts` — **수화(hydration) 경로로 곧장 `RESULT` 에 진입할 때도 암호값 메모리 폐기가 성립**하도록 시정(기존은 `applyNextView()` 경유 3경로만 폐기해 진입 경로 1건이 누락 — `DATA-001-03`, "결과 도달 시 폐기" 자가보고·commit 메시지·IA history 의 기존 단언을 사실로 회복). `ResultPanel` 제목 글꼴(`--font-size-2xl`)은 값·구조 무변경 — `ResultPanel.module.css`·`ResultPanel.tsx` 주석만 "StageTitle(결과 변형)" 표기로 정리(2026-07-26 04:30 entry 대로 재작업 없음, 재작업이 필요했던 쪽은 USR-02 소관 `ProgressPanel` 뿐). `npm --prefix apps/frontend run build` 에러 0건(자가 실측, `tsc -b && vite build`) — 작성만 수행, 리뷰·기능검증 별도 doer 대기 **[회귀 2회차]**(I-A, 코드리뷰 🔴) — **바로 위 [회귀 1회차]의 "값이 이미 2xl 이라 재작업 없음" 판단을 정정한다: 오판이었다.** `design-system-components.md` §StageTitle 이 신설한 크기 변형(기본·결과)의 목적은 값 통일이 아니라 **계약을 한 곳(StageTitle)에만 두는 것**이며("두 패널은 자기 제목 글꼴을 따로 규정하지 않고 이 컴포넌트를 합성한다 — 계약이 두 곳으로 갈리면 안 된다"), 값이 같아도 `ResultPanel` 이 자체 `<h1>`·`useStageFocus`·`.title` 을 유지하는 한 그 계약 위반은 그대로였다. 시정: `StageTitle` 에 `variant?: 'default' \| 'result'` prop 신설(`StageTitle.module.css` 에 `.titleDefault`(`--font-size-xl`)·`.titleResult`(`--font-size-2xl`) 2종만, `--font-size-lg` 변형 없음) → `ResultPanel` 이 자체 제목 마크업을 버리고 `<StageTitle title={resolvedTitle} variant="result" />` 를 합성 → `ResultPanel.module.css` 의 `.title`/`.title:focus` 삭제 → `useStageFocus.ts` 주석을 "직접 호출처는 StageTitle 하나뿐"로 정정(이전엔 StageTitle·ResultPanel 양쪽이 직접 호출한다고 서술했었다). **⚠️ 미해소 간격 판단(사양 결함 후보, 코드로 봉합하지 않음)**: `StageTitle` 은 자기 `.wrap` 에 `margin-bottom: --space-lg` 를 항상 갖고, `ResultPanel.module.css` 의 `.panel` 은 모든 자식에 `gap: --space-sm` 을 균일 적용한다 — 둘이 합성되면 제목→다음 형제 간격만 `space-lg+space-sm` 으로 다른 형제 간격(`space-sm`)과 달라진다. 두 사양 문서 어디에도 이 조합의 값이 없어 `--space-lg` 를 상쇄하는 코드를 넣지 않고(그러면 §StageTitle 이 정한 값을 조건부로 무시하는 것이라 "코드로 봉합"에 해당) `.panel` 의 `gap` 도 그대로 두었다(값을 새로 지어내지 않음) — **정본 판단 필요**(완료 보고에 사양 결함으로 별도 기재). `npm --prefix apps/frontend run build` 에러 0건 | `accountinterlockhub#491`·`#469` | 🚧 |
| 2026-07-26 00:45 | spec ⓒ | (공통 반영) 요구사항 회귀 — **복귀 이동 확정 · 결과 경로 4→3**. `service_SVC-005.md`·`process_PROC-105.md`·`screen_SCR-004.md` — 결과 경로를 **① 연동 완료 / ② 링크·복호화 오류 / ③ 수신처 전달 실패**로 **결번 없이 당겨 재번호**(경로의 뜻은 그대로)하고 `USER_DENIED` 패널을 제거했다. `MDL-009` 에 **선택 필드 `returnUrl`** 신설 — **경로 ① 한정**·절대 URL·**부재의 표현은 속성 부재**이며, **동봉 판정은 `PROC-105` `B3` 한 곳**이고 화면은 **필드 존재 여부만** 보고 이동한다(경로 번호로 다시 판정하지 않는다). `SCR-004` 에 §복귀 이동·복귀 안내 영역(고지·카운트다운·수동 이동 링크)·`Returning` 상태를 신설했고 **복귀 이동 대기 시간 3초의 정본이 이 문서**다(이동 실패는 예외가 아니라 잔류 + 수동 링크로 흡수). 신설 `BIZ-001-06`·`EXC-BIZ-15`·`BR-025`. TC `USR-05_011`~`_017` 신설 · `USR-05_002`(거부 패널) **결번** — common.md | `accountinterlockhub#469`·`#470`·`#471`·`#472`·`#474`·`#475`·`#476` | ℹ️ |
| 2026-07-25 22:00 | directing | (방향 부분 갱신) **결과 경로 4→3 축소 · 복귀 이동 = 연동 완료 한정** — 담당자 지시 `#467` journal 3285. 세 경로(연동 완료 · 링크·복호화 오류 · 수신처 전달 실패)로 줄이고 **완료만** 3초 표시 후 복귀 주소로 이동, 나머지는 **주소를 받지 못해** 잔류. `docs/prd/PRD.md` §수행 범위 6 · §미결 7 문구 정정 · §미결 10 ✅확정(**항번은 재배열하지 않음** — 후행 사양 인용 정합) · `ia/IA.md` USR-05 · `devspec/database.md` §추적 키 중복 수신(네 경로→세 경로 · 다섯째→넷째)·§결과 구분·§지표 집계 · `devspec/external-apis.md` §2 · `CLAUDE.env.md` 상수 설명. **역방향 전파 대상** = `BIZ-001` 경로 체계·`BIZ-001-06`·`BR-008`·`BR-025`·`PROC-105`·`SVC-005`·`MDL-009`·`SCR-004`·`tc_USR-05`·`mockup/SCR-004.html` | `accountinterlockhub#467`·`#469` | 🚧 |
| 2026-07-25 21:12 | directing | (방향 부분 갱신) **오류가 아닌 결과의 복귀 이동** 신설 — `docs/prd/PRD.md` §수행 범위 6(연동 완료·사용자 거부는 결과 3초 표시 후 요청 처리 응답의 복귀 주소로 자동 이동, 오류 경로 ③·④ 는 머문다) · §미결 7 신설(복귀 주소 출처 미확정)·10 신설(적용 경로 기본안) · `CLAUDE.env.md` `<COMPLETION_REDIRECT_URL>` 신설(잠정 `https://www.naver.com` — 개발 더미) · `devspec/external-apis.md` §2·§3·§미결(응답 동봉·출처 미확정) · `ia/IA.md` USR-05 설명 보강. 담당자 지시 `#467` journal 3277. **역방향 전파 대상** = `SVC-005`·`PROC-105`·`FN-014/015`·`MDL-009`·`SCR-004`·`tc_USR-05`·`mockup/SCR-004.html` | `accountinterlockhub#467`·`#469` | 🚧 |
| 2026-07-25 18:39 | spec ⓒ | (공통 반영) 사양 확정분 목업 동기화 — `mockup/SCR-004.html` 경로 ③ 은 확정 결과 재안내 대상 아님(`EXC-BIZ-14`)이 확정돼 재안내 토글을 경로③ 선택 시 `disabled`+자동 해제, 안내 문구로 대체하고 기존 노란 경고 배너 제거(해소). `mockup/INDEX.md` §사양 보완 후보 3건 "해소"로 갱신. Redmine `#477` 첨부 5건→6건 보완(누락 `index.html` 추가) — common.md | `accountinterlockhub#477` | ℹ️ |
| 2026-07-25 18:32 | spec ⓒ | (공통 반영) 목업 리뷰 발견 M-1 최종 처리 — `DECRYPT_FAILED` 레코드 미기록의 **구조적 한계 명문화(동작 무변경)**. `process_PROC-105.md` — `RECORD` 출처는 `SUCCESS`·`USER_DENIED`·`DELIVERY_FAILED` 셋만 실어 오며 `B2` `case 'DECRYPT_FAILED'` 는 **방어적 분기(도달 불가·삭제 금지)** 임을 의사코드 주석·구현 가이드에 명시 · `screen_SCR-004.md` — **경로 ③ 은 확정 결과 재안내 대상이 아니다**(배지는 ①·②·④), 상태 전이 `ReAnnounced`·§조건부 표시 동반 정정. 근거는 정책 신설 예외 `EXC-BIZ-14` — common.md | `accountinterlockhub#477`·`#474`·`#475` | ℹ️ |
| 2026-07-25 18:01 | spec | `mockup/SCR-004.html` 신규 — **연동 결과 안내 목업**. 결과 4경로(①완료·②거부·③오류·④전달실패) × 경로③ 사유 4종(`EX-SEC-001`·`EX-SEC-004`·`EX-SEC-002`·`EX-OPS-002`) × 확정 결과 재안내 배지(제목 바로 아래) on/off 전량 토글 구현, 아이콘 전량 인라인 SVG, 다섯 번째 경로 없음. **사양 보완 후보 발견**: PROC-102·103·105 교차 추적 결과 경로③(`DECRYPT_FAILED`)은 추적 키 확보 이전에 항상 확정돼 레코드가 만들어지지 않아, "경로③+재안내" 조합이 구조상 발생하지 않을 가능성 — 화면에 경고 노출 + `mockup/INDEX.md` §사양 보완 후보 1 기록(담당자 확인 요청) | `accountinterlockhub#477`·`#469` | 🚧 |
| 2026-07-25 17:36 | spec ⓒ | (공통 반영) 교차검증 2회차 경고 5건 보완(회귀 2회차 — 🟡 N-1~N-5 전건 종결) — `screen_SCR-004.md` 경로 ③ 사유 코드 목록에 `EX-OPS-002` 문구("처리 중 문제가 발생했습니다. 잠시 후 다시 시도해 주세요.") 추가 + **미등재 코드 폴백 규칙** 명시(§조건부 표시 동반 정정) · 연쇄 정정 `spec-screens.md` 이동 경로 2·`process_PROC-105.md` 3지점·`service_SVC-005.md` 예외 흐름 1행 — common.md | `accountinterlockhub#474`·`#475`·`#471` | ℹ️ |
| 2026-07-25 16:19 | spec ⓒ | (공통 반영) 검증 TC 도메인 신규 작성 — `docs/specs/qa/` **TC 232건**(커버리지 SVC 18/18 · PROC 18/18 · BR 24/24 · EX 13/13 · 정책 17/17 · 접점 11/11 — 미검증 0)·**횡단 시나리오 `SCEN_001`~`SCEN_023`**(여정 6 · 규약 대칭 왕복 3 · 기동·상수 검증 5 · 무저장 원칙 2 · 설계된 부재·수용 리스크 7)·**AUTH 치환 12건**·모의 수신처·C# 하네스·시드 카탈로그 37항·미해소 1건(성공률 정의 이월) — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-25 15:48 | spec | `docs/specs/qa/USR/tc_USR-05.md` 신규 — **연동 결과 안내 TC 10건**(웹·SPA UI). 결과 4경로 패널 각각(①완료·②거부는 중립 색·③오류·④전달 실패)·**경로 ③ 사유 3종이 같은 제목·아이콘·색으로 모임**(사유가 달라도 경로가 늘지 않는다)·**다섯 번째 경로 부재 검증**(`resultPath` 를 0·5·99·null·문자열로 변조해도 경로 ③)·확정 결과 재안내(배지 "이미 처리된 요청입니다" 위치·덧붙인 문장·전 컬럼 무갱신·무계수)·조작 요소 0(재시도·문의처·복사 부재)·내부 정보 미표시(추적 키·사유 코드·처리 일시·재시도 횟수)·표시 후 메모리 폐기·접근성(색+아이콘+문구 3중·대비 AA·`role="status"`·480/768 반응형). 검증 BR-008·BR-009 · EX `EX-BIZ-002`·`EX-SEC-001`·`EX-SEC-002`·`EX-SEC-004` | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec | `process_PROC-105.md` 신규 — **PROC-105 연동 결과 안내**(RR·동기·상위 프로세스 호출). **결과 구분 → 경로 번호 대응을 이 프로세스 한 곳에만** 둔다(화면·조회가 다시 구현하지 않는다). `B1` 네 출처(`ENTRY_FAILURE`·`REASON_CODE`·`RESULT_CODE`·`RECORD`) → `B2` 경로 1~4(미매핑은 전부 ③) → `B3` `MDL-009` 구성. **영속화·계수·결과 확인 표시(FN-010) 모두 호출하지 않는다**(`BIZ-002-04`·`BIZ-005-03`)·예외를 던지지 않는다 | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec ⓒ | (공통 반영) 프로세스 도메인 신규 작성 — `docs/specs/processes/` **PROC 18종 확정**(정책·서비스 예약분을 같은 코드·같은 의미로 — 통합·폐기·추가·재배치 0)·**단계 라벨 체계 확정**(`F` FE·`B` BE·`C` 호출측·`L` 라이브러리 내부, 레이어별 1부터 시간 순, 분기는 접미 문자)·선행 도메인 단계명 전건 라벨 매핑·의존관계 순환 0(호출 깊이 3층)·트랜잭션 경계와 멱등 수단 총괄·정책 규칙 86건 매핑(런타임 80 + 게이트·절차 6) — common.md | `accountinterlockhub#475`·`#469` | ℹ️ |
| 2026-07-25 14:40 | spec | `screen_SCR-004.md` 신규 — **SCR-004 연동 결과 안내**(단계 `RESULT`). **경로 4개를 4화면으로 나누지 않고 한 화면의 네 상태**로 확정(경로별 화면 분리는 다섯째 경로를 만들기 쉽다 — `BIZ-001-02`). 경로별 **색 + 아이콘 + 제목** 3중 구분 — ① 완료(초록·원 안 체크) ② 거부(**중립** 회청색·원 안 가로줄 — 오류 아님) ③ 링크·복호화 오류(빨강·원 안 느낌표) ④ 전달 실패(황갈·삼각 느낌표). **경로 ③ 의 설명은 사유 코드별 FN-014 기본 문구를 그대로** 쓰되 제목·아이콘·색은 동일해 경로가 늘지 않는다. **PRD 위임 ⑩ 확정 — 확정 결과 재안내 보조 표시** = 배지 "이미 처리된 요청입니다"를 **결과 제목 바로 아래·설명 문단 위**에 두고 설명 끝에 한 문장을 덧붙이며 그 밖의 차이는 없다. ②·④ 의 다음 안내는 **재연동 주체가 발송처**라는 `BIZ-004-04` 에서 도출. 액션 버튼·재시도·문의처·추적 키·사유 코드 원문·처리 일시 **전부 미도입**(근거 없음 · `DATA-001-04` · `SEC-002-05`). 502 `EX-BIZ-002` 도 정상 종료로 경로 ④ 를 그린다 | `accountinterlockhub#474`·`#469` | 🚧 |
| 2026-07-25 14:40 | spec ⓒ | (공통 반영) 화면 도메인 신규 작성 — `docs/specs/screens/` **화면 4건**(SCR-001~004 — 서비스 예약 대비 통합·폐기·추가·재배치 0)·**단일 경로 4단계 모델**(리다이렉트 없음·화면별 URL 없음)·**디자인 시스템 1벌**(표면이 하나라 토큰 분기·접두어 없음 · 색 23종 **실측 대비비 표** · WCAG 2.1 AA · 모바일 우선 2 브레이크포인트 · 히트 영역 44x44)·공통 컴포넌트 11종·**화면 없는 IA 노드 12건 근거 기재** — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-25 14:10 | spec | `spec-functions-api-user.md` — 결과 경로를 **별도 URL 없이 응답 상태 값**으로 전달하도록 확정(진입 초기 상태 · `verify`/`approve` 응답의 `resultPath`·`isReAnnouncement` — `MDL-009` 와 같은 값 체계). 502 `EX-BIZ-002` 도 오류 엔벨로프의 `code` 로 화면이 경로 ④를 고르게 해, **결과가 확정된 정상 종료**임을 인터페이스 수준에서 구분. 새로고침은 세션이 없어 진입부터 다시 시작하며 확정된 연동은 `verify` 가 재안내를 돌려준다. 결과 구분 → 경로 번호 대응은 단일 서비스 로직이라 **FN 미신설**(`PROC-105` 담당 — 대응을 여러 곳에 두면 경로가 갈린다). 안내에 원문·암호값·생년월일·키 미포함(FN-015) | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-25 14:10 | spec ⓒ | (공통 반영) 기능·API 도메인 신규 작성 — `docs/specs/functions/` **공통 기능 15건**(FN-001~015 · POL 5·DAT 8·CRS 2·**EXT 0**)·**인터페이스 11접점**(사용자 진입 표면 3 · 서버 대면 API 4 · 수신처 전달 1 · 명령 진입점 1 · 라이브러리 공개 진입점 2)·오류 응답 엔벨로프 `{code,message,details?}`·인증 0/요청 제한 0·**신규 EX `EX-OPS-002` 1건**을 정책 카탈로그에 등록 — common.md | `accountinterlockhub#473`·`#469` | ℹ️ |
| 2026-07-25 13:35 | spec ⓒ | (공통 반영) 데이터 도메인 신규 작성 — `docs/specs/datas/` **엔터티 3건**(ENT-001~003 = 저장 대상 셋)·**데이터 모델 22종**(MDL-001~022 확정 — 예약 대비 통합·추가·재배치 0)·인덱스 5·물리 FK 0·소프트 삭제 미도입 — common.md | `accountinterlockhub#472`·`#469` | ℹ️ |
| 2026-07-25 13:05 | spec | `service_SVC-005.md` 신규 — **SVC-005 연동 결과 안내**(매핑 PROC-105). Happy Path 4단계(결과 확보→경로 선택→안내 구성→확인·종료). BR-008 = 결과 구분 4종 → 4경로 매핑, BR-009 = **확정 결과 재안내**(같은 경로 재표시 + 보조 표시·요청 수 미계수·레코드 미갱신), BR-002 인용. 경로 ③ 은 두 출처(진입 단계 실패 · `DECRYPT_FAILED`)를 받으므로 **결과 구분 값 없이도 그릴 수 있어야 한다**. 예외 흐름은 유입 EX 4종(`EX-SEC-001`·`EX-SEC-004`·`EX-SEC-002`·`EX-BIZ-002`)의 경로 귀속으로 정의. 문구·표시 형식·화면 분할은 화면 도메인 소관으로 명시(SCR-004 예약) | `accountinterlockhub#471`·`#469` | 🚧 |
| 2026-07-25 13:05 | spec ⓒ | (공통 반영) 서비스 도메인 신규 작성 — `docs/specs/services/` **서비스 18건**(SVC-001~018)·사용자 역할 4종·분기 `BR-001`~`BR-024`·MDL 22종/SCR 4종 예약 채번·`PROC-403`·`PROC-404` 추가 채번 — common.md | `accountinterlockhub#471`·`#469` | ℹ️ |
| 2026-07-25 12:28 | spec | `policy_BIZ.md` `BIZ-001-02`·`BIZ-002-04` 신설 — 결과 화면은 **4경로뿐**이며 결과 구분 4종과 1:1 매핑(① 연동 완료←`SUCCESS` ② 사용자 거부←`USER_DENIED` ③ 링크·복호화 오류←`DECRYPT_FAILED` **및 추적 레코드가 만들어지지 않은 진입 단계 실패** ④ 수신처 전달 실패←`DELIVERY_FAILED`). **다섯 번째 경로 신설 금지.** 확정 결과 재진입은 해당 경로를 다시 표시하고 "이미 처리된 요청"을 **보조 표시**로 알리며, 지표 요청 수 미계수·추적 레코드 미갱신(보관 기산점 불변). 경로 ③ 은 결과 구분 없이도 그릴 수 있어야 한다 | `accountinterlockhub#470`·`#469` | 🚧 |
| 2026-07-25 12:28 | spec ⓒ | (공통 반영) 정책 도메인 신규 작성 — `docs/specs/policies/` 정책 17건·규칙 86건·EX 코드 12종(암호화 연동 규약·복호화 판정 4단계·결과 구분 4종·추적 레코드 생성 시점·보관·무저장·동의 증적·인증 부재·수용 리스크) — common.md | `accountinterlockhub#470`·`#469` | ℹ️ |
| 2026-07-25 10:05 | directing ⓒ | (공통 반영) 리셋 방향 담당자 확정분 반영 — 결과 안내를 **신설**(구 `USR-02` 에서 분리). 4경로 확정(완료·사용자 거부·링크/복호화 오류·전달 실패)·확정 결과 재진입 시 동일 경로 재안내(경로 추가 없음) — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
