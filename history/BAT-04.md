# IA: BAT-04 — 연동 추적 기록

> 본 IA 노드와 연관된 작업의 시계열 이력(최신순). 정책: ai/strategies/ia-history.md

| 일시 (KST) | 단계 | 산출물·결과 | 관련 일감 | 상태 |
|---|---|---|---|---|
| 2026-07-26 13:58 | spec | `docs/specs/processes/process_PROC-301.md`(§진입점 bullet 신설·실행 제약사항·`B3`·`B4` 의사코드·단계 통합 흐름 표) · `docs/specs/functions/function_FN-007-008.md`(`FN-007` §시그니처 bullet 신설 · `FN-008` 단계 5) · `docs/specs/functions/function_FN-009-011.md` `FN-009` 단계 3 — **실행 문맥(`exec`) 릴레이 사슬의 한가운데를 이었다**(13:52 회차와 같은 배경·같은 commit — 8파일이 호출 지점을, 이 3파일이 중간 계층을 맡는다). **이력·산출물 동일 commit**(작성 시점 미커밋 — 총 11파일). **[① 사슬이 여기서 끊겨 있었다]** `PROC-301` 은 10:42 회차에서 `exec` 를 **받는** 표기(§입력/출력 정의 — `LOOKUP` 외 전부 필수)를 이미 세웠는데 **하위로 넘기는 표기만 빠져** 있었다 — 받아 놓고 어디로 흘리는지가 없으면 참여는 호출측 한 칸에서 멈춘다. `B3` 을 `PROC-303({ kind: 'REQUEST', at, exec })`, `B4` 를 `PROC-303({ kind: 'RESULT', resultCode, at, exec })` 로 고치고 **경계를 여는 자리가 어디인지**를 각 주석에 못박았으며(`B3` → `PROC-102` `B6`·`PROC-103` `B3` / `B4` → `PROC-104` `B6` · 본 프로세스는 참여만 한다), §실행 제약사항에 **"받은 `exec` 를 하위 FN(`FN-007`~`FN-011`)과 `PROC-303`(`B3`·`B4`)에 그대로 넘긴다"** 를 명시했다. 단계 통합 흐름 표 `C1` 행도 "`kind`·추적 키·시각·`exec` 전달"로 정합. **§입력/출력 정의는 무접촉** — 이미 있던 것이 증거이자, 이번에 고친 것이 **수신이 아니라 중계**임을 그대로 보여 준다. **[② 시그니처 형식이 갈리는 이유를 적었다]** `FN-008` 단계 5·`FN-009` 단계 3 은 주석으로는 "`exec` 와 같은 실행 문맥에서 수행한다"를 **이미 못박아 두고 인자 목록에는 `exec` 가 없던** 자리라 `FN-013({ … }, exec)` 로 통일했다 — **`FN-013` 의 실행 문맥은 계기 객체가 아니라 두 번째 인자**(`function FN-013(event: MetricEvent, exec?: TxExecutor)` — §시그니처 실측)이기 때문이다. 반면 `PROC-301`·`PROC-303` 은 **단일 객체 시그니처**라 같은 전달이 `{ …, exec }` 형태로 나타난다. 두 표기를 섞으면 `exec` 가 계기 객체 안으로 들어가 **타입은 통과하되 실행자가 전달되지 않는** 조용한 실패가 되므로, 형식 차이를 주석에 남겨 다음 사람이 한쪽으로 정렬하지 않게 했다. **[③ 넘기지 않는 것이 옳은 자리 = `LOOKUP` 하나]** `PROC-301` §진입점과 `FN-007` §시그니처 **두 곳**에 그 호출 지점(`PROC-201` `B3` · `PROC-202` `B3` · `PROC-203` `B3` → `PROC-301` `B2`)이 **아직 경계를 열지 않은 자리라 넘길 실행 문맥이 없음**과 **표기 누락이 아니라 사양이며 나중에 채워 넣지 않는다**를 적었고, `FN-007` 쪽에는 **"`exec` 를 필수 인자로 바꾸지 않는다"** 까지 넣어 **역방향 훼손**(선택 인자를 필수로 조여 경계 없는 자리에 경계를 요구하게 되는 것)을 봉쇄했다. **[④ Resolved Phase 영향 판정 — (가) 표기만·구현 무영향]** `FN-007`~`FN-013` 은 **P04(`#481`)**, `PROC-301` 디스패처는 **P06(`#483`)** 로 **둘 다 🟢 Resolved** 인 범위라 오케스트레이터가 구현을 **직접 읽어** 판정했다 — `apps/backend/src/records/tracking-record.service.ts:96` 의 `recordEvent({ kind: 'REQUEST', at }, executor)` 가 `FN-008` 단계 5 개정분과 **문자 그대로 동일**, 같은 파일 `:148` 의 `recordEvent({ kind: 'RESULT', resultCode, at }, executor)` 가 `FN-009` 단계 3 개정분과 동일, `:44` 의 `lookup(trackingKey, executor = this.db)` 는 **선택·기본값**이라 `FN-007` 의 "`LOOKUP` 만 `exec` 없이"와 일치하며, `secure`·`fixResult`·`confirmResult`·`recordCallback`·`recordConsentProof` 는 전건 **executor 첫 필수 인자**, `metric-counter.service.ts:44` 의 `recordEvent(event, executor = this.db)` 는 **두 번째 위치·선택**으로 위 ② 의 형식 차이와도 정확히 맞는다. **`fdc9f76`(P04 회귀 1회차)가 뒤집은 트랜잭션 소유권 결정을 되돌리지 않았다** — 표기를 그 구현에 맞춘 방향이며, 이 노드 12:51 build row 가 적은 `B1` 의 런타임 재검증(`exec` 필수 여부를 `LOOKUP` 만 선택으로 강제)도 같은 규칙을 이미 코드로 들고 있었다. 즉 **사양이 구현을 뒤늦게 문서화한 것**이라 **P04·P06 재검증은 불요**다. **의사코드 로직·`ON CONFLICT … DO NOTHING RETURNING` 수렴·조건부 UPDATE·멱등 규약·TC 는 무접촉** · 호출 지점 쪽 반영은 USR-04.md·USR-02.md·API-01.md·API-03.md·BAT-05.md·BAT-06.md 의 13:52 항목, `FN-008` 의 본인확인 몫은 USR-03.md 항목 참고 | `accountinterlockhub#475`·`#473`·`#469` | 🚧 |
| 2026-07-26 12:51 | build | `apps/backend/src/records/tracking-record.process.ts`(신규) — **P06 PROC-301 kind 디스패처 최초 구현**. FN-007~011(P04, `tracking-record.service.ts`)이 이미 `B2`~`B6` 각 단계의 구현이었으나 process_PROC-301.md §진입점의 다섯 계기(`LOOKUP`·`SECURE`·`FIX_RESULT`·`CONFIRM_RESULT`·`RECORD_CALLBACK`)를 한 곳에서 받는 진입점(`B1` 입력 재검증 + `B7` 계기별 반환 조립)은 없었다 — `TrackingRecordProcessService.record(input)` 로 신설하고, `B1` 은 `kind` 열거형·`exec` 필수 여부(`LOOKUP` 만 선택)를 런타임으로도 재검증(`RecordWriteError` 신설 사유 `TRACKING_RECORD_INVALID_KIND`·`TRACKING_RECORD_MISSING_EXEC`)한 뒤 기존 서비스 메서드로 위임만 한다(계층 분리 — resultCode 3종 검증은 `fixResult()` 자신의 기존 판정을 재사용하고 중복 두지 않았다). 식별 유니온 `TrackingRecordInput`·`TrackingRecordOutput`(`tracking-record.types.ts` 확장)이 kind 별 `exec`·`at` 필수 여부를 컴파일 타임에도 강제한다. **PROC-302·PROC-303 은 이런 별도 디스패처가 불필요함을 확인**(진입 계기가 하나뿐이거나 FN 시그니처 자체에 kind 분기가 이미 있어 `ConsentProofRecordService.recordConsentProof()`·`MetricCounterService.recordEvent()` 가 이미 그 프로세스 전체다 — 상세 판단 근거는 각각 BAT-05.md·BAT-06.md 항목). `records.module.ts`·`records/index.ts` 에 신규 서비스 등록·배럴 반영. **[P02 코드리뷰 인계 사항 해소 — `#483` journal 3417]** ① `AppModule` 의 `DatabaseModule`·`RecordsModule` 배선은 **P04(`c27245f`)가 이미 선점 수행**(실측 확인 — `AppModule.imports` 에 두 모듈이 이미 등재돼 있었다. `database.module.ts`·`records.module.ts` 자체 주석이 "아직 등록 안 함/후속 Phase 배선"이라 적어 둔 것은 갱신되지 않은 문서 표류였다) → **무변경(추가 조치 불요)**. ② **DB 접속 실패의 기동 검증 정책 결정** — `database.config.ts` 에 근거 3곳(process_PROC-901.md §관련 사양 코드 "DB 엔터티: 없음"·§실행 제약사항 "데이터베이스를 쓰지 않는다"·§의존 프로세스 "데이터베이스 상태에 의존하지 않는다")을 명시하고 **② 별도 실패 경로로 결정**(EX-OPS-001/PROC-901 에 편입하지 않는다) — `MDL-022`·`MissingConstantKey`·`CLAUDE.env.md` §연동 구성 상수 어디에도 DB 키가 없다는 사실과 정확히 정합한다. `main.ts` 에 명시적 catch 배선(정리된 중단 메시지)은 **이 Phase 시점에 다른 세션이 그 파일을 편집 중이라(작업 규율상 미커밋 변경 위 수정 금지) 보류** — 현재도 `NestFactory.create()` 중 `DatabaseService` 생성자 예외가 처리되지 않은 프라미스 거부로 프로세스를 종료시켜 "부분 기동 안 함" 결과 자체는 유지되나 출력이 정돈되지 않는다(담당자 확인 필요 — 완료 보고 참고). **실측(실 DB, 호출측이 `pool.connect()`+`BEGIN` 을 직접 여는 패턴 — `db.withTransaction()` 미사용)**: `works/accountinterlockhub-483/verify.js` 34/34 PASS — ① SECURE 최초 생성(`isCreated:true`)·재호출 이어쓰기(`isCreated:false`, 무갱신) ② FIX_RESULT 최초 확정 + **확정 후 다른 resultCode 로 재진입해도 레코드·지표(`success`·`delivery_failed` 모두) 전량 무갱신**(제약 "확정 결과 재진입은 지표 요청 수에 계수하지 않고 추적 레코드도 갱신하지 않는다" 직접 확인) ③ CONFIRM_RESULT·RECORD_CALLBACK 멱등 ④ **동시 8건 경쟁**(전원 커넥션 확보 후 barrier 로 동시 진입) — 예외 0·`isCreated:true` 정확히 1건·REQUEST 카운터 정확히 1·최종 레코드 1행 ⑤ **호출측 의도적 실패 → ROLLBACK** — 추적 레코드 0행·지표 행 자체 부재(레코드·지표 동일 경계 원복) ⑥ `UNIDENTIFIED_FAILURE`(exec 생략) 풀 단독 원자 갱신 ⑦ `kind` 오값·`exec` 누락 시 명확한 `RecordWriteError` 로 즉시 실패 ⑧ **커넥션 점유 수** — 전용 `max:2` 풀로 2 동시 요청(각자 PROC-301 왕복 전 구간)이 전부 완주(요청 하나가 커넥션을 둘 점유했다면 데드락·타임아웃 났을 조건) + 3번째 요청도 이어서 완주 + 종료 후 idle=total(누수 0). PROC-302 검증(FOR UPDATE 잠금 흉내·스냅샷 재정렬 없음·동시 증적 2건 공존·롤백)은 BAT-05.md, 로케일 4종 무관성 검증은 BAT-05.md 항목 참고. 데이터는 `p06v483_` 접두어·2097-03-15~23 합성 일자로 격리, 종료 후 추적 레코드·동의 증적·지표 집계 3종 전량 잔존 0행 재확인. `npm --prefix apps/backend run build` 에러 0(재확인 완료) | `accountinterlockhub#483`·`#469` | 🚧 |
| 2026-07-26 10:42 | spec | `docs/specs/processes/process_PROC-301.md` · `docs/specs/functions/function_FN-009-011.md` — **트랜잭션 소유권·실행 불가 의사코드의 프로세스·기능 연쇄 정정**(commit `1491e4f` SD-1·SD-2 확산분). **근거 commit `b4fe224`** — 본 entry 작성 시점엔 미커밋이었고, 오케스트레이터가 산출물과 이 이력을 같은 commit 에 함께 넣었다(정책대로 동시 반영). **[① `PROC-301` `B3` 에 같은 실행 불가 의사코드가 남아 있었다]** `FN-008` 에서 정정한 "INSERT → 기본 키 충돌 catch → 같은 트랜잭션에서 재조회" 형태가 프로세스 쪽에 그대로 있어, 문 하나가 오류로 끝나면 트랜잭션이 **중단 상태**가 되어 되감기 전에는 뒤따르는 조회가 아예 실행되지 않는 채로 남아 있었다. **`INSERT … ON CONFLICT (tracking_key) DO NOTHING RETURNING tracking_key`** 로 정정 — 반환 행 수 0 이면 `B2` 를 재수행해 이어쓰기(`BIZ-002-03` ②)로 수렴하고, 충돌이 오류로 올라오지 않으므로 **같은 경계가 살아 있는 채로** 재조회가 이어진다. §실행 제약사항 동시성 제어 항목에 **반환 행의 유무가 곧 `isCreated`** 임을 명시하고, §구현 가이드 "기본 키 충돌을 오류로 다루지 않는다"에 **예외 포착 방식이 왜 아예 실행되지 않는지**를 근거로 붙여 되돌아가지 않게 했다. **[② 실행 문맥 전달을 못박음]** 입출력 정의에 `exec: TxExecutor` 를 추가(**`LOOKUP` 외 전부 필수** — 본 프로세스의 모든 문과 하위 FN(`FN-007`~`FN-011`·`FN-013`)을 이 실행자 위에서 수행하며 커넥션을 스스로 얻지 않는다. `LOOKUP` 은 주면 그 경계 안에서, 없으면 단독 읽기)하고, `C1` 전달 값·트랜잭션 규약에 **"상위가 `BEGIN` 으로 경계를 열고 그 커넥션·실행자를 `exec` 로 함께 넘긴다 — 넘기지 않으면 별도 커넥션에서 실행돼 참여가 성립하지 않는다"**(`LOOKUP` 은 경계 없이도 성립)를 명시했다. **[③ `FN-009`·`FN-010`·`FN-011` 소유권 표기 통일]** 세 기능 모두 시그니처에 `exec` 필수 인자 추가 + 입출력 정의 행 추가, "(트랜잭션 시작)"·"(트랜잭션 종료)" 주석을 **"(호출측 트랜잭션에 참여 — 새로 열지 않는다)"·"(커밋하지 않는다 — 경계를 닫는 것은 호출측이다)"** 로 교체, **경계를 여는 자리를 기능별로 특정**(`FN-009`→`PROC-104` `B6` · `FN-010`→`PROC-201` `B5` · `FN-011`→`PROC-203` `B4` — `PROC-301` `B4`·`B5`·`B6` 은 그 경계에 참여만 한다), 하위 `FN-007`·`FN-013` 호출도 같은 실행 문맥에서 수행함을 의존 기능표 아래에 명시, 오류 처리표의 "지표 갱신도 함께 되돌린다"를 **"본 기능은 되돌리지 않고 예외만 올린다 — 되돌림은 경계를 연 호출측"** 으로 정정했다. **[신설 0]** PROC·FN·EX·MDL·정책 규칙 무신설 · 실질 거동 변경은 ① 하나이고 나머지는 소유권 표기 정합 · 격리 수준은 `READ COMMITTED` 를 전제로 두되 그 위로 올리지 않는다(충돌 재조회의 성립 전제) | `accountinterlockhub#475`·`#473`·`#469` | 🚧 |
| 2026-07-26 10:26 | spec ⓒ | (공통 반영) 사양 결함 7건(SD-1~SD-7) 정정 — `function_FN-007-008.md` `FN-007`·`FN-008` 의 **트랜잭션 소유권을 호출측으로 통일**(SD-1)하고 `FN-008` 단계 4 의 **PostgreSQL 에서 실행 불가한 의사코드를 정정**(SD-2). `FN-008` 이 단계 4·6 을 "트랜잭션 시작"·"트랜잭션 종료"로 적어 스스로 경계를 소유하는 것으로 읽혔는데 `PROC-301` 은 "호출측 트랜잭션에 참여한다·새 트랜잭션을 열지 않는다"를 명시하고 있었고, **build 에서 커넥션 풀 교착으로 실현된 건**이다 — `PROC-301` 을 정본으로 삼아 표기를 통일하고 참여의 성립 조건인 **실행 문맥(`exec`)** 을 `FN-008` 필수 인자로(`FN-007` 은 조회 전용 접점의 기존 호출을 깨지 않도록 **선택 인자**) 명시했다. 의사코드는 **`ON CONFLICT (tracking_key) DO NOTHING RETURNING`** 으로 바꿔 **반환 행의 유무가 곧 `isCreated`** 가 되게 했고, `SAVEPOINT`(경계 중첩)·트랜잭션 밖 재조회(원자성 파괴 — `SVC-014` F-008) 미채택 근거를 구현 가이드에 남겼다. 근거 commit `1491e4f`(이력 누락 보정) — common.md | `accountinterlockhub#473`·`#472`·`#470`·`#469` | ℹ️ |
| 2026-07-26 08:45 | build | `apps/backend/src/records/{tracking-record.service.ts,records.errors.ts,metric-date.ts,consent-proof-record.service.ts,index.ts}` — P04(`#481`) **회귀 1회차**([I-1]·[I-2]·[I-3]·자체보완 S-1~S-4 시정, 상한 3 이내). **[I-1] 🔴 트랜잭션 소유권 역전** — `secure()`·`fixResult()`·`confirmResult()`·`recordCallback()` 4메서드가 자기 트랜잭션(`db.withTransaction()`)을 여는 설계였던 것을 **호출측이 이미 연 트랜잭션에 참여**하는 형태로 뒤집었다(FN-012·FN-013 이 이미 쓰던 `executor` 필수 첫 인자 패턴과 통일). 근거는 리뷰어 실측 반증 — `function_FN-009-011.md` `FN-010` 에도 "(트랜잭션 시작)"/"(트랜잭션 종료)" 주석이 있는데 `process_PROC-301.md` 는 그 같은 단계(`B5`)를 "(트랜잭션 참여)"로 적고, `B3`(FN-008)~`B6`(FN-011) 전부가 "트랜잭션 참여"이며 프로세스 자신도 "새 트랜잭션을 열지 않는다"고 명시한다 — FN 문서의 그 주석은 **원자 경계 표시일 뿐 소유권 선언이 아니고, 소유자는 언제나 호출측**(`PROC-102` `B6`·`PROC-103` `B3`·`PROC-104` `B6`·`PROC-201` `B5`·`PROC-203` `B4`, 전부 후속 Phase)이라는 뜻이었다. **`secure()` 의 INSERT 도 `INSERT ... ON CONFLICT (tracking_key) DO NOTHING` 로 교체** — 일반 INSERT 의 기본 키 충돌 오류는 그 트랜잭션을 즉시 중단시켜(PostgreSQL "current transaction is aborted") 호출측 트랜잭션 **안에서는** 이어 조회할 방법이 없었는데, `DO NOTHING` 은 충돌을 오류로 만들지 않아 같은 executor 로 그대로 재조회해 이어쓰기로 수렴시킬 수 있다(data_ENT-001.md §구현 가이드 "기본 키 충돌을 정상 경로로 다룬다"를 오류 없이 만족). **[동시성·원자성 재실측]** `works/accountinterlockhub-481/verify.js` 를 "각 시뮬레이션 요청이 `db.withTransaction()` 으로 독립된 BEGIN 을 여는" 형태로 다시 짜 재현 — 동시 8개의 **독립 트랜잭션**으로 `secure()` 경쟁 → 행 1·요청수 +1, `fixResult()` 8개 경쟁(SUCCESS/DELIVERY_FAILED 혼합) → 한 값 수렴·카운터 +1, 지표(poisoned `MetricCounterService`) 유도 실패 시 **호출측이 연 `withTransaction()` 전체가 롤백**됨을 재확인(고아 레코드 0). **58/58 관측점 일치**(이전 50 + 재설계 확대분, 확정 결과 재진입 무갱신·small-icu 무관 일자 결정성 케이스 추가). **[I-2] 🔴 증적 스냅샷 정렬 콜레이션 불일치**는 BAT-05.md 항목 참고. **[I-3] 이력 재배치** — `history/common.md` 의 P04 단일 entry 를 제거하고 본 entry 를 포함해 영향 IA 8개 파일(BAT-04·05·06·USR-02·03·04·API-01·03)에 정식 entry 로 분리했다. **[S-1]** `RecordWriteError` 하드코딩 사용자 문구 제거 — `common/errors/ex-catalog.ts`(P05 FN-014)가 이제 `exCode` 하나로 문구를 다시 계산하므로 `Error.message` 는 `reason`(내부 진단 코드)만 담는다(두 벌 방지). **[S-2]** `confirmResult()` 주석의 "FN-013 연계가 없는 유일한 조건부 갱신" 서술 정정(`recordCallback()` 도 FN-013 연계가 없다 — 사실 오류였다). **[S-3]** `recordCallback()` 의 `existing.record!.callbackReceivedAt as Date`(비-null 단언+무검사 캐스트)를 `existing.record === null \|\| existing.record.callbackReceivedAt === null` 명시적 검사로 교체(상세는 API-03.md 항목). **[정리]** `pg-error.util.ts`(`isUniqueViolation`) — `ON CONFLICT DO NOTHING` 채택으로 용도가 사라져 죽은 코드로 남기지 않고 삭제(barrel export 도 함께 정리). **[실측]** `npm --prefix apps/backend run build` 0에러(재확인 2회) · 실 `NestFactory.create()` 부팅(포트 48302, `RecordsModule`·`CommonModule`(P05) DI 정상 초기화 로그 확인 후 정상 종료) · 검증 데이터는 `test-481-` 접두어·합성 2099년 일자로 격리, 정리 후 3테이블 0행 재확인(잔존 테이블 7종 무접촉) **[사양 델타 정합 확인·시정 — `1491e4f`+`b4fe224` 후속, 2026-07-26]** P04(`#481`)·P01(`#478`) 가 `해결` 상태에 도달한 뒤 동시 진행 spec 세션이 트랜잭션 소유권·정렬 기준을 정정(`1491e4f` SD-1~3, `b4fe224` `FN-009`~`013`·`PROC-301`·`302` 확산분)해 판정 근거가 바뀐 데 따른 재확인. **SD-1**(`exec` 필수/선택) — `secure()`·`fixResult()`·`confirmResult()`·`recordCallback()` 는 회귀 1회차(`fdc9f76`)에서 이미 `executor` 필수 인자로 시정돼 있어 **충족 확인(무변경)**, `lookup()` 도 `exec?: TxExecutor` 선택 인자·기본값(`= this.db`, 생략 시 풀 단독 조회)과 **충족 확인**. **SD-2**(`ON CONFLICT DO NOTHING`) — `secure()` 의 INSERT 는 이미 `ON CONFLICT (tracking_key) DO NOTHING` + `rowCount` 판정 형태라 try/catch 재조회 패턴이 없고 **충족 확인**. **변경 1건**: `metrics.recordEvent()` 호출부 2곳(`secure()`·`fixResult()`)을 새 시그니처(이벤트 인자 우선)에 맞춰 인자 순서만 조정(`FN-013` 자체 변경은 BAT-06.md 항목). **실측(실 DB — 호출측이 직접 `pool.connect()`+`BEGIN` 을 여는 패턴으로 재현, `db.withTransaction()` 미사용)**: ① 정상 커밋 — `secure()` 커밋 전 별도 커넥션에 비가시(격리 확인)·커밋 후 가시·REQUEST 카운터 델타 정확히 +1 ② 호출측 `ROLLBACK` — `secure()`+`fixResult()` 결합 후 되돌리면 레코드·지표 전량 원복 ③ **동시 PK 충돌(SD-2 핵심 재현)** — A 가 미커밋 INSERT 를 쥔 상태에서 B 가 같은 키로 `secure()` 진입 → B 는 300ms+ **블록**(A 커밋 전 미해결 확인) → A 커밋 후 B 가 예외 없이 이어쓰기(`isCreated:false`)로 수렴하고 **B 트랜잭션이 abort 되지 않아** 추가 질의·`COMMIT` 이 정상 성공(`current transaction is aborted` 미재현) — 최종 행 1개·REQUEST 델타 +1(중복 계수 없음) ④ **커넥션 풀 무교착** — `pool.max=4` 로 4개 동시 요청(각자 커넥션 1개만 점유한 채 `secure`+`fixResult` 전 구간 수행)이 전부 완주(2차 커넥션을 요구했다면 `connectionTimeoutMillis` 로 실패했을 조건) ⑤ 회귀 스모크 — `lookup()` 기본 풀 읽기·`confirmResult()`·`recordCallback()`·확정 후 다른 값 재시도 무갱신 전부 정상. 검증 스크립트 `works/accountinterlockhub-481/verify-spec-delta.js`(11케이스 전부 PASS) — 데이터는 `sd481v_` 접두어·2088년 합성 일자로 격리, 종료 후 3테이블 0행 재확인. P01(`#478`, `interlock-config.loader.ts`/`consent-version.ts`)은 정렬 단일 지점·개행 정규화를 이미 정확히 구현하고 있어 **무변경(충족 확인)** — SD-3 상세는 BAT-05.md 항목 참고 | `accountinterlockhub#481`·`#478`·`#469` | 🚧 |
| 2026-07-26 02:10 | build | `apps/backend/migrations/0001_create_storage_tables.{up,down}.sql`(ENT-001 DDL 포함)·`apps/backend/src/entities/interlock-tracking.{entity,model}.ts`(MDL-001)·`apps/backend/src/database/**`(마이그레이션 실행기·DB 접속 공통 계층, ENT-001~003 공유) 신규 — P02 DB 스키마·마이그레이션·저장 도메인 모델. `tbl_interlock_tracking` 정확히 6컬럼 그대로 구현: PK `tracking_key`(VARCHAR(255))·CHECK `ck_result_pair`(결과 구분·처리 일시 동반)·`ck_interlock_tracking_result_code`(3종 열거형, `USER_DENIED` 없음)·`ck_interlock_tracking_tracking_key_length`(1자 이상). 인덱스 2 = `idx_interlock_tracking_result_confirmed_at`(부분)·`idx_interlock_tracking_created_at`. MDL-001 파생 4종(`isResultFixed`·`isSuccess`·`isResultConfirmed`·`isCallbackReceived`)은 컬럼화하지 않고 `toInterlockTrackingModel()`(ENT→도메인)에서 매 조회 시 산출. **마이그레이션은 이력 테이블 없이 전 DDL을 `IF NOT EXISTS`/`IF EXISTS`로 멱등 구현**(BAT-04_007 "저장 대상 3 테이블 외 구성 테이블 없음" 대응 — 별도 트래킹 테이블 자체를 두지 않았다). **실측**: 도커 `pg-AccountInterlockHub`에 `up`→`down`→`up` 3회 실접속 적용, 컬럼·제약·인덱스 전수 조회로 확인 + `ck_result_pair` 4조합·`USER_DENIED` 거부·`tracking_key` 경계(1/255/256자·공백만)를 실제 INSERT 로 검증(전부 기대대로 거부/허용). ⚠️ **발견(담당자 확인 필요)**: 같은 DB에 **프로젝트 리셋 이전 구(TypeORM) 스키마 7개 객체**(`TBL_ADMIN_ACCOUNT`·`TBL_AUDIT_LOG`·`TBL_INTERLOCK_CONFIG`·`TBL_INTERLOCK_CONSENT_ITEM`·`TBL_INTERLOCK_HISTORY`·`TBL_INTERLOCK_PROCESS_STATUS`·`migrations_history`, 실데이터 894행)가 삭제되지 않고 잔존 — 폐지 IA(`ADM`·`BAT-01`·`BAT-03`)에 대응하는 잔재로 추정된다. 저장 3종 원칙 실측 검증(BAT-04_007 "테이블 목록 정확히 3개")이 **현재 DB 상태로는 실패**하며, 본 Phase 는 신규 3테이블 생성만 수행하고 구 테이블 삭제는 범위 밖으로 남겼다. **회귀 1회차**: 독립 코드리뷰 [I-1](MDL-002 "읽기 전용 구조" 미이행) 시정 상세는 BAT-05.md 항목 참고. **훑음**: `model_MDL-001.md` §구현 가이드 "파생 속성은 읽기 전용으로 노출" 지시는 `interlock-tracking.model.ts` 파생 4종(`isResultFixed`·`isSuccess`·`isResultConfirmed`·`isCallbackReceived`)에 이미 `readonly` 로 적용돼 있어 변경 없음(재확인만). 자체보완 — 루트 `package.json` 에 `migration:revert` 프록시 스크립트 신설(`migration:run` 과 동일 형태 — 백엔드 `package.json` 은 기존재, 다른 스크립트 무변경). DATE 파서 주석 정정은 BAT-06.md 항목 참고. **실측**: `npx tsc --noEmit`(backend) exit 0 · 루트 `npm run build` 에러 0건 — DDL·마이그레이션 로직 무변경 | `accountinterlockhub#479`·`#469` | 🚧 |
| 2026-07-26 00:45 | spec ⓒ | (공통 반영) 요구사항 회귀 — **`ENT-001.result_code` CHECK 열거형 4종→3종**(`USER_DENIED` 제거). `data_ENT-001.md`·`service_SVC-014.md`·`process_PROC-301.md` — 미동의 제출은 동의 화면에서 막혀 결과 단계로 가지 않으므로 확정할 결과가 생기지 않고(`BIZ-003-03`), 도달할 수 없는 값을 제약에 남기면 발송처가 쓸모없는 처리 분기를 만든다는 근거를 속성 주석에 남겼다. 결과 확정 계기 목록에서 **미동의 이탈이 빠져** 그 연동은 결과 미확정으로 남아 **생성 기산 보관 상한**으로 삭제된다. TC `tc_BAT-04.md` 열거형·계기 기대값 동반 정정(TC 신설·결번 0) — common.md | `accountinterlockhub#469`·`#470`·`#471`·`#472`·`#475`·`#476` | ℹ️ |
| 2026-07-25 18:32 | spec ⓒ | (공통 반영) 목업 리뷰 발견 M-1 최종 처리 — `DECRYPT_FAILED` 레코드 미기록의 **구조적 한계 명문화(동작 무변경)**. `data_ENT-001.md` — `result_code` CHECK 열거형 **4종 무변경**, "이 컬럼에 `DECRYPT_FAILED` 가 기록되는 경로가 없다"는 사실과 근거(추적 키 미확보 실패 3자리·값 체계 공유 `BIZ-001-03`)를 속성 주석에 명시하고 §데이터 생명주기 결과 확정 계기에 "복호화 실패는 계기 목록에 없다" 병기. 근거는 정책 신설 예외 `EXC-BIZ-14` — common.md | `accountinterlockhub#477`·`#472` | ℹ️ |
| 2026-07-25 17:01 | spec ⓒ | (공통 반영) 교차검증 1회차 지적 보완(회귀 1회차 — 🟠 2·🟡 7 전건 해소) — `SVC-014` Happy Path 5 의 `PROC-303` 단계명을 확정명(**요청 수 계수 · 결과 구분 카운터 갱신**)으로 정정 — common.md | `accountinterlockhub#471` | ℹ️ |
| 2026-07-25 16:19 | spec ⓒ | (공통 반영) 검증 TC 도메인 신규 작성 — `docs/specs/qa/` **TC 232건**(커버리지 SVC 18/18 · PROC 18/18 · BR 24/24 · EX 13/13 · 정책 17/17 · 접점 11/11 — 미검증 0)·**횡단 시나리오 `SCEN_001`~`SCEN_023`**(여정 6 · 규약 대칭 왕복 3 · 기동·상수 검증 5 · 무저장 원칙 2 · 설계된 부재·수용 리스크 7)·**AUTH 치환 12건**·모의 수신처·C# 하네스·시드 카탈로그 37항·미해소 1건(성공률 정의 이월) — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-25 16:05 | spec | `docs/specs/qa/BAT/tc_BAT-04.md` 신규 — **연동 추적 기록 TC 13건**(API·서버 — 화면·명령 표면이 없어 접점·조회 API·DB 직접 조회로 관측). 최초 생성 컬럼 초기값(정확히 6컬럼)·미확정 이어쓰기(`created_at` 유지)·**결과 구분 레코드당 1회 확정**(조건부 UPDATE 가 0행 갱신)·`ck_result_pair` 동반 제약 4조합·`result_code` 열거형 4종 밖 거부·`tracking_key` CHECK 경계(1/255/256자·공백만)·**금지 값 컬럼 부재 + 저장 대상 3테이블 외 테이블 0**·생성자·수정자·논리 삭제 컬럼 0·**동시 진입 기본 키 충돌을 이어쓰기로 흡수**(요청 수 정확히 +1)·확정 결과 재안내 시 6컬럼 무갱신·세 조건부 UPDATE 멱등·저장 실패 시 되돌림·삭제 후 같은 키 재수신 시 새 레코드(수용 한계). 검증 BR-002·BR-010·BR-012·BR-018·BR-021 · EX `EX-BIZ-003` | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec | `process_PROC-301.md` 신규 — **PROC-301 연동 추적 기록**(EVT·**호출측 트랜잭션에 참여**). 다섯 계기(`LOOKUP`·`SECURE`·`FIX_RESULT`·`CONFIRM_RESULT`·`RECORD_CALLBACK`)를 한 프로세스가 받아 트랜잭션 경계와 갱신 형태를 통일. **네 갱신을 모두 조건부 UPDATE**("비어 있을 때만")로 만들고 **기본 키 충돌은 이어쓰기로 흡수**. `B3` 생성 시 PROC-303 요청 수 계수 동일 트랜잭션 · `B4` 결과 1회 확정 · `B5` 결과 확인 표시 · `B6` 콜백 기록. 4xx 를 만들지 않는다(호출측이 정한다) | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec ⓒ | (공통 반영) 프로세스 도메인 신규 작성 — `docs/specs/processes/` **PROC 18종 확정**(정책·서비스 예약분을 같은 코드·같은 의미로 — 통합·폐기·추가·재배치 0)·**단계 라벨 체계 확정**(`F` FE·`B` BE·`C` 호출측·`L` 라이브러리 내부, 레이어별 1부터 시간 순, 분기는 접미 문자)·선행 도메인 단계명 전건 라벨 매핑·의존관계 순환 0(호출 깊이 3층)·트랜잭션 경계와 멱등 수단 총괄·정책 규칙 86건 매핑(런타임 80 + 게이트·절차 6) — common.md | `accountinterlockhub#475`·`#469` | ℹ️ |
| 2026-07-25 14:40 | spec ⓒ | (공통 반영) 화면 도메인 신규 작성 — `docs/specs/screens/` **화면 4건**(SCR-001~004 — 서비스 예약 대비 통합·폐기·추가·재배치 0)·**단일 경로 4단계 모델**(리다이렉트 없음·화면별 URL 없음)·**디자인 시스템 1벌**(표면이 하나라 토큰 분기·접두어 없음 · 색 23종 **실측 대비비 표** · WCAG 2.1 AA · 모바일 우선 2 브레이크포인트 · 히트 영역 44x44)·공통 컴포넌트 11종·**화면 없는 IA 노드 12건 근거 기재** — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-25 14:10 | spec | `function_FN-007-008.md` · `function_FN-009-011.md` — 추적 레코드 기록 기능 5종 확정. **FN-007** 사전 조회·3분기 판정(`NONE`/`OPEN`/`FIXED` + `MDL-001` 파생 4종 산출, 예외 없음 — `NONE` 의 404 변환은 호출측 몫) · **FN-008** 확보(확정분 무갱신으로 보관 기산 보존 · **기본 키 충돌을 오류가 아니라 이어쓰기 정상 경로로 흡수** · 요청 수 계수 동일 트랜잭션) · **FN-009** 결과 구분 확정(`WHERE result_code IS NULL` 조건부 UPDATE·1회성·이미 확정분은 정상 종료) · **FN-010** 결과 확인 표시 · **FN-011** 완료 콜백 기록(멱등·결과 구분 불변). **다섯 기능 모두 "아직 비어 있을 때만 채운다" 형태로 통일**해 동시 요청·재요청·중복 통지에서 값이 덮이지 않게 했다. 레코드에 사용자 정보·암호값·복호화 원문을 담을 자리를 두지 않는다 | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-25 14:10 | spec ⓒ | (공통 반영) 기능·API 도메인 신규 작성 — `docs/specs/functions/` **공통 기능 15건**(FN-001~015 · POL 5·DAT 8·CRS 2·**EXT 0**)·**인터페이스 11접점**(사용자 진입 표면 3 · 서버 대면 API 4 · 수신처 전달 1 · 명령 진입점 1 · 라이브러리 공개 진입점 2)·오류 응답 엔벨로프 `{code,message,details?}`·인증 0/요청 제한 0·**신규 EX `EX-OPS-002` 1건**을 정책 카탈로그에 등록 — common.md | `accountinterlockhub#473`·`#469` | ℹ️ |
| 2026-07-25 13:35 | spec | `data_ENT-001.md`·`model_MDL-001.md` 신규 — **ENT-001 연동 추적 레코드**(`tbl_interlock_tracking`, 트랜잭션). **PK = `tracking_key VARCHAR(255)`**(대리 키 미채택 — 조회가 전부 추적 키 단건이라 사용 PROC 0건 컬럼·인덱스를 만들지 않는다). 컬럼 6 = `tracking_key`·`result_code`(NULL=미확정·CHECK 4종 열거형)·`result_at`·`result_confirmed_at`·`callback_received_at`·`created_at`. **처리 성공 여부·결과 확인 여부·콜백 수신 여부 boolean 컬럼 미도입** — 결과 구분·일시에서 결정되는 파생값이라 두 값이 어긋날 여지를 구조로 제거(MDL-001 이 `isResultFixed`·`isSuccess`·`isResultConfirmed`·`isCallbackReceived` 4종을 `ENT→도메인` 지점에서 산출). 테이블 CHECK `(result_code IS NULL) = (result_at IS NULL)`. **승인 여부 컬럼 미도입**(승인 사실은 ENT-002, 거부는 `USER_DENIED`). 인덱스 3 = PK(조회 5 PROC) + `result_confirmed_at` 부분(PROC-304 결과 확인 기산) + `created_at`(PROC-304 생성 기산) — 두 삭제 기준을 각 인덱스로 **분리 산정**(OR 미사용·멱등). **하드 삭제**(`deleted_at` 미도입 — 보관 정책의 목적이 삭제) · **생성자·수정자 컬럼 미도입**(`AUTH-001` 인증 부재) · **`updated_at` 미도입**(사건별 일시가 대체·사용 PROC 0건). ENT-002 와 **물리 FK 미설정**(보존 기간 상이 — FK 는 `DATA-002-02` 를 깬다). 추적 키 PK 가 `DATA-004-02` 에 저촉되지 않는 근거(제약 범위 = "보관 중인 레코드"와 동일·삭제 후 재수신은 새 레코드·요청 거절 없음) 명시 | `accountinterlockhub#472`·`#469` | 🚧 |
| 2026-07-25 13:35 | spec ⓒ | (공통 반영) 데이터 도메인 신규 작성 — `docs/specs/datas/` **엔터티 3건**(ENT-001~003 = 저장 대상 셋)·**데이터 모델 22종**(MDL-001~022 확정 — 예약 대비 통합·추가·재배치 0)·인덱스 5·물리 FK 0·소프트 삭제 미도입 — common.md | `accountinterlockhub#472`·`#469` | ℹ️ |
| 2026-07-25 13:05 | spec | `service_SVC-014.md` 신규 — **SVC-014 연동 추적 기록**(매핑 PROC-301). 기록 계기 5종(본인확인 최초 성공·승인/거부·전달 결과·결과 확인·완료 콜백)을 단일 레코드(MDL-001)에 모은다. Happy Path 5단계(계기 수신→사전 조회→생성·이어쓰기→결과 확정→계수 연계). BR-002 = 3분기, BR-021 = 확정 후 결과 갱신 무시(**완료 콜백만 예외로 이어쓰기**), BR-009·BR-012 인용. 예외 `EX-BIZ-003`(기록 실패 — 같은 경계의 지표 갱신도 롤백)·`EX-DATA-001`. 화면·명령 표면이 없는 내부 기록이지만 **발송처·수신처 조회의 근거**이므로 제공 기능단위로 정의(IA.md 각주 반영). 감사 로그·보안 이벤트 저장소 미도입 | `accountinterlockhub#471`·`#469` | 🚧 |
| 2026-07-25 13:05 | spec ⓒ | (공통 반영) 서비스 도메인 신규 작성 — `docs/specs/services/` **서비스 18건**(SVC-001~018)·사용자 역할 4종·분기 `BR-001`~`BR-024`·MDL 22종/SCR 4종 예약 채번·`PROC-403`·`PROC-404` 추가 채번 — common.md | `accountinterlockhub#471`·`#469` | ℹ️ |
| 2026-07-25 12:28 | spec | `policy_BIZ.md` `BIZ-002` 추적 레코드 생성 시점·중복 수신 신설 — **생성 시점을 "본인확인 최초 성공(복호화 판정 4단계 전부 통과)"으로 확정**. 근거(EXC-BIZ-03): PRD·database.md 는 "사용자가 진입한 시점"을 규정하지만 레코드 기준값인 추적 키가 X 내부 필드라 **복호화 전에는 알 수 없다** → "허브가 그 요청을 추적 키로 식별할 수 있게 된 최초 시점"으로 해석하며, 동의·승인 이전이라 원 취지(실제로 들어온 연동만 레코드)가 유지되고 무보관 원칙도 만족한다. 중복 수신 3분기(없음=신규 / 미확정=이어쓰기 / **확정=새 연동으로 받지 않고 확정 결과 재안내**), 재안내는 레코드 미갱신·보관 기산 불변. 결과 구분은 레코드당 1회 확정 후 불변(`BIZ-001-04`). 추적 키 미확보 실패는 레코드 미생성(`BIZ-002-02`) | `accountinterlockhub#470`·`#469` | 🚧 |
| 2026-07-25 12:28 | spec ⓒ | (공통 반영) 정책 도메인 신규 작성 — `docs/specs/policies/` 정책 17건·규칙 86건·EX 코드 12종(암호화 연동 규약·복호화 판정 4단계·결과 구분 4종·추적 레코드 생성 시점·보관·무저장·동의 증적·인증 부재·수용 리스크) — common.md | `accountinterlockhub#470`·`#469` | ℹ️ |
| 2026-07-25 10:05 | directing ⓒ | (공통 반영) 리셋 방향 담당자 확정분 반영 — 처리상태(구 `BAT-01`)와 연동이력(구 `BAT-03`)을 **단일 추적 레코드로 통합 신설**. 진입 시점 생성·결과 구분 보존·추적 키 중복 수신 3분기 규약(미존재=신규 / 진행 중=이어쓰기 / **확정 결과 재수신 = 새 연동으로 받지 않고 확정 결과를 그대로 재안내**) — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
