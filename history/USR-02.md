# IA: USR-02 — 연동 실행

> 본 IA 노드와 연관된 작업의 시계열 이력(최신순). 정책: ai/strategies/ia-history.md

| 일시 (KST) | 단계 | 산출물·결과 | 관련 일감 | 상태 |
|---|---|---|---|---|
| 2026-07-25 14:40 | spec | `screen_SCR-003.md` 신규 — **SCR-003 연동 진행 안내**(승인 제출부터 결과 확정까지의 대기 표시). 서비스 예약이 열어 둔 "`SCR-002` 상태로 통합" 안을 **채택하지 않고 독립 화면으로 유지**(폼·목록이 사라지고 진행 표시만 남는 다른 화면이며 흐름에 이미 단계 개념이 있다). `PROCESSING` 은 **화면이 소유한 단계**로 서버 `stage` 에 대응 값이 없다. **조작 요소 0**(취소 버튼 미도입 — 진행을 되돌릴 수단이 허브에 없다)·진행률·재시도 횟수·내부 단계 미표시(`SEC-002-05`). **화면 타임아웃 미도입** — 응답 시점은 `BIZ-004-02` 전달 재시도 총 소요 상한이 지배하며 수치를 복제하지 않는다. **응답 미수신(전송 단절)은 `Unconfirmed` 상태 + 재진입 안내**로 다루고 결과 구분을 화면이 추측하지 않는다(다섯 번째 경로 아님). 거부 제출은 이 화면을 거치지 않는다(`BIZ-003-03`) | `accountinterlockhub#474`·`#469` | 🚧 |
| 2026-07-25 14:40 | spec ⓒ | (공통 반영) 화면 도메인 신규 작성 — `docs/specs/screens/` **화면 4건**(SCR-001~004 — 서비스 예약 대비 통합·폐기·추가·재배치 0)·**단일 경로 4단계 모델**(리다이렉트 없음·화면별 URL 없음)·**디자인 시스템 1벌**(표면이 하나라 토큰 분기·접두어 없음 · 색 23종 **실측 대비비 표** · WCAG 2.1 AA · 모바일 우선 2 브레이크포인트 · 히트 영역 44x44)·공통 컴포넌트 11종·**화면 없는 IA 노드 12건 근거 기재** — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-25 14:10 | spec | `spec-functions-api-server.md` §수신처 전달 접점 · `spec-functions-api-user.md` §생년월일 재수신 규약 — **전달 성공 판정 기준 확정 = HTTP 200~299**(본문을 판정에 쓰지 않고 읽지도 않는다 · 3xx 미추종 · 연결/이름 해석/보안 연결 실패·응답 대기 상한 초과는 실패). 성공·실패가 결과 구분과 **1:1**(`SUCCESS`/`DELIVERY_FAILED`, 중간 상태 미승격 — `BIZ-004-03`). **복호화한 X 의 UTF-8 바이트열을 재직렬화 없이 그대로 본문으로 전송**(재직렬화 시 숫자 표현·유니코드 이스케이프·필드 순서가 달라진다). 타임아웃·재시도 횟수·간격은 `BIZ-004-02` 인용(수치 미복제). **승인 시점 재복호화의 생년월일은 승인 요청이 다시 실어 오는 것으로 확정** — 서버는 요청 사이에 보관하지 않고 화면이 페이지 메모리로만 보유(URL·숨은 필드·브라우저 저장소 금지). **EXT 분류 FN 0건**(유일한 외부 호출이 단일 SVC 로직이라 FN 기준 미충족 — 계약은 전달 접점 정의가 갖는다) | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-25 14:10 | spec ⓒ | (공통 반영) 기능·API 도메인 신규 작성 — `docs/specs/functions/` **공통 기능 15건**(FN-001~015 · POL 5·DAT 8·CRS 2·**EXT 0**)·**인터페이스 11접점**(사용자 진입 표면 3 · 서버 대면 API 4 · 수신처 전달 1 · 명령 진입점 1 · 라이브러리 공개 진입점 2)·오류 응답 엔벨로프 `{code,message,details?}`·인증 0/요청 제한 0·**신규 EX `EX-OPS-002` 1건**을 정책 카탈로그에 등록 — common.md | `accountinterlockhub#473`·`#469` | ℹ️ |
| 2026-07-25 13:35 | spec ⓒ | (공통 반영) 데이터 도메인 신규 작성 — `docs/specs/datas/` **엔터티 3건**(ENT-001~003 = 저장 대상 셋)·**데이터 모델 22종**(MDL-001~022 확정 — 예약 대비 통합·추가·재배치 0)·인덱스 5·물리 FK 0·소프트 삭제 미도입 — common.md | `accountinterlockhub#472`·`#469` | ℹ️ |
| 2026-07-25 13:05 | spec | `service_SVC-004.md` 신규 — **SVC-004 연동 실행**(매핑 PROC-104·PROC-301·PROC-303). Happy Path 8단계(재복호화→페이로드 구성→수신처 전달→전달 판정→결과 확정→카운터 갱신→원문 폐기→결과 안내 이관). BR-006 = 전달 응답 판정, BR-007 = 즉시 재시도 잔여·소진. 예외 `EX-BIZ-002`(재시도 소진 → `DELIVERY_FAILED`·경로 ④)·`EX-SEC-002`·`EX-AUTH-002`(재복호화 실패 분기). **PRD 미결 ⑦ 기본안 적용 명시**(사용자 진입 중에만 재시도 + 결과 확정 후 재연동은 발송처가 새 추적 키로 새 요청 발행). 전달은 서버 간 호출 고정(브라우저 미경유)·X 업무 유효성 미판단·전달 성공 판정의 구체 기준은 기능·API 도메인 위임 | `accountinterlockhub#471`·`#469` | 🚧 |
| 2026-07-25 13:05 | spec ⓒ | (공통 반영) 서비스 도메인 신규 작성 — `docs/specs/services/` **서비스 18건**(SVC-001~018)·사용자 역할 4종·분기 `BR-001`~`BR-024`·MDL 22종/SCR 4종 예약 채번·`PROC-403`·`PROC-404` 추가 채번 — common.md | `accountinterlockhub#471`·`#469` | ℹ️ |
| 2026-07-25 12:28 | spec | `policy_BIZ.md` `BIZ-004` 수신처 전달·재시도 신설 — 승인 확정 시 `<RECEIVER_DELIVERY_URL>` 로 복호화 X 전달, **허브 서버가 직접 호출(브라우저 미경유)**. 즉시 재시도는 사용자가 진입해 있는 동안만 — 최초 1회+재시도 2회(간격 1·2초, 회당 대기 5초, 총 20초 상한). 소진 시 `DELIVERY_FAILED` 확정·502 `EX-BIZ-002`·결과 경로 ④. 결과 확정 후 재연동은 발송처가 **새 추적 키로 새 요청 발행**(PRD 미결 ⑦ 기본안 적용 — EXC-BIZ-08). X 업무 유효성 미판단(`BIZ-004-05`). 잠정 전달 주소 상태에서는 전달 실패가 정상 동작(EXC-BIZ-10) | `accountinterlockhub#470`·`#469` | 🚧 |
| 2026-07-25 12:28 | spec ⓒ | (공통 반영) 정책 도메인 신규 작성 — `docs/specs/policies/` 정책 17건·규칙 86건·EX 코드 12종(암호화 연동 규약·복호화 판정 4단계·결과 구분 4종·추적 레코드 생성 시점·보관·무저장·동의 증적·인증 부재·수용 리스크) — common.md | `accountinterlockhub#470`·`#469` | ℹ️ |
| 2026-07-25 11:25 | directing ⓒ | (공통 반영) 값 확정 대기 잠정값 설정 — 수신처 전달 주소 `<RECEIVER_DELIVERY_URL>` 에 잠정값 적용(문서용 예약 도메인 기반이라 미교체 배포 시 외부 유출 없이 전달만 실패), build 착수 전 수신처 협의로 확정 — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
| 2026-07-25 10:05 | directing ⓒ | (공통 반영) 리셋 방향 담당자 확정분 반영 — 담당 범위를 **연동 실행(복호화·수신처 전달)** 으로 축소(결과 안내 → `USR-05` 분리 — 코드 재사용 아님). 구 사양·구현·목업 삭제 → **재작성 대상** — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
| 2026-07-20 08:03 | build | `#412` 오류 #465 회귀 수정(USR-02_003 = SCR-005 본인확인 `BirthDateField` 공유) — `components/user/BirthDateField.module.css` 포커스 경계 규칙 `.input:focus`→`.input:focus:not(.inputError)`(1줄 + 근거 주석). 에러 시 입력이 `input inputError` 두 클래스를 함께 받는데 `.input:focus`(특이도 0,2,0)가 `.inputError`(0,1,0)를 눌러, 복호화 실패(EX-SEC-006, 연동 실행 예외) 자동 포커스에서 error+focus 상시 공존 시 danger 경계(#DC2626)가 primary(#2563EB)에 가려지던 회귀 시정 — `:not(.inputError)` 로 포커스 규칙이 에러 상태에선 미적용(소스순서 무관)되어 danger 경계 우선(design-system.md §컴포넌트 `TextField(user/birthdate)` error=`color-u-danger-border` 정합). 캡션·aria·배경·모션·disabled·전역 `:focus-visible` 링 무변경. `npm run build` 성공(99모듈)·dist CSS 반영 확인. **3책임 통과**(리뷰 PASS·기능검증 🟢 — USR-02_003 재현 Pass: border-color=danger #DC2626·400 EX-SEC-006·레코드 delta 0·민감값 미노출·SCR-005 잔류·하드잠금 없음(정정 재제출 200)). main 미병합 — 담당자 배포판정·qa 축약 재검증 대기 | `accountinterlockhub#465`·`#412` | ✅ |
| 2026-07-18 23:16 | qa ⓒ | (공통 반영) `#416` Wave 3(최종) SCEN_001~006 E2E 여정 재검증 6/6 Pass(🟢3·🔵3) — common.md | `accountinterlockhub#416` | ℹ️ |
| 2026-07-18 22:50 | qa | `#416` Wave 2 — USR-02 전건 17 TC(Phase 20~36, `c7b0773`) 실행 → **16 Pass(6 🟢+7 🔵 mock B의존+3 🟣 정적trace)/1 🔴 Fail/0 🟠 Block**(17=수행 전건). SCR-006 결과 4유형×5요소(완료·거부[Wave1 재확인]·링크오류·**Fallback 최초 성공 캡처**) 전량 정본 1:1 일치, 아이콘 4종(원형64px·글리프28px·선2.5px) 전량 규격·색토큰 실측 일치. 재시도(최초1+2회, `MAX_ATTEMPT_INDEX=2`)·타임아웃(10s×3회≈30031ms 실측)·복호화 순서(독립 재구현 교차검증)·서버-서버 무노출(브라우저 네트워크 전량 계측)·무저장(DB 5테이블+로그파일+audit detail 값레벨+브라우저storage)·내부 차단 3종(BIZ-003-06/02/07, 코드상 도달 불가 확인+`DELIVERY_BLOCK` dead code 발견)·fault 주입(이력·상태 저장 2지점, Wave1 격리기법 재사용) 전부 사양대로 확인. **결함 1건**: `USR-02_003`(#438) 이 오류 `#465`(Wave1 발견)와 **동일 원인**(BirthDateField.module.css 특이도 충돌)으로 재현 — 신규 오류 일감 미생성, `#465` 에 노트+연관 누적(영향범위 USR-01_013+USR-02_003 양쪽으로 확장). **테스트 스크립트 자체 오탐 1건 정정**: `USR-02_004` 최초 판정에서 시간창 스코핑 오류로 오탐 FAIL 기록됐으나 `verify-p23.js` 정밀 재검증으로 실제 무결함 확인(🟢 Pass 정정). 검증 일감 `#436~452` 상태 동기화(해결 16·진행 1, GET 실측 확인) · 관련 관계 `#438→#465` 신설. spec 보완 제안 2건(USR-02_005/USR-01_015 카탈로그 중복, USR-02_017 "롤백" 문구를 "순차 오케스트레이션" 로 정밀화). 잔여=담당자 배포판정(`#465` 처리 후 USR-01_013·USR-02_003 재검증 권고) | `accountinterlockhub#416`(검증 `#436~452`, 오류 `#465` 연관) | ❌ |
| 2026-07-18 19:51 | build | `pages/ConsentResultPage.{tsx,module.css}` 전면 재작성 + `components/user/{ResultIcon,UserBadge}.{tsx,module.css}` 신규(Phase 3/3) — 결과 상태 아이콘 4종 인라인 SVG(design-system §결과 상태 아이콘, 이모지 ✅🚫⛔🔄 전량 교체·거부 🚫 오류기호 시정)·`Badge(user)` 신설(공통 Badge 규격 상이로 미재사용)·`Card(user)`(480px·radius-u-card·shadow-u-card·액센트 라인·space-u-stack 균일 적용)·`Banner(user)` 재사용(Phase 2 `UserBanner`). Fallback 설명 문구 정의서 고정 문구로 단문화(장문→"발송처에서 다시 진입해 주세요.", 담당자 미확정 [확인 필요] 유지). 브레이크포인트 min-width:640px 통일(이월-4, USR-01 이월 3건은 USR-01.md 참조). `npm run build` 0건(99모듈). **3책임 통과 + 런타임 게이트 통과**(리뷰 PASS·기능검증 🟢 8/8 — 배포 산출물(NestJS 서빙) 기준 실 왕복, 시각 실측 573/573 일치) | `accountinterlockhub#415`·`#412` | ✅ |
| 2026-07-18 17:38 | build | `#412` 사용자 표면 토큰(-u-) 신설(Phase 1/3) — `apps/frontend/src/styles/tokens.css`에 design-system.md §사용자 표면 확장 정본대로 `-u-` 접두 토큰 46종 신규 추가: 색 19(배경·경계·행상태 7 + success/info/danger/neutral 3종쌍 12) · 타이포 14(text-u-title/lead/section/item/value/caption 의 size·line 분리, section·value 만 자간; 굵기는 전역 --font-weight-* 재참조로 중복 미정의) · 간격 4(shell-y·card 는 브레이크포인트 640px 반응형 미디어쿼리 + section·stack) · 형태 4(radius-u-card/control/pill·shadow-u-card) · 모션 5(motion-u-fast/base/slow·ease-u-standard/decelerate). 기존 전역 토큰 **0건 변경**(git diff 83 insertions/0 deletions, 관리자 SCR-001~004 렌더 불변 전제 충족). `npm run build`(tsc -b && vite build) 0건. 화면·컴포넌트 적용(Phase 2·3)은 범위 밖(미착수). **3책임 통과**(리뷰 PASS·기능검증 🟢 4/4 — 전역 토큰 불변 바이트 증명, 신규 토큰 44종 런타임 실측 일치) | `accountinterlockhub#413`·`#412` | ✅ |
| 2026-07-18 14:55 | spec | `#408` 사용자 페이지 시각 개선(SCR-006) — `screen_SCR-006.md` 개정 + `design-system.md` §사용자 표면 확장 인용. **전역 토큰 키·값 변경 0건**. 결과 유형 4종별 3종 색 토큰 쌍 확정(완료=success·거부=info 중립·링크오류=danger·Fallback=neutral)·**결과 상태 아이콘 규격 신설**(A-4: `ResultIcon(user)` 원형 64px·글리프 28px·선 2.5px, 완료 체크/취소 **중립 가로막대**(거부=오류 아님, 오류 기호 금지)/링크오류 느낌표/Fallback 물음표, `aria-hidden=true` + 텍스트 라벨 병기, 라이브러리 비강제, 등장 `motion-u-slow` 1회)·요소별 렌더 조건을 §조건부 표시에 명시(A-11)·고정 문구 정본 표 추가(A-1). 카드 규격(480px·`radius-u-card` 16px·`shadow-u-card`·상단 4px 액센트)·`space-u-shell-y` 48/24px. **교차검증 회귀 1회차 반영**: 🟠 E-2 범위 위반(시각 폴리시 범위를 넘어 사용자 안내 카피를 삭제) 원상 복구 — 결과 설명 문구를 **4종 전부 렌더**로 되돌리고(문구 원문은 기 구축 `ConsentResultPage.tsx` 정본, Fallback 만 정의서 고정 문구 유지), **안내 마무리 문구를 요소로 복원**(완료·거부·링크오류 렌더 / Fallback 미렌더), `Badge(user)` 라벨을 기 구축 구현에 정합(완료 `연동 완료`/거부 **`거부`**/링크오류 `링크 오류`/Fallback **`안내`(neutral, 렌더)**), 요소 순서를 main 규정대로 아이콘·배지→제목→설명→(해당 시)`Banner`→마무리 문구로 복원. 개정 노트도 "삭제를 정의로 오기술" 상태에서 **실제 수행분(수치 확정·아이콘 규격·토큰 쌍·모션·조건부 표시 보강)만** 적도록 재작성. W-9 발송처 복귀 3절(§사용자 인터랙션·§조건부 표시·§이동 경로) 을 "데이터 출처 부재로 현행 미렌더"로 일관 정리(액션 추가·삭제 없음). **결과 유형 4종·고정 문구 8종·사용자 대면 문구·신규 서버 호출 금지·민감값 미노출 불변**. 담당자 확인 이관: 발송처 복귀 URL 데이터 출처 부재(A-5 — MDL-101 무·ENT-001 진입주소 `#214` 폐기)·거부 배지 라벨 `거부`→`연동 취소` 제안(제목·info 색과 일관되나 카피 변경이라 미적용)·Fallback 설명 문구 사양↔기 구축 FE 불일치. **교차검증 회귀 2회차**: `spec-screens.md` §담당자 확정 대기의 깨진 인용(회귀 1회차에 삭제된 "발송처 복귀(있을 시)" 표기를 그대로 인용) 을 현행 SCR-006 규정("데이터 출처가 없어 현행 미렌더", §사용자 인터랙션·§조건부 표시·§이동 경로·§구현 가이드 4절 동일)에 맞춰 정정 — 담당자 확인 항목 자체는 [확인 필요] 로 유지. `screen_SCR-006.md` 본문 무변경. **목업 리뷰 보완 반영(M-4, 경량 목업 제작 후)**: 목업이 근거 없는 "동의 화면으로 이동" 버튼을 제거한 것이 사양과 정합함을 확인하고, **Fallback 재진입 CTA 미렌더**를 §조건부 표시(행 신설)·§화면 상태 전이(Fallback 행 괄호)에 명문화 — 기존 §이동 경로·§구현 가이드 규정과 동일 내용이며 **액션 추가·삭제 0건**, 고정 문구·결과 유형 4종 불변 | `accountinterlockhub#409`·`#408` | 🚧 |
| 2026-07-12 23:45 | build | #238 encX/encY 완전누락 EX 코드 정합(EX-SEC-004→EX-SEC-007) — FN-020(`hub-decrypt.service.ts`) 부재 null 체크 EX-SEC-007 통합·`function_FN-020.md`·`policy_SEC-crypto.md` SEC-006-05·TC `tc_USR-02_004`. **실행검증 🟢**(prod 번들+PostgreSQL 55432+mock B): 부재·빈값·형식오류 400 EX-SEC-007, 정상 복호화·mock B 전달(X 무변형)·틀린 생년월일 400 EX-SEC-006 무회귀 | `accountinterlockhub#238`·`#236` | ✅ |
| 2026-07-12 22:00 | qa | USR-02_001~017 검증 → 16 Pass(🔵/🟣 포함)·1 Fail(004 완전누락 EX-SEC-004 vs 007). 복호화·서버-서버 전달·무저장 실증 | `accountinterlockhub#236` | 🚧 |
| 2026-07-12 11:43 | build | 사용자 동의 결과 FE 개정(P6, USR-02 — SCR-006) — `apps/frontend/src/pages/ConsentResultPage.tsx` 재정의(`ConsentResultPage.module.css` 무변경, 기존 클래스 재사용). 라우팅은 USR-01 SCR-005 와 같은 commit 의 `App.tsx` 에서 `/interlock/result`(정적 경로, 민감값 미포함)로 변경(구 `/consent/:requestKey/result` 폐기). 결과 계약 `#214`·`#215` 최신화 — `location.state.result` = `'completed'\|'rejected'\|'link-error'` 3종(구 AGREED/REJECTED/DELIVERY_FAILED 폐기, 전달실패(EX-BIZ-004)는 `#215` 로 SCR-005 잔류 재시도 처리라 본 화면 결과 유형에서 제외 유지): 완료=success Badge "연동 완료"+제목 "연동이 완료되었습니다." · 거부=info Badge "거부"+제목 "연동이 취소되었습니다." · 링크 오류=danger Badge "링크 오류"+제목 "연동 링크가 올바르지 않습니다."+error Banner "발송처에 문의해 주세요."(재입력 불가 안내, 내부 예외 코드 미노출). 결과 상태 없음(새로고침·직접 URL 진입)→Fallback 안내(발송처에서 전달받은 링크로 재진입 유도). 회원 키·연동 추적 키·처리 상태 값·내부 예외 코드 무노출 유지(DATA-001·SEC-007-02). `npm run build --prefix apps/frontend` 0건. ⚠ WARN: Fallback 안내에 SCR-005 복귀 CTA 버튼을 두지 않음 — 직접 진입 컨텍스트에는 유효한 accessAddressId 가 없어 목업의 정적 링크(`SCR-005.html`)에 대응하는 실 라우트를 구성할 수 없음(안내 문구만 제공, 확인 필요). 작성만 수행(리뷰·기능검증은 별도 doer 대기) | `accountinterlockhub#230` | ✅ |
| 2026-07-12 11:10 | build | 사용자 연동 실행 BE 통합(P5, USR-02 부분) — PROC-203 오케스트레이션(`user/interlock/interlock.service.ts` executeInterlock, POST /api/interlock/approve 승인 경로 내부). FN-020(HubDecryptService, P2 기 구현) 호출→**추적 키 길이 가드 신설**(trackingKey.length>255 → EX-BIZ-008, INSERT 전 확정 — P2 리뷰 S-2 DB CHECK 500 갭 해소)→FN-016 이력 생성(`user/history/interlock-history.service.ts` 신규, 전달에 앞서 INSERT 1건 surrogate id+tracking_key)→FN-012 전달(`user/delivery/delivery.service.ts` 재키 — 입력이 회원키+파라미터 리매핑(구 ENT-003)에서 복호화 원문 X 무변형으로 전환, DataSource 의존 제거, 재시도 최초1+2=3회 유지)→FN-009 상태 저장(`user/status/process-status.service.ts` 재키 — request_key PK→surrogate id+tracking_key 비유니크 인덱스). 성공 200 COMPLETED(원문·회원키·추적키 미노출)·실패 502 EX-BIZ-004(상태 is_success=false·이력 유지, EXC-BIZ-06·11). audit.constants.ts `DELIVERY_SUCCESS`·`CONSENT_AGREE` 신규(spec 요구·누락 발견, HISTORY_CREATE 주석도 진입시점→복호화후 정정). `npm run build` 0건·로컬 Postgres(pg-AccountInterlockHub) 대상 부팅 확인(TypeOrmModule 정상 초기화·전 라우트 매핑)·와이어링 smoke(500 없음, EX-SEC-004 엔벨로프 정상). 하류 영향(P7-9 대상, 컴파일 비영향 — raw SQL 문자열): `api/status/status.service.ts`·`api/interlock/{history-scope,completion,callback}.service.ts` 는 여전히 구 `request_key`/`user_key` 컬럼을 조회해 신 스키마(surrogate id+tracking_key) 대비 런타임 불일치 — 각 phase 에서 재작성 필요. 작성만 수행(리뷰·기능검증은 별도 doer 대기) | `accountinterlockhub#229` | ✅ |
| 2026-07-12 09:15 | build | `user/interlock/hub-decrypt.service.ts`·`hub-decrypt.module.ts` 신규(P2) — FN-020_decryptInterlock(encX, encY, birthDate, accessAddressId)→{X, trackingKey} 주입형 서비스: SEC-006 규약 결정론 구현(normalize32/iv16 `_`(0x5F) 우패딩·앞자름, Base64URL 문자셋·길이 사전검증 후 디코드, AES-256-CBC+PKCS#7(Node 기본 언패딩)로 encY→keyXstr→encX→X 순차 복호화, trackingKey 추출·완결성 검증). ex-code.map.ts 에 EX-SEC-006/007·EX-BIZ-008 3종, audit.constants.ts 에 DECRYPT_SUCCESS/FAIL/SENDER_DATA_ERR 3종 신규(FN-013 감사, trackingKey 는 FN-010 마스킹만). app.module 에 HubDecryptModule 등록(엔드포인트 미노출 — PROC-203 내부 전용, P5 연동 실행 오케스트레이션이 소비 예정). encX·encY·birthDate·keyXstr·X 원문 미기록(DATA-001-04·SEC-005-06). 컨트롤러·delivery 미변경(스코프 한정). `npm run build` 0건. 작성만 수행(리뷰·기능검증은 별도 doer 대기) | `accountinterlockhub#226` | ✅ |
| 2026-07-12 10:30 | spec | `#215` 목업 리뷰 보완 — ② 복호화 실패(EX-SEC-006) 사용자 문구 "사용자 정보가 일치하지 않습니다."로 고정: FN-020 에러 카탈로그·SVC-005·PROC-203 분기표. ③ 전달 실패(EX-BIZ-004) UX SCR-005 재시도로 이관·SCR-006 전달실패 결과 유형 제거(Loaded/조건부/Navigation/색·목업 res-fail 제거): PROC-203 FE 노트·SCR-006·tc_USR-02·tc_SCEN 정합. 처리상태·연동이력 저장(EXC-BIZ-06·11) 불변 | `accountinterlockhub#215` | 🚧 |
| 2026-07-11 23:30 | spec | `tc_USR-02.md` 개정 — 허브 복호화(encY→키→encX→X)·추적 키 추출·연동이력 복호화 후 생성·수신처 B 서버-서버 전달 재정의(회원 키 직전달·deliver_to_b 리매핑 폐기). BR-204·BR-202·EX-SEC-006/007·EX-BIZ-008·EX-BIZ-004·무저장·규약(AES-256-CBC·키/IV 정규화) 검증. 12→17 | `accountinterlockhub#222`·`#214` | 🚧 |
| 2026-07-11 23:30 | spec ⓒ | (공통 반영) #214 검증 TC 사양 개정 — 복호화·전달·추적 키 단독·시드(encX/encY·mock B)·신규 BR/EX(204·SEC-006/007·BIZ-008) — common.md | `accountinterlockhub#222`·`#214` | ℹ️ |
| 2026-07-11 22:30 | spec | `process_PROC-203.md` 개정(연동 실행·복호화·수신처 B 전달) — FN-020 복호화(생년월일→encY→키→encX→X·추적 키 추출)→FN-016 연동이력 생성(복호화 후·전달에 앞서 BIZ-004-07)→FN-012 수신처 B 서버-서버 POST(재시도 2회·SEC-007 브라우저 미경유)→FN-009 상태 저장 오케스트레이션. BR-204(복호화 성공/실패)·BR-202·EX-SEC-006/007·EX-BIZ-008·EX-BIZ-004. 회원 키 리매핑·전달 파라미터(ENT-003) 폐기·원문/암호값/생년월일 전량 미기록(전달 후 즉시 폐기) | `accountinterlockhub#221`·`#214` | 🚧 |
| 2026-07-11 22:30 | spec ⓒ | (공통 반영) #214 프로세스 사양 개정 — 복호화·전달·추적 키 단독·연동이력 복호화 후 생성·보관 fallback — common.md | `accountinterlockhub#221`·`#214` | ℹ️ |
| 2026-07-11 21:30 | spec | `screen_SCR-006.md` 개정(동의 결과) — 요청 키값 결과 경로 폐기·`/interlock/result`(민감값 미포함 client-state). 결과 유형에 **링크 오류(EX-SEC-007·EX-BIZ-008 발송처 문의·재입력 불가)** 추가 — 완료(success)·거부(info)·전달 실패(EX-BIZ-004 재시도)·링크 오류(error)·Fallback. 회원 키·연동 추적 키·처리 상태 미노출(SEC-007-02·DATA-001)·전달 실패/거부 상태 기록 정합(EXC-BIZ-06/11). 복호화 실패(EX-SEC-006)는 SCR-005 재입력이라 본 화면 미도달 | `accountinterlockhub#220`·`#214` | 🚧 |
| 2026-07-11 21:30 | spec ⓒ | (공통 반영) #214 화면 사양 개정 — 화면 목록·경로·design-system(생년월일 입력·상태 표현·민감값 마스킹) — common.md | `accountinterlockhub#220`·`#214` | ℹ️ |
| 2026-07-11 20:30 | spec | `function_FN-020.md` 신설(허브 복호화·연동 추적 키 추출 — 생년월일→encY→키→encX→X, normalize32/iv16 `_` 우패딩·AES-256-CBC·PKCS#7·추적 키 X 필드 추출·완결성 검증·전량 미기록, EX-SEC-007/006·EX-BIZ-008)·`function_FN-012.md` 개정(회원키 직전달→복호화 원문 X 서버-서버 POST SEC-007·재시도 2회·상태 저장)·`function_FN-016.md`(복호화 후 이력 생성·userKey 폐기·5항목)·`function_FN-009.md`(상태 저장 trackingKey). 승인 경로 PROC-203 오케스트레이션 FN-020→016→012→009 | `accountinterlockhub#219`·`#214` | 🚧 |
| 2026-07-11 20:30 | spec ⓒ | (공통 반영) #214 기능·API 사양 개정 — FN-020 허브 복호화 신규·추적 키 단독 추적·무저장·에러 체계 — common.md | `accountinterlockhub#219`·`#214` | ℹ️ |
| 2026-07-11 19:30 | spec | `model_user.md` MDL-204(전달 페이로드→**복호화 원문 X**: 회원 키·추적 키 등 무저장·무변형·서버-서버 POST SEC-007·완료 페이지 원문 미포함)·MDL-202(연동 추적 키 추출)·`data_ENT-007.md`·`data_ENT-004.md` 생성 시점 정합(복호화 성공 후 이력 생성→전달→처리상태 저장) | `accountinterlockhub#218`·`#214` | 🚧 |
| 2026-07-11 19:30 | spec ⓒ | (공통 반영) #214 데이터 사양 개정 — 무저장 강화·비영속 컨텍스트·연동 추적 키·surrogate PK·보관 fallback·ENT-003 폐기 — common.md | `accountinterlockhub#218`·`#214` | ℹ️ |
| 2026-07-11 18:30 | spec | `service_SVC-005.md` 개정 — 연동 실행을 **허브 복호화(생년월일→encY→키→encX→X)·연동 추적 키 추출·연동이력 생성·수신처 B 서버-서버 전달·완료 페이지**로 재정의. 연동이력 생성(BIZ-004-07·PROC-403) 편입·전달 페이로드=복호화 원문 X(SEC-007-01·SEC-002). BR-204(복호화 성공/실패 재입력)·EX-SEC-006/007·EX-BIZ-008 신규, BR-202·EX-BIZ-004 유지. 매핑 PROC +PROC-403 | `accountinterlockhub#217`·`#214` | 🚧 |
| 2026-07-11 18:30 | spec ⓒ | (공통 반영) #214 서비스 목록문서 개정 — spec-services.md 사용자 정의 4역할·시나리오·의존관계·BR/EX/MDL/POL 카탈로그 — common.md | `accountinterlockhub#217`·`#214` | ℹ️ |
| 2026-07-11 16:20 | spec ⓒ | (공통 반영) #214 암호화 연동 정책 개정 — BIZ-003 연동 실행(승인 후 복호화·상태전이·전달 재시도 2회)·SEC-006 이중 암호화·복호화 규약·SEC-007 복호화 원문 서버-서버 전달(브라우저 미경유)·DATA-001 무저장(원문·암호값·생년월일) — common.md | `accountinterlockhub#216`·`#214` | ℹ️ |
| 2026-07-09 03:20 | qa | USR-02 독립 검증 12 TC 전수(v0.1.0/688fec2, 실 round-trip+실 DB psql+mock 서비스 B) → 🔵6·🟣4·🟢2·🔴0·🟠0. 목 의존 전달레그🔵(상태저장 실 DB): B목200 최초성공·목1회(001)·1실패후 재시도성공 목2회(002)·항상500 최초1+재시도2 모두실패 502 EX-BIZ-004·목3회·DELIVERY_FAIL(003)·경계 2실패→3째성공 attempt<=2 3회(004)·타임아웃 3×10s=30s 502(005)·payload deliver_to_b=true만(userId·email)·internalFlag 제외·source_key_a→param_name 리매핑·memberKey 원문무변형(특수문자 é/~/+/대소문자 보존)(006). 실 DB🟢: 회원키 전테이블 무저장 scan(config/param/consent/history/status/audit=0)·지정키만 history·앞2뒤2 마스킹(010)·requestKey 단일실행 멱등 재제출차단(011). 정적🟣: 미동의 차단 defensive guard·유일호출자 confirmed=true(007)·targetUrl 구성바운드 URL오버라이드 부재(008)·역전이 1방향 confirm 가드 WHERE=false(009)·페이로드/PROC-401 저장오류 EX-FN-999(012). 오류일감 0. evidence works/accountinterlockhub-54/evidence/UQA/ | `accountinterlockhub#54` (검증 #128~#139) | ✅ |
| 2026-07-08 11:20 | build | `apps/frontend/src/pages/ConsentResultPage.tsx`·`.module.css` 신규(USR-P4 FE) — SCR-006 동의 결과 화면(`/consent/:requestKey/result`, App.tsx 라우팅 추가). SCR-005 제출 성공 시 네비게이션 상태(`state.result`: AGREED/REJECTED/DELIVERY_FAILED)만 읽어 렌더 — 신규 서버 호출·PROC 트리거 없음(§개요). 동의완료=success Badge·거부=info(중립) Badge·전달실패=error Banner(role=alert). 상태 없음(새로고침·직접 URL)→Fallback 안내(강제 이동 없음). 중앙 카드 480px(사용자 셸)·무노출(회원 키·요청 키값·처리 상태·내부 예외 코드 미표시)·상태 색+텍스트 병기(§접근성). `npm run build`(tsc -b && vite build) 0건. 3책임 통과(리뷰 PASS·React 안티패턴 0·tester 실 브라우저 4/4 🟢: 동의완료·거부·전달실패·Fallback end-to-end·무노출) | `accountinterlockhub#48` | ✅ |
| 2026-07-08 10:15 | build | `user/delivery/delivery.service.ts` 신규(USR-P2) — FN-012 deliverToServiceB(PROC-203): 사전조건(consentConfirmed, 미동의 시 DELIVERY_BLOCK 감사·내부차단)·전달대상 구성 한정(serviceBDeliveryUrl/Method)·MDL-204 페이로드(memberKey 원본 무변형·source_key_a→param_name 리매핑·configCode/requestKey 동봉, 무저장)·재시도(최초1+2회, Node fetch+AbortController 10s 타임아웃)·상태 저장 무조건 1건·실패 시 DELIVERY_FAIL 감사+502 EX-BIZ-004. `DELIVERY_BLOCK` 감사 이벤트 추가. HMAC 서명 미구현(사양 미결 — 완료보고 WARN). `npm run build` 0건. 3책임 통과(리뷰 PASS·tester 실 스텁 HTTP 전달 성공/실패 3회 재시도·502 EX-BIZ-004·상태 저장 🟢) | `accountinterlockhub#46` | ✅ |
| 2026-07-07 08:27 | spec | `model_user.md` MDL-204 개정 — configCode·requestKey 동봉 속성 확정(콜백 회신 계약, 지정·미지정 공통)·사용 SVC 에 SVC-009(회신 값 출처) 추가·매핑 표 확장 | `accountinterlockhub#27` | 🚧 |
| 2026-07-07 06:35 | spec | `service_SVC-005.md` 개정 — 전달 페이로드에 연동 구성 식별자·요청 키값 동봉 확정(F-003, 콜백 회신 계약의 출처 — BIZ-004 구현 가이드 위임 과제)·MDL-204 후보 항목 확장 | `accountinterlockhub#26` | 🚧 |
| 2026-07-07 06:20 | spec ⓒ | (공통 반영) 키값 용어 정의(W3)·DATA-001-01 개정(연동이력 예외) — common.md | `accountinterlockhub#25` | ℹ️ |
| 2026-07-06 17:10 | spec ⓒ | (공통 반영) DB 표기 PostgreSQL 정합화 — common.md | `accountinterlockhub#24` | ℹ️ |
| 2026-07-06 03:20 | spec | BIZ-003·SEC-002·SVC-005·FN-012·SCR-006·PROC-203·TC USR-02 | `accountinterlockhub#24` | 🚧 |
| 2026-07-06 03:20 | spec ⓒ | (공통 반영) 무저장·신뢰위임·공통 기능·디자인 시스템·시나리오 TC — common.md | `accountinterlockhub#24` | ℹ️ |
