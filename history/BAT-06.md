# IA: BAT-06 — 연동 지표 집계

> 본 IA 노드와 연관된 작업의 시계열 이력(최신순). 정책: ai/strategies/ia-history.md

| 일시 (KST) | 단계 | 산출물·결과 | 관련 일감 | 상태 |
|---|---|---|---|---|
| 2026-08-02 12:50 | qa ⓒ | (공통 반영) 전체 기능 품질검수 재수행 — 254 TC 전건 처리(🟢 55 · 🟣 2 · 🔴 0 · 🟠 195). 미해소 Fail 0 · 🟠 주 사유 = PostgreSQL 다운(#516). 구현↔검증 공백 2건 spec 보완 제안(#604·#605) — common.md | `accountinterlockhub#514` | ℹ️ |
| 2026-08-02 12:20 | build ⓒ | (공통 반영) 배포 산출물 산출·런타임 게이트(부분 확보 — DB 미가용분 qa 이월)·qa 인계 문서 신규 — common.md | `accountinterlockhub#514` | ℹ️ |
| 2026-07-26 18:45 | spec ⓒ | (공통 반영) `f4e515b` 하류 정합 — 신설 `OPS-004` 를 `process_PROC-303.md` §관련 사양 코드 정책 행에 1:1 반영: `OPS-004-01`(`B3`·`B4` 질의 실행)·`OPS-004-03`(**상위 경계에 참여** — 커넥션 소실은 상위의 500 `EX-BIZ-003` 으로 귀결되며 계수만 따로 되돌아가지 않는다)·`OPS-004-04`(로그 기록 — FN-015). 의사코드·분기표·단계표 무변경 — common.md | `accountinterlockhub#489`·`#494`·`#469` | ℹ️ |
| 2026-07-26 13:52 | spec | `docs/specs/processes/process_PROC-303.md`(§진입점 bullet 신설·입력/출력 정의·실행 제약사항·`C1` 호출 계약·`B4` 주석·단계 통합 흐름 표) · `docs/specs/functions/function_FN-012-013.md` `FN-013` §구현 가이드 **2 bullet** — **`exec` 전달 경로 표기 + 넘기지 않는 자리를 사양으로 확정**(`b4fe224` 결손 보완 — 배경은 USR-04.md 항목). **이력·산출물 동일 commit**(작성 시점 미커밋 — 8파일 일괄). **[① 계기별로 갈린다]** 입력표에 **`exec`(`TxExecutor`) 행을 선택으로** 신설하되 **"`UNIDENTIFIED_FAILURE` 외 전부 필수"** 조건을 달았고, §실행 제약사항에 "참여는 넘긴 `exec` 위에서만 성립한다 / **`UNIDENTIFIED_FAILURE` 만 예외로 참여할 경계가 없어 `exec` 없이 단독 갱신한다**"를 명시했다. `C1` 계약은 ①`REQUEST`·③`RESULT` 가 경계 안에서 부르며 그 커넥션·실행자를 함께 넘기고(성립 조건) ②는 넘길 문맥이 없어 빼는 것으로 갈랐으며(`전달 값: { kind, resultCode?, at, exec }` + ② 예외 주석), `B4` UPSERT 주석 3줄·단계 통합 흐름 표 1행(`C1` → "계기·시각·`exec` 전달 · ②는 `exec` 없음")을 동반 정합했다. **[② 누락이 아니라 사양이라고 못박았다]** §진입점 및 진입 조건에 **"`exec` 를 넘기지 않는 자리는 `UNIDENTIFIED_FAILURE` 하나뿐"** bullet 을 신설해 그 세 호출 지점(`PROC-101` `B5` · `PROC-102` `B4b` · `PROC-104` `B2`)이 **호출측이 경계를 열지 않아 넘길 실행 문맥이 없음**을 적고, **표기 누락이 아니므로 나중에 `exec` 인자를 채워 넣지 않는다**를 문면에 남겼다(`FN-013` §구현 가이드의 같은 bullet 에도 병기). 다음 사람이 "빠진 인자"로 오인해 채우면 **경계 없는 자리에 경계를 요구**하게 되므로 금지 자체를 사양에 둔 것이다. `FN-013` 에는 `REQUEST`·`RESULT` 의 전달 경로(`PROC-102` `B6`·`PROC-103` `B3` → `PROC-301` `B3`(FN-008) → 본 기능 / `PROC-104` `B6` → `PROC-301` `B4`(FN-009) → 본 기능, 각 구간이 받은 `exec` 를 **그대로** 넘긴다)도 한 줄로 이었다. **[③ P06 구현 무영향 — 실측 확인]** build **P06(`#483`)이 13:18 🟢 Resolved** 된 범위라 오케스트레이터가 구현을 **직접 읽어** 판정했다 — `apps/backend/src/records/metric-counter.service.ts:44` 의 `recordEvent(event, executor = this.db)` 가 **executor 를 선택·기본값**으로 받아 위 "`UNIDENTIFIED_FAILURE` 외 전부 필수"와 **이미 일치**하고, 같은 파일 `:25` 주석이 "`executor` 는 `REQUEST`·`RESULT` 계기에서는 반드시 넘겨야 한다"로 **사양과 같은 규칙**을 이미 적고 있다. 즉 **사양이 구현을 뒤늦게 문서화한 것**이라 **P06 재검증 불요**다. **원자적 UPSERT·카운터 4종·일자 경계(`Asia/Seoul`)·`B5` 지표 산출 절차·TC 는 무접촉** | `accountinterlockhub#475`·`#473`·`#469` | 🚧 |
| 2026-07-26 12:51 | build | `apps/backend/src/records/metric-counter.service.ts` — **P06 PROC-303 완결성 확인**(문서 주석 추가만, 로직 무변경). process_PROC-303.md 의 진입 계기(`kind` = `REQUEST`·`UNIDENTIFIED_FAILURE`·`RESULT`)가 FN-013(P04) 자신의 시그니처(`MetricEvent` 판별 유니온)와 이미 완전히 같고, `B1`(계수 계기 수신 — 열거형 재검증)·`B2`(일자 확정)·`B3`~`B4`(대상 컬럼 결정 + 원자적 UPSERT)가 `recordEvent()` 의 처리 순서와 1:1 이라 **PROC-301 과 달리 별도 kind 디스패처가 필요 없음을 확인**(하나의 함수 시그니처 안에 이미 계기별 분기가 있다 — 상세 판단 근거·PROC-301 신규 디스패처는 BAT-04.md 항목). `B5`(지표 산출)는 조회 API·화면이 없는 저장분 직접 확인 절차(`SVC-016` 사용자 정의)라 애플리케이션 코드로 구현하지 않는다 — 그대로 둠. **실측(실 DB)**: `works/accountinterlockhub-483/verify.js` — ① `UNIDENTIFIED_FAILURE` 를 `exec` 생략(풀 단독)으로 직접 호출 → `request_count`+1 **및** `decrypt_failed_count`+1 동시 갱신(원자적 UPSERT 한 문장) ② `REQUEST`·`RESULT` 계기는 PROC-301 dispatcher(SECURE·FIX_RESULT) 경유로 호출측 트랜잭션에 참여해 함께 커밋/롤백됨을 재확인(호출측 ROLLBACK 시 지표 행 자체가 남지 않음 — BAT-04.md 4·5번 항목) ③ 동시 8건 경쟁에서도 REQUEST 카운터가 정확히 1(경쟁·재시도로 인한 과다 계수 없음) ④ **확정 결과 재진입 시 결과 카운터 무갱신** — FIX_RESULT 를 다른 결과 구분으로 재호출해도 최초 확정값의 카운터만 유지되고 재진입분은 어느 카운터도 올리지 않음(제약 "확정 결과 재진입은 지표 요청 수에 계수하지 않는다" 직접 확인). 카운터 4종(요청 수 1 + 결과 구분 3, 거부 카운터 없음)·일자 경계 `Asia/Seoul`·원자적 UPSERT 자체는 P04(`#481`)가 이미 구현했고 이번 Phase 는 **무변경**(데이터 격리·정리·연결 점유 실측 방법론은 BAT-04.md 항목 참고). `npm --prefix apps/backend run build` 에러 0 | `accountinterlockhub#483`·`#469` | 🚧 |
| 2026-07-26 10:42 | spec | `docs/specs/functions/function_FN-012-013.md` `FN-013`(연동 지표 계수) — **트랜잭션 소유권 표기 통일 + 계기별 경계 유무 확정**(commit `1491e4f` SD-1 의 기능 도메인 확산분). **근거 commit `b4fe224`** — 본 entry 작성 시점엔 미커밋이었고, 오케스트레이터가 산출물과 이 이력을 같은 commit 에 함께 넣었다(정책대로 동시 반영). 시그니처에 `exec: TxExecutor` 를 **선택 인자**로 추가하되 **계기별로 필수 여부를 갈랐다** — ① **`REQUEST`·`RESULT` 는 반드시 넘긴다**(레코드 기록과 함께 커밋되거나 함께 되돌려져야 하므로 같은 커넥션·실행자가 필요하다 — `PROC-303` §실행 제약사항·`SVC-014` F-008. 생략하면 별도 커넥션에서 갱신돼 **호출측이 되돌려도 계수만 남고** 요청 하나가 커넥션을 둘 점유한다) ② **`UNIDENTIFIED_FAILURE` 만 `exec` 없이 부른다** — 그 계기의 호출 지점(`PROC-101` `B5`·`PROC-102` `B4b`·`PROC-104` `B2`)은 호출측이 경계를 열지 않는 자리라 넘길 실행 문맥이 없고, 단계 4 의 UPSERT 한 문장이 그 자체로 원자적이다. 단계 4·5 라벨을 **"(호출측 트랜잭션에 참여 — 새로 열지 않는다)"·"(커밋하지 않는다 — 경계를 닫는 것은 호출측이다)"** 로 교체하고 의사코드에 `exec` 유무별 실행 문맥 주석 2줄을 달았다. 오류 처리표의 "같은 트랜잭션의 레코드 기록과 함께 되돌린다"를 **"본 기능은 되돌리지 않고 예외만 올린다 — 되돌림은 경계를 연 호출측"** 으로 정정하고, **`UNIDENTIFIED_FAILURE` 는 되돌릴 경계가 없어 호출측이 실패 처리를 정한다**(`PROC-101` `B5` — 계수 실패가 응답을 막지 않는다)를 명시. 계기별 계수 표·§구현 가이드에도 같은 구분을 1줄씩 반영. **원자적 UPSERT·카운터 4개(요청 수 1 + 결과 구분 3)·감산 없음 등 계수 동작 자체는 무변경**. 판단 근거·`PROC-301` 연쇄 상세는 BAT-04.md 항목 참고 | `accountinterlockhub#475`·`#473`·`#469` | 🚧 |
| 2026-07-26 08:45 | build | `apps/backend/src/records/metric-date.ts` — P04(`#481`) 회귀 1회차 자체보완 [S-4] 시정. 일자 경계 산출이 `Intl.DateTimeFormat('en-CA', {...}).format()` 의 **완성 문자열 배치**(`YYYY-MM-DD` 순서)에 기대고 있어, `small-icu` 로 빌드된 Node 배포 환경에서 `en-CA` 로케일 데이터가 없으면 `en-US`(`MM/DD/YYYY`)로 조용히 폴백해 `MDL-003.metricDate` 형태가 깨질 위험이 있었다(ENT-003 §일자 경계 기준의 "배포 환경 무관 결정성" 요구와 상충 가능성). `formatToParts()` 로 연·월·일 **값**만 이름(`type`)으로 뽑아 직접 조립하도록 교체 — 로케일이 어느 영어권으로 폴백되든 개별 필드 값 자체는 달라지지 않으므로(그레고리력·서양 아라비아 숫자 공통) 배포 ICU 빌드와 무관하게 결정적이다. 요청 로케일도 `small-icu` 에 항상 포함되는 `en-US` 로 낮춰 로케일 미해석 여지 자체를 제거. **실측**: `works/accountinterlockhub-481/verify.js` 에 Asia/Seoul 자정 경계 전후(23:59/00:00 KST) 2케이스를 추가해 기존 로직과 동일한 결과(날짜가 정확히 하루 갈림)를 재확인. `FN-013` 원자적 UPSERT(`ON CONFLICT (metric_date) DO UPDATE`)·`executor` 필수 인자(호출측 트랜잭션 참여, 이번 회귀로 무변경) 등 전체 실측 상세는 BAT-04.md 항목 참고 **[사양 델타 정합 시정 — SD-1 후속(`b4fe224`) `FN-013` `exec` 선택 인자화, 2026-07-26]** P04(`#481`)가 `해결` 상태에 도달한 뒤 spec 세션이 `1491e4f`+`b4fe224` 로 `FN-013` 시그니처에 `exec?: TxExecutor` 를 **선택 인자**로 신설(`REQUEST`·`RESULT` 는 반드시 넘기고, 호출측이 경계를 열지 않는 `UNIDENTIFIED_FAILURE` — `PROC-101` `B5`·`PROC-102` `B4b`·`PROC-104` `B2` — 만 생략 가능)했다. 기존 `recordEvent(executor: QueryExecutor, event: MetricEvent)` 는 `executor` 가 **항상 필수**라 `UNIDENTIFIED_FAILURE` 호출부가 넘길 실행 문맥이 없는데도 인자를 강제해 신 사양과 어긋났다(미충족). **시정**: `MetricCounterService` 에 `DatabaseService` 를 주입하고 `recordEvent(event: MetricEvent, executor: QueryExecutor = this.db)` 로 교체 — 인자 순서를 사양 시그니처(이벤트 우선)와 `FN-007` `lookup()` 의 "생략 시 풀 단독 실행" 관례에 맞췄다. 호출부 2곳(`tracking-record.service.ts` `secure()`·`fixResult()`)의 인자 순서도 함께 조정(상세 BAT-04.md 항목). **실측(실 DB)**: exec 생략(`UNIDENTIFIED_FAILURE`) → 풀 단독 UPSERT 성공(요청수·복호화실패수 각 +1) / exec 지정(`REQUEST`) + 호출측 `ROLLBACK` → 지표 행 0(정상 원복, 참여 성립 확인). 원자적 UPSERT·컬럼 대응표·감산 없음 등 계수 로직 자체는 **무변경** | `accountinterlockhub#481`·`#469` | 🚧 |
| 2026-07-26 02:10 | build | `apps/backend/migrations/0001_create_storage_tables.{up,down}.sql`(ENT-003 DDL 포함)·`apps/backend/src/entities/interlock-metric-daily.{entity,model}.ts`(MDL-003) 신규 — P02(같은 커밋, BAT-04.md 항목 참고). `tbl_interlock_metric_daily` 정확히 5컬럼(PK `metric_date` + 카운터 4개 `request_count`·`success_count`·`decrypt_failed_count`·`delivery_failed_count`, 전부 `BIGINT NOT NULL DEFAULT 0` + `>= 0` CHECK) — **거부 카운터·행 단위 합계 등식 CHECK 미도입**(spec 확정대로, BAT-06_006·BAT-06_008 대응). 삭제용 인덱스 없음(PK 뿐, DATA-002-03). `pg` 드라이버가 BIGINT 를 문자열로 반환하는 특성을 그대로 ENT 타입에 반영하고 MDL-003 매핑(`toInterlockMetricDailyModel`)에서 `Number()` 변환. **DATE 컬럼 타임존 함정 선제 대응**: `pg` 기본 파서가 DATE 를 UTC 자정 JS Date 로 바꿔 로컬 해석 시 하루 밀리는 문제를 `src/database/pg-type-parsers.ts`(OID 1082 재정의)로 차단 — 실접속으로 재현(재정의 미적용 시 `2020-01-01` 삽입이 `2019-12-31T15:00:00Z` 로 되읽힘) 후 재정의 로드 시 원문 문자열 그대로 반환됨을 별도 검증. **실측**: 카운터 `>= 0` CHECK(음수 UPDATE 거부) 확인. ⚠️ 저장 3종 원칙 발견사항(구 스키마 잔존)은 BAT-04.md 항목 참고 — 본 Phase 범위 밖. **회귀 1회차**: 위 서술 중 DATE(OID 1082) 파서 메커니즘 설명을 실측에 맞게 정정 — pg 기본 파서는 UTC 자정이 아니라 `new Date(year, month-1, day)` 프로세스 로컬 자정 기준으로 Date 를 생성하며, 로컬 TZ 가 `Asia/Seoul`(UTC+9)인 이 환경에서 그 값을 ISO(UTC) 로 직렬화하면 하루가 밀려 표시된다(실측: 입력 '2026-07-25' → `toISOString()` 이 '2026-07-24T15:00:00.000Z' 반환 — 위 재현 사례 자체는 이 로컬-자정 메커니즘과 정합했으나 원인 설명 문구가 "UTC 자정"으로 반대로 적혀 있었다. 재정의로 원문 문자열을 그대로 반환하는 대응 로직 자체는 종전에도 옳게 구현돼 있었다). `src/database/pg-type-parsers.ts` **주석만** 정정(코드 동작 무변경). **훑음**: `model_MDL-003.md` 은 "읽기 전용" 구조 지시가 없어(전수 확인) `InterlockMetricDailyRow`·`InterlockMetricDailyModel` 은 변경하지 않음 — 근거 없는 확대 금지. 독립 코드리뷰 Important 지적([I-1], MDL-002)은 BAT-05.md 항목, `migration:revert` 프록시는 BAT-04.md 항목 참고. **실측**: `npx tsc --noEmit`(backend) exit 0 · 루트 `npm run build` 에러 0건 | `accountinterlockhub#479`·`#469` | 🚧 |
| 2026-07-26 00:45 | spec ⓒ | (공통 반영) 요구사항 회귀 — **`ENT-003.user_denied_count` 컬럼 삭제**(카운터 5→**4개** = 요청 수 1 + 결과 구분 3). `data_ENT-003.md`·`service_SVC-016.md`·`process_PROC-303.md` — `USER_DENIED` 가 값 체계에서 빠져(`BIZ-001-01`) 항상 0 인 컬럼을 남기지 않는다(`BIZ-005-01`). **미동의 이탈은 요청 수에만 남고 결과 구분 카운터를 올리지 않으므로** 한 행에서 세 결과 카운터의 합과 `request_count` 가 같지 않을 수 있음을 명시하고 **둘을 묶는 CHECK 제약을 두지 않는다**. 4개 카운터만으로 **두 성공률 정의(결과 미확정 분모 포함/제외)를 모두 산출**할 수 있음은 유지(`BIZ-005-06` 담당자 이월 무영향). TC `tc_BAT-06.md` 컬럼 목록·거부 카운터 부재 확인 기대값 정정(TC 신설·결번 0) — common.md | `accountinterlockhub#469`·`#470`·`#471`·`#472`·`#475`·`#476` | ℹ️ |
| 2026-07-25 17:01 | spec ⓒ | (공통 반영) 교차검증 1회차 지적 보완(회귀 1회차 — 🟠 2·🟡 7 전건 해소) — `FN-013` §계수 계기 대응의 '진입 단계 구조 위반으로 종료' 호출 지점에 **`PROC-102`(`EX-SEC-001` 경로)** 추가 — common.md | `accountinterlockhub#473` | ℹ️ |
| 2026-07-25 16:19 | spec ⓒ | (공통 반영) 검증 TC 도메인 신규 작성 — `docs/specs/qa/` **TC 232건**(커버리지 SVC 18/18 · PROC 18/18 · BR 24/24 · EX 13/13 · 정책 17/17 · 접점 11/11 — 미검증 0)·**횡단 시나리오 `SCEN_001`~`SCEN_023`**(여정 6 · 규약 대칭 왕복 3 · 기동·상수 검증 5 · 무저장 원칙 2 · 설계된 부재·수용 리스크 7)·**AUTH 치환 12건**·모의 수신처·C# 하네스·시드 카탈로그 37항·미해소 1건(성공률 정의 이월) — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-25 16:05 | spec | `docs/specs/qa/BAT/tc_BAT-06.md` 신규 — **연동 지표 집계 TC 13건**(API·서버). **성과 지표 목표치 달성 여부는 검증 대상에서 제외**(잠정값 — 미달을 결함으로 다루지 않는다)하고 **카운터 누적 정확성**을 검증 대상으로 고정. 레코드 최초 생성 시 요청 수 +1·추적 키 미확보 실패 시 요청 수 + 복호화 실패 동시 +1(3 경로)·결과 4종 각각 레코드당 1회·**재진입·재입력 10회·확정 재안내 전건 미계수**·자가진단 40회 미계수·카운터 5개 분리·파생 비율 컬럼 0·**일자 경계 `Asia/Seoul` 고정**(DB 세션 UTC 에서도 동일)·**행 합계 등식 미강제**(양방향 불일치가 정상)·동시 20건 UPSERT 유실 0·미확보 실패 중복 계수는 수용 한계·**두 성공률 정의 모두 산출 가능**(🟡 G-05)·UPSERT 실패 시 추적 레코드도 되돌림·지표는 보관 배치 대상 아님. 검증 BR-016·BR-018 · EX `EX-BIZ-003` | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec | `process_PROC-303.md` 신규 — **PROC-303 연동 지표 집계**(EVT·호출측 트랜잭션 참여). 계기 3종(`REQUEST`·`UNIDENTIFIED_FAILURE`·`RESULT`)과 **호출되지 않는 자리**(재진입·재시도·재안내·자가진단·완료 콜백) 명시. `B2` `Asia/Seoul` 고정 일자 산출(시간대 명시) → `B3`·`B4` **삽입과 증가를 한 문장(원자적 UPSERT)** · 파생 비율 미저장 · `B5` 지표 산출은 저장분 직접 조회(조회 API·화면 없음)로 **두 성공률 정의 모두 산출 가능**. 추적 키 미확보 실패의 중복 계수는 수용 한계 | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec ⓒ | (공통 반영) 프로세스 도메인 신규 작성 — `docs/specs/processes/` **PROC 18종 확정**(정책·서비스 예약분을 같은 코드·같은 의미로 — 통합·폐기·추가·재배치 0)·**단계 라벨 체계 확정**(`F` FE·`B` BE·`C` 호출측·`L` 라이브러리 내부, 레이어별 1부터 시간 순, 분기는 접미 문자)·선행 도메인 단계명 전건 라벨 매핑·의존관계 순환 0(호출 깊이 3층)·트랜잭션 경계와 멱등 수단 총괄·정책 규칙 86건 매핑(런타임 80 + 게이트·절차 6) — common.md | `accountinterlockhub#475`·`#469` | ℹ️ |
| 2026-07-25 14:40 | spec ⓒ | (공통 반영) 화면 도메인 신규 작성 — `docs/specs/screens/` **화면 4건**(SCR-001~004 — 서비스 예약 대비 통합·폐기·추가·재배치 0)·**단일 경로 4단계 모델**(리다이렉트 없음·화면별 URL 없음)·**디자인 시스템 1벌**(표면이 하나라 토큰 분기·접두어 없음 · 색 23종 **실측 대비비 표** · WCAG 2.1 AA · 모바일 우선 2 브레이크포인트 · 히트 영역 44x44)·공통 컴포넌트 11종·**화면 없는 IA 노드 12건 근거 기재** — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-25 14:10 | spec | `function_FN-012-013.md` §FN-013 — 지표 카운터 갱신 기능 확정. 계기 3종(`REQUEST`·`UNIDENTIFIED_FAILURE`·`RESULT`)과 갱신 컬럼 대응을 한 곳에 고정하고, **계기별 호출 지점 표**를 확정(레코드 최초 생성 = FN-008 / 진입 구조 위반 = PROC-101 / 판정 3·4단계 실패 = PROC-102·104 / 결과 확정 = FN-009 / 재진입·재시도·재안내·자가진단 = 미계수). 일자는 `Asia/Seoul` 을 **명시적으로 지정**해 변환하고, 삽입과 증가를 **한 문장(원자적 UPSERT)** 으로 처리해 동시 요청에서 계수가 유실되지 않게 한다. 감산·이동 없음, 파생 비율 미저장(성공률 정의는 담당자 이월 — 목표 수치는 PRD 참조하고 옮겨 적지 않는다) | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-25 14:10 | spec ⓒ | (공통 반영) 기능·API 도메인 신규 작성 — `docs/specs/functions/` **공통 기능 15건**(FN-001~015 · POL 5·DAT 8·CRS 2·**EXT 0**)·**인터페이스 11접점**(사용자 진입 표면 3 · 서버 대면 API 4 · 수신처 전달 1 · 명령 진입점 1 · 라이브러리 공개 진입점 2)·오류 응답 엔벨로프 `{code,message,details?}`·인증 0/요청 제한 0·**신규 EX `EX-OPS-002` 1건**을 정책 카탈로그에 등록 — common.md | `accountinterlockhub#473`·`#469` | ℹ️ |
| 2026-07-25 13:35 | spec | `data_ENT-003.md`·`model_MDL-003.md` 신규 — **ENT-003 연동 지표 집계**(`tbl_interlock_metric_daily`, 집계·일자당 1행·PK `metric_date`). **[진입 게이트 🟡 G-05 자체보완] 카운터 5개 분리 저장** = `request_count` + `success_count`·`user_denied_count`·`decrypt_failed_count`·`delivery_failed_count` → **전체 성공률(성공÷요청)과 거부 제외 기술 성공률(성공÷(요청−거부))을 둘 다 산출 가능**. 파생 비율 컬럼 미저장(`BIZ-005-01`), **목표치 판정에 쓸 정의 확정은 담당자 이월**(`BIZ-005-06`), 목표 수치 미복제(PRD §성과 지표 참조). **[위임 확정] 일자 경계 시간대 = `Asia/Seoul`(UTC+09:00) 고정** — 지표를 읽는 주체가 국내 운영 주체이고 한국은 서머타임이 없어 경계가 흔들리지 않으며 저장 일시가 전부 `TIMESTAMPTZ` 라 변환이 결정적이다. 일자 산출 시 시간대를 명시 지정(서버·세션 기본 시간대 의존 금지). **행 단위 합계 등식 CHECK 미도입** — ① 결과 미확정 연동 ② 일자 경계를 걸친 연동(요청 수와 결과가 다른 일자에 쌓임) 때문에 등식이 성립하지 않으며 제약을 두면 정상 데이터가 거부된다. 각 카운터는 `>= 0` 만 갖는다. **삭제 대상 아님**(`DATA-002-03`) → 삭제용 인덱스 없음·감사 컬럼 미도입(사용 PROC 0건). 계수 규칙 표(요청 수 2시점·결과 확정·재진입/재시도/재안내 미계수·자가진단 미계수·추적 키 미확보 실패의 중복 계수 수용) 반영 | `accountinterlockhub#472`·`#469` | 🚧 |
| 2026-07-25 13:35 | spec ⓒ | (공통 반영) 데이터 도메인 신규 작성 — `docs/specs/datas/` **엔터티 3건**(ENT-001~003 = 저장 대상 셋)·**데이터 모델 22종**(MDL-001~022 확정 — 예약 대비 통합·추가·재배치 0)·인덱스 5·물리 FK 0·소프트 삭제 미도입 — common.md | `accountinterlockhub#472`·`#469` | ℹ️ |
| 2026-07-25 13:05 | spec | `service_SVC-016.md` 신규 — **SVC-016 연동 지표 집계**(매핑 PROC-303). Happy Path 5단계(계수 계기 수신→일자 확정→요청 수 계수→결과 카운터 갱신→지표 산출), 출력 MDL-003. BR-018 = 계수 시점 분기(레코드 최초 생성 / 추적 키 미확보 실패 → 요청 수+`DECRYPT_FAILED` / 재진입·재시도·재안내는 미계수). 예외 `EX-BIZ-003`(같은 경계 롤백). **요청 수 1 + 결과 구분 4종 분리 저장·파생 비율 미저장**(G-05 대응), 자가진단 미계수, 추적 키 미확보 실패의 중복 계수는 수용 한계. **성공률 정의는 담당자 이월·목표치는 PRD 참조(수치 미복제)**. 보관 삭제 대상 아님 | `accountinterlockhub#471`·`#469` | 🚧 |
| 2026-07-25 13:05 | spec ⓒ | (공통 반영) 서비스 도메인 신규 작성 — `docs/specs/services/` **서비스 18건**(SVC-001~018)·사용자 역할 4종·분기 `BR-001`~`BR-024`·MDL 22종/SCR 4종 예약 채번·`PROC-403`·`PROC-404` 추가 채번 — common.md | `accountinterlockhub#471`·`#469` | ℹ️ |
| 2026-07-25 12:28 | spec | `policy_BIZ.md` `BIZ-005` 지표 집계 계수 기준 신설 — 집계는 **요청 수 1개 + 결과 구분 4종 각각을 분리 저장**하고 파생 비율(성공률)을 저장하지 않는다(진입 게이트 🟡 G-05 대응 — 두 성공률 정의를 모두 산출 가능하게). 요청 수 계수 시점은 둘뿐(① 추적 레코드 최초 생성 ② 추적 키 미확보 실패 종료 시 — 이때 `DECRYPT_FAILED` 동시 +1), 같은 키 재진입·본인확인 재시도·**확정 결과 재안내는 미계수**. 결과 구분 카운터는 확정 시점 1회(감산·이동 없음). 추적 키 미확보 실패는 중복 판정 불가라 시도마다 계수되며 **수용 한계**로 기록. **성공률 분모·분자 정의 확정은 담당자 이월**이고 목표치는 PRD §성과 지표 참조(수치 미복제). 지표 집계는 보관 삭제 대상 아님(`DATA-002-03`) | `accountinterlockhub#470`·`#469` | 🚧 |
| 2026-07-25 12:28 | spec ⓒ | (공통 반영) 정책 도메인 신규 작성 — `docs/specs/policies/` 정책 17건·규칙 86건·EX 코드 12종(암호화 연동 규약·복호화 판정 4단계·결과 구분 4종·추적 레코드 생성 시점·보관·무저장·동의 증적·인증 부재·수용 리스크) — common.md | `accountinterlockhub#470`·`#469` | ℹ️ |
| 2026-07-25 11:25 | directing ⓒ | (공통 반영) 값 확정 대기 잠정값 설정 — 성과 지표 목표치 잠정 설정(월 1,000건 · 성공률 95% 이상 · 발송처 통합 소요 5영업일 이내). 집계 근거는 본 노드가 제공하며, 확정 전 미달은 결함으로 다루지 않는다 — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
| 2026-07-25 10:05 | directing ⓒ | (공통 반영) 리셋 방향 담당자 확정분 반영 — 일자별 집계 카운터 **신설**(고도화 제안 채택). 요청·결과 구분별 건수·추적 키 미포함·삭제 대상 아님 — 보관 삭제 후 성과 지표 산출 근거 — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
