# IA: USR-03 — 본인확인

> 본 IA 노드와 연관된 작업의 시계열 이력(최신순). 정책: ai/strategies/ia-history.md

| 일시 (KST) | 단계 | 산출물·결과 | 관련 일감 | 상태 |
|---|---|---|---|---|
| 2026-07-26 18:37 | spec ⓒ | (공통 반영) 디자인 시스템 §접근성 기준 — **진입(최초 로드)에는 제목으로 프로그램 포커스를 옮기지 않는다**는 **화면 무관 일반 규칙** 신설(`design-system.md`·`design-system-components.md`). 이 노드에 닿는 것은 **`SCR-001`**(본인확인)이며 동작은 **무변경**이다 — 이 화면 제목이 제품에서 프로그램 포커스를 받는 자리가 없다는 기존 결론(진입은 규칙 대상 아님 · `BackToIdentity` 는 필드가 가져감)의 **근거 문장만** `SCR-001` 한정 서술에서 일반 규칙 참조로 정정됐다. 본문·근거는 common.md | `accountinterlockhub#493`·`#469` | ℹ️ |
| 2026-07-26 16:08 | build | `apps/frontend/src/hooks/useStageFocus.ts` — P15(`#492`) **회귀 1회차** 중 이 노드에 닿는 것은 **[S-1] 공유 파일 자체보완**뿐이다([I-1] 🔴 시정 본체는 `flow/ConsentScreen.tsx`(SCR-002) 라 USR-04.md 항목 참고). `skipFocusRef`(렌더 본문 ref 쓰기 — React 공식 지침이 금지하는 concurrent 비호환 패턴)를 제거하고 effect 안에서 `skipFocus` 를 직접 읽도록 정리했다 — deps 가 `[title]` 이라 effect 가 실행되는 시점의 클로저는 이미 그 렌더의 `skipFocus` 값을 그대로 캡처하므로 ref 경유로 "실행 시점의 최신값"을 따로 확보할 필요가 없었고, 오히려 ref 쪽이 "전환 시점이 아닌 호출 시점"의 값을 읽어 덜 정확했다(기능 무변화, `eslint-disable-next-line react-hooks/exhaustive-deps` 사유 주석은 유지). **`flow/IdentityScreen.tsx`(SCR-001) 는 무접촉** — 독립 code-reviewer(opus) 가 `format`·`mismatch` 는 `TextField` 가 항상 렌더돼 받아 갈 요소가 보장되고 `alert===null`(진입)은 "아무도 받지 않는 것"이 사양 요구 자체라고 판정해, 이 노드는 [I-1] 시정 대상에서 제외됐다. **실측**(CDP, 격리 프로필 헤드리스 Chrome 포트 3392/9392 — 이번 회귀 전용 신규 프로필, `apps/frontend/dist/` 정적 서빙 + fetch 모킹, 백엔드 미기동) — S-1 리팩터 전후로 이 노드의 두 관측이 무변화임을 재확인: 진입(최초 로드) `activeElement` **`BODY`**(X-2 유지, 직전 09:18 row 값과 동일) · `BackToIdentity` **`INPUT#birthDate`** 무변화(mismatch `selectionStart/End=0/6` 전체 선택 · format `6/6` 커서만, 둘 다 09:18 row 값과 동일) · 제목→보조 설명 `4px`·블록 아래 `24px`·포커스 링 `outline 2px solid rgb(11,79,168) offset 2px` 전부 무변화. `npm --prefix apps/frontend run build` 에러 0건. 작성만 수행 — 코드리뷰·기능검증은 별도 doer 대기 | `accountinterlockhub#492`·`#469` | 🚧 |
| 2026-07-26 15:35 | build | `apps/backend/src/interlock-verify/{verify.controller.ts,verify.module.ts,identity-verification.service.ts,verify-request.dto.ts,verify-response.model.ts,index.ts}` + `apps/backend/src/app.module.ts` — **PROC-102 본인확인 제출 `POST <INTERLOCK_ENTRY_PATH>/verify` 최초 구현**(P08, `#485`). `B1`(컨트롤러 — 형상 재검증)~`B8`(응답 구성)을 `IdentityVerificationService` 로 오케스트레이션한다 — `runIdentityCheckGate()`(`B2`~`B5`: FN-005 → FN-004 → 추적 키 추출, `judgeDecryption()` 반환값에서 `payload` 만 구조 분해해 `rawPlaintext` 는 애초에 참조를 만들지 않는다) → `B6`(`DatabaseService.withTransaction()` 으로 경계를 열고 `PROC-301`(`TrackingRecordProcessService.record`) `SECURE` 호출) → `B7`(`FIXED` 분기 — `ResultInfoBuilder` 재사용, 갱신·계수 없음) → `B8`(`CONSENT` — `ConsentConfig` 세 필드를 명시로 재구성). **`exec` 시그니처 3종을 정의 파일로 실측 확인**: `judgeDecryption()` 은 `exec` 자체가 없음(순수 함수, DB 미접근) · `TrackingRecordProcessService.record()`(`PROC-301`, 프로세스)는 **단일 입력 객체 안**(`{…,exec}`) · `MetricCounterService.recordEvent()`(`FN-013`, 함수)는 **두 번째 위치 인자**(`event, executor?`) — `B4b` 의 `UNIDENTIFIED_FAILURE` 계수는 트랜잭션이 아직 없는 자리라 이 인자를 아예 생략했다(누락이 아니라 사양, `LOOKUP` 과 같은 원리). **`B4b` 는 `PROC-104 B2` 와 다르다** — `process_PROC-102-logic.md` 그대로 `EX-SEC-001`(구조 위반)·`EX-SEC-002`(판정 3·4단계) **둘 다** 계수 대상으로 구현했다(`InterlockDeliveryService.runDecryptionGate` 는 `EX-SEC-002` 하나뿐이라 그 catch 패턴을 그대로 복사하지 않고 조건에 `ProtocolFormatError` 를 더했다 — 두 프로세스의 의도된 비대칭). **본문 JSON 파싱 실패 분류는 착수 시점에 이미 해소돼 있었다**(P05 회귀 2·3회차, commit `5a47542`·`b414a25` — `body-parse-failure.ts` `USER_SUBMIT_EX_BY_SUFFIX` 가 `/verify` → `EX-AUTH-001` 을 이미 매핑, `spec-functions-api.md:54` 와 일치) — 이 Phase 는 추가 조치 없이 실측 확인만 하고 그대로 재사용했다(착수 전 인계 노트가 가리킨 상태보다 진행돼 있었다). 응답 계약(`VerifyResponseBody`)은 이미 존재하던 `apps/frontend/src/api/types.ts` `VerifyResponseDto`(읽기 전용 확인·미편집)와 필드 단위로 대조해 맞췄다. 빌드 검증 `tsc --noEmit`·`npm run build` 에러 0건. 작성만 수행 — 코드리뷰·기능검증은 별도 doer 대기 | `accountinterlockhub#485`·`#469` | 🚧 |
| 2026-07-26 13:58 | spec | `docs/specs/functions/function_FN-007-008.md` `FN-008` **단계 5** — **요청 수 계수 호출에 실행 문맥(`exec`)을 인자로 명시**(13:52 회차와 같은 배경·같은 commit — 사슬 전체와 판정 근거는 BAT-04.md 항목). **이력·산출물 동일 commit**(작성 시점 미커밋 — 총 11파일). **[① 주석은 있었는데 인자가 없었다]** 추적 레코드 확보의 단계 5 가 `FN-013({ kind: 'REQUEST', at })` 로 부르면서 옆 주석에는 **"`exec` 와 같은 실행 문맥에서 수행한다"** 를 이미 못박아 두고 있었다 — 요구는 문서에 있는데 **그 실행 문맥을 어떻게 넘기는지가 호출 표기에 없던** 전형적인 결손이라 `FN-013({ kind: 'REQUEST', at }, exec)` 로 고치고 "받은 `exec` 를 두 번째 인자로 그대로 넘긴다 — 이 전달이 참여의 성립 조건이다" 주석 2줄을 달았다. **[② 왜 두 번째 인자인가]** `FN-013` 의 실행 문맥은 **계기 객체가 아니라 두 번째 인자**다(`function FN-013(event: MetricEvent, exec?: TxExecutor)` — §시그니처 실측). `exec` 를 `{ kind, at }` 안에 넣으면 **전달한 듯 보이되 실행자가 도달하지 않아** 계수만 별도 커넥션에서 커밋되므로, 형식을 인자 자리로 못박은 것이다(`PROC-301`·`PROC-303` 은 단일 객체 시그니처라 `{ …, exec }` 로 나타나는 것과 갈리는 지점 — BAT-04.md 항목). **[③ 이 노드의 몫]** `FN-008` 은 본인확인 제출이 여는 경계(`PROC-102` `B6`)에서 불리는 자리라, 이 전달이 끊기면 **레코드 생성과 요청 수 계수가 각각 커밋**돼 `SVC-014` F-008 원자성이 깨지고 요청 하나가 커넥션을 둘 점유한다 — 10:26 회차가 이 노드에 남긴 **"build 에서 커넥션 풀 교착으로 실현된 건"** 과 같은 뿌리이며, 그때 소유권을 통일하고 남은 마지막 결손을 이번에 닫았다. **[④ 구현 무영향 — 실측 확인]** `FN-007`~`FN-013` 은 build **P04(`#481`) 🟢 Resolved** 범위이나 오케스트레이터가 구현을 **직접 읽어** `apps/backend/src/records/tracking-record.service.ts:96` 의 `recordEvent({ kind: 'REQUEST', at }, executor)` 가 이번 개정분과 **문자 그대로 동일**함을 확인했다 — **사양이 구현을 뒤늦게 문서화한 것**이라 **재검증 불요**이고, `fdc9f76`(P04 회귀 1회차)가 뒤집은 트랜잭션 소유권 결정도 되돌리지 않았다. **[무접촉]** `FN-008` 단계 4 의 `ON CONFLICT (tracking_key) DO NOTHING RETURNING` 수렴·이어쓰기 판정·`isCreated` 규약, `PROC-102` 사양(부모·자식 둘 다), `tc_USR-03.md`(이 노드 TC 17건)·총 242건·결번 2건 전부 불변 · `LOOKUP` 예외를 못박은 `FN-007` §시그니처 bullet 은 IA `BAT-04` 몫이라 그쪽 항목 참고 | `accountinterlockhub#475`·`#473`·`#469` | 🚧 |
| 2026-07-26 13:35 | spec ⓒ | (공통 반영) 디자인 시스템 §접근성 기준 **포커스 우선순위 확정 — `QA-OPEN-02` 해소의 본체가 이 노드다** + 목업 4종 포커스 링 미충족 시정. **[① 필드 포커스가 이긴다]** 직전 `e39dd60` 이 등재한 `QA-OPEN-02`(영향 `USR-03`·`USR-04` · SCR-001 · TC 0건 · Block 0건)는 **본인확인 재입력 자리**의 충돌이었다 — `BackToIdentity`(SCR-002·SCR-003 → SCR-001 · 400 `EX-AUTH-001`·`EX-AUTH-002`) 도착이 곧 `ReEntry(형식)`·`ReEntry(불일치)` 라, §접근성 기준의 "새 제목으로 포커스"와 §InlineAlert 의 "재입력 안내는 해당 필드로 포커스"가 **함께 걸리는데 우선순위가 정본에 없었다**. **필드가 이기는 것으로 확정**했고, 그래서 `screen_SCR-001.md` §화면 상태 전이가 두 `ReEntry` 에 붙여 둔 **값 유지(전체 선택)** 설계가 살아난다(제목으로 보내면 `Tab` 이 한 번 더 필요해 오타 즉시 덮어쓰기가 무의미해진다 — 근거 ⓑ). **화면 사양은 한 글자도 고치지 않았다** — 도착 상태가 이미 정해 둔 도착지를 정본이 **추인**하는 방향이다. 파생 결론 — 이 확정으로 **`SCR-001` 제목은 제품에서 프로그램 포커스를 받는 자리가 없어져**, 이 화면에서 볼 것은 제목의 링이 아니라 **포커스가 필드로 갔는가**가 된다. **⚠️ 잔여(검증 도메인 후속)** — `spec-qa.md` §미해소 카탈로그 `QA-OPEN-02` 행의 **해소 상태 갱신**과 `USR-04_011` ③ 관측 추가는 **이번 회차 diff 에 없다**(미해소 집계 2건 표기 그대로). **[② 목업]** `mockup/SCR-001.html` 에 `h1.stage-title:focus` **1줄**(+주석 2줄) — 값은 조작 요소와 같은 한 벌(바탕색 2px 안쪽 여백 + 2px `--color-focus` `#0B4FA8`)이고, `316bf77` 이 세운 **재입력 상태 오류 표현(경계·`aria-invalid`·전체 선택)은 무접촉**이며 정상 상태 시각도 불변이다(마크업 0줄·새 토큰 0). 이 노드 TC **17건 무접촉** · 총 242건·결번 2건 불변. 규칙 본문·근거 셋·검증은 common.md 항목 참고 — common.md | `accountinterlockhub#474`·`#476`·`#469`·`#467` | ℹ️ |
| 2026-07-26 13:15 | spec | `docs/specs/processes/spec-process-mapping.md` `PROC-102` 행(`:121`) **2칸** + `spec-process-policy-map.md` `AUTH-002-04` 행(`:18`) **1칸 정정** + `docs/specs/qa/spec-qa.md` **`QA-OPEN-02` 신규 등재**. **이력·산출물 동일 commit**(작성 시점 미커밋). **[① 존재하지 않는 라벨을 가리키고 있었다]** 두 색인이 재입력 안내를 **`B4a` → `F4`** 로 적었는데 **`F4` 는 `PROC-102` 에 없다** — 정본 `process_PROC-102.md` §로직 실행 순서(`:74`)가 **FE 라벨 `F1`·`F2`·`F3`, 결번 없음**을 명시해(`PROC-103` 과 달리 결번이 없어 "폐지된 라벨"로도 설명되지 않는다) 정본 §단계명 매핑(`:89`)대로 **`F3`** 로 정정했다. 같은 색인 행의 **다음 단계 이관 `B8` → `F3`** 도 정본(`:91`)과 달라 **`F2`** 로 정정했다 — `B8` 은 200 `stage = CONSENT` **성공 응답**이라 받는 쪽은 성공 처리 `F2` 다. **두 어긋남이 나란히 한 칸씩 밀려 있어 결번 도입 이전 세대의 라벨 잔재로 보인다.** 정책맵 쪽은 `B4a` 가 내는 **400 `EX-AUTH-002`** 를 `F3`(실패 응답 처리)가 받는 것이 실제 핸드오프이고 `F3` 본문이 `AUTH-002-04` 를 정책 적용 지점으로 **직접 인용**해 교차 확증됐다. **정본은 한 글자도 고치지 않았다** — 3건 모두 색인이 틀린 방향이며 각 건을 로직 문서 의사코드·형제 프로세스 진입 트리거·정책맵으로 교차 확증했다(나머지 1건은 `PROC-101` — USR-01.md 항목). 직전 `7fd5bb3` 분할이 라벨 집합을 부모에 명시해 **기계 대조**가 가능해진 직후 **18 PROC · 98쌍 전수 대조**로 찾았다. **[② `SCR-001` 제목 포커스 관측은 세우지 못해 등재했다]** 같은 회차에 닫은 제목 포커스 링 검증 공백(USR-02·USR-04·USR-05 항목)에서 **`SCR-001` 만 기대 결과가 서지 않았다** — 이 화면으로 향하는 단계 전환은 `BackToIdentity`(400 `EX-AUTH-001`·`EX-AUTH-002`) 하나뿐인데 그 자리가 **단계 전환과 재입력 안내가 동시에 걸리는 유일한 전환**이라, §접근성 기준의 "새 제목으로 포커스"와 §InlineAlert 의 "재입력 안내는 **해당 필드**로 포커스"가 **함께 걸리고 우선순위가 정본에 없다**. 추측으로 기대 결과를 만들지 않고 §미해소 카탈로그에 **`QA-OPEN-02`**(영향 `USR-03`·`USR-04` · SCR-001 · **TC 0건 · Block 0건**)로 등재해 인계했다 — 우선순위가 확정되면 `USR-04_011` ③ 에 같은 관측을 더한다. 미해소 집계 **1건 → 2건**. **[무변경]** `tc_USR-03.md` **무접촉**(이 노드 TC 17건 그대로) · 총 **242건**·결번 2건 불변 · 저장소 전체 단계 라벨 인용 **447건 중 미실재 0** | `accountinterlockhub#475`·`#476`·`#469` | 🚧 |
| 2026-07-26 12:40 | spec | `docs/specs/processes/process_PROC-102.md` **293 → 128줄** + `process_PROC-102-logic.md` **173줄 신규**(git 미추적 — 같은 commit 에 함께 들어간다) — **300줄 제한 대응 순수 이동 분할**. **이력·산출물 동일 commit**(작성 시점 미커밋). **[함께 처리한 이유]** 여유(300 − 실측)가 **7줄**이라 `PROC-103`(1줄)과 함께 **착수 게이트에 걸린 두 파일**이었고, 실측 최대 섹션이 `PROC-103` 과 똑같이 **`### 로직 실행 순서` 172줄**(다음 큰 섹션 §단계명 매핑 16줄)이라 **같은 경계**를 적용했다 — 분할 판단의 정책 근거(`document-master-guide.md` §길이·구조 · 기존 `prd-to-process-logic-template.md` 가 이미 이 섹션 전용 템플릿이라는 점 · 격리 워크트리 점유 0건 대기)는 USR-04.md 항목 참고. **[내용 변경 0 — 실측]** 부모에서 뜯어낸 §로직 실행 순서 본문 **169줄**과 자식 본문 **169줄**이 **md5 동일**(`ab07211c261f…`) · 부모 삽입은 **3줄뿐**(참조 1 · 라벨 2 — `PROC-103` 과 달리 공백 1줄이 기존 줄과 맞물려 3줄이다) · §기본 정보·§관련 사양 코드·§진입점 및 진입 조건·§입력/출력 정의·§연관 데이터 및 외부 호출·§실행 제약사항·§단계명 매핑·§분기 및 예외 흐름·§실행 결과·§의존 프로세스·§구현 가이드 **전 절 무접촉** — 직전 10:42 회차가 §구현 가이드에 넣은 트랜잭션 경계 불릿과 `B6` 의 `BEGIN ISOLATION LEVEL READ COMMITTED` 서술도 **그대로 제자리**(전자는 부모에, 후자는 자식 `#### BE 측 처리` 에 있다). 자식은 `#### FE 측 처리`·`#### BE 측 처리`·`#### 데이터 변환 흐름`·`#### 단계 통합 흐름` 네 절을 원 순서 그대로 갖는다. **[인용 좌표 보존]** `tc_USR-03.md:13` 이 **파일을 지목해 라벨 범위(`B1`~`B8` · `F1`~`F3`)를 인용**하므로 부모 §로직 실행 순서에 **라벨 목록과 자식 위치를 남겼다** — FE `F1`·`F2`·`F3` · BE `B1`·`B2`·`B3`·`B4a`·`B4b`·`B5`·`B6`·`B7`·`B8`, **양쪽 다 결번 없음**(`PROC-103` 의 `F4`·`B5a` 결번과 대비되는 지점이라 명시했다). 자식 실측 라벨 **12개가 이 목록과 완전 일치**(소실 0) · 섹션 앵커 링크 저장소 전체 **0건** · 4파일 상대 링크 전수 **broken 0**. 자식 서두는 `정본은 process_PROC-102.md` 한 줄뿐이다. **⚠️ 이번 commit 에 내용 변경은 없다** | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-26 12:20 | spec | `docs/specs/qa/USR/tc_USR-03.md` `USR-03_015`(확장 — 번호 무변경, 제목 `재입력 UI 규칙` → **`재입력·제출 중 UI 규칙`**) — **커버리지 공백 시정: `SCR-001` 제출 중 `aria-busy` 를 보는 TC 가 0건이었다.** **이력·산출물 동일 commit**(작성 시점 미커밋). `de482d3` 의 F-1 이 목업 `SCR-001.html` 의 `submitting` 입력 필드에 `aria-busy="true"` 를 세웠고 `design-system.md` §상태 표현 로딩(제출 중) 행도 이를 요구하는데, **넣어 두고 아무도 보지 않는 상태**였다. 관측 3지점을 더했다 — **⑤** 응답을 붙잡은 채 `Submitting` 관측 → 버튼 Spinner·문구 "확인 중"·**생년월일 필드 잠금**(값을 지우지 않는 `readonly`)·그 필드에 **`aria-busy="true"`** **⑥** 응답을 흘려보내 `Submitting` 을 벗어나면 **성공(`stage = CONSENT`)·재입력(`EX-AUTH-002` → `ReEntry`) 어느 쪽으로 나가든 `aria-busy` 가 풀리고**(`false` 이거나 속성 부재) 잠금도 함께 풀린다 **⑦** `Initial`·`Editing`·`ReEntry`·`Retryable` 에서는 서지 않는다(잠금을 갖는 상태는 `Submitting` 하나뿐이고, 이 표현이 서는 화면도 `SCR-001` 하나다). **해제를 본 이유** — `Submitting` 을 벗어나는 자리가 곧 **InlineAlert 가 뜨고 필드로 포커스가 가는 자리**라, 켠 채 두면 보조기술에 "아직 갱신 중이니 지금 읽지 말라"는 신호가 남아 **그 자리에서 뜨는 재입력 안내를 미루거나 건너뛴다**(`SCR-003` 쪽 같은 원리는 `4f866b4` 가 `USR-02_015` 에 이미 반영 — 이번은 **`SCR-001` 몫**이다). **전제**에 응답 보류 수단(브라우저 자동화로 `POST <INTERLOCK_ENTRY_PATH>/verify` 응답을 붙잡았다 흘려보낸다 — `USR-03_017` 의 전송 계층 단절과 **같은 요청 가로채기 계열**이며 **서버 구현을 바꾸지 않는다**)을, **검증 대상** 칸에 §화면 상태 전이 `Submitting`·디자인 시스템 §상태 표현 정본을, **화면 검증** 줄에 `aria-busy="true"` 의 **범위 한정**(`Submitting` 시점 한정 · 서는 자리는 **잠기는 입력 필드**이며 카드·버튼·알림 영역으로 번지지 않는다)을 더했다. **[무변화 — 실측]** TC **신설 0** · 채번 연속(`_001`~`_017`)·이 노드 **17건 유지**(총 242건 불변) · 검증 PROC(`PROC-102`)·분기(`BR-003`)·우선순위·자동화 칸 무변경 · 단계 ①~④·판정 기준 무변경 · `screen_SCR-001.md`·`design-system.md` §상태 표현·`mockup/` **무접촉**. 같은 회차의 **공통 반영분**(§접근성 기준의 제목 포커스 표시 확정 + `design-system-components.md` §StageTitle §접근성 1줄 정합 — `SCR-001` 제목도 그 대상 · `spec-qa.md` 헤딩 244 각주 · `spec-qa-mapping.md` `SCR-001` 서술 보강)은 common.md 항목 참고 | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-26 11:54 | spec | `mockup/INDEX.md` §시연한 상태·변형 `SCR-001` **1행** — 직전 `de482d3` 의 **하류 정합** 중 이 노드 몫(색인 표기 보완). **이력·산출물 동일 commit**(작성 시점 미커밋). `de482d3` 이 목업 `SCR-001.html` 의 `submitting` 입력 필드에 `aria-busy="true"` 를 세웠는데(F-1 — 잠금 상태가 `submitting` 하나뿐이라 `readonly` 조건에 묶어 그 상태에서만 서고 나머지에서 자동으로 풀린다), **색인의 "확인 중(로딩)" 행은 "필드 잠금(readonly)" 에 머물러** 목업 실동작과도, `design-system.md` §상태 표현 로딩(제출 중) 행이 요구하는 "입력 필드 잠금(`aria-busy="true"`)"과도 어긋난 채였다. 그 칸을 `readonly`+`aria-busy="true"` 로 보완하고 **이 상태에서만 서고 나머지에서 해제**된다는 조건까지 함께 적어, 색인만 읽는 검토자에게도 해제 원리가 도달하게 했다. **표기만 고친 건** — 목업 `SCR-001.html`·`screen_SCR-001.md`·`design-system.md` **전부 무접촉**이고 `SCR-001` 상태 6종·문구·레이아웃·검증 TC(`tc_USR-03.md`) 무변경이다. 이번 정합의 본체인 `aria-busy` **해제 시점**은 `SCR-003` `Unconfirmed` 쟁점이라 USR-02.md 항목 참고 | `accountinterlockhub#477`·`#469` | 🚧 |
| 2026-07-26 11:32 | spec ⓒ | (공통 반영) 목업 잔여 접근성 3건 마감 중 **F-1 이 이 노드의 본체**다 — `mockup/SCR-001.html` 의 `submitting` 입력 필드에 `aria-busy="true"` 가 없었다(`design-system.md` §상태 표현 — 로딩(제출 중) 행이 "입력 필드 잠금(`aria-busy="true"`)"을 요구). **잠금 상태를 갖는 것은 `submitting` 하나뿐**이라 그 상태에서만 세우고 나머지 상태에서는 지우도록 했다(F-3 의 해제 원리와 같다). 구현은 이미 맞았고(P15 `#492` 의 `TextField.tsx` 가 `readOnly` 시 `aria-busy="true"` 를 붙인다) **목업만 틀린 건**이다. 제목 `h1` 에 `tabindex="-1"` 도 더해졌다(F-6, 목업은 화면당 1파일이라 **동작 영향 없는 정적 구조 계약 표기**). **본인확인 상태 정의·문구·오류 표현·포커스 거동 전부 무변경**(재입력 오류 표현 `316bf77` 시정분 포함) — common.md | `accountinterlockhub#474`·`#469`·`#467` | ℹ️ |
| 2026-07-26 10:42 | spec | `docs/specs/processes/process_PROC-102.md` — 본인확인 제출의 **트랜잭션 경계를 여는 자리 명시**(commit `1491e4f` SD-1 의 프로세스 확산분). **근거 commit `b4fe224`** — 본 entry 작성 시점엔 미커밋이었고, 오케스트레이터가 산출물과 이 이력을 같은 commit 에 함께 넣었다(정책대로 동시 반영). `B6` 의 `BEGIN ISOLATION LEVEL READ COMMITTED` 가 **경계를 여는 지점**임을 주석으로 못박고, `PROC-301` 호출 인자에 `exec`(여기서 연 커넥션·실행자)를 더해 **이 전달이 참여의 성립 조건**임을 명시했다. 하위 의사코드 인용도 `INSERT … ON CONFLICT DO NOTHING`(SD-2 정정분)으로 정합. §구현 가이드에 불릿 1개 추가 — **`B6` 의 `BEGIN` 이 경계를 여는 지점**이고 `PROC-301`·`PROC-303` 과 그 하위 FN 은 넘겨받은 실행 문맥으로 **참여만 한다**(열지도 닫지도 않는다), 하위가 열어 줄 것으로 기대해 `BEGIN` 을 빼거나 `exec` 를 넘기지 않으면 레코드 기록과 요청 수 계수가 각각 커밋돼 `SVC-014` F-008 의 원자성이 깨진다. 소유권·`ON CONFLICT` 판단 근거와 `FN-009`~`FN-011` 확산분은 BAT-04.md 항목 참고 | `accountinterlockhub#475`·`#473`·`#469` | 🚧 |
| 2026-07-26 10:37 | spec | `mockup/SCR-001.html` — 목업 **재입력 상태의 오류 표현 시정(D-1)**. **⚠️ 이력 누락 보정 — 산출물은 commit `316bf77` 로 이미 커밋·origin push 됐고(같은 commit 에 이력을 넣지 못했다) 본 entry 가 그 근거 해시로 추적을 잇는다.** build P15 doer 가 올린 건으로, 구현은 불일치 시 오류 표현을 정상적으로 세우는데 목업의 같은 상태에는 그 표현이 없어 담당자가 승인 검토에서 **틀린 화면**을 보게 되는 상태였다 — 목업 `hidden` 무력화(`9e6eb90`)와 같은 계열이며 **사양·구현이 맞고 목업이 틀린 건**이다. ① `reentryMismatch` 가 `fieldError:false` 여서 필드 경계가 `--color-danger` 로 바뀌지 않고 `aria-invalid` 도 `false` 였다(`screen_SCR-001.md` §화면 상태 전이는 ReEntry(형식)·ReEntry(불일치) **둘 다** "InlineAlert + 필드 포커스"를 요구하고 불일치에만 값 유지(전체 선택)가 더 붙을 뿐이며, `design-system.md` §상태 표현도 오류(입력)에 재입력 안내를 명시적으로 포함한다). ② `formatError` 는 `fieldError` 는 맞았으나 **포커스가 전혀 가지 않았다** — 포커스 여부가 `selectAll` 하나에 묶여 있어 "포커스는 주되 전체 선택은 하지 않는" 형식 오류를 표현할 수 없던 것이 원인이라, **`focusField` 를 분리**하고 `selectAll` 을 그 안쪽 조건으로 내렸다. ③ `retryable` 은 서버 처리 실패이지 입력값 문제가 아니므로 **무변경**(필드 경계 미부여 — §상태 표현 오류(처리)). **실측**(격리 프로필 헤드리스 Chrome + CDP 실렌더): `reentryMismatch` 경계 `rgb(179,38,30)`=`--color-danger` · `aria-invalid` true · 알림 `display:flex` · 문구 사양과 문자 단위 일치 · `selectionStart` 0/End 6 · `activeElement` 가 입력 필드 / `formatError` 경계·`aria-invalid`·알림·포커스 정상 + 전체 선택 없음(`selectionStart == selectionEnd`). **양성 대조** — 시정 전 사본에 같은 검사를 돌려 `reentryMismatch` 2건·`formatError` 1건이 FAIL 함을 확인(검사에 판별력이 있다). **1px 불변** — `initial`·`editing`·`submitting`·`retryable` 네 정상 상태 전체 페이지 픽셀 비교 **제품 카드 영역 0px**(`submitting` 의 31px 은 목업 도구모음 알약 모서리 안티에일리어싱으로, 같은 파일을 자기 자신과 비교해도 동일 좌표·동일 RGB 로 재현되는 렌더 비결정성이라 회귀가 아니다), `initial` 자동 포커스 링 회귀도 없음. **목업 원칙 유지** — 외부 자원 0건·오프라인 열람·인라인 SVG·상태 토글 6개 보존·새 CSS 규칙 0(`has-error` 는 이미 있었다) | `accountinterlockhub#474`·`#469`·`#467` | 🚧 |
| 2026-07-26 10:26 | spec ⓒ | (공통 반영) 사양 결함 7건(SD-1~SD-7) 정정 — 본인확인 제출이 쓰는 `function_FN-007-008.md` `FN-008`(레코드 확보)의 **트랜잭션 소유권을 호출측으로 통일**하고(SD-1), **경계를 실제로 여는 자리가 `PROC-102` `B6`**(승인 제출은 `PROC-103` `B3`)임을 기능 사양에 밝혔다. 참여의 성립 조건인 실행 문맥(`exec`)을 필수 인자로 명시했고, 단계 4 의 실행 불가 의사코드를 **`ON CONFLICT (tracking_key) DO NOTHING RETURNING`** 으로 정정했다(SD-2). 판단 근거 상세는 BAT-04.md 항목 참고. 근거 commit `1491e4f`(이력 누락 보정) — common.md | `accountinterlockhub#473`·`#472`·`#470`·`#469` | ℹ️ |
| 2026-07-26 09:18 | build | P15(`#492`) `SCR-001` 정밀 마감 — `flow/IdentityScreen.tsx` 는 P14 참조 조립을 사양·목업과 재대조해 변경 없이 확정(레이아웃 4영역·재입력 focus 이동·확인 버튼 항상 활성 전건 일치). 공유 파일 3건 정정: ① `components/StageTitle.module.css` 제목→보조 설명 여백을 `--space-xs`(8px, 값 미확정 시기의 P14 잠정 선택)에서 사양 6회차 확정값 `--space-2xs`(4px, `design-system-components.md` §StageTitle `9e6eb90`)로 정정 — 카드 직계(SCR-001·SCR-002) 거동 자체는 무변경, 값만 정합 ② `stage/transitions.ts`+`stage/useInterlockFlow.ts` — §입력 폼 정의 "FE 검증 의사코드" `/^\d{6}$/.test(value)` 를 `verify()` 제출 직전 클라이언트 검사로 신규 구현(`isBirthDateFormatValid`·`identityFormatInvalidView`) — 형식 위반 시 서버 왕복 없이 즉시 동일 문구 안내(`AUTH-002-02`) ③ `components/TextField.tsx` — 잠금(`readOnly`) 시 `aria-busy="true"` 추가(design-system.md §상태 표현 "입력 필드 잠금(aria-busy=true)"를 필드 자신에 결속, 기존 Button 의 aria-busy 는 유지). **실측**(전용 임시 프로필·헤드리스 크롬, CDP 직접 구사): 제목→보조 설명 4px·블록 아래 24px·형식 위반 즉시 안내+그 시점 네트워크 요청 0건·재입력(불일치) 값 유지+전체 선택·응답 미수신 `Retryable`(`EX-BIZ-003` 기본 문구 그대로·버튼 재활성·SCR-001 유지)·진입 시 자동 포커스 없음(포커스는 h1 으로만 프로그램적 이동)·로딩 중 input+button 양쪽 `aria-busy=true`. `npm --prefix apps/frontend run build` 에러 0건 · **[회귀 1회차]**(독립 code-reviewer FAIL 판정 대응, `#492` journal 3499) — 🔴 **[I-1]** `stage/useInterlockFlow.ts` 의 `setBirthDate` 가 `useState` 원시 setter 그대로 노출돼 입력이 바뀌어도 오류 알림·`aria-invalid`·빨간 경계가 해제되지 않았다(`screen_SCR-001.md:55` "입력 값 반영·**오류 알림 해제**" 위반 — 이 회차가 신설한 FE 형식 검증으로 정지 상태 도달 빈도가 상승해 상시 노출로 승격). `changeBirthDate` 래퍼 신설 — 값 반영(`setBirthDateRaw`)과 알림 해제(`setView`)를 별개의 순차 호출로 두어 updater 내부에 부수효과를 넣지 않음. kind 불문 해제(format·mismatch·retryable 전부). 🟡 자체보완 3건 — **S-1** `BIRTH_DATE_FORMAT_MESSAGE` 리터럴 폐기, `defaultMessageFor('EX-AUTH-001')` 로 단일화 · **S-2** `viewAfterVerify` 의 낡은 주석("사양에 없다")을 `screen_SCR-001.md` 70·75행 인용으로 갱신 · **S-3** `flow/stack.module.css` "P15 가 다듬어질 수 있다" 주석을 채택 결론으로 갱신(값 무변경). **실측**: 5자리 제출 → 형식 alert+`aria-invalid="true"` → 6번째 자리 입력 즉시 `alertPresent:false`·`ariaInvalid:null` 확인, 재입력(불일치) alert 도 값 수정 즉시 같은 방식으로 해제 확인. `npm --prefix apps/frontend run build` 에러 0건 · **[회귀 3회차]**(코드리뷰 재판정 FAIL 대응, `#492` journal 3535) — 🔴 **[I-4] 동반 이탈 시정** `stage/transitions.ts` 의 `viewAfterVerify` catch-all 이 `EX-BIZ-003` 전용 갈래 없이 **미분류 코드까지 전부 `SCR-001` `Retryable`** 로 보내고 있었다. `process_PROC-102.md` `F3` else("그 밖의 코드 — 위 넷에 없는 code → 단계 전이 → `SCR-004` 결과 경로 ②")대로 갈래를 분리 — `EX-BIZ-003` 은 그대로 `Retryable`(재시도 안내·버튼 재활성)에 남기고, 그 밖의 미분류 코드(대표 `EX-OPS-002`)만 `SCR-004` 결과 경로 ②(`reasonCode` 동봉 — `screen_SCR-004.md` §조건부 표시 "표에 없는 코드·코드 부재는 `EX-OPS-002` 문구")로 새로 갈랐다. 근거 — 본인확인 제출은 **어떤 결과도 확정하지 않아 뒤집을 결과가 없다**(`process_PROC-102.md` §구현 가이드) — `PROC-103` `F5` 의 같은 catch-all(승인 접점, `USR-04.md` 참고)이 정반대 목적지(`SCR-003` `Unconfirmed`)로 가는 것과 **의도적으로 비대칭**이다. **실측**(격리 헤드리스 Chrome+CDP, 전용 스텁 서버 포트 49501·49502): `/verify` 500 `EX-OPS-002` 응답 시 `h1:"연동을 진행할 수 없습니다"`(경로 ②)·설명 "처리 중 문제가 발생했습니다. 잠시 후 다시 시도해 주세요."(`EX-OPS-002` 기본 문구) 노출·`fetch` 호출 정확히 1건(자동 재시도 없음). `npm --prefix apps/frontend run build` 에러 0건 · **[재개방 시정 — X-1·X-2]**(`#492` 재개방 — `해결`🔵 판정 이후 확정된 사양 `a8058a0`·`513aca6` 미구현 2건, P16(`#493`) 독립 코드리뷰가 자기 범위 밖에서 발견) — 🔴 **[X-1]** `hooks/useStageFocus.ts` 가 `title` 변경 시 무조건 `h1.focus()` 를 불러 `design-system.md` §접근성 기준(`a8058a0`) "단계 전환과 필드에 매인 안내가 겹치면 포커스는 그 필드가 가져간다"(`BackToIdentity`·`BackToConsent`(`EX-BIZ-001`) 두 자리)의 예외를 구현하지 않고 있었다. **0단계로 실렌더 먼저 판정** — 독립 doer 둘(P16 코드리뷰="제목이 이긴다·미구현" vs 커버리지 매트릭스="자식(`StageTitle`)의 `useStageFocus` 가 부모보다 먼저 실행돼 필드가 우연히 이긴다")이 정적 판독만으로 갈렸던 것을 CDP 로 직접 측정한 결과 **`BackToIdentity`(mismatch·format 둘 다) 는 이미 필드가 이기고 있었다**(활성 요소 `INPUT#birthDate` — mismatch 는 `selectionStart/End=0/6`(전체 선택), format 은 `6/6`(커서만), 사양·목업과 문자 단위 일치) — **커버리지 매트릭스가 맞았다**. 지시대로 "동작하니 그냥 둔다"로 끝내지 않고 그 암묵 의존(React effect 가 자식→부모 순서로 실행된다는 사실에 기대는 것)을 명시 규칙으로 승격했다 — `useStageFocus(title, skipFocus)` 신설, `skipFocus=true` 면 `.focus()` 를 애초에 부르지 않아(부모가 나중에 덮어써서 우연히 맞는 구조 자체를 제거) 컴포넌트 트리 순서가 바뀌어도 결과가 흔들리지 않는다. `document.title` 갱신은 `skipFocus` 와 무관하게 그대로 수행(사양 명문). `StageTitle` 에 `skipFocus` prop 추가(`ProgressPanel`·`ResultPanel` 은 이 prop 을 넘기지 않아 `SCR-003`·`SCR-004` 는 전역 규칙 그대로 무변경). `IdentityScreen.tsx` 가 `view.alert` 값으로 `skipTitleFocus` 를 계산해 넘긴다 — 전이 그래프 전수 확인 결과 `SCR-001` 로 새로 마운트되는 경로는 **진입**(`alert===null`)과 **`BackToIdentity`**(`alert.kind` `format`·`mismatch`) 둘뿐이다(`useInterlockFlow.ts` `applyNextView` 불변조건). 🔴 **[X-2]** 같은 계산식이 진입(최초 로드)도 함께 닫는다 — "진입은 전환이 아니고 자동 포커스를 주지 않는다"(`a8058a0`)를 `useStageFocus` 가 마운트 시점에도 무조건 발화해 어기고 있었던 것을 시정(**포커스 링 CSS 자체는 무변경** — `513aca6` 대로 유지, 손댄 것은 `.focus()` 호출뿐이다). **[D-2]** `IdentityScreen.tsx`·`InterlockJourney.tsx`(USR-04.md 와 공유 파일) 의 "정확한 카피·인터랙션 다듬기는 P15 소관" 낡은 주석을 완료 시제로 정정. **실측**(CDP, 격리 프로필 헤드리스 Chrome, 포트 3312/9312 — 지시된 격리 대역, `apps/frontend/dist/` 정적 서빙 + `Page.addScriptToEvaluateOnNewDocument` 로 `/verify`·`/approve` fetch 모킹, **백엔드 미기동**): 시정 전/후 3회 반복 대조 — 진입 `activeElement` `H1`→**`BODY`**(X-2 해소, 3회 재현 일치) · `BackToIdentity`(mismatch·format) `activeElement` **`INPUT#birthDate` 무변화**(선택 상태도 무변화) · `document.title` 은 포커스 생략과 무관하게 정확히 갱신됨(`본인 확인` 확인, `skipFocus=true` 경로에서도 실측) · **회귀 없음**(양 화면 `wrapMarginBottom=24px`·`subtitleMarginTop=4px` · 포커스 링 `outline 2px solid rgb(11,79,168) offset 2px` 명시적 `.focus()` 로도 그대로 성립 · `SCR-002` 승인 버튼 `aria-disabled=true`+`nativeDisabled=false`+클릭 시 핸들러 도달 · `SCR-003`·`SCR-004` 도착은 제목 포커스 그대로 `H1` — 전부 시정 전/후 무변화). `npm --prefix apps/frontend run build` 에러 0건 | `accountinterlockhub#492`·`#469` | 🚧 |
| 2026-07-26 08:45 | spec ⓒ | (공통 반영) `StageTitle` 합성 간격 규칙 명문화·목업 `hidden` 무력화 시정(6회차 회귀) — 이 노드에 닿는 것은 **`SCR-001`**(`mockup/SCR-001.html` · `design-system-components.md` §StageTitle)이다. 본인확인 오류 알림(`InlineAlert`·`#alert`)이 저자 `display:flex` 선언에 UA 의 `[hidden]{display:none}` 이 원산지 우선순위로 밀려 **조건과 무관하게 항상 노출**되던 목업 결함이 `[hidden]{display:none !important}` 로 시정됐다 — 오류가 없는 상태에서도 **본문 없는 빈 오류 상자**가 떠 있어 승인 검토가 틀린 화면을 보던 건이며, **사양은 정확했고 렌더가 배반한 건**이라 `screen_SCR-001.md` 는 손대지 않았다(필드 오류 표현·`aria-invalid` 등 검증 거동 무변경). `StageTitle` 의 여백은 **카드 직계 자식일 때의 값**으로 한정됐고 `SCR-001` 이 그 자리라 **본인확인 화면의 간격·거동은 무변경**이다. **⚠️ 소급 보정 entry** — 근거 커밋 **`9e6eb90`**. 이력은 공유 워킹트리 경합으로 직전 build 커밋 `fdc9f76` 에 common.md 08:45 row 만 선행 반영되고 **각 IA 역링크가 누락**돼 지금 채운다 — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-26 08:45 | build | `apps/backend/src/records/tracking-record.service.ts` `secure()` — P04(`#481`) 회귀 1회차 [I-1] 🔴 시정. 추적 레코드 확보(`FN-008`, 본인확인 최초 성공 계기)가 자기 트랜잭션을 여는 설계였던 것을 **호출측(본인확인 오케스트레이션, 후속 Phase 의 `PROC-102` `B6` 경유)이 연 트랜잭션에 참여**하는 형태로 뒤집었다 — `secure(executor, trackingKey, at)` 로 시그니처 변경. INSERT 를 `INSERT ... ON CONFLICT (tracking_key) DO NOTHING` 로 교체해 같은 키의 동시 진입에서 기본 키 충돌이 **호출측 트랜잭션을 중단시키지 않고** 같은 트랜잭션 안에서 곧바로 이어쓰기로 수렴하게 했다(일반 INSERT 오류였다면 PostgreSQL 이 그 트랜잭션을 즉시 아보트시켜 안에서는 재조회할 수 없었다). **실측**: 같은 키로 **독립 트랜잭션 8개** 동시 진입 → DB 행 정확히 1·`isCreated=true` 정확히 1·요청 수 카운터 정확히 +1, 지표 계수 유도 실패 시 호출측 트랜잭션 전체가 롤백돼 고아 레코드가 남지 않음을 재확인. 트랜잭션 소유권 판단 근거·전체 실측 상세는 BAT-04.md 항목 참고 | `accountinterlockhub#481`·`#469` | 🚧 |
| 2026-07-26 06:19 | spec ⓒ | (공통 반영) 횡단 국소 회귀(5회차) — `spec-functions-api-user.md` §본인확인 제출 §에러 처리 표에 **`EX-OPS-002` 500 행 신설**(`process_PROC-102.md` 분기표와의 불일치 해소 · **이미 성립하는 사실의 계약 명문화라 거동 변경 0**). 화면은 `PROC-102` `F3` **미매핑 catch-all → 결과 경로 ②** 이며, 근거는 이 접점이 **어떤 결과도 확정하지 않아 뒤집을 결과가 없다**는 것이다(`SVC-005` F-003). 사용자 메시지는 기존 기본 문구 그대로(새 문구 0) — common.md | `accountinterlockhub#473`·`#470`·`#472`·`#469` | ℹ️ |
| 2026-07-26 04:57 | spec ⓒ | (공통 반영) 검증 TC 국소 회귀 — `tc_USR-03.md` **신설 1**(`USR-03_017` 본인확인 제출의 **응답 미수신** → `SCR-001` 유지·**`Retryable`**·`EX-BIZ-003` **기본 문구 그대로**(`SEC-002-05`)·화면 타임아웃 없음·같은 값 재제출로 정상 진행(`BIZ-002-03` ②)·승인 제출의 `Unconfirmed`(`USR-02_015`)와 **다른 상태**). `USR-03_014`(`EX-BIZ-003` 응답 기준)는 **무변경**. TC 16 → 17건 — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-26 04:40 | spec ⓒ | (공통 반영) 프로세스 연쇄 국소 회귀 — `process_PROC-102.md` FE `F3` 응답 처리에 **응답 미수신(전송 계층 단절)** 갈래 한 줄을 더해 `EX-BIZ-003` 과 **같은 재시도 안내·같은 문구**로 보냈다(사유를 구분해 알리지 않는다 — `SEC-002-05` · `SCR-001` `Retryable` 확장분 반영). §구현 가이드에 **비대칭 근거 불릿 1개** — `verify()` 는 어떤 결과도 확정하지 않아 같은 값 재제출이 **이어쓰기로 수렴**하고(`BIZ-002-03` ②), `approve()` 는 결과가 확정됐을 수 있어 `SCR-003` `Unconfirmed`(재진입 안내)로 간다. **화면 쪽 요청 타임아웃 무신설**(`SCR-003` §구현 가이드 유지) · 라벨·코드·표 행 무신설. **TC 미커버 1건 — 응답 미수신 → `Retryable` 검증 TC 없음**(`#474` 보고와 같은 건) — common.md | `accountinterlockhub#475`·`#469` | ℹ️ |
| 2026-07-26 04:30 | spec ⓒ | (공통 반영) build 사양 결함 국소 회귀(🟠 2건) — 본인확인 제출(`verify()`)의 **응답 미수신에 대응하는 상태가 없던 공백**을 `screen_SCR-001.md` 의 기존 **`Retryable` 진입 조건 확장**으로 닫았다(새 상태 무신설). 근거는 이 제출이 **어떤 결과도 확정하지 않아** 같은 값으로 다시 제출해도 서버가 같은 레코드를 이어 쓴다는 것(`BIZ-002-03` ②) — 그래서 옳은 안내가 "그대로 다시 시도"이고, 결과가 확정됐을 수 있는 승인 제출의 재진입 안내(`SCR-003` `Unconfirmed`)와 **합치지 않는다**. 문구·타임아웃 정책 무변경(기존 재시도 안내 문구 그대로 · 화면 쪽 요청 타임아웃 무신설). **TC 미커버 1건**(응답 미수신 → `Retryable` 검증 TC 없음, 신설 제안) — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-26 02:46 | build | `apps/frontend/src/` — **디자인 시스템·공통 컴포넌트 11종·FE 셸·단계 상태머신 신규 구현**(정본 `design-system.md`·`design-system-components.md`). 색 토큰 23종·타이포·간격·모션 값을 그대로 이식(`styles/tokens.css`) — 실측 대비비 27행 재계산 전건 표와 일치. 컴포넌트 11종(`components/`) 전량 신설. 단계 상태머신(`stage/useInterlockFlow.ts`)이 진입 응답 수화(hydration)·`/verify`·`/approve` 두 접점(`api/client.ts`) 응답을 서버가 정한 대로 `IDENTITY→CONSENT→PROCESSING→RESULT` 로 전이시킨다(화면은 스스로 건너뛰지 않는다) — `PROCESSING`(SCR-003) 만 화면 소유 단계로 별도 처리. 생년월일·암호값 쌍은 훅의 메모리 상태로만 보유하고 URL·저장소에 두지 않는다(`DATA-001-02`·`-03`). TextField 생년월일 변형(숫자만·6자리·값 유지+전체 선택 imperative handle)·재입력 안내 focus 이동을 `IdentityScreen`(`flow/`)에서 조립. `npm run build`(`tsc -b && vite build`) 에러 0건 **[회귀 2회차]**(S-1, `#491` 자체보완) — `stage/transitions.ts` 의 `viewAfterVerify` 성공 분기(`stage='RESULT'`)에서 `resultPath: data.resultPath` 를 `resultPath: normalizeResultPath(data.resultPath)` 로 교체. `/verify` 응답은 HTTP JSON 을 거쳐 `api/client.ts` 의 무검증 캐스팅(`data as TResponse`)으로 들어오므로 정적 타입 선언만으로는 런타임을 보장하지 않았다 — 회귀 1회차에서 만든 `normalizeResultPath()`(`api/types.ts`)를 이 지점도 거치게 해 그 함수 주석의 "전 지점이 이 함수를 거친다" 단언을 사실과 일치시켰다(주석만 좁히지 않고 코드를 실제로 고치는 쪽을 택함). `npm --prefix apps/frontend run build` 에러 0건 | `accountinterlockhub#491`·`#469` | 🚧 |
| 2026-07-26 00:45 | spec ⓒ | (공통 반영) 요구사항 회귀 — 결과 경로 4→3 의 연쇄 반영. `service_SVC-002.md`·`process_PROC-102.md` — 복호화 판정 3·4단계 실패(`EX-SEC-002`)와 구조 위반(`EX-SEC-001`)이 닿는 자리를 **경로 ③ → ②** 로 당겨 표기했다. 확정 결과 재안내 응답(`stage = RESULT`)은 **`returnUrl` 이 있으면 그대로 함께 나가고 없으면 나가지 않으며 동봉 여부를 여기서 다시 판정하지 않는다**(`BIZ-001-06` — 판정은 `PROC-105` `B3` 한 곳)는 근거를 남겼다. TC `tc_USR-03.md` — 지표 기대값을 **카운터 4종**으로, 기대 경로 번호를 ② 로 정정(TC 신설·결번 0) — common.md | `accountinterlockhub#469`·`#471`·`#475`·`#476` | ℹ️ |
| 2026-07-25 18:16 | spec ⓒ | (공통 반영) 목업 리뷰 발견 사항 반영(회귀 3회차) — 인라인 알림 위치를 **화면 사양의 알림 영역 한 곳**으로 통일(오류 유형별 이동 금지 · "카드 상단" 서술 폐기)·ConsentList **잠금(제출 중) 시각 처리 확정**(비활성 토큰 쌍 재사용·대비비 4.82:1·`opacity` 금지)·InlineAlert 사용 화면 목록에 SCR-003 추가. **M-1(`DECRYPT_FAILED` 레코드 확정 경로 복원) 미반영 — 구현 불가 판단으로 차단 보고** — common.md | `accountinterlockhub#477`·`#474` | ℹ️ |
| 2026-07-25 18:01 | spec | `mockup/SCR-001.html` 신규 — **본인확인 화면 목업**. 생년월일 6자리 입력(숫자 마스킹) + 형식 오류(`EX-AUTH-001`)·재입력 안내(`EX-AUTH-002`, **값 유지+전체 선택** 실제 `input.select()` 구현·시도 횟수 미표시)·처리 오류(`EX-BIZ-003`) 3종 InlineAlert 토글. 확인 버튼은 항상 활성(비활성 없음), 클릭 시 확인 중(로딩) 상태로 전환 | `accountinterlockhub#477`·`#469` | 🚧 |
| 2026-07-25 17:01 | spec ⓒ | (공통 반영) 교차검증 1회차 지적 보완(회귀 1회차 — 🟠 2·🟡 7 전건 해소) — `MDL-005`·`MDL-006` 사용 SVC·PROC 에 `SVC-013`·`PROC-204` 반영 — 같은 문서 §사용처 표와의 자기모순 해소 — common.md | `accountinterlockhub#472` | ℹ️ |
| 2026-07-25 16:19 | spec ⓒ | (공통 반영) 검증 TC 도메인 신규 작성 — `docs/specs/qa/` **TC 232건**(커버리지 SVC 18/18 · PROC 18/18 · BR 24/24 · EX 13/13 · 정책 17/17 · 접점 11/11 — 미검증 0)·**횡단 시나리오 `SCEN_001`~`SCEN_023`**(여정 6 · 규약 대칭 왕복 3 · 기동·상수 검증 5 · 무저장 원칙 2 · 설계된 부재·수용 리스크 7)·**AUTH 치환 12건**·모의 수신처·C# 하네스·시드 카탈로그 37항·미해소 1건(성공률 정의 이월) — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-25 15:48 | spec | `docs/specs/qa/USR/tc_USR-03.md` 신규 — **본인확인 TC 16건**(웹·SPA UI + API·서버). **판정 1·2단계 실패(재입력·무기록) ↔ 3·4단계 실패(`DECRYPT_FAILED`·레코드 미생성) 경계**를 핵심 검증 대상으로 고정 · 생년월일 형식 경계(5/6/7자·달력 유효성 미검사)·**재입력 횟수 제한 부재 10회 연속 검증**·최초 성공 시 레코드 1건 + 요청 수 +1(단일 트랜잭션)·미확정 재제출 = 이어쓰기·요청 수 미계수·확정 레코드 = 재안내(전 컬럼 불변)·응답 금지 값 0·**X 는 추적 키만 취해 즉시 폐기**·레코드 확보 실패 500 되돌림·SCR-001 재입력 UI(값 유지·전체 선택·시도 횟수 미표시)·**인증 요소 부재 확인**(헤더 무시·401/403 부재·세션 쿠키 미발급). 검증 BR-002·BR-003·BR-018 · EX 6종 | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec | `process_PROC-102.md` 신규 — **PROC-102 본인확인(복호화 판정)**(RR·동기·트리거=SCR-001 제출). `B2` FN-005 → `B3` FN-004 4단계 → **실패 분류 `B4a`(1·2단계=`EX-AUTH-002` 재입력·횟수 제한 없음·기록 없음) / `B4b`(3·4단계=`EX-SEC-002` 지표만 계수)** → `B5` **추적 키만 취하고 X 즉시 폐기**(`BIZ-002-06`) → `B6` 레코드 확보(PROC-301+PROC-303 **단일 트랜잭션**·기본 키 충돌을 이어쓰기로 흡수) → `B7` `FIXED` 면 **무갱신 재안내** → `B8` 동의 항목 구성 응답 | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec ⓒ | (공통 반영) 프로세스 도메인 신규 작성 — `docs/specs/processes/` **PROC 18종 확정**(정책·서비스 예약분을 같은 코드·같은 의미로 — 통합·폐기·추가·재배치 0)·**단계 라벨 체계 확정**(`F` FE·`B` BE·`C` 호출측·`L` 라이브러리 내부, 레이어별 1부터 시간 순, 분기는 접미 문자)·선행 도메인 단계명 전건 라벨 매핑·의존관계 순환 0(호출 깊이 3층)·트랜잭션 경계와 멱등 수단 총괄·정책 규칙 86건 매핑(런타임 80 + 게이트·절차 6) — common.md | `accountinterlockhub#475`·`#469` | ℹ️ |
| 2026-07-25 14:40 | spec | `screen_SCR-001.md` 신규 — **SCR-001 본인확인**(단계 `IDENTITY` · 트리거 PROC-102). 입력 폼 1필드 — 생년월일 `/^\d{6}$/`(`AUTH-002-02`)·**달력 유효성 미검사**(FN-005 정합 — 발송처 키 원문과 판정 기준이 갈리지 않게)·화면 검증은 편의이고 판정 근거는 서버. **생년월일 화면 취급 확정** — 페이지 메모리 전용(URL·숨은 필드·`localStorage`·`sessionStorage`·쿠키 금지 `DATA-001-02`), **값을 가리지 않고 표시**(오타 정정), **재입력 시 값 유지 + 전체 선택**, 자동 포커스 없음(진입 즉시 키보드가 안내를 가린다). 형식 안내(`EX-AUTH-001`)·재입력 안내(`EX-AUTH-002`)·재시도 안내(`EX-BIZ-003`)는 **단계를 바꾸지 않는 인라인 알림**이며 **시도 횟수·잠금 표시 없음**(`AUTH-002-04`). 확인 버튼은 항상 활성(정책이 제출 차단을 요구하는 자리가 아니다) | `accountinterlockhub#474`·`#469` | 🚧 |
| 2026-07-25 14:40 | spec ⓒ | (공통 반영) 화면 도메인 신규 작성 — `docs/specs/screens/` **화면 4건**(SCR-001~004 — 서비스 예약 대비 통합·폐기·추가·재배치 0)·**단일 경로 4단계 모델**(리다이렉트 없음·화면별 URL 없음)·**디자인 시스템 1벌**(표면이 하나라 토큰 분기·접두어 없음 · 색 23종 **실측 대비비 표** · WCAG 2.1 AA · 모바일 우선 2 브레이크포인트 · 히트 영역 44x44)·공통 컴포넌트 11종·**화면 없는 IA 노드 12건 근거 기재** — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-25 14:10 | spec | `spec-functions-api-user.md` §본인확인 제출 · `function_FN-004.md` · `function_FN-005-006.md` · `function_FN-007-008.md` — **`POST <INTERLOCK_ENTRY_PATH>/verify` 확정**(요청 `encX`·`encY`·`birthDate` → 응답 `stage` + 동의 항목 구성 `MDL-008`, 또는 확정 결과 재안내 `resultPath`·`isReAnnouncement`). **복호화 판정 4단계를 FN-004 단일 기능으로 확정** — 본인확인·연동 실행·자가진단 세 경로가 같은 절차를 공유해 "자가진단이 통과시킨 값이 연동에서 실패"하는 사고를 구조적으로 제거. 실패 분류 경계 확정: 구조 위반 `EX-SEC-001` / 판정 1·2단계 `EX-AUTH-002`(재입력·결과 미확정·횟수 제한 없음) / 3·4단계 `EX-SEC-002`(추적 키 미확보 → 레코드 미생성·지표만 계수). **`encY` 복호화 평문이 32바이트가 아닌 경우도 1단계 실패(`EX-AUTH-002`)로 확정**(`SEC-002-01` ① 이 "32바이트 키 획득"까지를 1단계로 규정). 추적 레코드 확보는 **FN-008**(확정분 무갱신·기본 키 충돌을 이어쓰기로 흡수·요청 수 계수 동일 트랜잭션). 응답에 추적 키·복호화 원문·암호값·생년월일 미포함 | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-25 14:10 | spec ⓒ | (공통 반영) 기능·API 도메인 신규 작성 — `docs/specs/functions/` **공통 기능 15건**(FN-001~015 · POL 5·DAT 8·CRS 2·**EXT 0**)·**인터페이스 11접점**(사용자 진입 표면 3 · 서버 대면 API 4 · 수신처 전달 1 · 명령 진입점 1 · 라이브러리 공개 진입점 2)·오류 응답 엔벨로프 `{code,message,details?}`·인증 0/요청 제한 0·**신규 EX `EX-OPS-002` 1건**을 정책 카탈로그에 등록 — common.md | `accountinterlockhub#473`·`#469` | ℹ️ |
| 2026-07-25 13:35 | spec ⓒ | (공통 반영) 데이터 도메인 신규 작성 — `docs/specs/datas/` **엔터티 3건**(ENT-001~003 = 저장 대상 셋)·**데이터 모델 22종**(MDL-001~022 확정 — 예약 대비 통합·추가·재배치 0)·인덱스 5·물리 FK 0·소프트 삭제 미도입 — common.md | `accountinterlockhub#472`·`#469` | ℹ️ |
| 2026-07-25 13:05 | spec | `service_SVC-002.md` 신규 — **SVC-002 본인확인**(매핑 PROC-102·PROC-301·PROC-303). Happy Path 7단계(입력→형식 검증→복호화 판정 4단계→추적 키 추출·원문 즉시 폐기→레코드 확보→요청 수 계수→동의·승인 이관). BR-002 = 추적 키 사전 조회 3분기(없음/미확정/확정), BR-003 = **재입력 루프(횟수 제한·잠금·지연 없음)**, BR-018 = 요청 수 계수 시점. 예외 `EX-AUTH-001`(형식)·`EX-AUTH-002`(판정 1·2단계)·`EX-SEC-002`(판정 3·4단계 → `DECRYPT_FAILED`)·`EX-BIZ-003`(기록 실패). 입력 MDL-004+MDL-006 → 출력 MDL-001·MDL-003. 복호화 판정 절차를 `SVC-004`·`SVC-013` 과 공유하도록 구현 가이드에 못박음 | `accountinterlockhub#471`·`#469` | 🚧 |
| 2026-07-25 13:05 | spec ⓒ | (공통 반영) 서비스 도메인 신규 작성 — `docs/specs/services/` **서비스 18건**(SVC-001~018)·사용자 역할 4종·분기 `BR-001`~`BR-024`·MDL 22종/SCR 4종 예약 채번·`PROC-403`·`PROC-404` 추가 채번 — common.md | `accountinterlockhub#471`·`#469` | ℹ️ |
| 2026-07-25 12:28 | spec | `policy_AUTH.md` `AUTH-002` 본인확인 + `policy_SEC.md` `SEC-002` 판정 1·2단계 + `policy_BIZ.md` `BIZ-002-01` 신설 — **생년월일 복호화 성공이 이 제품의 유일한 신원 확인 수단**(다른 인증 요소 없음). 입력은 `yyMMdd` 숫자 6자리(위반 400 `EX-AUTH-001`), 판정 1·2단계 실패는 전부 본인확인 실패로 분류해 **결과를 확정하지 않고 재입력**(400 `EX-AUTH-002`), **재입력 횟수 제한 없음**. 본인확인 최초 성공(판정 4단계 전부 통과) 시점이 **추적 레코드 생성 기점**이며, 이때 X 는 추적 키만 취하고 즉시 폐기한다(`BIZ-002-06`) | `accountinterlockhub#470`·`#469` | 🚧 |
| 2026-07-25 12:28 | spec ⓒ | (공통 반영) 정책 도메인 신규 작성 — `docs/specs/policies/` 정책 17건·규칙 86건·EX 코드 12종(암호화 연동 규약·복호화 판정 4단계·결과 구분 4종·추적 레코드 생성 시점·보관·무저장·동의 증적·인증 부재·수용 리스크) — common.md | `accountinterlockhub#470`·`#469` | ℹ️ |
| 2026-07-25 10:05 | directing ⓒ | (공통 반영) 리셋 방향 담당자 확정분 반영 — 생년월일 본인확인을 별도 기능단위로 **신설**(구 `USR-01` 에서 분리). 복원 실패 시 재입력 경로 — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
