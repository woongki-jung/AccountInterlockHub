# IA: USR-01 — 연동 링크 진입

> 본 IA 노드와 연관된 작업의 시계열 이력(최신순). 정책: ai/strategies/ia-history.md

| 일시 (KST) | 단계 | 산출물·결과 | 관련 일감 | 상태 |
|---|---|---|---|---|
| 2026-07-26 18:37 | spec ⓒ | (공통 반영) 디자인 시스템 §접근성 기준 — **진입(최초 로드)에는 제목으로 프로그램 포커스를 옮기지 않는다**는 **화면 무관 일반 규칙** 신설(`design-system.md`·`design-system-components.md`). 이 노드에 닿는 것은 **`SCR-001`**(연동 링크 진입의 첫 화면)이며, `a8058a0` 이 `SCR-001` 한정으로 적어 둔 "진입은 전환이 아니다"가 **전 화면 규칙으로 승격**돼 이 화면의 동작은 **무변경**이다. 본문·근거는 common.md | `accountinterlockhub#493`·`#469` | ℹ️ |
| 2026-07-26 14:16 | build | `apps/backend/src/interlock-entry/`(신규 7파일) — **PROC-101 연동 링크 진입·형식 판정 최초 구현**(P07). `GET <INTERLOCK_ENTRY_PATH>` 컨트롤러(`entry.controller.ts`, 그동안 404 였던 경로를 처음 배선) — B2 요청 URL 길이 판정(2,000자, `SEC-001-10`) → B3 파라미터 파싱(대소문자 구분·중복 거부, Express 배열 반환을 `ProtocolFormatError` 로 명시 변환) → B4 구조 판정(기존 `FN-003 parseCipherPair` 재사용) → 통과 시 `{stage:'IDENTITY'}` · 위반 시 B5(`PROC-303 UNIDENTIFIED_FAILURE` 계수, 레코드 미생성) → B6(`PROC-105` 결과 안내 구성, 신규 `result-info.builder.ts` — 4개 호출 출처 전부 사양대로 구현해 후속 Phase 재사용 가능) → B7 200 `text/html` 응답. **초기 상태 주입**(`entry-initial-state-escape.ts`) — `<script id="__INTERLOCK_INITIAL_STATE__" type="application/json">` 정확히 하나, 5문자(`<`·`>`·`&`·U+2028·U+2029) 무조건 치환(코드 포인트로 구성 — 정규식 리터럴 타이핑 금지 함정 회피). **200 폴백 2단**: ① 컨트롤러 `handleEntry()` 가장 바깥 try/catch(어떤 내부 실패도 `EX-OPS-002`+200 으로 흡수) ② `common/filters/global-exception.filter.ts` **최소 변경**(P05 확정 영역 — `respondEntryPathFallback()` 신규 메서드 1개 + import 2줄 + catch() 진입점 4줄) — Express 본문 파서가 라우팅 자체를 건너뛰는 극단 사례(GET+깨진 JSON 본문)까지 흡수, `body-parse-failure.ts` 가 명시적으로 열어 둔 P07 인계분을 닫음. 두 폴백이 공유하는 순수 문자열 폴백(`HARD_FALLBACK_DOCUMENT`, `entry-document.ts`)까지 실패해도 200 을 유지. `crypto/crypto.constants.ts`(`SEC-001-09`·`10` 확정값 2건 추가)·`crypto/crypto.errors.ts`(`ENC_X_DUPLICATE`·`ENC_Y_DUPLICATE` 사유 2건 추가, 같은 `EX-SEC-001`)·`app.module.ts`(`EntryModule.forRoot()` 배선) 도 함께 변경. `known-routes.ts` 변경 없음(경로가 이미 P05 선반영). **실측**(`npm run build` 에러 0 · 실 서버 기동 후 HTTP 왕복): 정상 진입 200+`IDENTITY` · 부재/중복/디코드실패/15·16·17B 경계/URL 2,100자 초과 전부 `EX-SEC-001`·`EX-SEC-004`+200 · 길이 판정이 구조 판정보다 우선(동시 위반 시 `EX-SEC-004`) · 무관 파라미터 무시 · 대소문자 구분 · POST→405(Allow: GET, HEAD) · 미지정 경로→본문없는404 · GET+깨진JSON본문(핸들러 밖)→200 `EX-OPS-002` 폴백 · 포이즌 페이로드(`</script><script>…`+U+2028/2029) 주입 후 전체 문서 바이트 스캔으로 잔존 0·정당한 `<script>` 요소 개수 불변·JSON round-trip 값 불변 확인 · 자가진단 경로 디코이 대조(8시나리오, GET/PUT/DELETE/HEAD/OPTIONS/POST 등) 전건 무회귀(1건 차이는 P05 기존 설계인 자가진단 POST 자기 응답, 내 변경과 무관 확인) · DB 델타 실측(`tbl_interlock_metric_daily` request/decrypt_failed 카운터 exec 생략·커넥션 풀 단독 경로로 정확히 반영, `tbl_interlock_tracking` 0건 유지 확인). 코드리뷰·기능검증은 별도 doer 대기 | `accountinterlockhub#484`·`#469` | 🚧 |
| 2026-07-26 13:35 | spec ⓒ | (공통 반영) 디자인 시스템 §접근성 기준 **포커스 우선순위 확정(`QA-OPEN-02` 해소)** + 목업 4종 포커스 링 미충족 시정 — 이 노드에 닿는 것은 **`SCR-001`**(연동 링크 진입의 착지 화면)이다. **[① 진입 경로는 이 예외의 적용 대상이 아니다]** 확정된 예외는 **단계 전환에 필드에 매인 안내가 함께 뜰 때** 제목 대신 그 필드로 포커스를 보내는 규칙인데, **진입(최초 로드)은 전환이 아니고** 진입 직후 자동 포커스도 주지 않아(§TextField) 이 노드가 그리는 **표기·거동·문구는 전부 무변경**이다. 다만 파생 결론 1건이 이 화면에 걸린다 — 이 예외로 **`SCR-001` 제목은 제품에서 프로그램 포커스를 받는 자리가 없어진다**(도착 전환이 `BackToIdentity` 하나뿐인데 그 자리를 필드가 가져간다). `screen_SCR-001.md` 는 **한 글자도 고치지 않았다**. **[② 목업]** `mockup/SCR-001.html` 에 `h1.stage-title:focus` **1줄**(+주석 2줄)을 더해 제목이 포커스를 받는 동안 사양의 링(바탕색 2px 안쪽 여백 + 2px `--color-focus` `#0B4FA8`)이 실제로 서게 했다 — 목업은 화면당 1파일이라 **동작 영향 없는 정적 계약 표기**이며, 진입 직후 상태(`initial`)를 포함한 **정상 상태 시각은 불변**이다(마크업 0줄·새 토큰 0·기존 `:focus-visible` 규칙 무접촉). 이 노드 TC **15건 무접촉** · 총 242건·결번 2건 불변. 규칙 본문·근거 셋·검증은 common.md 항목, `QA-OPEN-02` 본체는 USR-03.md 항목 참고 — common.md | `accountinterlockhub#474`·`#476`·`#469`·`#467` | ℹ️ |
| 2026-07-26 13:15 | spec | `docs/specs/processes/spec-process-mapping.md` §단계명 매핑 `PROC-101` 행(`:120`) **1칸 정정** — "응답 구성 → `B6`·`B7`" 에서 **`B7` 을 뺐다**(→ **`B6` 단독**). **이력·산출물 동일 commit**(작성 시점 미커밋). **[정본이 아니라 색인이 틀렸다]** 정본 `process_PROC-101.md` §단계명 매핑(`:225`·`:226`)은 이 구간을 **"응답 구성 → `B6`"** 과 **"다음 단계 이관 → `B7` → `F3`"** 두 행으로 **나눠** 적는다 — 색인의 `B7` 은 **같은 색인 행이 이미 "다음 단계 이관 → `B7`→`F3`" 으로 가리키던 이중 배정**이었고, 그대로 두면 "응답을 구성하는 자리가 어디냐"를 색인으로 되짚을 때 답이 둘 나온다. **정본은 한 글자도 고치지 않았다.** **[전수 대조로 찾았다]** 직전 `7fd5bb3` 분할이 부모 문서에 **라벨 집합을 명시**해 색인 인용의 **기계 대조**가 가능해진 직후라 §단계명 매핑 색인 **18 PROC · 98쌍 전수 대조**를 돌렸고 불일치 **3건**을 확인했다(나머지 2건은 `PROC-102` — USR-03.md 항목). 이 건은 **미실재 라벨이 아니라 중복 배정**이라 다른 2건과 성격이 다르다. **[진입 사양·거동 무변경]** `SVC-001`·`PROC-101` 의 단계 정의·의사코드·§분기 및 예외 흐름·화면·목업 **전 절 무접촉**이고 `B7`→`F3` 이관 자체는 색인·정본 양쪽에 **그대로 남아 있다**. 이 노드 TC 15건 무접촉 · 총 **242건**·결번 2건 불변. 저장소 전체 단계 라벨 인용 **447건 재검사에서 미실재 0**(오케스트레이터 기계 재검사) | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-26 12:20 | spec ⓒ | (공통 반영) 디자인 시스템 §접근성 기준의 **제목 프로그램 포커스 표시 확정** — 이 노드에 닿는 것은 **`SCR-001`**(연동 링크 진입의 착지 화면)이다. 단계 전환으로 포커스를 받는 제목(`tabindex="-1"`)에도 **공통 포커스 링이 예외 없이 선다**로 못박혀, 이 화면의 제목이 프로그램 포커스를 받는 자리에서도 브라우저 기본 링(1px)이 아니라 사양의 링(2px 안쪽 여백 + 2px `--color-focus`)이 그려진다 — 요구가 **조건(`:focus-visible`)이 아니라 결과**로 적혔다. 같은 회차에 `design-system-components.md` §StageTitle §접근성도 1줄 정합돼 그 정본을 가리킨다 — `SCR-001` 제목은 `StageTitle` 기본 변형 합성이라 계약을 그대로 받는다. **진입 경로가 그리는 표기·거동·문구는 전부 무변경**이고 `screen_SCR-001.md`·`mockup/` 무접촉이며 **새 토큰·새 색 0** 이다. 같은 회차의 검증 색인 정합(`spec-qa.md` 헤딩 244 각주 · `spec-qa-mapping.md` `SCR-001` 서술 보강)도 **수치·총계 무변경**이라 `USR-01_001` 을 포함한 이 노드 TC 15건은 손대지 않았다 — common.md | `accountinterlockhub#474`·`#476`·`#469` | ℹ️ |
| 2026-07-26 11:54 | spec | `mockup/INDEX.md` §시연한 상태·변형 `SCR-001` **1행** — 직전 `de482d3` 의 **하류 정합**(색인 표기 보완). **이력·산출물 동일 commit**(작성 시점 미커밋). `SCR-001` 은 연동 링크 진입의 착지 화면이라 §화면 ↔ SCR ↔ IA 매핑표가 이 노드도 함께 걸지만, 이번에 고친 것은 **"확인 중(로딩)" 한 칸**(제출 중 잠금 표기에 `aria-busy="true"` 보완)뿐이라 **진입 직후 상태(`initial`)를 포함해 진입 경로가 그리는 표기·거동은 전부 무변경**이다. 목업 `SCR-001.html`·화면 사양 무접촉. 잠금 표기 상세는 USR-03.md 항목, 이번 정합의 본체(`aria-busy` 해제 시점)는 USR-02.md 항목 참고 | `accountinterlockhub#477`·`#469` | 🚧 |
| 2026-07-26 11:32 | spec ⓒ | (공통 반영) 목업 잔여 접근성 3건(F-1·F-3·F-6) 마감 — 이 노드에 닿는 파일은 `mockup/SCR-001.html` 이다. 진입 착지 화면의 제목 `h1`("본인 확인")에 **`tabindex="-1"` 을 부여**했고(F-6 — `design-system-components.md` §StageTitle §접근성의 **단계 전환 시 새 제목으로 포커스 이동**이 받을 자리이며, 목업은 화면당 1파일이라 **동작 영향 없는 정적 구조 계약 표기**다), 제출 중 입력 필드에 `aria-busy="true"` 가 더해졌다(F-1). **진입 직후 상태(`initial`)를 포함한 화면 거동·문구·레이아웃 전부 무변경**이며 진입 경로가 그리는 것도 그대로다. 제출 중 잠금 표기 상세는 USR-03.md 항목 참고 — common.md | `accountinterlockhub#474`·`#469`·`#467` | ℹ️ |
| 2026-07-26 10:37 | spec | `mockup/SCR-001.html` — 목업 **재입력 상태의 오류 표현 시정(D-1)**. **⚠️ 이력 누락 보정 — 산출물은 commit `316bf77` 로 이미 커밋·origin push 됐고(같은 commit 에 이력을 넣지 못했다) 본 entry 가 그 근거 해시로 추적을 잇는다.** `SCR-001` 은 연동 링크 진입의 착지 화면이자 본인확인 입력 화면이라 두 노드에 걸린다 — 이번 시정은 **본인확인 재입력 상태 한정**이며, 진입 직후 상태(`initial`)를 포함한 정상 4상태는 전체 페이지 픽셀 비교로 **제품 카드 영역 0px 불변**·자동 포커스 링 회귀 0 을 실측 확인했다(진입 경로 거동 무변경). 시정 내용·검증 상세는 USR-03.md 항목 참고 | `accountinterlockhub#474`·`#469`·`#467` | 🚧 |
| 2026-07-26 08:45 | spec ⓒ | (공통 반영) `StageTitle` 합성 간격 규칙 명문화·목업 `hidden` 무력화 시정(6회차 회귀) — 이 노드에 닿는 것은 **`SCR-001`**(`design-system-components.md` §StageTitle · `mockup/SCR-001.html`)이다. `StageTitle` 의 여백(제목→보조 설명 `--space-2xs` · 블록 아래 `--space-lg`)이 **카드 직계 자식일 때의 값**으로 한정됐는데 **`SCR-001` 이 바로 그 자리**라 진입 착지 화면의 간격은 그대로다(**거동 무변경** · `screen_SCR-001.md` 무수정 · 제목→보조 설명 값은 이번에 처음 명문화됐다). 목업에는 `[hidden]{display:none !important}` 가 들어가, 저자 `display:flex` 선언이 UA 의 `[hidden]{display:none}` 을 원산지 우선순위로 이겨 **조건과 무관하게 늘 떠 있던 빈 알림 상자**가 사양대로 숨는다 — 진입 직후 오류 없는 화면이 비로소 실제로 그려진다(알림 상세는 USR-03.md 항목 참고). **⚠️ 소급 보정 entry** — 근거 커밋 **`9e6eb90`**. 이력은 공유 워킹트리 경합으로 직전 build 커밋 `fdc9f76` 에 common.md 08:45 row 만 선행 반영되고 **각 IA 역링크가 누락**돼 지금 채운다 — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-26 06:19 | spec ⓒ | (공통 반영) 횡단 국소 회귀(5회차) — `spec-functions-api-user.md` §연동 요청 진입의 **`EX-OPS-002` 200 매핑 특례가 화면 문서를 돌려주는 `GET` 접점 한정**임을 기존 불릿에 명문화했다(제출 접점 `POST` 2종에는 특례 미적용 — 카탈로그대로 500 + 오류 응답 엔벨로프). 진입 접점의 **표 행·거동은 무변경**이며 200 화면 + 결과 경로 ② 안내도 그대로다 — common.md | `accountinterlockhub#473`·`#470`·`#472`·`#469` | ℹ️ |
| 2026-07-26 05:34 | spec | 교차검증 4회차 🟡 보완 회귀 — `tc_USR-01.md`·`spec-qa.md` **신설 0 · 개정 4**. **W-6** `USR-01_014` 단계 ① 에서 **정적 서빙 배선 확인 구절을 뺐다** — 그 배선은 런타임 입력으로 재현할 성질이 아니라 **구현 배치**의 문제라(전제에 만들 수단이 없어 Block 사유만 늘던 자리) TC 로 재현하지 않는다. 배선이 뒤집히면 `stage = IDENTITY` 를 기대하는 **정상 진입 TC 가 전건 실패**하고 `SEED-VEC-01` **시드 준비 단계에서 먼저 드러나 커버리지가 비지 않는다**(신규 하네스 0). 사양 쪽 배선 순서 요구는 `PROC-101` §구현 가이드 · 인터페이스 §초기 상태 주입 형식 4 에 **그대로 있고**, 단계 ① 이 두 출처를 명시 인용한다. **W-2 연쇄** — 같은 TC 검증 대상 `3·4` → **`1·3·4`**, 단계 ② 에 **"첫 요소만 읽어 `IDENTITY` 로 진행하면 Fail"** 판별 관측점 명시(검출 수단 정본 = §초기 상태 주입 형식 1 담는 자리 2). **W-1** `spec-qa.md` 시스템예외 근거 문장의 `EX-OPS-002` **4 → 5건** 정정(`spec-qa-mapping.md` 실측 일치 · `EX-BIZ-003` 9건은 정합이라 무변경). **W-5** 같은 문서 `MOCK-RESP` **배타 한정("…데만 쓴다") 해제** — 세 번째 용도(진입 문서의 초기 상태 블록 조작)를 더하고 **용도 열거 정본을 `spec-qa-seed.md` §모킹 전략**으로 넘겨 두 문서가 같은 결론을 내게 했다. **총 TC 241건 무변화 · 결번 2건(`USR-04_008`·`USR-05_002`) 무변화 · 재채번 0 · 유형 분포 7종 실측 일치** | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-26 04:57 | spec ⓒ | (공통 반영) 검증 TC 국소 회귀 — `tc_USR-01.md` **신설 2**(`USR-01_014` 초기 상태 **부재 판정 4조건 → 전건 경로 ②**·`IDENTITY` 폴백 금지·사유 코드 미생성 → SCR-004 `EX-OPS-002` 문구 수렴, **정적 서빙 배선**을 ① 케이스에 한 구절로 흡수 / `USR-01_015` **주입 측 5문자 무조건 치환**을 문서 바이트로 확인·`JSON.parse` 후 값 불변·`</script>` 무파손) · **개정 1**(`USR-01_001` 단계 ②를 확정 주입 형식 — 요소 정확히 하나·JSON 1건·UTF-8·`id` 로 찾기 — 으로 구체화). TC 13 → 15건 · 채번 변경·결번 0 — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-26 04:40 | spec ⓒ | (공통 반영) 프로세스 연쇄 국소 회귀 — `process_PROC-101.md` `F1` 초기 상태 해석을 확정된 **부재 판정 4조건**(요소 없음 · 같은 `id` 둘 이상 · `JSON.parse` 실패 · `stage` 두 값 밖 → `stage=RESULT`·경로 ②)으로 넓히고 **`IDENTITY` 폴백 = 사양 위반**(`BIZ-001-02`)을 못박았다. 한 문서 안에서 둘로 갈리던 `initial` 형상 표기를 §입력/출력 정의 쪽으로 통일(`isReAnnouncement?`·`returnUrl?` 포함). `B7` 에 **담는 형식**(`<script id="__INTERLOCK_INITIAL_STATE__" type="application/json">` 정확히 하나 · JSON 직렬화 1건 · UTF-8) 인용과 **이스케이프의 주입 측(서버) 의무**(5문자 무조건 치환 · 값 불변 · 화면 역이스케이프 없음)를 명시하고, §구현 가이드의 정적 자산 라우팅 순서 불릿에 **왜**(값 없이 서빙되면 부재 판정이 발동해 정상 진입까지 경로 ② 로 끝난다)를 덧붙였다. 폴백은 **`reasonCode` 를 만들지 않으며** 그 상태는 `SCR-004` 의 **미상 코드 → `EX-OPS-002` 문구 수렴**·미매핑 경로 ② 안내가 받는다(`FN-014`·`SVC-005` F-003) — **새 EX 코드·라벨 무신설 · 분기·예외 표 행 무추가**. **TC 미커버 — 주입 형식·부재 판정 4조건·서버 이스케이프 검증 TC 0건**(신설 제안, 본 작업 범위 밖) — common.md | `accountinterlockhub#475`·`#469` | ℹ️ |
| 2026-07-26 04:30 | spec ⓒ | (공통 반영) build 사양 결함 국소 회귀(🟠 2건) — `screen_SCR-001.md` §화면 상태 전이의 **`Retryable` 진입 조건에 "본인확인 제출의 응답 미수신(전송 계층 단절)" 을 더했다**(새 상태 무신설 · PROC 단계 매핑에 "서버 단계와 무관" 병기)하고, §조건부 표시 재시도 안내에 같은 사유와 **기존 문구 그대로**(`EX-BIZ-003` 기본 문구 · `SEC-002-05`)를 명시했다. 승인 제출의 응답 미수신(`SCR-003` `Unconfirmed`)과는 **결과 확정 가능성의 비대칭** 때문에 다른 상태로 간다. 화면 쪽 요청 타임아웃은 신설하지 않았다. `spec-screens.md` §확정값의 "응답을 받지 못한 경우" 행도 두 접점으로 갈랐다 — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-26 04:23 | spec | `spec-functions-api-user.md` §연동 요청 진입 — **§초기 상태 주입 형식 (확정) 신설**(build 사양 결함 ④ 🔴 국소 회귀). 규약 5 가 "문서에 초기 상태를 실어 준다"만 정하고 **주입 형식(wire format)의 정본이 없어** 서버·화면·정적 서빙 셋이 공유할 약속이 사양에 없던 공백을 닫았다. build 관례를 정본으로 승격 — 진입 화면 문서에 **`<script id="__INTERLOCK_INITIAL_STATE__" type="application/json">` 요소 정확히 하나**, 텍스트 = 초기 상태 JSON 직렬화 문자열 1건(UTF-8), 화면은 **`id` 로 찾아** `JSON.parse`(문서 내 위치·순서 비의존). 근거: 데이터 블록이라 미평가 · 전역 변수 주입은 값이 곧 실행 코드라 배제 · 별도 API 는 첫 그림 공백(`SVC-005` F-003)+왕복 증가(`PROC-101` `F1` "추가 요청 없음"). **주입 측 무조건 이스케이프 의무 명문화** — `<`→`\u003c`·`>`→`\u003e`·`&`→`\u0026`·`U+2028`→`\u2028`·`U+2029`→`\u2029`, 전부 유효 JSON 이스케이프라 **값 불변**(화면 역이스케이프 없음). 근거: `</script`(및 `<!--`) 시퀀스 미성립 · `U+2028`/`U+2029` 의 JS 리터럴 문맥 구문 파괴 · **값 출처로 방어를 조건화하지 않음**(`returnUrl` 은 출처 자체가 미확정 — PRD §미결·확인 필요 ⑦ → 외부 입력화 가능). **값 부재 4조건**(요소 없음·둘 이상·`JSON.parse` 실패·`stage` 두 값 밖) → `PROC-101` `F1` 규칙 그대로 **`stage=RESULT`·경로 ②**, **`IDENTITY` 폴백은 사양 위반**으로 못박음(`BIZ-001-02` 미매핑 catch-all · `SVC-005` F-003 의 대응은 결과 안내이지 다음 단계 진행이 아님). 정적 서빙 배선(`PROC-101` §구현 가이드 순서 규칙과 동일 사실)·경계(값 형상은 규약 5 정본·`GET` 전용·암호값/추적 키 금지 규약 유지) 포함. 규약 5 에 한 줄 참조 추가. **코드 체계 신설 0·결과 경로 3종 불변·타 파일 무변경** | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-26 02:46 | build | `apps/frontend/src/` — **디자인 시스템·공통 컴포넌트 11종·FE 셸·단계 상태머신 신규 구현**(정본 `design-system.md`·`design-system-components.md`). 색 토큰 23종·타이포·간격·모션 값을 그대로 이식(`styles/tokens.css`) — 실측 대비비 27행 재계산 전건 표와 일치. 컴포넌트 11종(`components/`) 전량 신설. 단계 상태머신(`stage/useInterlockFlow.ts`)이 진입 응답 수화(hydration)·`/verify`·`/approve` 두 접점(`api/client.ts`) 응답을 서버가 정한 대로 `IDENTITY→CONSENT→PROCESSING→RESULT` 로 전이시킨다(화면은 스스로 건너뛰지 않는다) — `PROCESSING`(SCR-003) 만 화면 소유 단계로 별도 처리. 생년월일·암호값 쌍은 훅의 메모리 상태로만 보유하고 URL·저장소에 두지 않는다(`DATA-001-02`·`-03`). 승인 버튼은 `aria-disabled`(네이티브 `disabled` 아님)로 클릭·유효성 안내를 받는다. `IdentityScreen`·`ConsentScreen`·`InterlockJourney`(`flow/`)로 4화면을 실제로 잇는 참조 조립까지 완성 — 정밀 카피·레이아웃 마감은 P15(SCR-001·002)·P16(SCR-003·004) 소관. `npm run build`(`tsc -b && vite build`) 에러 0건 **[회귀 1회차]**(코드리뷰 Important 2건 + spec 4회차 역전파 2건 — 5건 중 진입 영향분) — `api/hydration.ts` **부재 판정 4조건 확정**(요소 없음·같은 `id` 요소 둘 이상·`JSON.parse` 실패·`stage` 두 값 밖 → 전건 `stage=RESULT`·경로 ②·`reasonCode` 미생성 — `process_PROC-101.md` `F1`·`spec-functions-api-user.md` §초기 상태 주입 형식 원문 그대로), 조건 ② 검출을 `document.getElementById`(첫 요소만 반환해 영원히 미발동)에서 `querySelectorAll('[id="…"]')` 개수 확인으로 교체. **운영 경로 `IDENTITY` 폴백 제거**(`PROC-101` `F1` 정면 위배 해소) — 개발 전용 폴백은 `import.meta.env.DEV` 가드로 분리해 운영 빌드 미탑재. 결과 경로 **`normalizeResultPath()`** 신설(`api/types.ts`, 1~3 밖·부재 → 경로 ②, `SVC-005` F-003)을 본 파일·`stage/transitions.ts`(`initialViewFromEntryState`)·`ResultPanel`(USR-05 영향분)이 공유 — `RESULT_PATH_META[resultPath]` 미가드로 인한 화면 전체 공백화(TypeError) 차단. `stage/useInterlockFlow.ts` **수화 경로 암호값 폐기 누락 시정**(기존 `applyNextView()` 경유 3경로만 폐기해 초기 진입 경로 1건 누락 — `DATA-001-03`, USR-05.md 도 참조). `npm --prefix apps/frontend run build` 에러 0건(자가 실측, `tsc -b && vite build`) — 작성만 수행, 리뷰·기능검증 별도 doer 대기 **[회귀 2회차]**(코드리뷰 Important 중 진입 영향분 I-B — tester 실측 🔴 Fail 0 은 회귀 1회차 5건 전건 확인, 별도 이월 없음) — `api/hydration.ts` 의 `import.meta.env.DEV` 분기·`DEV_ONLY_STARTING_STATE` 를 완전히 제거해 **조건 ①(요소 없음)도 ②③④와 동일하게** 예외 없이 `ABSENT_INITIAL_STATE`(경로 ②)로 간다 — 회귀 1회차의 그 가드 자체가 잘못된 위치였다(판독 함수가 사양의 무조건 규칙 밖으로 빠져나갔고, `vite dev` 에서는 조건이 항상 성립해 조건 ②③④·`JSON.parse`·`normalizeResultPath` 검증 경로 전체가 dev 환경에서만 죽은 코드가 됐다 — `tc_USR-01.md` `USR-01_014` dev 재현 불가 위험). 개발 편의는 **주입 계층**으로 이관 — `apps/frontend/vite.config.ts` 에 `apply: 'serve'` 전용 `devOnlyInitialStatePlugin`(`transformIndexHtml` 로 `{stage:'IDENTITY'}` 주입, §초기 상태 주입 형식 2 의 5문자 무조건 치환 계약도 그대로 구현) 신설. **자가 실측**: 임시 `vite dev`(포트 5183) 기동 후 `curl` 로 `` `<script id="__INTERLOCK_INITIAL_STATE__" type="application/json">{"stage":"IDENTITY"}</script>` `` 주입 확인 → 서버 정지·포트 해제 확인. `dist/index.html`(운영 빌드)에는 그 스크립트·`import.meta`·`DEV_ONLY_STARTING_STATE`·플러그인 이름 전부 0건(grep 실측) — 마커 문자열이 번들에 남는 유일한 자리는 `hydration.ts` 자신의 런타임 `querySelectorAll` 참조 1건뿐(정상). `npm --prefix apps/frontend run build` 에러 0건 | `accountinterlockhub#491`·`#469` | 🚧 |
| 2026-07-26 00:45 | spec ⓒ | (공통 반영) 요구사항 회귀 — 결과 경로 4→3 재번호의 연쇄 반영. `service_SVC-001.md`·`process_PROC-101.md` — 진입 단계 실패(`EX-SEC-001`·`EX-SEC-004`·`EX-OPS-002`)를 안내하는 자리를 **결과 경로 ③ → ②** 로 당겨 표기했다(경로의 뜻은 그대로 링크·복호화 오류이며 판정·분기는 무변경). 초기 상태 형상에 `returnUrl?` 이 더해졌으나 **진입 판정 실패는 항상 경로 ② 라 실리지 않는다**는 근거(`BIZ-001-06` 동봉 조건 미성립)를 의사코드에 남겨 두었다. `screen_SCR-001.md` 는 PRD 인용 문구만 갱신. TC `tc_USR-01.md` 기대 경로 번호 동반 정정(TC 신설·결번 0) — common.md | `accountinterlockhub#469`·`#471`·`#474`·`#475`·`#476` | ℹ️ |
| 2026-07-25 18:16 | spec ⓒ | (공통 반영) 목업 리뷰 발견 사항 반영(회귀 3회차) — 인라인 알림 위치를 **화면 사양의 알림 영역 한 곳**으로 통일(오류 유형별 이동 금지 · "카드 상단" 서술 폐기)·ConsentList **잠금(제출 중) 시각 처리 확정**(비활성 토큰 쌍 재사용·대비비 4.82:1·`opacity` 금지)·InlineAlert 사용 화면 목록에 SCR-003 추가. **M-1(`DECRYPT_FAILED` 레코드 확정 경로 복원) 미반영 — 구현 불가 판단으로 차단 보고** — common.md | `accountinterlockhub#477`·`#474` | ℹ️ |
| 2026-07-25 18:01 | spec | `mockup/SCR-001.html` 신규 — **SCR-001 본인확인 목업**(USR-01 진입 판정 통과 시 첫 화면). 진입 응답의 `stage=IDENTITY` 대응 카드 + 6개 상태 토글(초기·입력중·확인중·형식오류·재입력안내·처리오류), 인라인 SVG 아이콘·외부 자원 0. `mockup/index.html`(허브)·`INDEX.md`(매핑) 동반 | `accountinterlockhub#477`·`#469` | 🚧 |
| 2026-07-25 17:36 | spec ⓒ | (공통 반영) 교차검증 2회차 경고 5건 보완(회귀 2회차 — 🟡 N-1~N-5 전건 종결) — `tc_USR-01.md` `USR-01_013` §검증 대상에 `OPS-003-05`(`EX-OPS-002` 정의 규칙) 병기(TC 신설 0) · `OPS-003-05` 적용 PROC 을 FN-014 호출 9개(`PROC-101`~`PROC-105`·`PROC-201`~`PROC-204`)로 축소 · TC 도메인 이력 일시를 실제 commit 시각으로 정정 — common.md | `accountinterlockhub#476`·`#470` | ℹ️ |
| 2026-07-25 17:01 | spec ⓒ | (공통 반영) 교차검증 1회차 지적 보완(회귀 1회차 — 🟠 2·🟡 7 전건 해소) — 진입 접점(`spec-functions-api-user.md`) §에러 처리에 `EX-OPS-002` 행 추가 — 진입 접점은 4xx·5xx 본문을 만들지 않으므로 **200 화면 + 결과 경로 ③**(접점별 HTTP 매핑이 갈리는 근거 명시) — common.md | `accountinterlockhub#473` | ℹ️ |
| 2026-07-25 16:19 | spec ⓒ | (공통 반영) 검증 TC 도메인 신규 작성 — `docs/specs/qa/` **TC 232건**(커버리지 SVC 18/18 · PROC 18/18 · BR 24/24 · EX 13/13 · 정책 17/17 · 접점 11/11 — 미검증 0)·**횡단 시나리오 `SCEN_001`~`SCEN_023`**(여정 6 · 규약 대칭 왕복 3 · 기동·상수 검증 5 · 무저장 원칙 2 · 설계된 부재·수용 리스크 7)·**AUTH 치환 12건**·모의 수신처·C# 하네스·시드 카탈로그 37항·미해소 1건(성공률 정의 이월) — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-25 15:48 | spec | `docs/specs/qa/USR/tc_USR-01.md` 신규 — **연동 링크 진입 TC 13건**(웹·SPA UI). 구조 판정 통과/위반 전 경로(`encX` 부재·`encY` 빈 값·Base64URL 디코드 실패·**암호문 16배수 경계 15/16/17B**·파라미터 중복·대소문자 구분)·**요청 URL 길이 상한 경계 3종**(`EX-SEC-004`)·판정 실패도 200 `text/html` 유지·초기 상태 금지 값 0·미지정 파라미터 무시(TTL·1회성 표시 부재)·경로 외 요청 일반 404·**실패 시 추적 레코드 0건 + 지표만 계수**(`BIZ-002-02`·`BIZ-005-02` ②)·내부 오류 시 경로 ③. 검증 BR-001 · EX `EX-SEC-001`·`EX-SEC-004`·`EX-OPS-002` | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec | `process_PROC-101.md` 신규 — **PROC-101 연동 링크 진입·형식 판정**(RR·동기·트리거=발송처 링크 진입). `B2` 요청 URL 길이 판정(`SEC-001-10`) → `B3` 파라미터 파싱(대소문자 구분·중복 거부·이중 인코딩 금지) → `B4` 구조 판정(FN-003 — 복호화 미시도) → `B5` 실패 시 **지표만 계수**(추적 레코드 미생성 `BIZ-002-02`) → `B6`·`B7` **판정 실패도 200 `text/html`**(4xx 를 만들지 않는다)·초기 상태에 암호값 미포함. FE 는 `F2` 에서 **자기 URL 쿼리로 암호값을 읽어 페이지 메모리로만** 보유 | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec ⓒ | (공통 반영) 프로세스 도메인 신규 작성 — `docs/specs/processes/` **PROC 18종 확정**(정책·서비스 예약분을 같은 코드·같은 의미로 — 통합·폐기·추가·재배치 0)·**단계 라벨 체계 확정**(`F` FE·`B` BE·`C` 호출측·`L` 라이브러리 내부, 레이어별 1부터 시간 순, 분기는 접미 문자)·선행 도메인 단계명 전건 라벨 매핑·의존관계 순환 0(호출 깊이 3층)·트랜잭션 경계와 멱등 수단 총괄·정책 규칙 86건 매핑(런타임 80 + 게이트·절차 6) — common.md | `accountinterlockhub#475`·`#469` | ℹ️ |
| 2026-07-25 14:40 | spec | `spec-screens.md` · `screen_SCR-001.md` · `screen_SCR-004.md` — **진입 전용 화면을 별도 채번하지 않음**을 확정. 진입 응답이 곧 첫 화면 문서이고 초기 상태 `stage` 가 `IDENTITY` 면 **SCR-001**, `RESULT` 면 **SCR-004**(경로 ③)가 첫 화면이 된다. 화면은 **암호값을 자기 URL 쿼리에서 읽어 페이지 메모리로만 보유**(초기 상태에 미포함 — `DATA-001-04`)하고 요청마다 다시 싣는다. 진입 판정 실패(`EX-SEC-001`·`EX-SEC-004`)는 추적 레코드 없이 경로 ③ 으로 안내하고 재진입해도 같은 판정이다(`BIZ-002-02`). 새로고침·재진입은 **진입부터 다시 시작**(세션 없음 — `AUTH-001-02`). 링크 만료·1회성·요청 제한을 나타내는 표시 미도입(`OPS-002-05`) | `accountinterlockhub#474`·`#469` | 🚧 |
| 2026-07-25 14:40 | spec ⓒ | (공통 반영) 화면 도메인 신규 작성 — `docs/specs/screens/` **화면 4건**(SCR-001~004 — 서비스 예약 대비 통합·폐기·추가·재배치 0)·**단일 경로 4단계 모델**(리다이렉트 없음·화면별 URL 없음)·**디자인 시스템 1벌**(표면이 하나라 토큰 분기·접두어 없음 · 색 23종 **실측 대비비 표** · WCAG 2.1 AA · 모바일 우선 2 브레이크포인트 · 히트 영역 44x44)·공통 컴포넌트 11종·**화면 없는 IA 노드 12건 근거 기재** — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-25 14:10 | spec | `spec-functions-api-user.md` §연동 요청 진입 · `function_FN-001-003.md` — **진입 접점 확정**: `GET <INTERLOCK_ENTRY_PATH>` 를 **리다이렉트 없이 200 `text/html`** 로 응답하고 진입 화면 문서에 초기 상태(`stage`·`resultPath`·`reasonCode`)를 주입한다(암호값·추적 키 미포함 — 화면은 자기 URL 쿼리에서 암호값을 읽는다). **진입 파라미터 이름 `encX`·`encY` 단일 확정**(라이브러리·허브 공유 값이라 여기서 한 번만 정한다). URL 길이 상한 판정(`SEC-001-10` → `EX-SEC-004`)은 원본 URL 을 가진 진입 접점이, 구조 판정(부재·빈 값·Base64URL 디코드·16배수)은 **FN-003**(→ `EX-SEC-001`)이 담당하도록 경계 확정. 판정 실패도 200 화면으로 안내하고 **추적 레코드 미생성·지표만 계수**(FN-013 `UNIDENTIFIED_FAILURE`). 진입 경로가 아닌 요청은 본문 없는 일반 404. 인증·요청 제한 미도입(`AUTH-001-02`·`OPS-002-03`) | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-25 14:10 | spec ⓒ | (공통 반영) 기능·API 도메인 신규 작성 — `docs/specs/functions/` **공통 기능 15건**(FN-001~015 · POL 5·DAT 8·CRS 2·**EXT 0**)·**인터페이스 11접점**(사용자 진입 표면 3 · 서버 대면 API 4 · 수신처 전달 1 · 명령 진입점 1 · 라이브러리 공개 진입점 2)·오류 응답 엔벨로프 `{code,message,details?}`·인증 0/요청 제한 0·**신규 EX `EX-OPS-002` 1건**을 정책 카탈로그에 등록 — common.md | `accountinterlockhub#473`·`#469` | ℹ️ |
| 2026-07-25 13:35 | spec ⓒ | (공통 반영) 데이터 도메인 신규 작성 — `docs/specs/datas/` **엔터티 3건**(ENT-001~003 = 저장 대상 셋)·**데이터 모델 22종**(MDL-001~022 확정 — 예약 대비 통합·추가·재배치 0)·인덱스 5·물리 FK 0·소프트 삭제 미도입 — common.md | `accountinterlockhub#472`·`#469` | ℹ️ |
| 2026-07-25 13:05 | spec | `service_SVC-001.md` 신규 — **SVC-001 연동 링크 진입**(매핑 PROC-101). 고정 진입 경로에서 암호값 쌍을 읽고 **생년월일 입력 이전에 판정 가능한 구조 위반만** 먼저 거른다(F-001~F-006). BR-001 = 구조 판정 통과 여부 → 통과는 본인확인(SCR-001) 이관, 위반은 `SVC-005` 결과 경로 ③ 우회. 예외 `EX-SEC-001`(부재·빈 값·디코드 실패·암호문 길이 16 배수 아님)·`EX-SEC-004`(URL 길이 상한) — 둘 다 **추적 레코드 미생성**·지표만 계수. 진입 요청의 출처를 검증하지 않는 점·링크 재사용을 막지 않는 점은 수용 리스크로 명시. 연관 정책 9건(`SEC-002-02`·`SEC-001-08`·`SEC-001-10`·`BIZ-002-02`·`BIZ-005-02`·`DATA-001-02`·`SEC-002-05`·`OPS-003-03`·`AUTH-001-02`) | `accountinterlockhub#471`·`#469` | 🚧 |
| 2026-07-25 13:05 | spec ⓒ | (공통 반영) 서비스 도메인 신규 작성 — `docs/specs/services/` **서비스 18건**(SVC-001~018)·사용자 역할 4종·분기 `BR-001`~`BR-024`·MDL 22종/SCR 4종 예약 채번·`PROC-403`·`PROC-404` 추가 채번 — common.md | `accountinterlockhub#471`·`#469` | ℹ️ |
| 2026-07-25 12:28 | spec | `policy_SEC.md` `SEC-002` 진입 단계 구조 판정 신설 — 생년월일 입력 **이전에** 판정 가능한 위반(암호 파라미터 부재·빈 값·Base64URL 디코드 실패·암호문 길이가 16의 배수 아님·URL 길이 상한 초과)은 복호화를 시도하지 않고 즉시 실패 확정(400 `EX-SEC-001` / 길이 초과 `EX-SEC-004`). 추적 키를 알 수 없으므로 **추적 레코드 미생성**(`BIZ-002-02`), 화면은 결과 경로 ③(링크·복호화 오류), 지표만 요청 수+1·`DECRYPT_FAILED`+1(`BIZ-005-02`). `SEC-001-08`(Base64URL)·`SEC-001-10`(URL 2,000자) 이 진입 판정 근거 | `accountinterlockhub#470`·`#469` | 🚧 |
| 2026-07-25 12:28 | spec ⓒ | (공통 반영) 정책 도메인 신규 작성 — `docs/specs/policies/` 정책 17건·규칙 86건·EX 코드 12종(암호화 연동 규약·복호화 판정 4단계·결과 구분 4종·추적 레코드 생성 시점·보관·무저장·동의 증적·인증 부재·수용 리스크) — common.md | `accountinterlockhub#470`·`#469` | ℹ️ |
| 2026-07-25 10:05 | directing ⓒ | (공통 반영) 리셋 방향 담당자 확정분 반영 — 담당 범위를 **연동 링크 진입**으로 축소(본인확인 → `USR-03`, 동의·승인 → `USR-04` 분리 — 코드 재사용 아님). 구 사양 `docs/specs/`·구현 `apps/`·목업 `mockup/` 삭제 → **재작성 대상**(이력은 git `main` `0d83d60` 보존) — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
| 2026-07-20 08:03 | build | `#412` 오류 #465 회귀 수정 — `components/user/BirthDateField.module.css` 포커스 경계 규칙 `.input:focus`→`.input:focus:not(.inputError)`(1줄 + 근거 주석). 에러 시 입력이 `input inputError` 두 클래스를 함께 받는데(BirthDateField.tsx) 기존 `.input:focus`(특이도 0,2,0)가 `.inputError`(0,1,0)를 눌러, 복호화 실패(EX-SEC-006) 자동 포커스(SCR-005 §구현 가이드 AUTH-004-02)에서 error+focus 상시 공존 시 danger 경계(#DC2626)가 primary(#2563EB)에 가려지던 회귀 시정 — `:not(.inputError)` 로 포커스 규칙이 에러 상태에선 미적용(소스순서 무관)되어 danger 경계가 우선(design-system.md §컴포넌트 `TextField(user/birthdate)` error=`color-u-danger-border` 정합). 캡션·aria·배경·모션·disabled·전역 `:focus-visible` 링 무변경. `npm run build`(tsc -b && vite build) 성공(99모듈)·dist CSS 에 `._input…:focus:not(._inputError…)` 반영 확인. **3책임 통과**(리뷰 PASS·기능검증 🟢 — USR-01_013 재현 Pass: 복호화실패+포커스 동시상태 border-color=danger rgb(220,38,38)/#DC2626 확인, 400 EX-SEC-006·캡션 정본·aria-invalid·role=alert·값유지+포커스+전체선택·배경 무착색·레코드 delta 0·정정 재제출 200 COMPLETED 무회귀; 28/28). 단계 종료 후 main 미병합 — 담당자 배포판정·qa 축약 재검증 대기 | `accountinterlockhub#465`·`#412` | ✅ |
| 2026-07-18 23:16 | qa ⓒ | (공통 반영) `#416` Wave 3(최종) SCEN_001~006 E2E 여정 재검증 6/6 Pass(🟢3·🔵3) — common.md | `accountinterlockhub#416` | ℹ️ |
| 2026-07-18 22:15 | qa | `#416` Wave 1 — USR-01 전건 19 TC(Phase 1~19, `c7b0773`) 실행 → **16 Pass(14 🟢+2 🔵 mock B의존)/3 🔴 Fail/0 🟠 Block**(19=수행 전건). SCR-005 고정 문구 6종·SCR-006 결과 4유형×5요소 전량 정본 1:1 일치, 시각 폴리시(레이아웃·색·타이포·모션·히트영역 44x44px·포커스링) 실측 일치(#408 사양 대비). Block 예정 6항목(BLK-06·07·08·12·19·Q3)은 전부 기본안 기준 실행·판정 완료(자동 Block 0건). **결함 2건 신규 발견**: ① 요청 제한(FN-014) consent/approve 스코프 미독립 — 감사 target 오분류 + 버킷 공유 실증(오류 `#464`, USR-01_003·012 Fail). ② 생년월일 필드 복호화실패+포커스 동시상태에서 경계색이 danger 대신 primary 로 렌더(CSS 특이도 충돌, 오류 `#465`, USR-01_013 Fail). 검증 일감 `#417~435` 상태 동기화(해결 16·진행 3, GET 실측 확인). 증빙 스크린샷 13건 첨부. 잔여=담당자 배포판정(오류 `#464`·`#465` 처리 후 재검증 권고), Fallback 문구 정본 확정(`#430` 참조) | `accountinterlockhub#416`(검증 `#417~435`, 오류 `#464`·`#465`) | ❌ |
| 2026-07-18 19:51 | build | `#412` Phase 3 이월 3건 — `components/Modal.module.css`(`.modalUser` 블록에 `box-shadow: var(--shadow-md)` 1줄, 관리자 클래스 무변경)·`components/user/ConsentItemRow.{module.css,tsx}`(`.main`에 `align-self:stretch` 추가로 히트영역=행 전체 실질화(70px·[상세] 있는 행 72px)+stale 주석(구 68px) 정정). SCR-005 렌더 무회귀 확인(시각 변화 0, `git diff` 상 관리자 파일·`ConsentPage.*` 미포함). SCR-006 Phase 3(USR-02.md 참조)와 동일 commit. `npm run build` 0건(99모듈). **3책임 통과**(리뷰 PASS·기능검증 🟢 — SCR-005 무회귀·[상세] 독립 클릭·약관모달 정상) | `accountinterlockhub#415`·`#412` | ✅ |
| 2026-07-18 18:54 | build | `#412` 리뷰 후속 보완(Phase 2/3, Suggestion 6건 반영) — `pages/ConsentPage.{tsx,module.css}`·`components/user/{UserButton,ConsentItemRow,UserBanner,BirthDateField}.{tsx,module.css}`·`components/Modal.module.css`(`userTerms` 블록만) 수정. 약관모달 푸터 버튼 내용맞춤(`fitContent` prop 신설, S-1)·데스크톱 액션 버튼 `min-width:140px`(S-3, `fitContent` 로 모달과 분리)·동의항목 행 히트영역 48→44px(목업 실측 렌더 68px 로 동일화, `Checkbox(user)` 48px·접근성 44px 두 제약 유지 확인, S-2)·설명 이중 낭독 제거(`aria-describedby` 제거, S-4)·리터럴→토큰 치환 다수(gap·font-weight·일부 padding, S-5)·본 파일 최상단 entry 압축(S-6, 아래 행). 관리자 컴포넌트(`Button`·`TextField`·`Checkbox`·`Card`·`Banner`) 무변경, `Modal` 은 `userTerms` 블록만 수정(관리자 클래스 미변경 `git diff` 확인). `npm run build` 0건(95모듈). **3책임 통과**(재리뷰 PASS·기능검증 🟢 7/7) | `accountinterlockhub#414`·`#412` | ✅ |
| 2026-07-18 18:25 | build | `#412` SCR-005 시각 폴리시 구현(Phase 2/3) — `pages/{ConsentPage.tsx,.module.css}` 재작성 + 사용자 표면 전용 컴포넌트 신규 `components/user/{UserButton,BirthDateField,ConsentItemRow,UserBanner}.*`+`index.ts` + `Modal.tsx/.module.css`에 additive `chrome='userTerms'` prop(관리자 호출부 미전달로 렌더 불변). 관리자 공통 `Button`·`TextField`·`Checkbox`·`Card`·`Banner` 무변경. Phase 1 `-u-` 토큰 전량 소비(Card/Button/TextField/Checkbox/ConsentItem/Banner/Modal 사용자 변형), 구분선 2곳·오버플로·모션 규격 반영. 사양 정정 3건(안내 캡션 마침표 제거·약관모달 버튼순서 동의→닫기·에러 role=alert EX-SEC-006 한정). `npm run build` 0건(95모듈)·관리자 파일 변경 0건 확인. **3책임 통과**(리뷰 PASS·기능검증 🟢 7/7 — 실 백엔드·실 DB·실 암복호화 왕복, 시각 실측 114건 일치) | `accountinterlockhub#414`·`#412` | ✅ |
| 2026-07-18 17:38 | build | `#412` 사용자 표면 토큰(-u-) 신설(Phase 1/3) — `apps/frontend/src/styles/tokens.css`에 design-system.md §사용자 표면 확장 정본대로 `-u-` 접두 토큰 46종 신규 추가: 색 19(배경·경계·행상태 7 + success/info/danger/neutral 3종쌍 12) · 타이포 14(text-u-title/lead/section/item/value/caption 의 size·line 분리, section·value 만 자간; 굵기는 전역 --font-weight-* 재참조로 중복 미정의) · 간격 4(shell-y·card 는 브레이크포인트 640px 반응형 미디어쿼리 + section·stack) · 형태 4(radius-u-card/control/pill·shadow-u-card) · 모션 5(motion-u-fast/base/slow·ease-u-standard/decelerate). 기존 전역 토큰 **0건 변경**(git diff 83 insertions/0 deletions, 관리자 SCR-001~004 렌더 불변 전제 충족). `npm run build`(tsc -b && vite build) 0건. 화면·컴포넌트 적용(Phase 2·3)은 범위 밖(미착수). **3책임 통과**(리뷰 PASS·기능검증 🟢 4/4 — 전역 토큰 불변 바이트 증명, 신규 토큰 44종 런타임 실측 일치) | `accountinterlockhub#413`·`#412` | ✅ |
| 2026-07-20 08:10 | build | 진입 요청제한 스코프 오분류 핫픽스(오류 #464 — release #224 이월 결함, FN-014 은 consent·approve 를 독립 스코프로 규정) — `apps/backend/src/user/interlock/entry-rate-limit.middleware.ts` 스코프 판별을 `req.path`→`req.originalUrl`(쿼리 제외)로 정정 + `interlock.module.ts` 오해 소지 주석(미들웨어가 전역 라우팅으로 매칭된다는 가정) 정정. **근본원인(실측 확인)**: NestJS 는 `forRoutes` 경로마다 미들웨어를 Express `app.use('/api/consent', …)` 형태로 마운트하므로 미들웨어 내부에서 `req.path` 가 마운트 상대경로로 재작성됨 — `GET /api/consent/:id` 요청 시 `req.path=/:id`·`req.baseUrl=/api/consent`·`req.originalUrl=/api/consent/:id` 로 관측됨 → `req.path.startsWith('/api/consent')` 항상 false → 스코프 항상 `approve` 로 고정. 결과: consent·approve 가 단일 `approve:IP` 버킷 공유(한쪽 소진이 다른 쪽 차단)·429 감사 `target` 이 늘 `approve`(FN-014 독립 스코프 위반). `req.originalUrl` 은 마운트에 재작성되지 않는 원본 경로라 두 경로를 정확히 구분. `nest build` 0건. 축약 실행검증(빌드 자가점검, prod 번들 :3110·PostgreSQL 55432): consent 60통과→429 / approve **첫 요청 비-429**(독립 버킷 입증)→60통과→429 / RATE_LIMIT_EXCEEDED 감사 `target` consent·approve 각각 정확 관측. 60/분 한도·출발지 IP 판별·EX-OPS-001 엔벨로프 무변경. **3책임 통과**(리뷰 PASS·기능검증 🟢 — USR-01_003·USR-01_012 재현 Pass: consent 60통과→61째 429 EX-OPS-001·감사 target=consent(정정 확인, 구 항상 approve), approve 독립버킷(consent 소진에도 첫 요청 비-429)·양방향 교차오염 해소·감사 target=approve, 복호화실패 400 EX-SEC-006 무하드잠금·IP 마스킹; 11/11). 핫픽스 브랜치, main 미병합 — 담당자 배포판정·qa 축약 재검증 대기 | `accountinterlockhub#464`·`#416` | ✅ |
| 2026-07-18 14:55 | spec | `#408` 사용자 페이지 시각 개선(SCR-005) — `screen_SCR-005.md` 개정 + `design-system.md` §사용자 표면 확장 신설. **전역 토큰 키·값 변경 0건**(관리자 SCR-001~004 렌더 불변 — 전역 `Button`·`Badge` 두 행은 규칙 문구만 해석 명문화·변형 분리 기술, 관리자 인용처 없어 렌더 영향 0). 접두어 `-u-` 사용자 전용 토큰 신설: 보조 텍스트 `color-u-text-muted` #5F6773·경계 `color-u-border` #7D8698(3.66:1)·`color-u-canvas` #EEF2F7·행 hover/선택·타이포 6종(색 명시)·간격/형태 8종·모션 5종(`prefers-reduced-motion` 대응)·상태 3종 쌍(success #E7F6EC/#166534/#15803D · info #E6F6FB/#155E75/#0E7490 · danger #FEF2F2/#B91C1C/#DC2626 · neutral #F1F5F9/#334155/#64748B). 컴포넌트 사용자 변형 `Card(user)`·`Button(user/primary·secondary·ghost-link)`·`TextField(user/birthdate)`·`Checkbox(user)`·`ConsentItem(user)`·`Badge(user)`·`Banner(user)`·`Modal(user/terms)`. 시정: 접근성 대비 미달(A-3)·터치 타깃 모순(A-2, [상세] 가시 32px+히트 44x44px·`sm` 미사용)·모션 토큰 부재(A-9)·콘텐츠 상한(A-7, notice 144px(6줄)·항목 목록 상시 360/320px 스크롤·라벨/설명 말줄임 금지·Modal min(520,vw−32)×min(640,vh−64))·복호화 실패 후 필드 상태 확정(A-8, **값 유지+포커스 이동+전체 선택**)·오류 문구 정본 표(A-1)·`#215` 개정 노트 누락(A-10). **교차검증 회귀 1회차 반영**: 🟠 E-1 캡션 색 미지정→렌더 조합 AA 미달(#6B7280 on `u-row-selected` 4.38) 해소 — 전용 토큰 `color-u-text-muted` #5F6773 신설(5.72/5.33/5.18)·사용자 화면에서 전역 `color-text-muted` 퇴출(lead·section·caption·placeholder 전량)·본문은 `color-text` 유지로 위계 보존, 대비표를 **실제 렌더 조합 전량 등재**(20행, 흰색·행 hover·행 선택·primary·primary-hover·각 `-bg` 배경 망라)로 완결하고 거짓 단언을 "등재 조합은 전부 충족 + 신규 조합 등재 필수"로 교체. W-1 구 표기 통일(`TextField(user/birthdate)`·`Badge(user)`·`Banner(user)`·`text-u-caption`·`Modal(user/terms)`, 링크오류 error→danger)·W-2 `ease-u-standard` 인용처 부여·W-3 원시색 #1F2937→`color-text`·W-4 EX-OPS-001 `Banner(user)`(danger·`role=alert`) 명시·W-5 콘텐츠 상한 자기모순(항목 수 무관 상시 상한)·W-6 132px≈6줄 산술오류→144px(행간 24px×6). **기능·플로우·트리거 PROC·유효성 규칙·민감값 노출 규칙·고정 문구 8종 불변**. **교차검증 회귀 2회차(잔여 🟡 7건 정리)**: `Banner(user)` 계열명 error/info→**danger 단일 변형**(`role=alert`, 사용자 `Banner` 인스턴스는 전부 4xx/5xx)·좌측 20x20px 아이콘 글리프 색 `-fg` 명시, `color-u-info-*` 용도를 실사용(거부 배지·결과 아이콘)으로 정정(미사용 "안내 배너" 서술 제거), `ConsentItem(user)` 필수(*)/선택 텍스트 토큰 배정(`text-u-caption` 크기 13px/18px — 필수 700·`color-text` / 선택 400·`color-u-text-muted`, 오류 계열 미사용), `Modal(user/terms)` 패널 배경 `color-bg`·헤더 제목 `text-h2`+`color-text`·약관 본문 15px/24px+`color-text` 배정 + §대비 검증 배경 목록에 모달 표면 추가·표 **1행 신규 등재**(모달 제목/본문 17.74:1), SCR-005 §사용자 인터랙션 대상 요소를 사용자 변형 표기(`TextField(user/birthdate)`·`Checkbox(user)`)로 통일. **전역 토큰 §디자인 토큰 main 과 바이트 동일·기존 대비표 20행 값 불변·고정 문구 전량 불변**. **목업 리뷰 보완 반영(M-1~M-3, 경량 목업 제작 후)**: 필수/선택 표시를 **텍스트 태그로 단일화**(별표(`*`) 폐지 — `screen_SCR-005.md` §레이아웃·§데이터 표시·§입력 폼 정의 + `design-system.md` `ConsentItem(user)`·`TextField(user/birthdate)`, 생년월일 라벨 필수 태그 명시·간격 4px/항목 8px)·승인/거부 **데스크톱 정렬 확정**(≥640px 가로·우측 정렬 `justify-content:flex-end`, 좌→우 승인→거부, 간격 12px)·**섹션 구분선 위치 2곳 확정**(상단 블록↔본인확인, 본인확인↔동의 항목 / 액션 앞 없음, 상하 여백 각 14px). 토큰 값·고정 문구·대비표 행 불변(신규 색 조합 0건) | `accountinterlockhub#409`·`#408` | 🚧 |
| 2026-07-12 23:55 | build | #239 필수동의 집계 신뢰 확정(사양 긴장 해소) — FN-008(`function_FN-008.md`) 의사코드 `allChecked`→집계 신뢰(empty(requiredItems) OR requiredConsentMet)·구현가이드·note 정합 + `consent.service.ts` WARN 주석을 확정 표기로 갱신(**코드 로직 무변경**, BIZ-002-04 동의원장 미저장과 정합) + TC `tc_USR-01_008`(집계 false→REJECTED 기대) | `accountinterlockhub#239`·`#236` | ✅ |
| 2026-07-12 23:45 | build | #238 encX/encY 완전누락 EX 코드 정합(EX-SEC-004→EX-SEC-007) — 코드 `approve.dto.ts`(@IsNotEmpty 제거→@IsOptional, 부재 선차단 폐지)·`interlock.service.ts`(B3b 가드 encX/encY 제외·birthDate 만 EX-SEC-004)·`hub-decrypt.service.ts`(FN-020 부재 null 체크 EX-SEC-007 통합) + 사양 `function_FN-020.md`·`policy_SEC-crypto.md`(SEC-006-05 부재 명문화) + TC `tc_USR-01_014`. **실행검증 🟢**(prod 번들+PostgreSQL 55432+mock B): 완전누락/빈값/형식오류 400 EX-SEC-007·정상 AGREE 200 COMPLETED(mock B X 무변형 수신)·REJECT 200·틀린 생년월일 400 EX-SEC-006·birthDate 누락 400 EX-SEC-004(#213 무회귀) | `accountinterlockhub#238`·`#236` | ✅ |
| 2026-07-12 22:00 | qa | USR-01_001~019 검증 → 16 Pass(🔵 mock B 포함)·3 Fail(008 필수동의 재검증갭·014 완전누락 EX코드). SCR-005 UI 실증. 오류 #237/238/239 | `accountinterlockhub#236` | 🚧 |
| 2026-07-12 11:43 | build | 사용자 이용 동의 FE 개정(P6, USR-01 — SCR-005 실 백엔드 연동) — `apps/frontend/src/pages/{ConsentPage.tsx,ConsentPage.module.css}` 전면 재작성 + `lib/consentApi.ts` 재정의 + `App.tsx` 라우팅(`/interlock/entry/:accessAddressId`, 구 `/consent/:requestKey` 폐기, USR-02 SCR-006 라우팅과 같은 commit) + `components/{TextField.tsx,TextField.module.css}` birthdate 변형 신설(6자리 숫자·inputMode=numeric·maxLength=6·YYMMDD 플레이스홀더, 네이티브 type=text 렌더). 실 백엔드(P5) 연동: mount 시 GET /api/consent/:accessAddressId(consentNotice+items) 조회 → 제목 아래 consentNotice(있으면)·본인확인(생년월일)·동의 항목(Checkbox+[상세]→약관 Modal) 렌더 → 승인 POST /api/interlock/approve `{decision:'AGREE',accessAddressId,requiredConsentMet,encX,encY,birthDate}` / 거부 `{decision:'REJECT',accessAddressId,requiredConsentMet:false}`(encX·encY·birthDate 미포함). encX·encY 는 mount 시 URL 쿼리에서 1회만 읽어 컴포넌트 상태(메모리)로 고정·미렌더·URL 재기록·저장 없음, 생년월일도 입력 필드 상태로만 보유(로그·URL·화면 어디에도 미노출). 응답 상태 전이 전량 구현(SCR-005 §화면 상태 전이): 200 COMPLETED/REJECTED→SCR-006 `{result:'completed'/'rejected'}` · 400 EX-SEC-006→본 화면 유지+생년월일 인라인 에러("사용자 정보가 일치하지 않습니다.", FE 형식 문구 "생년월일 6자리(YYMMDD)를 정확히 입력해 주세요."와 캡션 영역 공유·문구만 교체)+재입력(즉시 해제)·재제출 허용(하드 잠금 없음) · 400 EX-SEC-007/EX-BIZ-008→SCR-006 `{result:'link-error'}` · 502 EX-BIZ-004→본 화면 유지+error Banner("동의 처리에 실패했습니다. 다시 시도해주세요.")+재승인(동일 accessAddressId·encX·encY·현재 birthDate 재제출) 허용 · 429 EX-OPS-001→Banner("잠시 후 다시 시도해 주세요.") · 400 EX-SEC-004/413→Banner("요청이 올바르지 않습니다."). Submitting=승인/거부 버튼 Spinner+폼(입력·체크박스·상세 버튼) disabled. 승인 버튼 활성=생년월일 형식 유효 AND 필수 동의 전부 체크. `npm run build --prefix apps/frontend` 0건. ⚠ WARN: FN-008 processDecision 의 항목별 재검증 불가 갭(P5 리뷰에서 이미 발견·확인 필요 표기)은 FE 영향 없음(FE 는 집계값 requiredConsentMet 만 전송, 계약대로 구현). 작성만 수행(리뷰·기능검증은 별도 doer 대기) | `accountinterlockhub#230` | ✅ |
| 2026-07-12 11:10 | build | 사용자 연동 실행 BE 통합(P5, USR-01 부분) — 진입/동의/승인 게이팅 재작성. **`GET /api/consent/:accessAddressId`**(무상태, FN-008 buildConsentView — 요청 키값 저장소 폐기) + **`POST /api/interlock/approve`**(신규, FN-008 processDecision 게이팅→승인 시 PROC-203 트리거, `{decision,accessAddressId,requiredConsentMet,encX?,encY?,birthDate?}`→`{result:'COMPLETED'\|'REJECTED'}`)로 구 `POST /interlock/entry`+`POST /api/consent/:requestKey` 통합·대체. `user/entry-context/*`(EntryContextStore·Module) 삭제 — 접근 컨텍스트는 요청 본문 경유·무저장(스코프 종료 자연 해제). `entry-rate-limit.middleware.ts` 재배치(FN-014 scope `consent`/`approve` 독립 카운트, 경로 판별). `app.module.ts` ServeStaticModule exclude `/interlock/{*splat}` 제거(`/api/{*splat}` 만 남김) — SPA 라우트(`/interlock/entry/:accessAddressId`·`/interlock/result`) index.html 폴백 복원(P6 FE 선행 조건). `dto/entry.dto.ts`·`dto/submit-consent.dto.ts` 삭제, `dto/approve.dto.ts` 신규(ValidateIf AGREE 시 encX·encY·birthDate NotBlank, Base64URL 형식은 미검증 — FN-020 이 EX-SEC-007 로 판정). audit.constants.ts 에 `CONSENT_AGREE`·`DELIVERY_SUCCESS` 2종 신규(spec 요구·누락 발견). `npm run build` 0건·부팅 확인(TypeORM 연결·라우트 매핑·`/api/consent`+`/api/interlock/approve` 정상, 구 bare `/interlock` 컨트롤러 잔존 없음)·와이어링 smoke 3건(EX-SEC-004 400·필드 details 정상). ⚠ WARN: FN-008 `allChecked(requiredItems, decision)` 의사코드가 항목별 체크 재검증을 서술하나 MDL-203 은 집계 boolean(requiredConsentMet) 1개뿐 — 항목별 서버 재검증은 와이어 계약상 불가(확인 필요, consent.service.ts 주석). 작성만 수행(리뷰·기능검증은 별도 doer 대기) | `accountinterlockhub#229` | ✅ |
| 2026-07-12 10:30 | spec | `#215` 목업 리뷰 보완(3건+확인) — ① **동의 대상 설명 문구(consentNotice)** SCR-005 상단(제목 아래) 노출: FN-008 buildConsentView 반환 `{ consentNotice, items }`·PROC-201·SVC-004 F-003·BIZ-002-08 신설·SCR-005 레이아웃/데이터표시/mount. ② **복호화 실패(EX-SEC-006) 문구 "사용자 정보가 일치하지 않습니다."로 고정**(FE 형식 안내 "생년월일 6자리…"와 같은 캡션 영역서 텍스트만 구분 교체): SCR-005 상태전이·조건부표시·PROC-202 FE·design-system. ③ **전달 실패(EX-BIZ-004) UX**: SCR-006 이동 폐지→SCR-005 잔류 error Banner "동의 처리에 실패했습니다. 다시 시도해주세요."+승인 재제출(재승인): SCR-005/SCR-006/PROC-202/design-system/spec-screens·tc_USR-01_016·spec-qa BLK-20 **해소**. ④ 거부 배지 info(청록) 확정(변경 없음). SCEN_004·SCR-005/006 목업 반영 | `accountinterlockhub#215` | 🚧 |
| 2026-07-11 23:30 | spec | `tc_USR-01.md` 개정 — 발송처 링크 진입(접근 주소=발송처)·생년월일 본인확인·동의/거부·승인 게이팅 재정의(요청 키값·회원 키 진입 폐기). 승인=POST /api/interlock/approve(접근 컨텍스트 본문). BR-201·전파 EX-SEC-006/007·EX-BIZ-008·EX-BIZ-004·거부/실패 레코드 미생성·약관 UI. 20→19 | `accountinterlockhub#222`·`#214` | 🚧 |
| 2026-07-11 23:30 | spec ⓒ | (공통 반영) #214 검증 TC 사양 개정 — 복호화·전달·추적 키 단독·시드(encX/encY·mock B)·신규 BR/EX(204·SEC-006/007·BIZ-008) — common.md | `accountinterlockhub#222`·`#214` | ℹ️ |
| 2026-07-11 22:30 | spec | `process_PROC-201.md`(진입·표시)·`PROC-202`(동의·거부·승인 게이팅) 개정 — 요청 키값(UUID) 발급·진입 컨텍스트 저장·진입 시 연동이력 생성(구 PROC-403) 폐기. 진입=GET /api/consent/:accessAddressId(무상태), 승인=POST /api/interlock/approve(접근 컨텍스트 encX·encY·생년월일 본문). FN-008 승인 게이팅(필수 동의 서버 재검증)·거부 시 복호화 미수행·상태/이력 미생성(감사만). EX-SEC-006/007·EX-BIZ-008·EX-BIZ-004 PROC-203 전파·BR-201 유지·BR-203/EX-BIZ-007/EX-DATA-002 결번 | `accountinterlockhub#221`·`#214` | 🚧 |
| 2026-07-11 22:30 | spec ⓒ | (공통 반영) #214 프로세스 사양 개정 — 복호화·전달·추적 키 단독·연동이력 복호화 후 생성·보관 fallback — common.md | `accountinterlockhub#221`·`#214` | ℹ️ |
| 2026-07-11 21:30 | spec | `screen_SCR-005.md` 개정(사용자 이용 동의) — 요청 키값 경로(`/consent/:requestKey`) 폐기·발송처 링크 진입(`/interlock/entry/:accessAddressId`+encX·encY, 접근 주소=발송처 판별). **생년월일 입력 필드 신설**(yyMMdd 6자리·형식 FE 검증·민감값 미노출 AUTH-004). 승인 제출=POST /api/interlock/approve(MDL-203+접근 컨텍스트 encX·encY·생년월일, PROC-202 내부 PROC-203 FN-020 복호화·FN-016·FN-012). 복호화 실패(EX-SEC-006) 화면 유지·생년월일 재입력 재시도(BR-204)·승인 게이팅(생년월일 형식+필수 동의 BIZ-002-06)·약관 [상세] 모달 유지 | `accountinterlockhub#220`·`#214` | 🚧 |
| 2026-07-11 21:30 | spec ⓒ | (공통 반영) #214 화면 사양 개정 — 화면 목록·경로·design-system(생년월일 입력·상태 표현·민감값 마스킹) — common.md | `accountinterlockhub#220`·`#214` | ℹ️ |
| 2026-07-11 20:30 | spec | `function_FN-008.md` 개정(사용자 동의 처리) — buildConsentView accessAddressId(고유 ID) 기반·processDecision 승인 게이팅(BIZ-002-06 서버 재검증). 구 요청 키값(UUID) 발급·진입 컨텍스트 저장·거부 시 처리상태 저장·FN-012 직접 호출 폐기 — 거부·필수 미충족은 복호화 미수행·최소 감사(CONSENT_REJECT)·추적 키 없어 상태/이력 미생성(EXC-BIZ-11). 승인은 SVC-005(PROC-203) 트리거 | `accountinterlockhub#219`·`#214` | 🚧 |
| 2026-07-11 20:30 | spec ⓒ | (공통 반영) #214 기능·API 사양 개정 — FN-020 허브 복호화 신규·추적 키 단독 추적·무저장·에러 체계 — common.md | `accountinterlockhub#219`·`#214` | ℹ️ |
| 2026-07-11 19:30 | spec | `model_user.md` MDL-201(진입 요청→**접근 컨텍스트(무저장)**: accessAddressId·encX·encY·생년월일 — 메모리 전용·전량 미기록 SEC-005-06)·MDL-203(동의 결과 — 요청 키값 폐기, decision·accessAddressId·requiredConsentMet 재검증). 복호화 이전이라 추적 키 없어 처리상태·연동이력 미생성(감사만) | `accountinterlockhub#218`·`#214` | 🚧 |
| 2026-07-11 19:30 | spec ⓒ | (공통 반영) #214 데이터 사양 개정 — 무저장 강화·비영속 컨텍스트·연동 추적 키·surrogate PK·보관 fallback·ENT-003 폐기 — common.md | `accountinterlockhub#218`·`#214` | ℹ️ |
| 2026-07-11 18:30 | spec | `service_SVC-004.md` 개정 — 이용 동의를 **접근·생년월일 입력·동의/거부·승인 게이팅**으로 재정의. 허브 발급 요청 키값(UUID)·진입 시 연동이력 생성(구 F-002/007)·지정 파라미터 검증(EX-BIZ-007) 폐기 — 연동이력 생성은 복호화 후 SVC-005 로 이동. AUTH-004 생년월일 복호화 요소·BIZ-002-06 필수 동의 게이팅·BIZ-002-07 거부 시 복호화 미수행 최소 기록·BR-201 재정의·BR-203 결번 | `accountinterlockhub#217`·`#214` | 🚧 |
| 2026-07-11 18:30 | spec ⓒ | (공통 반영) #214 서비스 목록문서 개정 — spec-services.md 사용자 정의 4역할·시나리오·의존관계·BR/EX/MDL/POL 카탈로그 — common.md | `accountinterlockhub#217`·`#214` | ℹ️ |
| 2026-07-11 16:20 | spec ⓒ | (공통 반영) #214 암호화 연동 정책 개정 — AUTH-004 생년월일 본인확인(비인증)·BIZ-002 승인 게이팅(필수 동의 체크·약관 선택·거부 시 복호화 미수행 최소기록)·OPS-001 사용자 진입 요청 제한 — common.md | `accountinterlockhub#216`·`#214` | ℹ️ |
| 2026-07-09 09:35 | build | `apps/backend/src/user/interlock/entry-rate-limit.middleware.ts`·`common/middleware/source-ip.util.ts`(신규)·`admin-ip.middleware.ts` — **진입 요청제한 XFF 미반영 핫픽스(#213)**. EntryRateLimitMiddleware 가 Express `req.ip`(app.set('trust proxy') 미설정 시 소켓 ::1 단일 버킷) 대신 공유 `resolveClientIp`(TRUST_PROXY 시 XFF 최좌측, 아니면 소켓 IP)+normalizeIp 로 출발지 판별 → 원 출발지 IP 격리(OPS-001-01). AdminIpMiddleware 도 동일 유틸로 리팩터(판별 단일화·무거동변경). nest build 0 에러. 축약 재검증 🟢: XFF .201 61req→60통과+429·.202 독립 버킷 전부 통과·감사 마스킹이 실 XFF(`20*********01`) 반영 | `accountinterlockhub#213`·`#54` | ✅ |
| 2026-07-09 09:20 | spec ⓒ | (공통 반영) SEC-004-01 바인딩 단독 방어 역전파 — `tc_USR-01.md` USR-01_007 개정(진입 파라미터 주입 문자 200 안전 저장/형식·크기 위반 400, 전송 검증 유지). 재검증 🟢(주입 param 값 200·ENT-007 원문 바인딩 저장/비문자열·512자 초과 400) — common.md | `accountinterlockhub#54` | ℹ️ |
| 2026-07-09 03:20 | qa | USR-01 독립 검증 20 TC 전수(배포산출물 v0.1.0/688fec2, localhost:3100 실 round-trip+실 DB psql+실 브라우저 Playwright chromium) → 🟢16·🔵2·🟣2·🔴0·🟠0. 실측: 진입 200 UUIDv4·회원키 전테이블 무저장·ENT-004 미생성·지정키만 ENT-007(EXC-DATA-07)·HISTORY_CREATE 앞2뒤2 마스킹(001)/구성소속 항목·display_order 정렬·terms_content(002)/비존재·비활성 400 EX-SEC-004(003)/미발급 400 EX-DATA-002(004)/요청제한 60분→61째 429 EX-OPS-001+RATE_LIMIT_EXCEEDED BLOCKED 감사(005)/진입·동의 본문>1MB 413 EX-SEC-005(006·013)/주입·형식 400 EX-SEC-004 필드details(007·014)/AGREE→B목200 전달·ENT-004 is_success=true·컨텍스트 폐기(008🔵)/REJECT ENT-004 false·목미호출·CONSENT_REJECT(009)/구성불일치·재제출 400 EX-DATA-002·중복없음(010·011)/AGREE→B목500 502 EX-BIZ-004·상태저장·목3회·DELIVERY_FAIL(012🔵)/SCR-005·약관모달·[상세]게이팅·모달[동의]/[닫기]·회원키·요청키 무노출 실브라우저(017~020). 정적🟣: 서버 필수항목 게이트 확정MDL-203 부재·FE선검증(015)·EX-FN-999 전역필터(016). 🔴 오류일감 #213: 진입 요청제한 XFF 미반영(소켓 req.ip=::1 집계·app trust proxy 미설정→프록시 뒤 단일버킷·OPS-001 위반). spec-sync: 001 TC "requestKey 무저장" vs 확정 연동이력 저장·015 서버 필수항목 차단 vs 확정 모델. evidence works/accountinterlockhub-54/evidence/UQA/ | `accountinterlockhub#54` (검증 #108~#127, 오류 #213) | ✅ |
| 2026-07-08 12:40 | build | `apps/frontend/src/pages/{ConsentPage.tsx,ConsentPage.module.css}` 신규·`lib/consentApi.ts` 신규·`App.tsx` 라우팅(USR-P3 FE) — SCR-005 사용자 이용 동의 화면. `/consent/:requestKey`(Public, catch-all 앞). mount 시 GET /api/consent/:requestKey(getConsentView) 조회→Loading Skeleton/Loaded/Error 상태 전이. 동의 항목 Checkbox 목록(라벨·설명 text-caption·필수(*)/선택 텍스트 병기·order 정렬), 약관 컨텐츠 있는 항목만 [상세]→약관 Modal(콘텐츠 변형: [동의]=체크 후 닫기·[닫기]=닫기만, 서버 호출 없음, EXC-BIZ-08). 필수 항목 전부 체크 시에만 동의 버튼 활성(BIZ-002-02 FE 1차 방어). 동의(AGREE)/거부(REJECT) 제출(submitConsent POST)→성공 시 `/consent/:requestKey/result` 네비게이션(state.result=AGREED/REJECTED), 502 EX-BIZ-004→DELIVERY_FAILED 이동, EX-DATA-002/EX-OPS-001·기타→Banner. 헤더 없는 중앙 카드 480px·상하 48px·모바일 좌우 16px·버튼 세로 스택. 무노출(회원키·요청키·configCode 미표시, configCode 제출용 메모리 보유·requestKey 경로만). 결과 페이지(SCR-006)는 USR-P4 소관 미구현(경로 이동만). `npm run build` 0건. 3책임 통과(리뷰 PASS·React 안티패턴 0·tester 실 브라우저 Playwright 12/12 🟢: 항목 렌더·약관 모달·필수 게이팅·제출 발화·무노출) | `accountinterlockhub#47` | ✅ |
| 2026-07-08 11:45 | build | `user/consent/{consent.service,consent.controller}.ts` 개정(USR-P3 BE 사전) — GET /api/consent/:requestKey 응답에 configCode 동봉(FE 동의/거부 제출 MDL-203 `{decision, configCode}` 의 configCode 회신용, PROC-202 ctx.configCode 정합). 응답 형상 `ConsentItemResponse[]`(배열)→`{ configCode: string, items: ConsentItemResponse[] }`(객체 `ConsentViewResponse`)로 변경. configCode 는 구성 식별 메타(개인정보 아님, 화면 미표시·FE 메모리 보유). 동의 항목 필드(label·description·termsContent·required·order)·회원키/요청키 등 민감값 미포함 유지. POST(processDecision) 로직 무변경. `npm run build` 0건. 3책임 통과(FE 연계 검증) | `accountinterlockhub#47` | ✅ |
| 2026-07-08 11:20 | build | `apps/backend/src/user/interlock/interlock.service.ts` FN-016 정정(USR-P2 중 발견) — 지정 사용자 키값 추출 키 `param_name`→`source_key_a`(ENT-003 §구현 가이드 정합). param_name≠source_key_a 지정 구성에서 진입 값 미검출로 오검출되던 것을 정정, 진입(PROC-201)→전달(PROC-203, source_key_a 기준 정합) end-to-end 정합 복원. 인접 주석 정정. `npm run build` 0건. 3책임 통과(리뷰 PASS·tester end-to-end 🟢 param_name≠source_key_a 정합) | `accountinterlockhub#45`·`#46` | ✅ |
| 2026-07-08 10:15 | build | `user/consent/{consent.controller,consent.service,consent.module}.ts`·`consent/dto/submit-consent.dto.ts` 확장(USR-P2) — POST /api/consent/:requestKey 동의/거부 처리(FN-008 processDecision BR-201): requestKey UUID 검증(EX-SEC-004)·진입 컨텍스트+configCode 매칭(불일치·만료 EX-DATA-002)·활성 구성 특정. REJECT→처리상태 실패 1건 저장·컨텍스트 폐기·CONSENT_REJECT 감사·200(EXC-BIZ-03). AGREE→PROC-203 전달 내부호출·컨텍스트 폐기(성공·실패 공통). `CONSENT_REJECT` 감사 이벤트 추가. `npm run build` 0건. 3책임 통과(리뷰 PASS·tester 실 DB round-trip 🟢: AGREE/REJECT·재제출·불일치 EX-DATA-002) | `accountinterlockhub#46` | ✅ |
| 2026-07-08 06:02 | build | `apps/backend/src/user/interlock/*`·`user/consent/*`·`user/entry-context/*` 신규(USR-P1) — POST /interlock/entry(회원키 본문 수신·요청키 UUID v4 발급 FN-007·진입 컨텍스트 인메모리 TTL 10분 무저장·요청제한 60/분 IP 미들웨어 FN-014)·GET /api/consent/:requestKey(구성 소속 동의항목만 조회 FN-008)·app.module 등록·EX-BIZ-007/EX-DATA-002 문구 사양 정정·HISTORY_CREATE 감사 추가. `npm run build` 0건. 3책임 통과(리뷰 PASS·요청제한 누수 회귀 수정·tester 실 DB round-trip 🟢, 무저장 경계 검증) | `accountinterlockhub#45` | ✅ |
| 2026-07-07 10:30 | spec | `process_PROC-201.md` 개정 — 지정 구성 진입 시 내부 PROC-403→FN-016 연동이력 생성 흐름 추가(활성 구성 조회에 user_key_param_id·전달 파라미터 정의(ENT-003) 로드·지정 값 완결성 400 EX-BIZ-007·미지정 미기록 BR-203, 단건 INSERT 트랜잭션)·BE B1a·단계 통합 7단계·분기/예외·CRUD(ENT-007 C)·의존(PROC-403 호출) 재편·`spec-process.md` SCR-005 매핑·의존관계 반영 | `accountinterlockhub#30` | 🚧 |
| 2026-07-07 09:30 | spec | `function_FN-016.md` 신설(연동 요청 진입 처리 중 연동이력 생성 — PROC-201 내부 호출)·`spec-functions.md` 카탈로그 `/interlock/entry`(PROC-201) 행 주요 호출 FN 에 FN-016 추가 | `accountinterlockhub#28` | 🚧 |
| 2026-07-07 08:27 | spec | `model_user.md` MDL-201 개정 — 지정 구성의 지정 파라미터 값 필수·비공백 검증(BIZ-004-02) 명시, 지정 값의 연동이력(ENT-007.user_key) 원문 유입 주석(모델 자체는 무저장 전송 전용 유지) | `accountinterlockhub#27` | 🚧 |
| 2026-07-07 06:35 | spec | `service_SVC-004.md` 개정 — F-006 지정 파라미터 값 검증(400 EX-BIZ-007)·F-007 연동이력 1건 생성(진입 시, PROC-403 예약)·BR-203 신설·Happy Path 7단계 재편·관련 IA + BAT-03 | `accountinterlockhub#26` | 🚧 |
| 2026-07-07 06:20 | spec | 진입 시 연동이력 기록 개시 — BIZ-004-01(지정 구성 진입 시 이력 1건 생성: 키값·구성·요청 키값·요청 일시)·BIZ-004-02(지정 파라미터 값 누락·공백 시 진입 거부 400 EX-BIZ-007, 기본안) | `accountinterlockhub#25` | 🚧 |
| 2026-07-07 06:20 | spec ⓒ | (공통 반영) 키값 용어 정의(W3)·DATA-001-01 개정(연동이력 예외) — common.md | `accountinterlockhub#25` | ℹ️ |
| 2026-07-06 17:10 | spec ⓒ | (공통 반영) DB 표기 PostgreSQL 정합화 — common.md | `accountinterlockhub#24` | ℹ️ |
| 2026-07-06 04:05 | spec | 동의 약관 상세/모달 반영 — BIZ-002-05·ENT-002.terms_content·FN-008·SCR-005([상세]+약관모달)·PROC-201·TC USR-01(_018~020)·목업 SCR-005 | `accountinterlockhub#24` | 🚧 |
| 2026-07-06 03:20 | spec | BIZ-002·DATA-002·SVC-004·FN-007/008·SCR-005·PROC-201/202·TC USR-01 | `accountinterlockhub#24` | 🚧 |
| 2026-07-06 03:20 | spec ⓒ | (공통 반영) 무저장·신뢰위임·공통 기능·디자인 시스템·시나리오 TC — common.md | `accountinterlockhub#24` | ℹ️ |
