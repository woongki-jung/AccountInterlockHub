# IA: USR-04 — 동의·승인

> 본 IA 노드와 연관된 작업의 시계열 이력(최신순). 정책: ai/strategies/ia-history.md

| 일시 (KST) | 단계 | 산출물·결과 | 관련 일감 | 상태 |
|---|---|---|---|---|
| 2026-08-02 12:20 | build ⓒ | (공통 반영) 배포 산출물 산출·런타임 게이트(부분 확보 — DB 미가용분 qa 이월)·qa 인계 문서 신규 — common.md | `accountinterlockhub#514` | ℹ️ |
| 2026-07-26 20:57 | build | `apps/backend/src/interlock-approve/consent-approval.service.ts` — P09 표지 검사 시계 fail-open 시정 구현(담당자 승인 1회 예외, spec `5054df1` 대응). `B6` 승인 확정에서 응용 시계로 캡처하던 `const consentedAt = new Date()` 와 `recordConsentProof()` 호출의 `at` 인자를 제거했다 — `consented_at` 은 전적으로 `ENT-002` 컬럼 기본값 `now()`(DB 시계)가 쓴다. 전달 시도 표지 검사(`consented_at >= locked.created_at`) 주석에 두 시각이 같은 DB 시계에서 나오고 `B3` 트랜잭션이 `B6` 보다 먼저 커밋되므로 이 비교가 시계 정합이 아니라 구조로 성립한다는 근거를 추가했다(`process_PROC-103-logic.md` `B6` 127~133행 인용). 짝 변경 `apps/backend/src/records/consent-proof-record.service.ts`(`BAT-05` — `RecordConsentProofInput.at` 제거·INSERT 컬럼 재정렬)는 같은 commit — 상세는 `history/BAT-05.md`. 변경 범위는 이 2파일로 엄격 한정(담당자 지정 1회 예외) — `docs/specs/` 미접촉. | `accountinterlockhub#486` | 🚧 |
| 2026-07-26 19:40 | spec ⓒ | (공통 반영) 🔴 **전달 시도 표지 검사의 조용한 fail-open 제거**(E7-2 — 이 노드의 핵심). `PROC-302`·`FN-012` 가 **승인 확정 시각을 인자로 받지 않도록** 바꿔 `consented_at` 을 `ENT-002` 기본값 `now()`(DB 시계)로 기록하고 `RETURNING` 으로 반환한다 — `created_at` 과 같은 시계가 되어야 `B6` 표지 검사(`consented_at >= locked.created_at`)가 **시계 정합이 아니라 구조로** 성립한다(레코드 생성 갈래는 두 시각 간격이 밀리초라 응용 시계가 조금만 뒤져도 증적 2행·수신처 이중 전달이 재현됐다). `process_PROC-103.md` §구현 가이드에 🔴 항 신설 + **영구 500 귀결 명문화**(W7-1 — 설계된 귀결·보관 기한 삭제·자동 복구/재전달 경로 금지) · `process_PROC-103-logic.md` 표지 검사 주석·`FN-007` 변환 재사용 지시(W7-2) · `process_PROC-302.md` 미호출 경우 4번째 추가(W7-3)·BR-022 술어 통일 · `service_SVC-015.md` **BR-022 정본 술어 통일**(계기 종류 → 계기 종류 + 이번 레코드의 증적 유무)·`service_SVC-003.md` F-004·Happy 5·예외 흐름 반영(E7-6) · `data_ENT-002.md` §구현 가이드에 DB 시계 요구 명문화 — common.md | `accountinterlockhub#486`·`#493`·`#489`·`#469` | ℹ️ |
| 2026-07-26 18:58 | build | **이력 분리 커밋 — 산출물은 `ae54a22`**(`apps/backend/src/interlock-approve/{consent-approval.service.ts,approve.errors.ts}` 2파일, `git show --name-only ae54a22` 실측). 바로 아래 `18:51` row(`PROC-103 B6 전달 시도 표지 검사 신설` 상세 서술)가 **바로 이 작업의 이력 entry** 다 — 착수 당시 그 row 를 이 파일 최상단에 삽입했으나, 같은 파일을 동시 편집 중이던 spec 레그가 자신의 `53b5ce0` 커밋에 그 미커밋 삽입을 함께 흡수해(`git show 53b5ce0:history/USR-04.md` 로 내용 무손실 확인 — 삭제·수정 없이 그대로 존재) **산출물 commit(`ae54a22`)과 이력이 서로 다른 commit 에 놓였다.** 본 row 는 커밋 게이트("IA 이력 entry 를 산출물과 같은 commit 에")를 산출물 commit 이후 시점에서나마 충족시키려는 **연결 전용 entry** 이며, 위 `18:51` row 의 상세 내용을 재기술하지 않는다(append-only·중복 방지 — 상세는 그 row 참고). 동시 승인 2건 재현(`Promise.all`·증적 1행·전달 1회)·보관 주기 시나리오 실측(추적 레코드 삭제 후 재수신 정상 승인) 등 전체 실측 근거도 `18:51` row 에 있다. | `accountinterlockhub#486`·`#469` | 🚧 |
| 2026-07-26 18:51 | build | `apps/backend/src/interlock-approve/{consent-approval.service.ts,approve.errors.ts}` — **P09(`#486`) 회귀 3회차: `PROC-103` `B6` 전달 시도 표지 검사 신설**(spec 이 사양 결함 5·9 를 닫은 `964e8d0` 재착수 — 결함은 사양 미비였고 build 구현은 의사코드 그대로였다). **[변경 요지]** `B6` 의 행 잠금 조회를 `tracking_key` 단일 컬럼에서 **6컬럼 전체**(`SELECT … FOR UPDATE`)로 넓히고, `toInterlockTrackingModel()`(B3·PROC-301 과 같은 변환)로 `MDL-001` 을 구성한 뒤 `PROC-302` INSERT **이전**에 `tbl_consent_proof` 전달 시도 표지를 확인한다 — `WHERE tracking_key = $1 AND consented_at >= $2`(`$2` = 방금 잠근 행의 `created_at`). 표지가 있으면 INSERT·`B7`(전달) 모두 건너뛰고, `locked.resultCode`로 갈라 **확정**(`!== null`)이면 `B4` 와 완전히 같은 호출(`resultInfoBuilder.build({source:'RECORD', resultCode, isReAnnouncement:true})`)로 200 재안내, **미확정**(`=== null`)이면 신설 `DeliveryInProgressError`(`EX-BIZ-003`/500)를 던진다 — 새 `EX`·`BR`·`SCR` 코드 **0**(둘 다 기존 `EX-BIZ-003` 카탈로그 엔트리 재사용, 카탈로그 메시지 그대로 `super()` 에 박아 `ConsentValidationError`·`DeliveryFailedError` 관례를 그대로 따랐다). **트랜잭션 COMMIT 순서를 의사코드 그대로 맞췄다** — `already>0` 판정 자체는 예외를 던지지 않고 `{kind:'ALREADY_ATTEMPTED', locked}` 를 정상 반환해 `withTransaction()` 이 COMMIT 하게 하고, 200/500 분기는 그 **COMMIT 이후**(트랜잭션 밖)에서 수행한다 — 의사코드의 `COMMIT; … if (…) return 200 … else return 500` 순서와 문자 그대로 대응시켰다(대안 — 미확정 분기를 예외로 롤백시켜도 그 트랜잭션엔 애초에 쓰기가 없어 DB 관측 결과는 같지만, 의사코드가 명시한 순서와 다르게 구현할 이유가 없었다). **`LockTargetMissingError` 방어적 분기 신설** — 잠금 대상 행 부재(이론상 도달 불가, `B3`(PROC-301 SECURE)가 같은 요청 안에서 이미 그 행을 확보했고 보관 삭제는 월 단위 기산이라 같은 요청 처리 중 사라질 수 없다) 시에도 `toInterlockTrackingModel(undefined)` 의 미분류 TypeError(→`EX-OPS-002`) 대신 분류된 `EX-BIZ-003` 으로 떨어지게 했다 — `tracking-record.service.ts` 의 `SECURE_RACE_LOOKUP_EMPTY`·`FIX_RESULT_TARGET_MISSING` 과 같은 "이론상 도달 불가 — 방어적으로 실패 처리" 관례. **[설계 선택 — 소유 경계 유지]** 잠금 조회·표지 조회 둘 다 `records/**`(P04 `#481`)에 새 메서드를 얹지 않고 `consent-approval.service.ts` 자신의 인라인 SQL로 뒀다 — 기존 코드가 이미 `tbl_interlock_tracking` FOR UPDATE 를 그렇게 인라인으로 열던 전례를 그대로 넓혔을 뿐이고, `records/**`·`docs/**` 는 이 착수 시점에 다른 doer(spec 레그·타 build lane)가 같은 워킹트리에서 동시에 수정 중이었다(`git status` 실측 — `retention-scheduler.service.ts`·`docs/specs/qa/**` 10파일 타인 변경분 확인, 무접촉) — 좁은 pathspec 커밋 리스크를 원천적으로 줄였다. **[사양 문면 1:1 대조]** `locked = SELECT … FOR UPDATE`→행 잠금 쿼리(6컬럼) · `already = SELECT COUNT(*)…`→마커 쿼리+`Number()` 캐스팅(pg 는 `COUNT(*)` 를 문자열로 반환 — `pg-type-parsers.ts` 관례 그대로) · `if(already>0)`→`existingProofCount>0` · `COMMIT`→정상 반환으로 위임 · `gate.payload 를 폐기한다`→B4 분기와 동일하게 지역 스코프 이탈에 위임(명시적 폐기 코드 없음, 기존 관례) · `if(locked.result_code IS NOT NULL)…return 200`→`resultCode!==null` 분기 · `else return 500`→`DeliveryInProgressError` · `proof=PROC-302(…)`→`recordConsentProof()` 호출 **무변경** · 최종 `COMMIT`/`실패→500`→`withTransaction()` 위임. **미구현 단계 없음.** **[동시 승인 2건 실측 — 이번 회귀의 존재 이유]** 격리 DB(`p09_dev_486`, 임시 롤, 종료 후 DROP)+로컬 스텁 수신처(포트 3426, `<RECEIVER_DELIVERY_URL>` 상수 주입 — 실 도메인은 RFC 2606 예약이라 상시 실패)+포트 3416 백엔드로 자체 벡터 생성기(Node 내장 crypto, policy_SEC-crypto.md 그대로) 기반 실 HTTP 왕복 검증. ① 같은 `encX`/`encY`/`birthDate`를 `Promise.all` 로 완전 동시 2회 발신 — 매 회 증적 **정확히 1행**·스텁 수신 **정확히 1회**, 응답은 한쪽 200(`isReAnnouncement:false`)·다른 쪽 500 `EX-BIZ-003`(메시지 카탈로그와 바이트 일치)로 수렴(재현 2회, `TK-RACE-001`·`TK-RETRY-001`). ② 지연 스텁(1.2~2초)으로 미확정 창을 넓혀 같은 키에 **61건**(승인 1+probe 60, 25ms 간격) 을 흘린 스트레스 테스트에서도 증적 **1행**·전달 **1회** 불변 — dist 전용 임시 계측(소스 미변경, 검증 직후 `npm --prefix apps/backend run build` 로 clean rebuild 확인)으로 `ALREADY_ATTEMPTED` 두 하위분기가 각각 정확히 겨냥한 만큼 실제로 실행됨을 직접 확인(미확정→500 분기 49회, **확정→200 재안내 분기 정확히 1회** — 나머지 10회의 200 재안내는 `B4` 단축 경로였다). ③ 500 수신 후 즉시 재제출 → 200 재안내로 안전 수렴(증적·전달 추가 없음, `TK-RETRY-001`) — "재시도는 안전하다" 실증. **[보관 주기 시나리오 실측 — 사양 핵심 의도]** `TK-RETENTION-001`: 200일 전 `created_at`/`consented_at` 의 추적 레코드+증적을 직접 INSERT 후 **추적 레코드만 DELETE**(PROC-304 시뮬레이션 · 증적은 `DATA-002-02` 대로 생존) → 같은 트래킹키로 **새 승인**(새 `created_at`) 제출 시 정상 승인(200, `isReAnnouncement:false`, 신규 증적 1건 추가로 총 2건 공존 확인 — `EXC-BIZ-04` 실증) · 신규 전달 정확히 1회. **대조 실측** — 같은 사전 상태에서 `tracking_key` 만으로 센 나이브 카운트는 **1**(>0) 이었다 — 범위 한정이 없었다면 이 정상 재수신이 잘못 차단됐을 것임을 직접 수치로 확인. **[인접 회귀 확인]** `EX-BIZ-001`(필수 동의 미충족, `B5b`) 무변경 재확인(400, 증적 0). `B7`·`B8`(`EX-BIZ-002`/성공 응답 구성) 코드 **무변경**이라 별도 재검증 생략(기본 승인 흐름 테스트가 그 경로를 이미 통과). **[환경 발견 — 별도 기록 가치]** 이 Git Bash 에서 `TZ='Asia/Seoul' date` 는 tzdata 부재로 **조용히 UTC 를 반환**한다(실측 — `+0000` 오프셋) — 이 PC의 OS 시간대 자체가 이미 KST(Windows `TimeZoneInfo.Local`="Korea Standard Time" 확인)라 **TZ 오버라이드 없는 plain `date`** 가 정확하다. 본 entry 의 KST 시각도 이 방법으로 재확인해 채웠다(다음 build doer 를 위해 기록 — `TZ=Asia/Seoul` 신뢰 금지, plain `date` 사용). `npm --prefix apps/backend run build` 에러 0건(수정 직후 1회 · 계측 제거 후 clean rebuild 재확인 1회). 종료 시 서버·스텁·임시 DB(`p09_dev_486`)·롤(`p09_role_486`)·워크트리 전부 잔존 0 실측(포트 3416·3426 LISTENING 0건, `pg_database` 목록에 `accountinterlockhub` 만 남음, `git worktree list` 에 본 작업 워크트리 없음). 작성만 수행 — 코드리뷰·기능검증은 별도 doer 대기 | `accountinterlockhub#486`·`#469` | 🚧 |
| 2026-07-26 18:48 | spec | `docs/specs/qa/USR/tc_USR-04.md` — 3건. ① 🔴 **`QA-OPEN-02` 해소가 약속한 관측 신설**(`a8058a0`) — 해소는 됐는데 검증 TC 가 부재해 **신설 규칙("필드에 매인 안내가 겹치면 그 요소가 가져간다")이 검증되지 않는 상태**였다. `USR-04_011` ③④ 에 "포커스가 **제목이 아니라 필드**로 간다"·"제목에 링 픽셀 0"·값 유지/전체 선택 관측을, **충돌 지점 2번째**인 `USR-04_003` ④(`SCR-003` → `SCR-002` `Blocked`)에 같은 규칙의 관측을 세웠다(특례가 아니라 **일반 규칙**임이 이 두 번째 관측의 요지). 둘 다 **`CHK-FOCUSEMU`** 를 전제로 걸었다 ② `USR-04_014` — ② 가 `tbl_consent_proof` **행 수만** 보던 것에 **"수신처 전달 호출이 정확히 1회"** 단언을 더했다(**실질 피해는 증적 2행이 아니라 수신처 이중 전달**이고, 증적만 1행으로 접고 전달은 2회 나가는 구현이 성립할 수 있어 두 관측이 서로를 대체하지 못한다). ③ 에 **500 `EX-BIZ-003` 도 정상 귀결**임을 추가했다 — 진짜 동시 요청에서는 전달(`B7`)·결과 확정(PROC-104 `B6`) 순서상 뒤선 요청이 표지 검사를 통과할 때 `result_code` 가 아직 NULL 이라 **이쪽이 보통**이며 결함으로 올리지 않는다 ③ 🔴 **`USR-04_018` 신설**(EXC-BIZ-04 오탐 방어) — 앞선 보관 주기의 증적이 남은 키로 **재수신된 정상 연동이 승인되는지**. 표지 검사의 범위 한정(`tracking_key` + `consented_at >= 잠근 레코드의 created_at`)이 누락되면 **정상 연동이 영구히 500** 이 되는데 `_014` 로는 절대 잡히지 않는다(두 TC 는 같은 규칙의 **반대 방향 경계**). 신규 시드 `SEED-PROOF-STALE`. TC 16→17 | `accountinterlockhub#469` | 🚧 |
| 2026-07-26 18:48 | spec ⓒ | (공통 반영) `docs/specs/qa/` 카탈로그 3종 정합 — `QA-OPEN-02` **해소 표기 + 본문 오기 정정**(충돌 지점 2건) · **`CHK-FOCUSEMU`** 신설(이 노드의 `USR-04_004`·`_011`·`_003` 관측이 전부 적용 대상) · `CHK-PGKILL`·`SEED-PROOF-STALE` 신설 · `SCEN_024`~`_026` 신설 · 집계 전면 재실측(TC 242→247·정책 17→18건·규칙 88→93). 이 노드분은 `SVC-003` 16→17 · `BR-002` 42→45·`BR-022` 14→15 · `EX-BIZ-003` 9→11 · **`ENT-002` R 매핑 정정**(`964e8d0` 으로 `tracking_key` 조회 PROC 가 생겨 "조회 PROC 부재"가 거짓이 됐다 — `PROC-103` `B6` 표지 검사) · SCR-001/SCR-002 UI 검증 항목에 포커스 도착지 관측 추가로 반영 — common.md | `accountinterlockhub#469` | ℹ️ |
| 2026-07-26 18:45 | spec ⓒ | (공통 반영) `f4e515b`·`964e8d0` 하류 정합 — ① `OPS-004` 를 `process_PROC-103.md` §관련 사양 코드 정책 행에 반영(`OPS-004-01` `B3`·`B6` 질의 실행 · `OPS-004-03` `B3`·`B6` 트랜잭션 경계 · `OPS-004-04` 로그) ② **`EX-BIZ-003` 전역 성질 정정** — `spec-policies.md` EX 카탈로그가 "결과 미확정·재시도 가능"만 적어 **전달이 이미 수행됐을 수 있다**는 사실이 빠져 있던 것을 발생 지점별(전달 이전 `B3`·`B6` / 전달 이후 PROC-104 `B6`)로 명시하고 재제출 안전 근거를 `B6` 전달 시도 표지 검사로 지시 ③ `spec-process.md` §트랜잭션 경계·멱등성 요약의 PROC-103 멱등 수단을 **행 잠금 + 기존 증적 확인**으로 정정하고 "재시도는 재전달로 수렴한다"는 거짓 서술 제거 — common.md | `accountinterlockhub#489`·`#494`·`#469` | ℹ️ |
| 2026-07-26 18:37 | spec | `docs/specs/screens/screen_SCR-002.md` — 직전 `964e8d0`(`EX-BIZ-003` 뜻 정정)의 화면측 정합. §화면 상태 전이 `Retryable`(500 `EX-BIZ-003`) 행의 PROC 매핑이 **`PROC-302 동의 증적 기록` 단독**이라 발생 지점 절반이 빠져 있던 것을 채웠다(`PROC-103` `B3` · `PROC-103` `B6` 증적 기록·전달 시도 표지 검사 · **`PROC-104` `B6` 결과 확정 — 전달 이후**). 불릿 1개 신설 — **`Retryable` 을 "전달 미수행"으로 읽지 않는다**(발생 지점에 따라 전달 수행 여부가 갈린다), 그럼에도 **재제출이 수신처에 두 번 전달되지 않는 근거**는 `PROC-103` `B6` 의 **전달 시도 표지 검사**(증적이 전달 **직전**에 커밋되므로 그 존재가 곧 전달 시도 표지 — `BIZ-003-04`)다. **동작 무변경** — 버튼 재활성·재제출 허용 그대로이고, 화면은 재제출을 스스로 막지 않는다(중복 방지는 서버 판정). | `accountinterlockhub#493`·`#469` | 🚧 |
| 2026-07-26 18:37 | spec ⓒ | (공통 반영) 디자인 시스템 §접근성 기준 — **진입(최초 로드) 제목 자동 포커스 금지** 일반 규칙 신설(`design-system.md`·`design-system-components.md`). 이 노드에 닿는 것은 **`SCR-002`** 이며, 이 화면은 진입 응답이 산출할 수 없는 화면이라(본인확인 응답 전용) **규칙이 닿을 자리가 없고 배선하지 않아도 구현 갭이 아니다**. 본문·근거는 common.md | `accountinterlockhub#493`·`#469` | ℹ️ |
| 2026-07-26 18:16 | spec | `docs/specs/processes/process_PROC-103.md`·`process_PROC-103-logic.md`·`docs/specs/functions/spec-functions-api-user.md`·`docs/specs/policies/policy_BIZ.md`(`BIZ-003-04`) — build P09(`#486`)가 상신한 **사양 결함 5·9 회귀**(뿌리가 하나라 함께 닫음). **결함 5 — `B6` 존재 검사 부재**(build tester 가 증적 2행 + 수신처 이중 전달을 실측 재현): `-logic.md` `B6` 의사코드가 `SELECT … FOR UPDATE` 뒤 곧장 `PROC-302` INSERT 로 가 **기존 증적을 확인하지 않았다** — 잠금은 동시 요청의 *순서만* 세우고 뒤선 요청을 멈추지 않으며, `result_code` 는 전달 **이후**(PROC-104 `B6`)에야 확정돼 `B4` 재안내로도 걸러지지 않았다. 같은 잠금 안에 **전달 시도 표지 검사**를 넣어 닫았다(증적이 전달 **직전**에 커밋되므로 그 존재가 곧 표지) — 증적 있으면 INSERT·전달 없이, 확정이면 `B4` 와 **완전히 동일한 귀결**(`source:'RECORD'`+`isReAnnouncement:true`, 경로 ①·③), 미확정이면 500 `EX-BIZ-003`. 새 EX 코드 0. **🔴 조회 범위를 `consented_at >= 잠근 레코드의 created_at` 으로 한정한 것이 이 설계의 핵심** — `tracking_key` 만으로 세면 앞선 보관 주기의 증적(EXC-BIZ-04 · 증적이 추적 레코드보다 오래 남는다)에 걸려 **삭제 후 재수신된 정상 연동이 영구히 승인되지 않는다**(매 제출이 500 으로 끝난다). 한 레코드의 생애에 승인 주기는 하나뿐이라 이 조건이 "이번 레코드에 속한 증적"과 정확히 같다. **대안 ⓑ(`tbl_consent_proof(tracking_key)` 유일 제약)는 채택하지 않았다 — 사유는 재작업 비용이 아니라 성립 불가**: 같은 키의 증적이 보관 주기를 넘어 공존하는 것이 정상이라 제약을 걸면 `DATA-002-02`(증적 존속)가 깨진다(ENT-002 §관계 정의 · `spec-datas.md` §엔터티 관계 요약 2). 다시 열리지 않도록 `PROC-103` §구현 가이드에 근거를 명문화했다. **결함 9 — `EX-BIZ-003` 의 거짓 단정**: 접점 계약(`spec-functions-api-user.md`)과 `PROC-103` §구현 가이드가 *"전달 미수행"*·*"발생 지점이 전달 이전으로 특정돼 결과 미확정이 보장된다"*고 적었으나, 같은 사양이 PROC-104 `B6`(전달 **후** 결과 확정 COMMIT 실패)에 **같은 코드**를 배정해 자기모순이었다. **코드는 유지**하고(분류 가능한 실패를 `EX-OPS-002` 로 뭉뚱그리지 않는다 — `OPS-003-05`) 뜻을 **"결과 미확정"**으로 정정, 전달 수행 여부는 발생 지점에 따라 갈린다는 표를 신설했으며, **재제출 안전성의 근거를 `B6` 표지 검사로 이전**했다. `F5` catch-all 의 `EX-OPS-002` 목적지(전이 없이 `SCR-003` `Unconfirmed`·결과 패널 미생성)는 **무변경**이되 그 근거를 "재시도 기구가 위험해서"에서 **"문구가 참이 아니게 되기 때문"**(분류되지 않은 실패라 '결과 미확정'을 단정할 수 없다)으로 정정했다. `BIZ-003-04` 에 "살아 있는 레코드 1건에 증적 1건" 불변식과 **"행 잠금만 걸고 기존 증적을 확인하지 않는 사양·구현은 반려"**를 추가. 결과 경로 3종·거부 비경로·증적↔전달 순서·`B6` 이 경계를 여는 규칙은 무변경. | `accountinterlockhub#486`·`#469` | 🚧 |
| 2026-07-26 16:08 | build | `apps/frontend/src/flow/ConsentScreen.tsx`+`apps/frontend/src/hooks/useStageFocus.ts` — P15(`#492`) **회귀 1회차**([I-1] 🔴 시정, 독립 code-reviewer(opus) 재재판정 대응). **직전 09:18 row 의 X-1 시정이 예외의 결과절만 구현하고 전제절을 구현하지 않았던 결함** — `a8058a0` 의 예외는 조건문이다("입력 요소에 매인 InlineAlert(첫 미충족 항목 안내)가 함께 뜨면 그 안내가 매인 요소로 포커스를 옮긴다"), 전제 = 매인 요소 존재 / 결과 = 그 요소로 이동인데, `ConsentScreen.tsx:61` 은 `view.alert?.kind === 'blocked'` 만으로 전제 성립을 단정해 결과절만 구현했었다. **실도달 경로 전수 확인** — `Blocked` 도착은 `handleApproveClick` 의 로컬 게이트(`allRequiredMet`)를 통과해야만 서버 응답을 받고, `BackToConsent` 복귀가 `agreedCodes` 를 비우지 않으며(회귀 2회차 I-3) `lastConsent` 도 같은 항목 집합을 그대로 쓰므로, **실제로 도달 가능한 모든 경우에 미충족 항목이 이미 없다** — `ConsentList.tsx` 의 `focusFirstUnmet()` 이 대상을 찾지 못해 무동작이었고 포커스가 사양이 규정한 두 도착지(제목·필드) 어느 쪽도 아닌 `BODY` 로 떨어졌었다(ARIA 상으로도 전제 불성립 — SCR-002 의 InlineAlert 는 어떤 요소에도 `aria-describedby` 로 결속돼 있지 않다). **시정** — 이미 `:48` 에서 계산돼 있던 `allRequiredMet` 을 조건에 더해 `skipTitleFocus = view.alert?.kind === 'blocked' && !allRequiredMet` 로 전제 성립 여부를 직접 확인(새 연산 신설 없음) — 전제가 거짓이면(= 현재 도달 가능한 전 경로) 예외를 적용하지 않고 기본 규칙(제목 포커스)으로 되돌린다. `:54-59` 주석의 "위 useEffect 가 그 항목으로 옮긴다"(도달 가능 경로에서 사실이 아니었다)도 실제 근거로 정정. **`IdentityScreen.tsx` 는 무접촉** — `format`·`mismatch` 는 `TextField` 가 항상 렌더돼 받아 갈 요소가 보장되고, `alert===null` 은 "아무도 받지 않는 것"이 사양 요구 자체라는 리뷰 판정 그대로다. **[S-1] 자체보완(저비용 병행 처리)** — `hooks/useStageFocus.ts` 의 `skipFocusRef`(렌더 본문 ref 쓰기, React 공식 지침이 금지하는 concurrent 비호환 패턴)를 제거하고 effect 안에서 `skipFocus` 를 직접 읽음 — deps 가 `[title]` 이라 effect 클로저가 이미 그 렌더의 값을 캡처해 ref 경유가 불필요했고, 오히려 ref 쪽이 "전환 시점이 아닌 최신값"을 읽어 덜 정확했다(기능 무변화, `eslint-disable-next-line react-hooks/exhaustive-deps` 사유 주석은 유지). **실측**(CDP, 격리 프로필 헤드리스 Chrome 포트 3392/9392 — 이번 회귀 전용 신규 프로필, `apps/frontend/dist/` 정적 서빙 + fetch 모킹, 백엔드 미기동): `backToConsent_blocked_active_element` **시정 전(직전 09:18 row 실측 · `works/accountinterlockhub-492/after-fix-result.json`) `BODY` → 시정 후 `H1`**(`h1Text:"연동 동의"`, 사양 기본 규칙대로 제목 포커스 복귀) — 같은 시나리오(체크박스 상태 `[true]` 무변화)로 대조 확인. **회귀 없음 재확인** — `wrapMarginBottom:24px`·`subtitleMarginTop:4px`(진입·승인 전·Blocked 도착 3지점 전부 동일) · 포커스 링 `outline 2px solid rgb(11,79,168) offset 2px`(명시적 `.focus()`, 진입·Blocked 도착 2지점) · 승인 버튼 `ariaDisabled:"true"`+`nativeDisabled:false`+미충족 클릭 시 Gated 알림 노출로 핸들러 도달 확인(`mockCallLog` 에 `/approve` 미기록 — 로컬 게이트가 네트워크 호출 자체를 막는 것이라 회귀 아님) · `SCR-003`(`Unconfirmed`)·`SCR-004`(결과) 도착 제목 포커스 `H1` 유지 · `EX-BIZ-003`(Retryable) 은 이번 회귀의 조건식 미접촉이라 시정 대상이 아니다. `npm --prefix apps/frontend run build` 에러 0건. 작성만 수행 — 코드리뷰·기능검증은 별도 doer 대기 | `accountinterlockhub#492`·`#469` | 🚧 |
| 2026-07-26 15:12 | build | `apps/backend/src/interlock-approve/{approve.controller.ts,approve.module.ts,consent-approval.service.ts,approve-request.dto.ts,approve.errors.ts,delivery.constants.ts,interlock-delivery.service.ts,index.ts}` + `apps/backend/src/models/consent-submission.model.ts`(MDL-007 정식 모델 신설 — `records/consent-proof-record.types.ts` 의 임시 로컬 타입은 별칭으로 정리) + `apps/backend/src/app.module.ts` — **PROC-103 동의·승인 제출 `POST <INTERLOCK_ENTRY_PATH>/approve` 최초 구현**(P09, `#486`). `B1`(`agreedItemCodes` 형상 재검증)~`B8`(결과 안내)을 `ConsentApprovalService` 로 오케스트레이션하고, 복호화·전달은 `InterlockDeliveryService`(PROC-104, USR-02.md 항목)에 위임한다. 트랜잭션 3분리(`B3` SECURE·`B6` 증적 기록+행 잠금·PROC-104 내부 `B6` FIX_RESULT) 전부 `DatabaseService.withTransaction()` 으로 경계를 열고 그 `exec` 를 하위에 그대로 넘긴다 — **`PROC-301`(`TrackingRecordProcessService.record`)은 `exec` 를 단일 객체 안에, `PROC-302`(`ConsentProofRecordService.recordConsentProof`)는 `executor` 를 별도 첫 인자로 받는 서로 다른 시그니처**임을 각 정의 파일 실측으로 확인해 그대로 맞춰 호출했다(인계 노트가 경고한 함정). `B5b` 서버 재검증(미지의 코드 → 필수 미충족 순서, `process_PROC-103-logic.md` 그대로)을 `PROC-302` 호출 **이전**에 둬, `PROC-302` 자신의 동일 검증(`FN-012` §처리 흐름 1·2, "마지막 방어" 500 `EX-BIZ-003`)이 정상 트래픽에서 도달하지 않게 했다. **거부(`decision`/`REJECT`) 인터페이스 미도입** — 승인 성립 조건은 `agreedItemCodes` 필수 충족 하나뿐이다. **본문 JSON 파싱 실패 분류는 착수 시점에 이미 해소돼 있었다**(P05 회귀 2·3회차, commit `5a47542`·`b414a25` — `body-parse-failure.ts` `USER_SUBMIT_EX_BY_SUFFIX` 가 `/approve` → `EX-BIZ-001` 을 이미 매핑) — 이 Phase 는 추가 조치 없이 실측 확인만 하고 그대로 재사용했다(인계 노트 원문이 가리킨 상태보다 진행돼 있었다 — 완료 보고 참고). `EX-BIZ-002`(502)는 결과가 이미 확정된 뒤(PROC-104 `B6` 커밋 후)에만 던져 정상 종료를 화면에 옮긴다. 빌드 검증 `npm run build` 에러 0건. 작성만 수행 — 코드리뷰·기능검증은 별도 doer 대기 | `accountinterlockhub#486`·`#469` | 🚧 |
| 2026-07-26 13:52 | spec | `docs/specs/processes/process_PROC-103.md` §구현 가이드 **+1줄** · `process_PROC-103-logic.md` `B6` 의사코드 · `docs/specs/functions/function_FN-012-013.md` `FN-012` §구현 가이드 **+1줄** — **실행 문맥(`exec`) 전달 경로를 호출 지점에 표기**(`1491e4f`·`b4fe224` 소유권 확정의 마지막 결손 보완). **이력·산출물 동일 commit**(작성 시점 미커밋 — 8파일 일괄). **[① 요구만 있고 경로가 없던 상태]** 앞선 두 commit 이 **트랜잭션 소유권을 호출측으로** 확정하고 `PROC-301`~`PROC-303`·`FN-007`~`FN-013` 을 **참여 전용**(열지도 닫지도 않는다)으로 못박았는데, **참여가 성립하려면 호출측이 같은 커넥션·실행자를 넘겨야 한다는 전달 경로가 호출 지점에 없었다** — 넘기지 않으면 하위가 경계 **밖**의 별도 커넥션을 잡아 같은 경계의 변경이 보이지 않고 **요청 하나가 커넥션을 둘 점유**한다. 가정이 아니라 **build 에서 커넥션 풀 교착으로 실현된 결함**이라 사양 쪽 결손을 닫았다. 착수 선행 조건이던 **끊어진 단계 좌표 전수 대조가 직전 `e39dd60` 로 끝나** 어긋난 좌표를 굳히지 않는 시점에 손댔다. **[② 분할을 실제로 나눠 쓴 첫 사례]** 12:40 분할(부모 133줄 + 자식 174줄) 이후 처음 내용을 고친 회차라 **부모·자식을 성격대로 갈랐다** — **의사코드는 자식**(`-logic.md` `B6`: `BEGIN;` 에 "경계를 여는 자리는 여기다(`B3` 와 별개의 경계)" · `PROC-302({ …, exec })` 인자 · "이 전달이 참여의 성립 조건이다 — 행 잠금을 건 커넥션과 같아야 직렬화가 성립한다" 주석 2줄), **판단·제약은 부모**(§구현 가이드에 "트랜잭션 경계를 여는 자리가 이 프로세스다" bullet 신설). 자식 `B3` 은 `b4fe224` 가 이미 `exec` 를 달아 둔 자리라 **무접촉**이고 이번에 더한 것은 `B6` 하나다(자식 176줄·부모 134줄 — 둘 다 300줄 여유 유지). **[③ 부모 bullet 이 못박은 것]** 경계를 여는 두 지점은 `B3`(레코드 확보)·`B6`(증적 기록)이며, 넘기지 않으면 `B3` 은 레코드 기록과 요청 수 계수가 **각각 커밋**되고(`SVC-014` F-008 원자성 파괴) `B6` 은 `FOR UPDATE` 를 건 커넥션과 갈려 **직렬화가 성립하지 않는다**(`BIZ-003-04`). **`B7` 의 결과 확정 경계만 예외로 `PROC-104` `B6` 이 스스로 연다** — 전달 호출을 트랜잭션 안에 넣지 않으려 구간이 나뉘어 있기 때문이다(USR-02.md 항목). **[④ `FN-012` 은 경로를 한 줄로 이었다]** `PROC-103` `B6` → `PROC-302` → `FN-012` 로 **받은 `exec` 를 그대로** 넘기며, 중간 어느 지점에서든 새 커넥션을 얻으면 `B6` 의 행 잠금과 갈려 동시 승인 직렬화가 무너진다. `PROC-302` 입력표·실행 제약 반영과 **P06 구현 무영향 판정**은 BAT-05.md 항목, **넘기지 않는 것이 옳은 자리**(`UNIDENTIFIED_FAILURE`)의 사양 본체는 BAT-06.md 항목 참고. **의사코드 로직·단계 라벨 집합·화면 사양·TC 는 무접촉**(TC 신설 0 · 이 노드 16건 그대로) | `accountinterlockhub#475`·`#473`·`#469` | 🚧 |
| 2026-07-26 13:35 | spec ⓒ | (공통 반영) 디자인 시스템 §접근성 기준 **포커스 우선순위 확정(`QA-OPEN-02` 해소)** + 목업 4종 포커스 링 미충족 시정 — 이 노드에 닿는 것은 **`SCR-002`**(동의·승인)이며, **예외가 적용되는 두 번째 전환이 이 화면 도착이다**. **[① `Blocked` 도착은 제목이 아니라 첫 미충족 항목]** `BackToConsent` 중 400 `EX-BIZ-001`(SCR-003 → SCR-002) 도착이 `screen_SCR-002.md` §화면 상태 전이 `Blocked` 의 **"첫 미충족 항목으로 포커스"** 라, 단계가 바뀌어도 제목이 포커스를 가져가지 않는다 — **`QA-OPEN-02` 가 지목한 `BackToIdentity` 한 자리만 닫지 않고 같은 공백이 남아 있던 이 자리까지 함께 규정**했다. 반대로 **정상 전환**(SCR-001 제출이 `stage = CONSENT` 를 돌려주는 자리)은 필드에 매인 안내가 없어 종전 그대로 제목으로 포커스가 가며, 직전 `e39dd60` 이 `USR-04_004` 에 세운 관측 ⑥⑦⑧ 은 **기대 결과 그대로 유효**하다. 화면 사양은 **무접촉**(도착 상태가 이미 정해 둔 도착지를 정본이 추인). **⚠️ 잔여** — `QA-OPEN-02` 가 이 노드를 영향으로 지목하며 예고한 **`USR-04_011` ③ 관측 추가**와 카탈로그 행의 해소 상태 갱신은 **이번 회차 diff 에 없다**(검증 도메인 후속 · 미해소 집계 2건 표기 그대로). **[② 목업]** `mockup/SCR-002.html` 에 `h1.stage-title:focus` **1줄**(+주석 2줄)을 더해 제목이 포커스를 받는 동안 사양의 링(바탕색 2px 안쪽 여백 + 2px `--color-focus` `#0B4FA8`)이 실제로 서게 했다 — 그 전까지는 브라우저 기본 1px 링으로 떨어져 **정본이 Fail 로 규정한 케이스**였다. 동의 목록·승인 버튼 상태 표현은 **무접촉**이고 정상 상태 시각도 불변이다(마크업 0줄·새 토큰 0). 이 노드 TC **16건 무접촉** · 총 242건·결번 2건(`USR-04_008` 포함) 불변. 규칙 본문·근거 셋·검증은 common.md 항목, `QA-OPEN-02` 본체는 USR-03.md 항목 참고 — common.md | `accountinterlockhub#474`·`#476`·`#469`·`#467` | ℹ️ |
| 2026-07-26 13:15 | spec | `docs/specs/qa/USR/tc_USR-04.md` `USR-04_004` **확장**(번호 무변경 · 제목에 **"단계 제목 포커스 표시"** 추가) + `docs/specs/qa/spec-qa-mapping.md` `SCR-002` 행 + `spec-qa.md` `QA-OPEN-02` 영향 IA 지목. **이력·산출물 동일 commit**(작성 시점 미커밋). **[`SCR-002` 몫]** 직전 `513aca6` 이 세운 **제목의 프로그램 포커스 표시**를 보는 TC 가 전 도메인 0건이던 공백을 이 노드에서 닫았다(공백의 성격·Fail 판별 기준은 USR-02.md 항목). 단계 ⑥⑦⑧ 을 더했다 — ⑥ 본인확인 제출이 `stage = CONSENT` 를 돌려줘 카드가 `SCR-002` 로 **교체되는 순간** `document.activeElement` 가 새 `<h1>`("연동 동의" · `StageTitle` 기본 변형) ⑦ 그 제목에 **바탕색 2px → `--color-focus` `#0B4FA8` 2px** 띠가 **렌더 픽셀에** 보인다(기본 링 1px·여백 0 도, 아무 표시 없음도 **Fail**) ⑧ `tabindex="-1"` 이라 탭 순서 밖이며 **⑥ 직후 첫 `Tab` 은 제목을 건너뛰어 첫 조작 요소인 동의 체크박스로 간다**(§레이아웃 구성 1→3). **[전제를 함께 고쳤다]** 상태를 직접 주입해 `SCR-002` 를 그리면 **전환 시점 자체가 사라져** ⑥~⑧ 이 관측 불가라, §전제에 **"화면에서 실제로 제출해 전환이 일어나게 한다"** 를 못박았다 — 이 TC 가 여기서 다른 UI TC 와 갈린다. §화면 검증란에도 제목이 그 탭 순서에 없음을 명시했다. **[확장으로만 닫았다]** 신설 **0** · 재채번 **0** · 총 **242건**·결번 2건(이 파일 `USR-04_008` 포함) **불변** · 이 노드 TC 16건 그대로. ①~⑤(승인 버튼 `aria-disabled` 전이·`필수` 배지·거부 수단 부재)는 **무접촉**. **[`QA-OPEN-02` 영향]** 같은 회차에 등재된 `QA-OPEN-02` 가 이 노드를 영향 범위로 지목한다 — `SCR-001` 로 되돌아가는 `BackToIdentity` 전환의 포커스 우선순위가 정본에 없어 보류했고, 확정되면 **이 파일 `USR-04_011` ③**(카드가 `SCR-001` 로 교체되는 자리)에 같은 관측을 더한다(본문은 USR-03.md 항목). 사양 문서·`mockup/`·`apps/` 무접촉 | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-26 12:40 | spec | `docs/specs/processes/process_PROC-103.md` **299 → 133줄** + `process_PROC-103-logic.md` **174줄 신규**(git 미추적 — 같은 commit 에 함께 들어간다) — **300줄 제한 대응 순수 이동 분할**. **이력·산출물 동일 commit**(작성 시점 미커밋). **[착수 게이트]** 여유(300 − 실측)가 **1줄**뿐이라 다음 회차의 한 줄 추가도 제한을 넘기는 상태였다 — 내용 작업 전에 먼저 분할했고, 격리 워크트리(`_aih-wt`)가 분할 이전 사본을 들고 있으면 병합이 분할을 되돌리므로 **점유 0건이 된 뒤 착수**했다. **[경계를 발명하지 않았다]** `document-master-guide.md` §길이·구조가 **"가장 내용이 많은 섹션을 하위 문서로 분기한다"**·**"길이 조절을 위해 불필요한 내용 압축을 시도하지 않는다"** 를 규정하는데, 실측 최대 섹션이 정확히 **`### 로직 실행 순서` 173줄**로 다음 큰 섹션(§분기 및 예외 흐름 16줄)의 **10배 이상**이었다. 게다가 워크스페이스에 이미 `ai/agents/workflow-prd-to-spec/prd-to-process-logic-template.md`(**"로직 실행 순서 — FE/BE 의사코드 템플릿"**)가 **이 섹션 전용 산출물 템플릿**으로 존재해, 분기 경계가 이미 사양 체계 안에 있던 자리다. **[내용 변경 0 — 실측]** 부모에서 뜯어낸 §로직 실행 순서 본문 **170줄**과 자식 본문 **170줄**이 **md5 동일**(`5cac17eeebd4…`) · 부모 삽입은 **4줄뿐**(참조 1 · 라벨 2 · 공백 1) · §기본 정보·§관련 사양 코드·§진입점 및 진입 조건·§입력/출력 정의·§연관 데이터 및 외부 호출·§실행 제약사항·§단계명 매핑·§분기 및 예외 흐름·§실행 결과·§의존 프로세스·§구현 가이드 **전 절 무접촉**(의사코드 한 글자도 고치지 않았다). 자식은 `#### FE 측 처리`·`#### BE 측 처리`·`#### 데이터 변환 흐름`·`#### 단계 통합 흐름` 네 절을 원 순서 그대로 갖는다. **[인용 좌표 보존]** 섹션 앵커 링크는 저장소 전체 **0건**이고 단계 인용은 **논리 좌표**(`` `PROC-103` `B6` `` 형태)라 라벨 집합만 보존되면 유효한데, `tc_USR-04.md:13` 처럼 **파일을 지목해 라벨 집합을 인용**하는 서술이 있어 부모 §로직 실행 순서에 **라벨 목록과 자식 위치를 남겼다** — FE `F1`·`F2`·`F3`·`F5`(**`F4` 결번**) · BE `B1`·`B2`·`B3`·`B4`·`B5b`·`B6`·`B7`·`B8`(**`B5a` 결번**). 자식에서 실측한 라벨 **12개가 이 목록과 완전 일치**(소실 0)하고 4파일 상대 링크 전수 **broken 0**. 자식 서두는 `정본은 process_PROC-103.md` 한 줄뿐이라 §하위문서 참조 규칙(하위는 자신의 정본 1개만 밝힌다)도 지킨다. **⚠️ 이번 commit 에 내용 변경은 없다** — 이월된 `exec` 전달 표기 등은 다음 commit 에서 한다. 짝인 `PROC-102` 분할은 USR-03.md 항목 참고 | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-26 12:20 | spec | `docs/specs/qa/USR/tc_USR-04.md` `USR-04_017`(확장 — **기존 8관측 무접촉**, ⑨ 추가) — **커버리지 공백 시정: 승인 제출이 500 `EX-OPS-002` 로 `Unconfirmed` 에 도달했을 때 진행 영역 라이브 리전을 보는 관측이 없었다.** **이력·산출물 동일 commit**(작성 시점 미커밋). `4f866b4` 가 `de482d3` 의 해제 규칙을 `USR-02_015`(**응답 미수신** 경로)에만 반영해, **같은 `Unconfirmed` 에 다른 경로로 도달하는 이 TC** 는 옛 상태로 남아 있었다. **⑨** 를 더해 `Unconfirmed` 시점에 **`aria-busy` 가 풀려 있고**(`false` 이거나 속성 부재) **`role="status"`·`aria-live="polite"` 는 그대로 남는다**(영역을 라이브 리전에서 내리지 않는다)를 관측하게 하고, **두 경로**(응답 미수신 vs 응답으로 받은 500 `EX-OPS-002`)로 도달해도 **해제 규칙이 갈리지 않음**을 명시했다 — 켠 채 두면 보조기술이 단계 ⑤ 의 **재진입 안내**를 미루거나 건너뛰는데, 그 안내를 읽어야 결과를 볼 수 있다. **검증 대상** 칸에 `SCR-003` §구현 가이드(라이브 리전 계약)·디자인 시스템 §ProgressPanel §접근성 정본을 더해 이 단언의 출처가 복제값이 아님을 드러냈다. **[무변화 — 실측]** TC **신설 0** · 번호·결번(`_008`) 그대로·이 노드 **16건 유지**(총 242건 불변) · 검증 PROC(`PROC-103`)·분기(`EX-OPS-002`)·우선순위·자동화·**전제 칸 무변경** · 단계 ①~⑧·데이터 검증·판정(Block 사유 — fault 주입 하네스 미구축) 무변경 · `screen_SCR-003.md`·`mockup/`·`design-system-components.md` §ProgressPanel **무접촉**(같은 문서 §StageTitle 1줄 정합은 공통 반영분이다). 같은 회차의 **공통 반영분**(§접근성 기준의 제목 포커스 표시 확정 + `design-system-components.md` §StageTitle §접근성 1줄 정합 — `SCR-002` 제목도 그 대상 · `spec-qa.md` 헤딩 244 각주가 든 결번 `USR-04_008` 은 **정의·번호 무변경** · `spec-qa-mapping.md` `SCR-003` 서술 보강)은 common.md 항목 참고 | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-26 11:32 | spec ⓒ | (공통 반영) 목업 잔여 접근성 3건 마감 중 **F-6 만 이 노드에 닿는다** — `mockup/SCR-002.html` 의 제목 `h1`("연동 동의")에 **`tabindex="-1"` 을 부여**했다(`design-system-components.md` §StageTitle §접근성·`design-system.md` §접근성 기준이 요구하는 **단계 전환 시 새 제목으로 포커스 이동**이 받을 자리). **목업은 화면당 1파일이라 실제 단계 전환이 없어 동작 영향은 없고 정적 구조 계약 표기**이며, **포커스를 실제로 옮기는 스크립트는 만들지 않았다**. 동의 항목·`ConsentList` 잠금·유효성 안내·버튼 등 **화면 거동과 문구는 전부 무변경**이고 `SCR-002` 사양 파일도 손대지 않았다 — common.md | `accountinterlockhub#474`·`#469`·`#467` | ℹ️ |
| 2026-07-26 10:42 | spec | `docs/specs/processes/process_PROC-103.md` — 동의·승인 제출의 **트랜잭션 경계를 여는 자리 명시 + 스냅샷 재정렬 주체 중복 제거**(commit `1491e4f` SD-1·SD-3 의 프로세스 확산분). **근거 commit `b4fe224`** — 본 entry 작성 시점엔 미커밋이었고, 오케스트레이터가 산출물과 이 이력을 같은 commit 에 함께 넣었다(정책대로 동시 반영). **[①]** `B3` 의 `BEGIN;` 을 **`BEGIN ISOLATION LEVEL READ COMMITTED`** 로 바꿔 격리 수준을 명시했다 — 이 수준은 문마다 새 스냅샷을 보므로 먼저 커밋된 상대 요청의 행이 이어지는 조회에 보이며, **`PROC-301` `B3` 충돌 재조회의 성립 전제**다(그 위로 올리면 재조회가 성립하지 않는다). `PROC-301` 호출 인자에 `exec`(여기서 연 커넥션·실행자)를 더해 **이 전달이 참여의 성립 조건**임을 주석으로 못박고, "경계를 여는 자리는 여기다"를 명시했다. **[②]** 데이터 변환 흐름 표의 `도메인→ENT` `B6`(PROC-302) 칸이 스냅샷을 "코드 오름차순 정렬"한다고 적어 **정렬 주체가 `MDL-008` 과 둘로 갈리던 것**을 "항목 순서는 `MDL-008` 결과 그대로"로 정정했다(`PROC-302` `B3`·`FN-012` 단계 3 정정과 짝 — BAT-05.md 항목 참고). **[③]** `docs/specs/functions/function_FN-012-013.md` `FN-012`(동의 증적 기록)도 같은 회차에서 시그니처에 `exec` 필수 인자를 더하고 **경계를 여는 자리를 `PROC-103` `B6` 으로 특정**했다 — 경계 없이 호출되면 증적 기록이 승인 확정과 따로 커밋돼 **증적 없는 승인·승인 없는 증적**이 생긴다(`BIZ-003-04`). `FN-009`(결과 확정, `B7` 경유)·`FN-008`(`B3` 경유)의 소유권 표기 통일 상세는 BAT-04.md 항목 참고 | `accountinterlockhub#475`·`#473`·`#469` | 🚧 |
| 2026-07-26 10:26 | spec ⓒ | (공통 반영) 사양 결함 7건(SD-1~SD-7) 정정 — `model_MDL-007-010.md` `MDL-008`(동의 항목 구성) `items` 의 **정렬 비교 기준 정본을 `data_ENT-002.md` §버전 식별자 산출 규칙 3 으로 지정**(SD-3, 본 사양에 복제하지 않는다). §구현 가이드의 "항목 정렬을 이 모델에서 한 번만 수행하면 버전 식별자 산출과 스냅샷이 자동으로 같은 순서를 갖는다" 서술에도 같은 정본 참조를 달아, 같은 문구가 흩어져 있던 `MDL-008`·`MDL-002`·`ENT-002` 사이에서 **정렬 주체를 하나로** 모았다(기준 자체는 **UTF-8 바이트열 사전식 = 코드 포인트 오름차순** — 상세는 BAT-05.md 항목 참고). 승인 제출이 쓰는 `FN-008`(레코드 확보)의 트랜잭션 소유권 통일은 BAT-04.md 항목 참고. 근거 commit `1491e4f`(이력 누락 보정) — common.md | `accountinterlockhub#473`·`#472`·`#470`·`#469` | ℹ️ |
| 2026-07-26 09:18 | build | P15(`#492`) `SCR-002` 정밀 마감 — `flow/ConsentScreen.tsx` 는 P14 참조 조립을 사양·목업과 재대조해 변경 없이 확정(승인 버튼 `aria-disabled`·네이티브 `disabled` 미사용·거부/취소 인터페이스 부재·필수 충족 판정 단일 소스로 버튼 활성/게이팅 공유·본인확인 복귀 시 동의 선택 자동 초기화 전건 일치). 공유 컴포넌트 정정은 `components/StageTitle.module.css` 1건(USR-03.md 참고 — 제목→보조 설명 `--space-2xs` 정합, SCR-002 도 카드 직계라 동일 적용). **실측**(전용 임시 프로필·헤드리스 크롬, CDP 직접 구사): 승인 버튼 초기 `aria-disabled="true"`·네이티브 `disabled` 는 `false` 확인 → 미체크 상태에서 버튼 3연속 클릭에도 `submitApproval()` 자체가 한 번도 호출되지 않음(전용 플래그로 직접 확인, 네트워크 계층이 아니라 호출 여부를 검사)+Gated 알림 "모든 항목에 동의해 주셔야 합니다" 노출+첫 미충족 체크박스로 포커스 이동 → 체크 시 `aria-disabled` 해제 → 승인 클릭 시 요청 발신 즉시 카드가 SCR-003 으로 교체(승인 버튼·동의 목록 DOM 에서 소멸, 스피너·폼 잠금 미노출). NoticeBlock(주입 문구 그대로 렌더)·ConsentList(항목 1건도 목록·`필수` 배지·체크박스 `disabled` 상태 없음) 렌더 확인. `npm --prefix apps/frontend run build` 에러 0건 · **[회귀 1회차]**(독립 code-reviewer FAIL 판정 대응, `#492` journal 3499) — 🔴 **[I-2]** `flow/ConsentScreen.tsx` 의 `toggle()` 이 로컬 `agreedCodes` 만 갱신해, 필수 항목을 모두 체크해도 Gated 알림이 남았다(`screen_SCR-002.md:106` "안내가 뜬 뒤 필수 항목을 모두 체크하면 **안내를 지우고** 버튼을 활성으로 바꾼다" — 한 문장이 요구한 두 동작 중 버튼 활성만 구현). `isAllRequiredMet()` 공유 판정 함수 신설(버튼 활성·클릭 시 게이팅·체크 시 해제 판정을 한 곳으로 단일화 — screen_SCR-002.md §구현 가이드) + `onRequiredSatisfied`/`clearConsentGatedAlert` 신설. 값 반영(`setAgreedCodes`)과 알림 해제 부수효과를 별개의 순차 호출로 분리(updater 내부 부수효과 없음), 서버 재검증 결과인 `blocked` 알림은 사양이 해제를 요구하지 않아 `gated` 만 해제 대상으로 한정. **실측**: 미체크 승인 클릭 → Gated alert+`aria-disabled="true"` → 필수 체크 → `alertPresent:false`·`ariaDisabled:null` 동시 확인(버튼 활성과 알림 해제가 같은 시점에 일어남). `npm --prefix apps/frontend run build` 에러 0건 · **[회귀 2회차]**(코드리뷰 재판정 FAIL 대응, `#492` journal 3510) — 근거 commit `2bd7858`(이력 누락 보정 — 본 entry 작성 시점엔 미커밋이었고, 다른 세션의 commit `a1273f2` 가 서술 자체는 이미 흡수했다) — 🔴 **[I-3]** `agreedCodes` 가 `ConsentScreen` 로컬 `useState` 라 승인 발신 즉시 SCR-003 전환으로 컴포넌트가 언마운트돼, `EX-BIZ-003`(Retryable)·`EX-BIZ-001`(Blocked) 로 SCR-002 에 되돌아오면 재마운트와 함께 체크가 전부 풀리고 승인 버튼이 다시 비활성됐다(`process_PROC-103.md` F5 "본인확인으로 되돌아가면 agreed 를 비운다" — 비우는 경우를 BackToIdentity 로 한정 · `screen_SCR-002.md:73` Retryable "그대로 다시 제출할 수 있다" · 목업 `SCR-002.html` retryable 상태 `checked:true`+버튼 활성, 3중 일치). `agreedCodes` 를 `stage/useInterlockFlow.ts` 훅 메모리로 승격(`toggleConsent` 신설)하고 `ConsentScreen` 은 controlled 컴포넌트로 전환, SCR-001·SCR-004 전이에서만 비운다. `isAllRequiredMet` 공유 판정도 화면 밖 `stage/transitions.ts` 로 옮겨 화면·훅이 같은 함수를 쓴다. `approve()` 는 파라미터 없이 훅 자신의 agreedCodes(`Array.from`)를 싣는다. `ConsentScreen.tsx` 의 "본인확인 복귀(BackToIdentity)로만 새로 마운트된다"는 기전 오서술 주석을 정정(실제로는 SCR-002 를 떠나는 모든 전이가 언마운트를 일으킨다 — 위 첫 문단의 "전건 일치" 서술도 같은 이유로 부정확했다). 🟡 **[S-4]** `flow/stack.module.css` 주석이 "본문↔알림 16px"을 단언했으나 `ConsentList.module.css` 의 `.list{margin-bottom:--space-lg}` 와 인접 형제 마진 상쇄로 SCR-002 는 실제 24px 이 렌더됐다(토큰 이탈은 아니었음 — `design-system.md` 에 알림 전용 gap 값이 없다). `.list` 자체 마진을 제거해 간격 책임을 조립 유틸(`stack.module.css`) 한 곳으로 모으고 주석을 실측대로 갱신 — 이제 두 화면 다 16px 로 렌더. **실측**(격리 헤드리스 Chrome+CDP, 전용 스텁 서버 포트 49511·49512): 체크 후 1차 승인 클릭(스텁 500 EX-BIZ-003) → SCR-002 복귀 시 `checked:true`·`aria-disabled:null`(배경 `rgb(31,94,174)`=`--color-primary` 유지)·알림 "처리 중 문제가 발생했습니다…" 그대로 표시·list→alert 간격 16px 확인 → 같은 버튼 2차 클릭(스텁 200 성공) 시 Gated 문구("모든 항목에 동의해 주셔야 합니다") 20샘플·약 3초 관측 전 구간 미노출·서버 수신 `agreedItemCodes:["THIRD_PARTY_PROVISION"]` 유지(승인 2회 모두 접수, SCR-003→SCR-004 정상 전환). `npm --prefix apps/frontend run build` 에러 0건 · **[회귀 3회차]**(코드리뷰 재판정 FAIL 대응, `#492` journal 3535) — 🔴 **[I-4]** `stage/transitions.ts` 의 `viewAfterApprove` catch-all 이 미분류 오류(대표 `EX-OPS-002`)를 **`SCR-002` 재시도 안내(체크 유지+버튼 활성)로 되돌리고** 있었다. `approve()` 는 `B7`(전달 실행·결과 확정) 다음에 `B8`(응답 구성)이 와 미분류 실패는 **결과가 이미 확정된 뒤에도 날 수 있는데**, 살아 있는 승인 버튼이 재제출을 유도해 **승인이 2건 접수될 위험**이 있었다(리뷰어 실측 확인). `process_PROC-103.md` `F5` else("단계 전이를 하지 않는다 · `SCR-003` 에 머문 채 상태 = `Unconfirmed`") + §구현 가이드("재시도 안내도 쓸 수 없다 — 재시도 자체가 안전하지 않기 때문이다")대로 `EX-BIZ-003` 전용 갈래(기존 `backToConsentOrFallback` 유지)를 먼저 분리하고, 그 밖의 미분류 코드는 전이 없이 `{screen:'SCR-003', unconfirmed:true}` 로 고쳤다. `applyNextView` 가 `SCR-003` 도착에서 `agreedCodes` 를 비우지 않으므로 회귀 2회차 [I-3] 과 충돌하지 않는다. ⚠️ `process_PROC-102.md` `F3` 의 같은 catch-all(본인확인 접점, `USR-03.md` 참고)은 결과 경로 ②로 가지만 그 패턴을 여기서 복사하지 않았다 — 본인확인 제출은 어떤 결과도 확정하지 않아 뒤집을 결과가 없는 반면 이 접점은 있다(의도적 비대칭, 근거는 같은 §구현 가이드). 🟡 **[S-8] 자체보완** — `applyNextView`(`stage/useInterlockFlow.ts`) 를 우회하는 직접 `setView` 3곳(`verify()`·`approve()`·`reportConsentValidationFailed()`)의 불변조건("이 함수를 거치지 않는 `setView` 는 `SCR-001`·`SCR-004` 를 목적지로 삼지 않는다")이 코드 어디에도 적혀 있지 않아 [I-3] 이 조용히 되살아날 위험이 있었다 — 함수 docstring 에 명시(거동 무변경). **실측**(격리 헤드리스 Chrome+CDP, 전용 스텁 서버 포트 49501·49502): `/approve` 500 `EX-OPS-002` 응답 시 화면이 `SCR-003`(`h1:"연동을 진행하고 있습니다"`)에 잔류·고정 `Unconfirmed` 안내("처리 결과를 확인하지 못했습니다…") 노출·재시도 안내·결과 패널 어느 것도 미노출·페이지 내 `<button>` 0개(재제출 경로 없음)·`fetch` 호출 정확히 1건(800ms 추가 관찰에도 재시도 없음). **회귀 없음 재확인** — `EX-BIZ-003`(Retryable) 복귀: 체크 유지·`aria-disabled` 없음(활성)·재시도 클릭 시 서버가 두 요청 모두 동의 코드 포함 수신·2차 성공 시 `SCR-004` 경로 ① 정상 전환. `EX-BIZ-001`(Blocked) 복귀: 체크 유지·버튼 활성·Blocked 알림 유지. `npm --prefix apps/frontend run build` 에러 0건 · **[재개방 시정 — X-1]**(`#492` 재개방 — `해결`🔵 판정 이후 확정된 사양 `a8058a0` 미구현, P16(`#493`) 독립 코드리뷰 발견 — 메커니즘·`useStageFocus`/`IdentityScreen` 실측 근거는 USR-03.md 항목 참고) — `ConsentScreen.tsx` 가 `view.alert?.kind === 'blocked'` 일 때만 `StageTitle` 의 `skipFocus` 를 true 로 넘겨 `BackToConsent`(`SCR-003`→`SCR-002` · 400 `EX-BIZ-001`) 도착에서 제목이 포커스를 가져가지 않게 했다(`Retryable`(`EX-BIZ-003`)·`Gated`(화면 게이팅)는 사양상 이 예외에 해당하지 않아 무변경 — 전자는 그대로 제목으로, 후자는 이미 마운트된 화면의 갱신이라 `title` 불변으로 `useStageFocus` 의 effect 자체가 재실행되지 않는다). ⚠️ **실측 특기 — 이 전환이 실제 UI 로 도달 가능한 유일한 경로에서는 미충족 항목이 구조적으로 존재하지 않는다.** `Blocked` 는 로컬 게이트(`isAllRequiredMet`)를 통과해야만(= 필수 항목을 전부 체크해야만) 서버 응답을 받을 수 있고, 그 체크 상태는 이 전이에서 비워지지 않는다(회귀 2회차 [I-3] 그대로 유지) — 그래서 시정 전·후 어느 쪽이든 `focusFirstUnmet()` 은 대상을 찾지 못해 무동작이었다(기존 이월 사양 공백 [S-7]과 같은 구조이지 새 결함이 아니다). 그 결과 실측 `activeElement` 가 시정 **전** `H1`(제목 포커스가 우연히 살아남음) → **후** `BODY`(제목 포커스를 명시적으로 생략했고 필드 쪽도 옮길 대상이 없어서)로 바뀐다 — **이 변화는 회귀가 아니다.** `InlineAlert` 가 `role="status"`(`components/InlineAlert.tsx`)라 포커스 위치와 무관하게 Blocked 알림이 라이브 리전으로 낭독되고, `document.title` 도 `연동 동의` 로 정확히 갱신됨을 실측 확인했다(스크린리더 사용자에게 실질적 정보 손실 없음). 회귀 확인 4종(간격·포커스 링·승인 버튼 `aria-disabled` 계약·`SCR-003`/`SCR-004` 도착 시 제목 포커스 유지)은 USR-03.md 항목과 **동일 CDP 하네스·동일 세션**으로 함께 확인(전부 무변화). `npm --prefix apps/frontend run build` 에러 0건 | `accountinterlockhub#492`·`#469` | 🚧 |
| 2026-07-26 08:45 | spec ⓒ | (공통 반영) `StageTitle` 합성 간격 규칙 명문화·목업 `hidden` 무력화 시정(6회차 회귀) — 이 노드에 닿는 것은 **`SCR-002`**(`design-system-components.md` §StageTitle · `mockup/SCR-002.html`)이다. `StageTitle` 의 여백(제목→보조 설명 `--space-2xs` · 블록 아래 `--space-lg`)이 **카드 직계 자식일 때의 값**으로 한정됐고 **`SCR-002` 가 그 자리**라 동의·승인 화면의 간격은 달라지지 않는다(**거동 무변경** · `screen_SCR-002.md` 무수정). 목업은 `[hidden]{display:none !important}` 로, 저자 `display:flex` 선언이 UA 의 `[hidden]{display:none}` 을 이겨 **조건과 무관하게 늘 떠 있던 빈 알림 상자**(`#alert` — 동의 유효성 안내가 실릴 자리)가 사양대로 숨어 항목을 모두 체크한 정상 상태가 알림 없이 그려진다. 동의 항목·`ConsentList` 잠금·`NoticeBlock`·버튼 등 화면 거동과 문구는 **전부 무변경**이다. **⚠️ 소급 보정 entry** — 근거 커밋 **`9e6eb90`**. 이력은 공유 워킹트리 경합으로 직전 build 커밋 `fdc9f76` 에 common.md 08:45 row 만 선행 반영되고 **각 IA 역링크가 누락**돼 지금 채운다 — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-26 08:45 | build | `apps/backend/src/records/{tracking-record.service.ts,consent-proof-record.service.ts}` — P04(`#481`) 회귀 1회차 [I-1]·[I-2] 🔴 2건 시정(둘 다 승인 제출 오케스트레이션 — 후속 `PROC-103` — 이 참여할 지점). **[I-1] 결과 확정(`FN-009`)** `fixResult()` 를 자기 트랜잭션 소유에서 **호출측 트랜잭션 참여**(`executor` 필수 첫 인자)로 뒤집었다 — 승인 확정→전달→결과 확정 흐름에서 상위(`PROC-103`)가 여는 트랜잭션에 자연스럽게 참여할 수 있게 됐다. **[I-2] 동의 증적(`FN-012`)** 스냅샷 정렬을 `.localeCompare()`(로케일 의존)에서 정본 `sortConsentItemsByCode()`(코드 유닛 비교 — 버전 식별자 해시·화면 노출과 같은 기준)로 교체 — 동의 항목이 늘면 두 정렬이 실제로 갈릴 수 있었다(리뷰어 격리 DB 실측 확인). `recordConsentProof()` 자체가 호출측(`FOR UPDATE` 잠금 트랜잭션)에 참여하는 설계는 이번 회귀로 바뀌지 않았다. **실측**: `fixResult()` 동시 8경쟁 롤백·수렴 재확인 + 정렬 갈림 케이스(`ITEM10`/`ITEM_2`) 재현으로 정본 순서 일치 확인. 상세는 BAT-04.md(트랜잭션)·BAT-05.md(정렬) 항목 참고 | `accountinterlockhub#481`·`#469` | 🚧 |
| 2026-07-26 06:19 | spec ⓒ | (공통 반영) 횡단 국소 회귀(5회차) — `spec-functions-api-user.md` §동의·승인 제출 §에러 처리 표에 **`EX-OPS-002` 500 행 신설** + 표 아래 **근거 불릿 1개**(결과를 그리지 않는 이유 · 발생 지점이 전달 **이전**으로 특정돼 "결과 미확정·재시도 가능"이 보장되는 `EX-BIZ-003` 과의 대비). `process_PROC-103.md` 분기표와의 불일치 해소이며 **거동 변경 0 · 기존 행 무수정** — common.md | `accountinterlockhub#473`·`#470`·`#472`·`#469` | ℹ️ |
| 2026-07-26 06:18 | spec | `docs/specs/qa/USR/tc_USR-04.md` **`USR-04_017` 신설**(국소 회귀 5회차) — 승인 제출의 **500 `EX-OPS-002` → 단계 전이 없이 `SCR-003` `Unconfirmed`**(결과 패널 미생성). 관측 8지점 — `SCR-004`·`SCR-002`·`SCR-001` **어디로도 전이하지 않음** · 경로 ①②③ **어느 패널도 그리지 않음**(화면이 결과 구분을 추측하지 않는다 — `BIZ-001-02`) · `Unconfirmed` InlineAlert 노출·**사유 코드 미노출**(`SEC-002-05`) · 재진입 시 **확정 결과 그대로 재안내**(`BIZ-002-03` ③, `B7` 결과 확정 → `B8` 응답 순서라 확정 후에도 이 코드가 난다) · **`verify()` 의 같은 코드는 경로 ② 로 가는 비대칭** 확인. Block 사유 = fault 주입 하네스 미구축. `spec-qa-mapping.md` 정합 — `SVC-003` 15→16 · `PROC-103` 23→24 · `EX-OPS-002` 5→6 · `POST .../approve` 접점 30→31 · `SCR-003` 행에 `USR-04_017` · POL 3행(`BIZ-001`·`BIZ-002`·`SEC-002`) +1. **교차검증 4회차 §7-4 해소** — EX 표에 **계수 기준 1줄 신설**(`TC 수` = 그 코드를 각 TC 의 검증 분기/예외 칸에 인용한 TC 전수 · `대표 TC` 는 그 전수를 빠짐없이 나열), 실측 결과 `EX-AUTH-001` 은 **수치 5가 옳고 나열이 1건 누락**이라 `API-04_006` 을 보탬(수치 변경 0) · **13행 전건 일치**. **총 TC 241 → 242** · 결번 2건(`USR-04_008`·`USR-05_002`) 유지 · 기존 번호 변경 0 · `spec-qa.md` 계층(E2E 24)·유형(시스템예외 18)·자동화(반자동 31) 3축과 파일별 합 전부 **242** 정합 | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-26 02:46 | build | `apps/frontend/src/` — **디자인 시스템·공통 컴포넌트 11종·FE 셸·단계 상태머신 신규 구현**(정본 `design-system.md`·`design-system-components.md`). 색 토큰 23종·타이포·간격·모션 값을 그대로 이식(`styles/tokens.css`) — 실측 대비비 27행 재계산 전건 표와 일치. 컴포넌트 11종(`components/`) 전량 신설(NoticeBlock·ConsentList·ConsentItem 포함). 단계 상태머신(`stage/useInterlockFlow.ts`)이 `/approve` 접점(`api/client.ts`) 응답을 서버가 정한 대로 전이시킨다 — 승인 요청은 발신 즉시 `PROCESSING`(SCR-003) 으로 전환하고 응답으로 `RESULT`·`BackToConsent`(`EX-BIZ-001`·`EX-BIZ-003`)·`BackToIdentity` 를 가른다. **승인 버튼은 `aria-disabled`(네이티브 `disabled` 아님)로 클릭을 계속 받아 유효성 안내를 띄운다**(상위 제약 12 — Button 컴포넌트가 이 규약을 지원). `ConsentList` 는 잠금(제출 중) 상태를 두지 않는다(사양 확정과 합치). `ConsentScreen`(`flow/`)에서 필수 미충족 시 화면 게이팅(서버 미호출)·서버 재검증 알림을 같은 알림 영역에 통합. `npm run build`(`tsc -b && vite build`) 에러 0건 **[회귀 2회차]**(S-1, `#491` 자체보완) — `stage/transitions.ts` 의 `viewAfterApprove` 성공 분기에서 `resultPath: data.resultPath` 를 `resultPath: normalizeResultPath(data.resultPath)` 로 교체 — `/approve` 응답도 `/verify` 와 같은 HTTP JSON 무검증 캐스팅 경로를 거치므로 같은 정규화가 필요했다(USR-03.md 도 같은 시정, 상세 근거 동일). `npm --prefix apps/frontend run build` 에러 0건 | `accountinterlockhub#491`·`#469` | 🚧 |
| 2026-07-26 00:45 | spec ⓒ | (공통 반영) 요구사항 회귀 — **사용자 거부 결과 경로 폐지**. `service_SVC-003.md`·`process_PROC-103.md`·`screen_SCR-002.md` — 전 항목 동의가 진행 조건이 되어 **거부 제출 인터페이스가 사라졌고**(`BIZ-003-03`), 미동의 제출은 화면 유효성 안내(`Gated` — "모든 항목에 동의해 주셔야 합니다")에서 끝나 **접점에 도달하지 않는다**. 종료 수단은 **창 닫기**이며 그 연동은 **결과 미확정**으로 남는다(증적·전달·결과 구분 카운터 전부 0). `PROC-103` **`F4`·`B5a` 결번**(뒤 라벨 미당김) · **`BR-004` 슬롯 내용 교체**(승인·거부 선택 → 필수 동의 충족 판정 = 화면 게이팅 1차 방어) · 승인 요청 `decision` 필드 **폐지**(값이 하나뿐이라 분기가 성립하지 않는다). 디자인시스템 연쇄 — `secondary` 버튼·ConsentList **잠금 상태 사용처 소멸**(승인 제출은 카드가 SCR-003 으로 교체돼 스피너·폼 잠금이 이 화면에 서지 않는다). TC `USR-04_016` 신설(거부·취소 종료 수단 부재 확인) · `USR-04_008` **결번** — common.md | `accountinterlockhub#469`·`#470`·`#471`·`#473`·`#474`·`#475`·`#476` | ℹ️ |
| 2026-07-25 22:00 | directing | (방향 부분 갱신) **사용자 거부 종료 폐지 — 전 항목 동의가 진행 조건** — 담당자 지시 `#467` journal 3285. 미동의 제출은 동의 화면의 **유효성 안내**("모든 항목에 동의해 주셔야 합니다")로 막고 결과 단계로 보내지 않는다. 동의하지 않으려는 사용자의 종료 수단은 **창을 닫는 것**이며 그 연동은 **결과 미확정**으로 남아 보관 기한에 삭제된다(거부라는 확정 결과를 쓰지 않는다). `docs/prd/PRD.md` §서비스 이용 대상 3·§제공 가치·§수행 범위 4·9·11 · `ia/IA.md` USR-04·BAT-06 · `devspec/database.md` §추적 레코드 갱신·§결과 구분(거부 값 미도입)·§지표 집계. **역방향 전파 대상** = `BIZ-003` 게이팅·`BR-004`·`PROC-103` `B5a`·`SCR-002` 거부 버튼·`spec-functions-api-user.md` `decision` 필드·`FN-012`·`tc_USR-04`·`mockup/SCR-002.html` | `accountinterlockhub#467`·`#469` | 🚧 |
| 2026-07-25 18:39 | spec ⓒ | (공통 반영) 사양 확정분 목업 동기화 — `mockup/SCR-002.html` ConsentList 잠금(제출 중) 시각 처리를 `opacity:.6` → 비활성 토큰 쌍(`--color-disabled-bg`/`--color-disabled-fg`, 4.82:1 재사용) + `aria-busy="true"` 로 교체(확정 사양 반영). Redmine `#477` 첨부 최신화 — common.md | `accountinterlockhub#477` | ℹ️ |
| 2026-07-25 18:16 | spec ⓒ | (공통 반영) 목업 리뷰 발견 사항 반영(회귀 3회차) — 인라인 알림 위치를 **화면 사양의 알림 영역 한 곳**으로 통일(오류 유형별 이동 금지 · "카드 상단" 서술 폐기)·ConsentList **잠금(제출 중) 시각 처리 확정**(비활성 토큰 쌍 재사용·대비비 4.82:1·`opacity` 금지)·InlineAlert 사용 화면 목록에 SCR-003 추가. **M-1(`DECRYPT_FAILED` 레코드 확정 경로 복원) 미반영 — 구현 불가 판단으로 차단 보고** — common.md | `accountinterlockhub#477`·`#474` | ℹ️ |
| 2026-07-25 18:01 | spec | `mockup/SCR-002.html` 신규 — **동의·승인 화면 목업**. NoticeBlock(안내 문구 상수, 값 없을 때 영역 미렌더 변형 포함)·ConsentList(1건도 목록 렌더, `필수` 배지)·승인 버튼 `aria-disabled` 토글(체크박스 실시간 반영)·거부 버튼 아래 "되돌릴 수 없음" 고정 문구. 6개 상태(초기미동의·동의완료·안내문구없음·필수미충족오류·거부처리중·처리오류) | `accountinterlockhub#477`·`#469` | 🚧 |
| 2026-07-25 17:01 | spec ⓒ | (공통 반영) 교차검증 1회차 지적 보완(회귀 1회차 — 🟠 2·🟡 7 전건 해소) — `SCR-002` 상태 전이의 `PROC-104` 단계명을 확정명 **복호화 판정(재복호화)** 로 정정 — common.md | `accountinterlockhub#474` | ℹ️ |
| 2026-07-25 16:19 | spec ⓒ | (공통 반영) 검증 TC 도메인 신규 작성 — `docs/specs/qa/` **TC 232건**(커버리지 SVC 18/18 · PROC 18/18 · BR 24/24 · EX 13/13 · 정책 17/17 · 접점 11/11 — 미검증 0)·**횡단 시나리오 `SCEN_001`~`SCEN_023`**(여정 6 · 규약 대칭 왕복 3 · 기동·상수 검증 5 · 무저장 원칙 2 · 설계된 부재·수용 리스크 7)·**AUTH 치환 12건**·모의 수신처·C# 하네스·시드 카탈로그 37항·미해소 1건(성공률 정의 이월) — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-25 15:48 | spec | `docs/specs/qa/USR/tc_USR-04.md` 신규 — **동의·승인 TC 15건**(웹·SPA UI + API·서버). 승인 = 증적 1건 + 전달 + 경로 ① / **거부 = 증적 0건·전달 0건·진행 화면 미경유·경로 ②** · 필수 동의 **서버 재검증**(화면 비활성은 1차 방어)·`decision` 값 이탈·없는 항목 코드·**필수/선택 경계**(선택만 체크 시 400)·거부의 `agreedItemCodes` 미검증 경계·**증적 기록 실패 시 전달 0건**(`BIZ-003-04`)·확정 레코드 승인 재제출 시 결과 불변·승인 시점 재복호화 실패 → SCR-001 되돌림·**문구 무가공 대조**(상수↔화면↔증적 스냅샷)·안내 문구 빈 값/400자 3단락 경계·동시 승인 2건 → 증적 정확히 1행·거부 후 재진입 경로 ② 재안내. 검증 BR-002·BR-004·BR-005·BR-022 · EX 7종 | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec | `process_PROC-103.md` 신규 — **PROC-103 동의·승인 제출**(RR·WF·동기·트리거=SCR-002 승인/거부). 승인 제출 하나가 재복호화·증적·전달·결과 확정을 발화시키는 **오케스트레이션 지점**. `F1` 상수 무가공 렌더 → `B2` PROC-104 복호화 구간 호출 → `B3` 레코드 확보 → `B4` 확정 재안내 → **`B5a` 거부=`USER_DENIED` 확정(증적·전달 없음) / `B5b` 필수 동의 서버 재검증(항목 코드 실재까지 확인)** → `B6` 추적 레코드 `FOR UPDATE` 잠금 후 증적 기록 → `B7` 전달 구간 → `B8` 결과 안내(502 `EX-BIZ-002` 도 정상 종료). 12 단계 | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec ⓒ | (공통 반영) 프로세스 도메인 신규 작성 — `docs/specs/processes/` **PROC 18종 확정**(정책·서비스 예약분을 같은 코드·같은 의미로 — 통합·폐기·추가·재배치 0)·**단계 라벨 체계 확정**(`F` FE·`B` BE·`C` 호출측·`L` 라이브러리 내부, 레이어별 1부터 시간 순, 분기는 접미 문자)·선행 도메인 단계명 전건 라벨 매핑·의존관계 순환 0(호출 깊이 3층)·트랜잭션 경계와 멱등 수단 총괄·정책 규칙 86건 매핑(런타임 80 + 게이트·절차 6) — common.md | `accountinterlockhub#475`·`#469` | ℹ️ |
| 2026-07-25 14:40 | spec | `screen_SCR-002.md` 신규 — **SCR-002 동의·승인**(단계 `CONSENT` · 트리거 PROC-103). 상수 유래 항목명·설명·필수 여부·안내 문구를 **가공 없이 그대로** 노출(`DATA-003-05`)하고 항목은 **1건이어도 목록으로 렌더**(상수로 늘 수 있다). 안내 영역은 **최대 400자·최대 3단락·평문 전제로 접기·더 보기 없이 단락 그대로**, **값이 비면 영역 자체를 미렌더**(`EXC-BIZ-07`). 필수 미충족 시 승인 버튼 비활성 = **1차 방어**이고 판정 근거는 서버 재검증(`BIZ-003-01`·`BIZ-003-02`). **거부는 진행 화면을 거치지 않고**(전달·증적이 없어 기다림이 없다 — `BIZ-003-03`) 버튼 안 진행 표시로 처리하며, **확인 대화상자 대신** 되돌릴 수 없음을 버튼 아래 한 줄로 알린다(모달 미도입 = 포커스 가둠 0). 본인확인으로 되돌아가면 **동의 선택을 초기화**(승인은 제출 시점의 명시적 행위 — `DATA-003-04`). 동의 항목 버전 식별자·추적 키 미표시 | `accountinterlockhub#474`·`#469` | 🚧 |
| 2026-07-25 14:40 | spec ⓒ | (공통 반영) 화면 도메인 신규 작성 — `docs/specs/screens/` **화면 4건**(SCR-001~004 — 서비스 예약 대비 통합·폐기·추가·재배치 0)·**단일 경로 4단계 모델**(리다이렉트 없음·화면별 URL 없음)·**디자인 시스템 1벌**(표면이 하나라 토큰 분기·접두어 없음 · 색 23종 **실측 대비비 표** · WCAG 2.1 AA · 모바일 우선 2 브레이크포인트 · 히트 영역 44x44)·공통 컴포넌트 11종·**화면 없는 IA 노드 12건 근거 기재** — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-25 14:10 | spec | `spec-functions-api-user.md` §동의·승인 제출 · `function_FN-012-013.md` §FN-012 — **`POST <INTERLOCK_ENTRY_PATH>/approve` 확정**. **승인·거부 전송 표현 확정 = `decision` 필드에 `APPROVE`·`REJECT`**(도메인 표현을 그대로 전송 표현으로 써 변환 지점을 없앤다 — `BIZ-001-03` 정신) + `agreedItemCodes` 문자열 배열. 필수 동의 미충족·`decision` 값 밖·스냅샷에 없는 항목 코드는 400 `EX-BIZ-001`(결과 미확정). 승인 확정 시 **FN-012** 가 동의 증적 1건을 승인과 **같은 처리 경계**에서 기록(스냅샷 = `notice` + `items` 코드 오름차순, 버전 식별자는 기동 산출값 재사용). 증적 실패 시 500 `EX-BIZ-003`·결과 미확정·전달 미수행. 거부는 `USER_DENIED` 확정 후 결과 경로 ②(증적 미생성·전달 미수행) | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-25 14:10 | spec ⓒ | (공통 반영) 기능·API 도메인 신규 작성 — `docs/specs/functions/` **공통 기능 15건**(FN-001~015 · POL 5·DAT 8·CRS 2·**EXT 0**)·**인터페이스 11접점**(사용자 진입 표면 3 · 서버 대면 API 4 · 수신처 전달 1 · 명령 진입점 1 · 라이브러리 공개 진입점 2)·오류 응답 엔벨로프 `{code,message,details?}`·인증 0/요청 제한 0·**신규 EX `EX-OPS-002` 1건**을 정책 카탈로그에 등록 — common.md | `accountinterlockhub#473`·`#469` | ℹ️ |
| 2026-07-25 13:35 | spec ⓒ | (공통 반영) 데이터 도메인 신규 작성 — `docs/specs/datas/` **엔터티 3건**(ENT-001~003 = 저장 대상 셋)·**데이터 모델 22종**(MDL-001~022 확정 — 예약 대비 통합·추가·재배치 0)·인덱스 5·물리 FK 0·소프트 삭제 미도입 — common.md | `accountinterlockhub#472`·`#469` | ℹ️ |
| 2026-07-25 13:05 | spec | `service_SVC-003.md` 신규 — **SVC-003 동의·승인**(매핑 PROC-103·PROC-302). Happy Path 6단계(항목 노출→동의 선택→승인 제출→서버 재검증→동의 증적 기록→연동 실행 이관). BR-004 = 승인/거부(거부는 `USER_DENIED` 확정·증적 미생성·전달 미수행), BR-005 = 필수 동의 충족 서버 재검증. 예외 `EX-BIZ-001`(필수 미충족)·`EX-BIZ-003`(증적 기록 실패 → 연동 미진행). 동의 항목·안내 문구는 상수(MDL-008)를 **그대로** 노출하고 화면이 가공하지 않는다. 선택 항목은 승인을 막지 않으며 문구 부재도 게이팅 조건이 아니다 | `accountinterlockhub#471`·`#469` | 🚧 |
| 2026-07-25 13:05 | spec ⓒ | (공통 반영) 서비스 도메인 신규 작성 — `docs/specs/services/` **서비스 18건**(SVC-001~018)·사용자 역할 4종·분기 `BR-001`~`BR-024`·MDL 22종/SCR 4종 예약 채번·`PROC-403`·`PROC-404` 추가 채번 — common.md | `accountinterlockhub#471`·`#469` | ℹ️ |
| 2026-07-25 12:28 | spec | `policy_BIZ.md` `BIZ-003` 동의·승인 게이팅 + `policy_DATA.md` `DATA-003` 개인정보 제3자 제공 동의 신설 — 노출 항목은 `<CONSENT_ITEMS>` 상수가 정본이고 화면이 문구를 가공하지 않는다(`DATA-003-05`). 필수 항목 전부 동의해야 승인 제출 가능하며 **서버가 재검증**(미충족 400 `EX-BIZ-001`). 거부는 `USER_DENIED` 확정·복호화/전달 미수행·증적 미생성. 승인 확정 시 동의 증적 1건 생성, 실패 시 500 `EX-BIZ-003` 로 연동 중단. 근거 법규 = 개인정보 보호법 제17조(개인정보의 제공). `<CONSENT_NOTICE>` 잠정 문구·검토 주체 담당자(EXC-DATA-07) | `accountinterlockhub#470`·`#469` | 🚧 |
| 2026-07-25 12:28 | spec ⓒ | (공통 반영) 정책 도메인 신규 작성 — `docs/specs/policies/` 정책 17건·규칙 86건·EX 코드 12종(암호화 연동 규약·복호화 판정 4단계·결과 구분 4종·추적 레코드 생성 시점·보관·무저장·동의 증적·인증 부재·수용 리스크) — common.md | `accountinterlockhub#470`·`#469` | ℹ️ |
| 2026-07-25 11:25 | directing ⓒ | (공통 반영) 값 확정 대기 잠정값 설정 — 동의 안내 문구 `<CONSENT_NOTICE>` 잠정 문구 유지·표기 정합(허용 형태 = 최대 400자·3단락·평문), 확정 시 상수 값만 교체(화면 사양 개정 불요) — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
| 2026-07-25 10:05 | directing ⓒ | (공통 반영) 리셋 방향 담당자 확정분 반영 — 동의 항목 노출·동의/거부·승인을 별도 기능단위로 **신설**(구 `USR-01` 에서 분리). 동의 항목 상수화(1건 `THIRD_PARTY_PROVISION`)·동의 증적(`BAT-05`) 연계 — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
