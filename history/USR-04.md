# IA: USR-04 — 동의·승인

> 본 IA 노드와 연관된 작업의 시계열 이력(최신순). 정책: ai/strategies/ia-history.md

| 일시 (KST) | 단계 | 산출물·결과 | 관련 일감 | 상태 |
|---|---|---|---|---|
| 2026-07-26 11:32 | spec ⓒ | (공통 반영) 목업 잔여 접근성 3건 마감 중 **F-6 만 이 노드에 닿는다** — `mockup/SCR-002.html` 의 제목 `h1`("연동 동의")에 **`tabindex="-1"` 을 부여**했다(`design-system-components.md` §StageTitle §접근성·`design-system.md` §접근성 기준이 요구하는 **단계 전환 시 새 제목으로 포커스 이동**이 받을 자리). **목업은 화면당 1파일이라 실제 단계 전환이 없어 동작 영향은 없고 정적 구조 계약 표기**이며, **포커스를 실제로 옮기는 스크립트는 만들지 않았다**. 동의 항목·`ConsentList` 잠금·유효성 안내·버튼 등 **화면 거동과 문구는 전부 무변경**이고 `SCR-002` 사양 파일도 손대지 않았다 — common.md | `accountinterlockhub#474`·`#469`·`#467` | ℹ️ |
| 2026-07-26 10:42 | spec | `docs/specs/processes/process_PROC-103.md` — 동의·승인 제출의 **트랜잭션 경계를 여는 자리 명시 + 스냅샷 재정렬 주체 중복 제거**(commit `1491e4f` SD-1·SD-3 의 프로세스 확산분). **근거 commit `b4fe224`** — 본 entry 작성 시점엔 미커밋이었고, 오케스트레이터가 산출물과 이 이력을 같은 commit 에 함께 넣었다(정책대로 동시 반영). **[①]** `B3` 의 `BEGIN;` 을 **`BEGIN ISOLATION LEVEL READ COMMITTED`** 로 바꿔 격리 수준을 명시했다 — 이 수준은 문마다 새 스냅샷을 보므로 먼저 커밋된 상대 요청의 행이 이어지는 조회에 보이며, **`PROC-301` `B3` 충돌 재조회의 성립 전제**다(그 위로 올리면 재조회가 성립하지 않는다). `PROC-301` 호출 인자에 `exec`(여기서 연 커넥션·실행자)를 더해 **이 전달이 참여의 성립 조건**임을 주석으로 못박고, "경계를 여는 자리는 여기다"를 명시했다. **[②]** 데이터 변환 흐름 표의 `도메인→ENT` `B6`(PROC-302) 칸이 스냅샷을 "코드 오름차순 정렬"한다고 적어 **정렬 주체가 `MDL-008` 과 둘로 갈리던 것**을 "항목 순서는 `MDL-008` 결과 그대로"로 정정했다(`PROC-302` `B3`·`FN-012` 단계 3 정정과 짝 — BAT-05.md 항목 참고). **[③]** `docs/specs/functions/function_FN-012-013.md` `FN-012`(동의 증적 기록)도 같은 회차에서 시그니처에 `exec` 필수 인자를 더하고 **경계를 여는 자리를 `PROC-103` `B6` 으로 특정**했다 — 경계 없이 호출되면 증적 기록이 승인 확정과 따로 커밋돼 **증적 없는 승인·승인 없는 증적**이 생긴다(`BIZ-003-04`). `FN-009`(결과 확정, `B7` 경유)·`FN-008`(`B3` 경유)의 소유권 표기 통일 상세는 BAT-04.md 항목 참고 | `accountinterlockhub#475`·`#473`·`#469` | 🚧 |
| 2026-07-26 10:26 | spec ⓒ | (공통 반영) 사양 결함 7건(SD-1~SD-7) 정정 — `model_MDL-007-010.md` `MDL-008`(동의 항목 구성) `items` 의 **정렬 비교 기준 정본을 `data_ENT-002.md` §버전 식별자 산출 규칙 3 으로 지정**(SD-3, 본 사양에 복제하지 않는다). §구현 가이드의 "항목 정렬을 이 모델에서 한 번만 수행하면 버전 식별자 산출과 스냅샷이 자동으로 같은 순서를 갖는다" 서술에도 같은 정본 참조를 달아, 같은 문구가 흩어져 있던 `MDL-008`·`MDL-002`·`ENT-002` 사이에서 **정렬 주체를 하나로** 모았다(기준 자체는 **UTF-8 바이트열 사전식 = 코드 포인트 오름차순** — 상세는 BAT-05.md 항목 참고). 승인 제출이 쓰는 `FN-008`(레코드 확보)의 트랜잭션 소유권 통일은 BAT-04.md 항목 참고. 근거 commit `1491e4f`(이력 누락 보정) — common.md | `accountinterlockhub#473`·`#472`·`#470`·`#469` | ℹ️ |
| 2026-07-26 09:18 | build | P15(`#492`) `SCR-002` 정밀 마감 — `flow/ConsentScreen.tsx` 는 P14 참조 조립을 사양·목업과 재대조해 변경 없이 확정(승인 버튼 `aria-disabled`·네이티브 `disabled` 미사용·거부/취소 인터페이스 부재·필수 충족 판정 단일 소스로 버튼 활성/게이팅 공유·본인확인 복귀 시 동의 선택 자동 초기화 전건 일치). 공유 컴포넌트 정정은 `components/StageTitle.module.css` 1건(USR-03.md 참고 — 제목→보조 설명 `--space-2xs` 정합, SCR-002 도 카드 직계라 동일 적용). **실측**(전용 임시 프로필·헤드리스 크롬, CDP 직접 구사): 승인 버튼 초기 `aria-disabled="true"`·네이티브 `disabled` 는 `false` 확인 → 미체크 상태에서 버튼 3연속 클릭에도 `submitApproval()` 자체가 한 번도 호출되지 않음(전용 플래그로 직접 확인, 네트워크 계층이 아니라 호출 여부를 검사)+Gated 알림 "모든 항목에 동의해 주셔야 합니다" 노출+첫 미충족 체크박스로 포커스 이동 → 체크 시 `aria-disabled` 해제 → 승인 클릭 시 요청 발신 즉시 카드가 SCR-003 으로 교체(승인 버튼·동의 목록 DOM 에서 소멸, 스피너·폼 잠금 미노출). NoticeBlock(주입 문구 그대로 렌더)·ConsentList(항목 1건도 목록·`필수` 배지·체크박스 `disabled` 상태 없음) 렌더 확인. `npm --prefix apps/frontend run build` 에러 0건 · **[회귀 1회차]**(독립 code-reviewer FAIL 판정 대응, `#492` journal 3499) — 🔴 **[I-2]** `flow/ConsentScreen.tsx` 의 `toggle()` 이 로컬 `agreedCodes` 만 갱신해, 필수 항목을 모두 체크해도 Gated 알림이 남았다(`screen_SCR-002.md:106` "안내가 뜬 뒤 필수 항목을 모두 체크하면 **안내를 지우고** 버튼을 활성으로 바꾼다" — 한 문장이 요구한 두 동작 중 버튼 활성만 구현). `isAllRequiredMet()` 공유 판정 함수 신설(버튼 활성·클릭 시 게이팅·체크 시 해제 판정을 한 곳으로 단일화 — screen_SCR-002.md §구현 가이드) + `onRequiredSatisfied`/`clearConsentGatedAlert` 신설. 값 반영(`setAgreedCodes`)과 알림 해제 부수효과를 별개의 순차 호출로 분리(updater 내부 부수효과 없음), 서버 재검증 결과인 `blocked` 알림은 사양이 해제를 요구하지 않아 `gated` 만 해제 대상으로 한정. **실측**: 미체크 승인 클릭 → Gated alert+`aria-disabled="true"` → 필수 체크 → `alertPresent:false`·`ariaDisabled:null` 동시 확인(버튼 활성과 알림 해제가 같은 시점에 일어남). `npm --prefix apps/frontend run build` 에러 0건 · **[회귀 2회차]**(코드리뷰 재판정 FAIL 대응, `#492` journal 3510) — 근거 commit `2bd7858`(이력 누락 보정 — 본 entry 작성 시점엔 미커밋이었고, 다른 세션의 commit `a1273f2` 가 서술 자체는 이미 흡수했다) — 🔴 **[I-3]** `agreedCodes` 가 `ConsentScreen` 로컬 `useState` 라 승인 발신 즉시 SCR-003 전환으로 컴포넌트가 언마운트돼, `EX-BIZ-003`(Retryable)·`EX-BIZ-001`(Blocked) 로 SCR-002 에 되돌아오면 재마운트와 함께 체크가 전부 풀리고 승인 버튼이 다시 비활성됐다(`process_PROC-103.md` F5 "본인확인으로 되돌아가면 agreed 를 비운다" — 비우는 경우를 BackToIdentity 로 한정 · `screen_SCR-002.md:73` Retryable "그대로 다시 제출할 수 있다" · 목업 `SCR-002.html` retryable 상태 `checked:true`+버튼 활성, 3중 일치). `agreedCodes` 를 `stage/useInterlockFlow.ts` 훅 메모리로 승격(`toggleConsent` 신설)하고 `ConsentScreen` 은 controlled 컴포넌트로 전환, SCR-001·SCR-004 전이에서만 비운다. `isAllRequiredMet` 공유 판정도 화면 밖 `stage/transitions.ts` 로 옮겨 화면·훅이 같은 함수를 쓴다. `approve()` 는 파라미터 없이 훅 자신의 agreedCodes(`Array.from`)를 싣는다. `ConsentScreen.tsx` 의 "본인확인 복귀(BackToIdentity)로만 새로 마운트된다"는 기전 오서술 주석을 정정(실제로는 SCR-002 를 떠나는 모든 전이가 언마운트를 일으킨다 — 위 첫 문단의 "전건 일치" 서술도 같은 이유로 부정확했다). 🟡 **[S-4]** `flow/stack.module.css` 주석이 "본문↔알림 16px"을 단언했으나 `ConsentList.module.css` 의 `.list{margin-bottom:--space-lg}` 와 인접 형제 마진 상쇄로 SCR-002 는 실제 24px 이 렌더됐다(토큰 이탈은 아니었음 — `design-system.md` 에 알림 전용 gap 값이 없다). `.list` 자체 마진을 제거해 간격 책임을 조립 유틸(`stack.module.css`) 한 곳으로 모으고 주석을 실측대로 갱신 — 이제 두 화면 다 16px 로 렌더. **실측**(격리 헤드리스 Chrome+CDP, 전용 스텁 서버 포트 49511·49512): 체크 후 1차 승인 클릭(스텁 500 EX-BIZ-003) → SCR-002 복귀 시 `checked:true`·`aria-disabled:null`(배경 `rgb(31,94,174)`=`--color-primary` 유지)·알림 "처리 중 문제가 발생했습니다…" 그대로 표시·list→alert 간격 16px 확인 → 같은 버튼 2차 클릭(스텁 200 성공) 시 Gated 문구("모든 항목에 동의해 주셔야 합니다") 20샘플·약 3초 관측 전 구간 미노출·서버 수신 `agreedItemCodes:["THIRD_PARTY_PROVISION"]` 유지(승인 2회 모두 접수, SCR-003→SCR-004 정상 전환). `npm --prefix apps/frontend run build` 에러 0건 · **[회귀 3회차]**(코드리뷰 재판정 FAIL 대응, `#492` journal 3535) — 🔴 **[I-4]** `stage/transitions.ts` 의 `viewAfterApprove` catch-all 이 미분류 오류(대표 `EX-OPS-002`)를 **`SCR-002` 재시도 안내(체크 유지+버튼 활성)로 되돌리고** 있었다. `approve()` 는 `B7`(전달 실행·결과 확정) 다음에 `B8`(응답 구성)이 와 미분류 실패는 **결과가 이미 확정된 뒤에도 날 수 있는데**, 살아 있는 승인 버튼이 재제출을 유도해 **승인이 2건 접수될 위험**이 있었다(리뷰어 실측 확인). `process_PROC-103.md` `F5` else("단계 전이를 하지 않는다 · `SCR-003` 에 머문 채 상태 = `Unconfirmed`") + §구현 가이드("재시도 안내도 쓸 수 없다 — 재시도 자체가 안전하지 않기 때문이다")대로 `EX-BIZ-003` 전용 갈래(기존 `backToConsentOrFallback` 유지)를 먼저 분리하고, 그 밖의 미분류 코드는 전이 없이 `{screen:'SCR-003', unconfirmed:true}` 로 고쳤다. `applyNextView` 가 `SCR-003` 도착에서 `agreedCodes` 를 비우지 않으므로 회귀 2회차 [I-3] 과 충돌하지 않는다. ⚠️ `process_PROC-102.md` `F3` 의 같은 catch-all(본인확인 접점, `USR-03.md` 참고)은 결과 경로 ②로 가지만 그 패턴을 여기서 복사하지 않았다 — 본인확인 제출은 어떤 결과도 확정하지 않아 뒤집을 결과가 없는 반면 이 접점은 있다(의도적 비대칭, 근거는 같은 §구현 가이드). 🟡 **[S-8] 자체보완** — `applyNextView`(`stage/useInterlockFlow.ts`) 를 우회하는 직접 `setView` 3곳(`verify()`·`approve()`·`reportConsentValidationFailed()`)의 불변조건("이 함수를 거치지 않는 `setView` 는 `SCR-001`·`SCR-004` 를 목적지로 삼지 않는다")이 코드 어디에도 적혀 있지 않아 [I-3] 이 조용히 되살아날 위험이 있었다 — 함수 docstring 에 명시(거동 무변경). **실측**(격리 헤드리스 Chrome+CDP, 전용 스텁 서버 포트 49501·49502): `/approve` 500 `EX-OPS-002` 응답 시 화면이 `SCR-003`(`h1:"연동을 진행하고 있습니다"`)에 잔류·고정 `Unconfirmed` 안내("처리 결과를 확인하지 못했습니다…") 노출·재시도 안내·결과 패널 어느 것도 미노출·페이지 내 `<button>` 0개(재제출 경로 없음)·`fetch` 호출 정확히 1건(800ms 추가 관찰에도 재시도 없음). **회귀 없음 재확인** — `EX-BIZ-003`(Retryable) 복귀: 체크 유지·`aria-disabled` 없음(활성)·재시도 클릭 시 서버가 두 요청 모두 동의 코드 포함 수신·2차 성공 시 `SCR-004` 경로 ① 정상 전환. `EX-BIZ-001`(Blocked) 복귀: 체크 유지·버튼 활성·Blocked 알림 유지. `npm --prefix apps/frontend run build` 에러 0건 | `accountinterlockhub#492`·`#469` | 🚧 |
| 2026-07-26 08:45 | build | `apps/backend/src/records/{tracking-record.service.ts,consent-proof-record.service.ts}` — P04(`#481`) 회귀 1회차 [I-1]·[I-2] 🔴 2건 시정(둘 다 승인 제출 오케스트레이션 — 후속 `PROC-103` — 이 참여할 지점). **[I-1] 결과 확정(`FN-009`)** `fixResult()` 를 자기 트랜잭션 소유에서 **호출측 트랜잭션 참여**(`executor` 필수 첫 인자)로 뒤집었다 — 승인 확정→전달→결과 확정 흐름에서 상위(`PROC-103`)가 여는 트랜잭션에 자연스럽게 참여할 수 있게 됐다. **[I-2] 동의 증적(`FN-012`)** 스냅샷 정렬을 `.localeCompare()`(로케일 의존)에서 정본 `sortConsentItemsByCode()`(코드 유닛 비교 — 버전 식별자 해시·화면 노출과 같은 기준)로 교체 — 동의 항목이 늘면 두 정렬이 실제로 갈릴 수 있었다(리뷰어 격리 DB 실측 확인). `recordConsentProof()` 자체가 호출측(`FOR UPDATE` 잠금 트랜잭션)에 참여하는 설계는 이번 회귀로 바뀌지 않았다. **실측**: `fixResult()` 동시 8경쟁 롤백·수렴 재확인 + 정렬 갈림 케이스(`ITEM10`/`ITEM_2`) 재현으로 정본 순서 일치 확인. 상세는 BAT-04.md(트랜잭션)·BAT-05.md(정렬) 항목 참고 | `accountinterlockhub#481`·`#469` | 🚧 |
| 2026-07-26 06:19 | spec ⓒ | (공통 반영) 횡단 국소 회귀(5회차) — `spec-functions-api-user.md` §동의·승인 제출 §에러 처리 표에 **`EX-OPS-002` 500 행 신설** + 표 아래 **근거 불릿 1개**(결과를 그리지 않는 이유 · 발생 지점이 전달 **이전**으로 특정돼 "결과 미확정·재시도 가능"이 보장되는 `EX-BIZ-003` 과의 대비). `process_PROC-103.md` 분기표와의 불일치 해소이며 **거동 변경 0 · 기존 행 무수정** — common.md | `accountinterlockhub#473`·`#470`·`#472`·`#469` | ℹ️ |
| 2026-07-26 06:18 | spec | `docs/specs/qa/USR/tc_USR-04.md` **`USR-04_017` 신설**(국소 회귀 5회차) — 승인 제출의 **500 `EX-OPS-002` → 단계 전이 없이 `SCR-003` `Unconfirmed`**(결과 패널 미생성). 관측 8지점 — `SCR-004`·`SCR-002`·`SCR-001` **어디로도 전이하지 않음** · 경로 ①②③ **어느 패널도 그리지 않음**(화면이 결과 구분을 추측하지 않는다 — `BIZ-001-02`) · `Unconfirmed` InlineAlert 노출·**사유 코드 미노출**(`SEC-002-05`) · 재진입 시 **확정 결과 그대로 재안내**(`BIZ-002-03` ③, `B7` 결과 확정 → `B8` 응답 순서라 확정 후에도 이 코드가 난다) · **`verify()` 의 같은 코드는 경로 ② 로 가는 비대칭** 확인. Block 사유 = fault 주입 하네스 미구축. `spec-qa-mapping.md` 정합 — `SVC-003` 15→16 · `PROC-103` 23→24 · `EX-OPS-002` 5→6 · `POST .../approve` 접점 30→31 · `SCR-003` 행에 `USR-04_017` · POL 3행(`BIZ-001`·`BIZ-002`·`SEC-002`) +1. **교차검증 4회차 §7-4 해소** — EX 표에 **계수 기준 1줄 신설**(`TC 수` = 그 코드를 각 TC 의 검증 분기/예외 칸에 인용한 TC 전수 · `대표 TC` 는 그 전수를 빠짐없이 나열), 실측 결과 `EX-AUTH-001` 은 **수치 5가 옳고 나열이 1건 누락**이라 `API-04_006` 을 보탬(수치 변경 0) · **13행 전건 일치**. **총 TC 241 → 242** · 결번 2건(`USR-04_008`·`USR-05_002`) 유지 · 기존 번호 변경 0 · `spec-qa.md` 계층(E2E 24)·유형(시스템예외 18)·자동화(반자동 31) 3축과 파일별 합 전부 **242** 정합 | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-26 02:46 | build | `apps/frontend/src/` — **디자인 시스템·공통 컴포넌트 11종·FE 셸·단계 상태머신 신규 구현**(정본 `design-system.md`·`design-system-components.md`). 색 토큰 23종·타이포·간격·모션 값을 그대로 이식(`styles/tokens.css`) — 실측 대비비 27행 재계산 전건 표와 일치. 컴포넌트 11종(`components/`) 전량 신설(NoticeBlock·ConsentList·ConsentItem 포함). 단계 상태머신(`stage/useInterlockFlow.ts`)이 `/approve` 접점(`api/client.ts`) 응답을 서버가 정한 대로 전이시킨다 — 승인 요청은 발신 즉시 `PROCESSING`(SCR-003) 으로 전환하고 응답으로 `RESULT`·`BackToConsent`(`EX-BIZ-001`·`EX-BIZ-003`)·`BackToIdentity` 를 가른다. **승인 버튼은 `aria-disabled`(네이티브 `disabled` 아님)로 클릭을 계속 받아 유효성 안내를 띄운다**(상위 제약 12 — Button 컴포넌트가 이 규약을 지원). `ConsentList` 는 잠금(제출 중) 상태를 두지 않는다(사양 확정과 합치). `ConsentScreen`(`flow/`)에서 필수 미충족 시 화면 게이팅(서버 미호출)·서버 재검증 알림을 같은 알림 영역에 통합. `npm run build`(`tsc -b && vite build`) 에러 0건 **[회귀 2회차]**(S-1, `#491` 자체보완) — `stage/transitions.ts` 의 `viewAfterApprove` 성공 분기에서 `resultPath: data.resultPath` 를 `resultPath: normalizeResultPath(data.resultPath)` 로 교체 — `/approve` 응답도 `/verify` 와 같은 HTTP JSON 무검증 캐스팅 경로를 거치므로 같은 정규화가 필요했다(USR-03.md 도 같은 시정, 상세 근거 동일). `npm --prefix apps/frontend run build` 에러 0건 | `accountinterlockhub#491`·`#469` | 🚧 |
| 2026-07-26 00:45 | spec ⓒ | (공통 반영) 요구사항 회귀 — **사용자 거부 결과 경로 폐지**. `service_SVC-003.md`·`process_PROC-103.md`·`screen_SCR-002.md` — 전 항목 동의가 진행 조건이 되어 **거부 제출 인터페이스가 사라졌고**(`BIZ-003-03`), 미동의 제출은 화면 유효성 안내(`Gated` — "모든 항목에 동의해 주셔야 합니다")에서 끝나 **접점에 도달하지 않는다**. 종료 수단은 **창 닫기**이며 그 연동은 **결과 미확정**으로 남는다(증적·전달·결과 구분 카운터 전부 0). `PROC-103` **`F4`·`B5a` 결번**(뒤 라벨 미당김) · **`BR-004` 슬롯 내용 교체**(승인·거부 선택 → 필수 동의 충족 판정 = 화면 게이팅 1차 방어) · 승인 요청 `decision` 필드 **폐지**(값이 하나뿐이라 분기가 성립하지 않는다). 디자인시스템 연쇄 — `secondary` 버튼·ConsentList **잠금 상태 사용처 소멸**(승인 제출은 카드가 SCR-003 으로 교체돼 스피너·폼 잠금이 이 화면에 서지 않는다). TC `USR-04_016` 신설(거부·취소 종료 수단 부재 확인) · `USR-04_008` **결번** — common.md | `accountinterlockhub#469`·`#470`·`#471`·`#473`·`#474`·`#475`·`#476` | ℹ️ |
| 2026-07-25 22:00 | directing | (방향 부분 갱신) **사용자 거부 종료 폐지 — 전 항목 동의가 진행 조건** — 담당자 지시 `#467` journal 3285. 미동의 제출은 동의 화면의 **유효성 안내**("모든 항목에 동의해 주셔야 합니다")로 막고 결과 단계로 보내지 않는다. 동의하지 않으려는 사용자의 종료 수단은 **창을 닫는 것**이며 그 연동은 **결과 미확정**으로 남아 보관 기한에 삭제된다(거부라는 확정 결과를 쓰지 않는다). `docs/prd/PRD.md` §서비스 이용 대상 3·§제공 가치·§수행 범위 4·9·11 · `ia/IA.md` USR-04·BAT-06 · `devspec/database.md` §추적 레코드 갱신·§결과 구분(거부 값 미도입)·§지표 집계. **역방향 전파 대상** = `BIZ-003` 게이팅·`BR-004`·`PROC-103` `B5a`·`SCR-002` 거부 버튼·`spec-functions-api-user.md` `decision` 필드·`FN-012`·`tc_USR-04`·`mockup/SCR-002.html` | `accountinterlockhub#467`·`#469` | 🚧 |
| 2026-07-25 18:39 | spec ⓒ | (공통 반영) 사양 확정분 목업 동기화 — `mockup/SCR-002.html` ConsentList 잠금(제출 중) 시각 처리를 `opacity:.6` → 비활성 토큰 쌍(`--color-disabled-bg`/`--color-disabled-fg`, 4.82:1 재사용) + `aria-busy="true"` 로 교체(확정 사양 반영). Redmine `#477` 첨부 최신화 — common.md | `accountinterlockhub#477` | ℹ️ |
| 2026-07-25 18:16 | spec ⓒ | (공통 반영) 목업 리뷰 발견 사항 반영(회귀 3회차) — 인라인 알림 위치를 **화면 사양의 알림 영역 한 곳**으로 통일(오류 유형별 이동 금지 · "카드 상단" 서술 폐기)·ConsentList **잠금(제출 중) 시각 처리 확정**(비활성 토큰 쌍 재사용·대비비 4.82:1·`opacity` 금지)·InlineAlert 사용 화면 목록에 SCR-003 추가. **M-1(`DECRYPT_FAILED` 레코드 확정 경로 복원) 미반영 — 구현 불가 판단으로 차단 보고** — common.md | `accountinterlockhub#477`·`#474` | ℹ️ |
| 2026-07-25 18:01 | spec | `mockup/SCR-002.html` 신규 — **동의·승인 화면 목업**. NoticeBlock(안내 문구 상수, 값 없을 때 영역 미렌더 변형 포함)·ConsentList(1건도 목록 렌더, `필수` 배지)·승인 버튼 `aria-disabled` 토글(체크박스 실시간 반영)·거부 버튼 아래 "되돌릴 수 없음" 고정 문구. 6개 상태(초기미동의·동의완료·안내문구없음·필수미충족오류·거부처리중·처리오류) | `accountinterlockhub#477`·`#469` | 🚧 |
| 2026-07-25 17:01 | spec ⓒ | (공통 반영) 교차검증 1회차 지적 보완(회귀 1회차 — 🟠 2·🟡 7 전건 해소) — `SCR-002` 상태 전이의 `PROC-104` 단계명을 확정명 **복호화 판정(재복호화)** 로 정정 — common.md | `accountinterlockhub#474` | ℹ️ |
| 2026-07-25 16:19 | spec ⓒ | (공통 반영) 검증 TC 도메인 신규 작성 — `docs/specs/qa/` **TC 232건**(커버리지 SVC 18/18 · PROC 18/18 · BR 24/24 · EX 13/13 · 정책 17/17 · 접점 11/11 — 미검증 0)·**횡단 시나리오 `SCEN_001`~`SCEN_023`**(여정 6 · 규약 대칭 왕복 3 · 기동·상수 검증 5 · 무저장 원칙 2 · 설계된 부재·수용 리스크 7)·**AUTH 치환 12건**·모의 수신처·C# 하네스·시드 카탈로그 37항·미해소 1건(성공률 정의 이월) — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-25 15:48 | spec | `docs/specs/qa/USR/tc_USR-04.md` 신규 — **동의·승인 TC 15건**(웹·SPA UI + API·서버). 승인 = 증적 1건 + 전달 + 경로 ① / **거부 = 증적 0건·전달 0건·진행 화면 미경유·경로 ②** · 필수 동의 **서버 재검증**(화면 비활성은 1차 방어)·`decision` 값 이탈·없는 항목 코드·**필수/선택 경계**(선택만 체크 시 400)·거부의 `agreedItemCodes` 미검증 경계·**증적 기록 실패 시 전달 0건**(`BIZ-003-04`)·확정 레코드 승인 재제출 시 결과 불변·승인 시점 재복호화 실패 → SCR-001 되돌림·**문구 무가공 대조**(상수↔화면↔증적 스냅샷)·안내 문구 빈 값/400자 3단락 경계·동시 승인 2건 → 증적 정확히 1행·거부 후 재진입 경로 ② 재안내. 검증 BR-002·BR-004·BR-005·BR-022 · EX 7종 | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec | `process_PROC-103.md` 신규 — **PROC-103 동의·승인 제출**(RR·WF·동기·트리거=SCR-002 승인/거부). 승인 제출 하나가 재복호화·증적·전달·결과 확정을 발화시키는 **오케스트레이션 지점**. `F1` 상수 무가공 렌더 → `B2` PROC-104 복호화 구간 호출 → `B3` 레코드 확보 → `B4` 확정 재안내 → **`B5a` 거부=`USER_DENIED` 확정(증적·전달 없음) / `B5b` 필수 동의 서버 재검증(항목 코드 실재까지 확인)** → `B6` 추적 레코드 `FOR UPDATE` 잠금 후 증적 기록 → `B7` 전달 구간 → `B8` 결과 안내(502 `EX-BIZ-002` 도 정상 종료). 12 단계 | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec ⓒ | (공통 반영) 프로세스 도메인 신규 작성 — `docs/specs/processes/` **PROC 18종 확정**(정책·서비스 예약분을 같은 코드·같은 의미로 — 통합·폐기·추가·재배치 0)·**단계 라벨 체계 확정**(`F` FE·`B` BE·`C` 호출측·`L` 라이브러리 내부, 레이어별 1부터 시간 순, 분기는 접미 문자)·선행 도메인 단계명 전건 라벨 매핑·의존관계 순환 0(호출 깊이 3층)·트랜잭션 경계와 멱등 수단 총괄·정책 규칙 86건 매핑(런타임 80 + 게이트·절차 6) — common.md | `accountinterlockhub#475`·`#469` | ℹ️ |
| 2026-07-25 14:40 | spec | `screen_SCR-002.md` 신규 — **SCR-002 동의·승인**(단계 `CONSENT` · 트리거 PROC-103). 상수 유래 항목명·설명·필수 여부·안내 문구를 **가공 없이 그대로** 노출(`DATA-003-05`)하고 항목은 **1건이어도 목록으로 렌더**(상수로 늘 수 있다). 안내 영역은 **최대 400자·최대 3단락·평문 전제로 접기·더 보기 없이 단락 그대로**, **값이 비면 영역 자체를 미렌더**(`EXC-BIZ-07`). 필수 미충족 시 승인 버튼 비활성 = **1차 방어**이고 판정 근거는 서버 재검증(`BIZ-003-01`·`BIZ-003-02`). **거부는 진행 화면을 거치지 않고**(전달·증적이 없어 기다림이 없다 — `BIZ-003-03`) 버튼 안 진행 표시로 처리하며, **확인 대화상자 대신** 되돌릴 수 없음을 버튼 아래 한 줄로 알린다(모달 미도입 = 포커스 가둠 0). 본인확인으로 되돌아가면 **동의 선택을 초기화**(승인은 제출 시점의 명시적 행위 — `DATA-003-04`). 동의 항목 버전 식별자·추적 키 미표시 | `accountinterlockhub#474`·`#469` | 🚧 |
| 2026-07-25 14:40 | spec ⓒ | (공통 반영) 화면 도메인 신규 작성 — `docs/specs/screens/` **화면 4건**(SCR-001~004 — 서비스 예약 대비 통합·폐기·추가·재배치 0)·**단일 경로 4단계 모델**(리다이렉트 없음·화면별 URL 없음)·**디자인 시스템 1벌**(표면이 하나라 토큰 분기·접두어 없음 · 색 23종 **실측 대비비 표** · WCAG 2.1 AA · 모바일 우선 2 브레이크포인트 · 히트 영역 44x44)·공통 컴포넌트 11종·**화면 없는 IA 노드 12건 근거 기재** — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-25 14:10 | spec | `spec-functions-api-user.md` §동의·승인 제출 · `function_FN-012-013.md` §FN-012 — **`POST <INTERLOCK_ENTRY_PATH>/approve` 확정**. **승인·거부 전송 표현 확정 = `decision` 필드에 `APPROVE`·`REJECT`**(도메인 표현을 그대로 전송 표현으로 써 변환 지점을 없앤다 — `BIZ-001-03` 정신) + `agreedItemCodes` 문자열 배열. 필수 동의 미충족·`decision` 값 밖·스냅샷에 없는 항목 코드는 400 `EX-BIZ-001`(결과 미확정). 승인 확정 시 **FN-012** 가 동의 증적 1건을 승인과 **같은 처리 경계**에서 기록(스냅샷 = `notice` + `items` 코드 오름차순, 버전 식별자는 기동 산출값 재사용). 증적 실패 시 500 `EX-BIZ-003`·결과 미확정·전달 미수행. 거부는 `USER_DENIED` 확정 후 결과 경로 ②(증적 미생성·전달 미수행) | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-25 14:10 | spec ⓒ | (공통 반영) 기능·API 도메인 신규 작성 — `docs/specs/functions/` **공통 기능 15건**(FN-001~015 · POL 5·DAT 8·CRS 2·**EXT 0**)·**인터페이스 11접점**(사용자 진입 표면 3 · 서버 대면 API 4 · 수신처 전달 1 · 명령 진입점 1 · 라이브러리 공개 진입점 2)·오류 응답 엔벨로프 `{code,message,details?}`·인증 0/요청 제한 0·**신규 EX `EX-OPS-002` 1건**을 정책 카탈로그에 등록 — common.md | `accountinterlockhub#473`·`#469` | ℹ️ |
| 2026-07-25 13:35 | spec ⓒ | (공통 반영) 데이터 도메인 신규 작성 — `docs/specs/datas/` **엔터티 3건**(ENT-001~003 = 저장 대상 셋)·**데이터 모델 22종**(MDL-001~022 확정 — 예약 대비 통합·추가·재배치 0)·인덱스 5·물리 FK 0·소프트 삭제 미도입 — common.md | `accountinterlockhub#472`·`#469` | ℹ️ |
| 2026-07-25 13:05 | spec | `service_SVC-003.md` 신규 — **SVC-003 동의·승인**(매핑 PROC-103·PROC-302). Happy Path 6단계(항목 노출→동의 선택→승인 제출→서버 재검증→동의 증적 기록→연동 실행 이관). BR-004 = 승인/거부(거부는 `USER_DENIED` 확정·증적 미생성·전달 미수행), BR-005 = 필수 동의 충족 서버 재검증. 예외 `EX-BIZ-001`(필수 미충족)·`EX-BIZ-003`(증적 기록 실패 → 연동 미진행). 동의 항목·안내 문구는 상수(MDL-008)를 **그대로** 노출하고 화면이 가공하지 않는다. 선택 항목은 승인을 막지 않으며 문구 부재도 게이팅 조건이 아니다 | `accountinterlockhub#471`·`#469` | 🚧 |
| 2026-07-25 13:05 | spec ⓒ | (공통 반영) 서비스 도메인 신규 작성 — `docs/specs/services/` **서비스 18건**(SVC-001~018)·사용자 역할 4종·분기 `BR-001`~`BR-024`·MDL 22종/SCR 4종 예약 채번·`PROC-403`·`PROC-404` 추가 채번 — common.md | `accountinterlockhub#471`·`#469` | ℹ️ |
| 2026-07-25 12:28 | spec | `policy_BIZ.md` `BIZ-003` 동의·승인 게이팅 + `policy_DATA.md` `DATA-003` 개인정보 제3자 제공 동의 신설 — 노출 항목은 `<CONSENT_ITEMS>` 상수가 정본이고 화면이 문구를 가공하지 않는다(`DATA-003-05`). 필수 항목 전부 동의해야 승인 제출 가능하며 **서버가 재검증**(미충족 400 `EX-BIZ-001`). 거부는 `USER_DENIED` 확정·복호화/전달 미수행·증적 미생성. 승인 확정 시 동의 증적 1건 생성, 실패 시 500 `EX-BIZ-003` 로 연동 중단. 근거 법규 = 개인정보 보호법 제17조(개인정보의 제공). `<CONSENT_NOTICE>` 잠정 문구·검토 주체 담당자(EXC-DATA-07) | `accountinterlockhub#470`·`#469` | 🚧 |
| 2026-07-25 12:28 | spec ⓒ | (공통 반영) 정책 도메인 신규 작성 — `docs/specs/policies/` 정책 17건·규칙 86건·EX 코드 12종(암호화 연동 규약·복호화 판정 4단계·결과 구분 4종·추적 레코드 생성 시점·보관·무저장·동의 증적·인증 부재·수용 리스크) — common.md | `accountinterlockhub#470`·`#469` | ℹ️ |
| 2026-07-25 11:25 | directing ⓒ | (공통 반영) 값 확정 대기 잠정값 설정 — 동의 안내 문구 `<CONSENT_NOTICE>` 잠정 문구 유지·표기 정합(허용 형태 = 최대 400자·3단락·평문), 확정 시 상수 값만 교체(화면 사양 개정 불요) — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
| 2026-07-25 10:05 | directing ⓒ | (공통 반영) 리셋 방향 담당자 확정분 반영 — 동의 항목 노출·동의/거부·승인을 별도 기능단위로 **신설**(구 `USR-01` 에서 분리). 동의 항목 상수화(1건 `THIRD_PARTY_PROVISION`)·동의 증적(`BAT-05`) 연계 — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
