# strategies-ai-pm-context-overload (2026-07-06)

> 개정한 운영 문서의 변경 경위. 본문 정본은 해당 문서.

- **무엇**:
	- [`ai-pm.md`](../../../ai/strategies/ai-pm.md) §세션 리셋 — **컨텍스트 과부하 경고(300k) 신설**: 세션 누적 컨텍스트가 300k 토큰을 넘어선 시점부터 리셋 전까지 모든 Slack 응답 말미에 과부하 경고를 덧붙인다(전체 멘션 `<!channel>` 포함, 스레드 응답은 `reply_broadcast` 로 채널 노출, 초기화 명령 안내 동반). "담당자 지시 재기동"을 **초기화 명령 재기동**으로 정비 — 트리거를 초기화 명령("ai-pm 초기화" 류) 인입으로 명문화하고, 리셋 게시에 in-flight 디스패치 목록 포함을 추가.
	- [`ai/bots/ai-pm/ai-pm.md`](../../../ai/bots/ai-pm/ai-pm.md) §처리 절차 — 의도 분류에 초기화 명령 추가(리셋 절차 연결), 컨텍스트 상시 점검·임계 초과 시 경고 부착 단계(7) 추가.
- **왜**: 담당자 지시(2026-07-06) — 봇 세션의 컨텍스트 누적 대응이 자동 `/compact`·수동 재기동뿐이라 임계 기반 대응이 없었음. 300k 초과 시 모든 사용자가 인지하도록 경고를 노출하고, 초기화 명령으로 즉시 리셋 가능하게 개선.
- **영향**: 기존 자동 리셋 주기(`/compact`)·래퍼 `.restart` 재기동 구조는 그대로다(래퍼 변경 없음 — 초기화 명령은 기존 플래그 경로 사용). 구동 중인 세션에는 다음 리셋부터 반영된다.
- **관련 일감**: 없음 (담당자 직접 지시 세션).
