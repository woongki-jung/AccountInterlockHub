# config-ready-flag-notice (2026-07-03)

> 개정한 운영 문서의 변경 경위. 본문 정본은 해당 문서.

- **무엇**:
	- [`CLAUDE.md`](../../../CLAUDE.md) §워크스페이스 구성 준비 — **준비완료 플래그** 정의(`CLAUDE.local.md` §준비 상태의 `준비완료` 값 `예`/`아니오`, 담당자가 etc/init 완주 후 직접 갱신)와 **안내 노출 규칙** 추가: 매 응답 시작 시 `예` 가 아니면(값 미기재·파일 부재 포함) **부족한 설정 내역을 항목별로 나열하고 보완 후 세션 재시작을 안내하는 블록**을 노출한다. 내역 출처는 §준비 상태 잔여 메모 + 자동 로드 설정에서 직접 확인되는 공백(미기재 키·`TBD` 잔존 등)이며, `etc/init/` 파일은 열지 않는다(같은 날 후속 지시로 최초의 한 줄 경고 양식을 이 블록 양식으로 개정).
	- `CLAUDE.local.md`(git 비관리) — §준비 상태 섹션 신설, 이 PC 현재값 `아니오 — 잔여: Slack 구성`.
	- `etc/init/` 배선(사람 전용) — 02-workspace 의 CLAUDE.local.md 생성 항목에 §준비 상태 포함(초기값 `아니오`), 05-verify 마지막에 "준비완료 플래그 `예` 설정" 항목, README §다른 PC 에 플래그 재시작 언급.
- **왜**: 담당자 지시(2026-07-03) — 같은 날 [`config-init-checklist-replacement.md`](config-init-checklist-replacement.md) 로 응답 시작 경고 규칙이 사라진 것을, 체크리스트 파일 파싱 대신 **CLAUDE.local.md 의 준비완료 값으로 안내 노출을 판정**하는 방식으로 복원. CLAUDE.local.md 는 매 세션 자동 로드 + git 비관리(PC별)라, 에이전트가 `etc/init/` 를 참조하지 않고도 PC 준비 상태를 알 수 있다.
- **영향**: 준비 누락 감지가 자동 안내로 복원된다(체계 대체 개정의 "경고 규칙 소멸" 영향을 이 개정이 상쇄). 플래그는 수동 갱신 — 에이전트가 etc/init 체크 상태를 검사하지 않으므로 실제 준비 상태와 플래그의 정합은 담당자 책임.
- **관련 일감**: 없음(담당자 직접 요청 세션).
