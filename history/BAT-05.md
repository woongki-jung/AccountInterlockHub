# IA: BAT-05 — 동의 증적 기록

> 본 IA 노드와 연관된 작업의 시계열 이력(최신순). 정책: ai/strategies/ia-history.md

| 일시 (KST) | 단계 | 산출물·결과 | 관련 일감 | 상태 |
|---|---|---|---|---|
| 2026-07-25 16:19 | spec ⓒ | (공통 반영) 검증 TC 도메인 신규 작성 — `docs/specs/qa/` **TC 232건**(커버리지 SVC 18/18 · PROC 18/18 · BR 24/24 · EX 13/13 · 정책 17/17 · 접점 11/11 — 미검증 0)·**횡단 시나리오 `SCEN_001`~`SCEN_023`**(여정 6 · 규약 대칭 왕복 3 · 기동·상수 검증 5 · 무저장 원칙 2 · 설계된 부재·수용 리스크 7)·**AUTH 치환 12건**·모의 수신처·C# 하네스·시드 카탈로그 37항·미해소 1건(성공률 정의 이월) — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-25 16:05 | spec | `docs/specs/qa/BAT/tc_BAT-05.md` 신규 — **동의 증적 기록 TC 12건**(API·서버 — 증적 조회 API 가 없어 DB 직접 확인). 승인 확정 시 1건 INSERT(6컬럼)·**승인 외 6 계기 전건 증적 0**(거부·본인확인 실패·진입 실패·필수 미충족·재복호화 실패·자가진단)·**스냅샷 = 화면 노출 내용 그대로·`code` 오름차순**·`consent_version` 은 기동 시 산출값 재사용(재계산 금지)·**항목/문구 1자 변경 시 식별자 변경**·**정규화 경계**(속성 순서·공백·배열 순서·끝 개행만 다르면 같은 식별자 / 문구 중간 공백은 다른 식별자)·CHECK 소문자 16진수 64자·`agreed_item_codes` ⊆ 스냅샷 코드·**개인정보 컬럼 0**·생성 후 불변(U 수행 PROC 0)·**같은 추적 키 증적 2건 공존**(대리 키 근거)·INSERT 실패 시 전달 0건·되돌림. 검증 BR-004·BR-022 · EX `EX-BIZ-003` | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec | `process_PROC-302.md` 신규 — **PROC-302 동의 증적 기록**(EVT·호출측 트랜잭션 참여·**PROC-103 `B6` 이 유일한 호출 지점**). `B1` 승인 계기·항목 코드 실재 재검증 → `B2` **기동 시 산출한 버전 식별자를 그대로 사용**(기록 시점 재계산 금지) → `B3` 스냅샷(코드 오름차순·문구 무가공) → `B4` INSERT 1건. 상위가 추적 레코드 행을 잠근 상태에서 호출해 중복 증적 차단·증적 테이블 유일 제약 부재 근거 명시. **생성 후 불변**(갱신 경로 없음) | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec ⓒ | (공통 반영) 프로세스 도메인 신규 작성 — `docs/specs/processes/` **PROC 18종 확정**(정책·서비스 예약분을 같은 코드·같은 의미로 — 통합·폐기·추가·재배치 0)·**단계 라벨 체계 확정**(`F` FE·`B` BE·`C` 호출측·`L` 라이브러리 내부, 레이어별 1부터 시간 순, 분기는 접미 문자)·선행 도메인 단계명 전건 라벨 매핑·의존관계 순환 0(호출 깊이 3층)·트랜잭션 경계와 멱등 수단 총괄·정책 규칙 86건 매핑(런타임 80 + 게이트·절차 6) — common.md | `accountinterlockhub#475`·`#469` | ℹ️ |
| 2026-07-25 14:40 | spec ⓒ | (공통 반영) 화면 도메인 신규 작성 — `docs/specs/screens/` **화면 4건**(SCR-001~004 — 서비스 예약 대비 통합·폐기·추가·재배치 0)·**단일 경로 4단계 모델**(리다이렉트 없음·화면별 URL 없음)·**디자인 시스템 1벌**(표면이 하나라 토큰 분기·접두어 없음 · 색 23종 **실측 대비비 표** · WCAG 2.1 AA · 모바일 우선 2 브레이크포인트 · 히트 영역 44x44)·공통 컴포넌트 11종·**화면 없는 IA 노드 12건 근거 기재** — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-25 14:10 | spec | `function_FN-012-013.md` §FN-012 — 동의 증적 기록 기능 확정. **승인 확정 계기에서만 1건 INSERT**(거부·본인확인 실패·진입 실패는 미생성 — 계기가 승인이 아니면 500 `EX-BIZ-003`). `agreedItemCodes` 의 각 코드가 스냅샷 항목 코드에 있는지 **응용 계층에서 정합 검증**(JSONB 간 참조를 DB 제약으로 강제하지 않는 데이터 도메인 결정과 정합). 스냅샷은 `notice` + `items`(코드 오름차순)로 **화면 노출과 같은 구성 모델(`MDL-008`)에서** 만들어 증적과 실제 노출이 어긋나지 않게 한다. 버전 식별자는 **기동 시 산출값을 그대로 사용**(기록 시점 재계산 금지). 증적 기록 실패 시 결과 미확정·수신처 전달 미수행·재시도 가능 | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-25 14:10 | spec ⓒ | (공통 반영) 기능·API 도메인 신규 작성 — `docs/specs/functions/` **공통 기능 15건**(FN-001~015 · POL 5·DAT 8·CRS 2·**EXT 0**)·**인터페이스 11접점**(사용자 진입 표면 3 · 서버 대면 API 4 · 수신처 전달 1 · 명령 진입점 1 · 라이브러리 공개 진입점 2)·오류 응답 엔벨로프 `{code,message,details?}`·인증 0/요청 제한 0·**신규 EX `EX-OPS-002` 1건**을 정책 카탈로그에 등록 — common.md | `accountinterlockhub#473`·`#469` | ℹ️ |
| 2026-07-25 13:35 | spec | `data_ENT-002.md`·`model_MDL-002.md` 신규 — **ENT-002 동의 증적**(`tbl_consent_proof`, 이력·생성 후 불변). **대리 키 PK 채택**(`consent_proof_id UUID`) — 보관 기간이 지나 추적 레코드가 삭제된 뒤 같은 키가 재수신되면(EXC-BIZ-04) 새 증적이 생기는데 앞선 증적은 아직 보존 기간 안이라 **같은 추적 키의 증적이 시간차로 공존**한다. 따라서 `tracking_key` 는 키도 유일 제약도 아니다. 컬럼 6 = `consent_proof_id`·`tracking_key`·`consented_at`(동의 일시 = 생성 시각, 별도 `created_at` 미도입)·`consent_version`·`consent_snapshot`(JSONB — `notice` + `items`)·`agreed_item_codes`(JSONB 배열 — 선택 항목 동의 여부 포함). **[위임 확정] 동의 항목 버전 식별자 = 상수 값의 결정적 SHA-256 해시**(소문자 16진수 64자, `VARCHAR(64)` + 정규식 CHECK). 산출 7단계(파싱→항목 코드 오름차순 정렬→속성 순서 고정→공백 없는 JSON 재직렬화→안내 문구 우측 트림→`0x1F` 구분자 결합→SHA-256). **수동 채번 대신 해시를 택한 근거** = `DATA-003-03`(항목·문구 변경 시 식별자 동반 변경)이 자동으로 지켜지며, 수동 채번은 갱신 누락 시 같은 식별자가 다른 내용을 가리키는 위반이 조용히 발생한다. 정규화가 필요한 근거(공백·순서 차이로 같은 내용에 다른 식별자가 붙는 역방향 위반 방지)도 명시. 인덱스 2 = PK + `consented_at`(PROC-304) — **`tracking_key` 인덱스 미신설(사용 PROC 0건)**. UPDATE PROC 0건(불변)·하드 삭제·**개인정보 보호법 제17조** 명시. 스냅샷과 동의 목록을 별도 컬럼으로 나눈 근거(상수 유래 값과 사용자 입력 값의 출처 분리) 기재 | `accountinterlockhub#472`·`#469` | 🚧 |
| 2026-07-25 13:35 | spec ⓒ | (공통 반영) 데이터 도메인 신규 작성 — `docs/specs/datas/` **엔터티 3건**(ENT-001~003 = 저장 대상 셋)·**데이터 모델 22종**(MDL-001~022 확정 — 예약 대비 통합·추가·재배치 0)·인덱스 5·물리 FK 0·소프트 삭제 미도입 — common.md | `accountinterlockhub#472`·`#469` | ℹ️ |
| 2026-07-25 13:05 | spec | `service_SVC-015.md` 신규 — **SVC-015 동의 증적 기록**(매핑 PROC-302). Happy Path 5단계(계기 수신→버전 식별자 확보→스냅샷 구성→증적 기록→결과 반환), 입력 MDL-007+MDL-008 → 출력 MDL-002. BR-022 = 생성 조건(**승인 확정에만** 1건 — 거부·본인확인 실패·진입 실패는 미생성), BR-004 인용. 예외 `EX-BIZ-003`(증적 실패 시 연동 미진행). 개인정보 미포함(추적 키·동의 일시·버전 식별자·항목 내용 스냅샷만)·승인 1건당 1건 상한·추적 레코드보다 긴 보존(`<CONSENT_PROOF_RETENTION_MONTHS>` 상수 참조·수치 미복제). 선택 항목의 동의 여부도 기록 대상임을 명시 | `accountinterlockhub#471`·`#469` | 🚧 |
| 2026-07-25 13:05 | spec ⓒ | (공통 반영) 서비스 도메인 신규 작성 — `docs/specs/services/` **서비스 18건**(SVC-001~018)·사용자 역할 4종·분기 `BR-001`~`BR-024`·MDL 22종/SCR 4종 예약 채번·`PROC-403`·`PROC-404` 추가 채번 — common.md | `accountinterlockhub#471`·`#469` | ℹ️ |
| 2026-07-25 12:28 | spec | `policy_DATA.md` `DATA-003` 동의 증적 + `DATA-002-02` 신설 — 증적은 **개인정보를 담지 않는다**(연동 추적 키·동의 일시·동의 항목 **버전 식별자**+그 시점 항목 내용 **스냅샷**만). **동의 항목(`<CONSENT_ITEMS>`)·안내 문구(`<CONSENT_NOTICE>`)가 바뀌면 버전 식별자도 바뀐다**(같은 식별자가 다른 내용을 가리키는 상태 금지) — 상수 값에서 결정적으로 산출하면 자동 충족(산출 방식은 데이터 도메인 확정). 승인 확정 시점에만 1건 생성(거부·본인확인 실패·진입 실패는 미생성). 보존은 동의 일시 기산 `<CONSENT_PROOF_RETENTION_MONTHS>` 로 추적 레코드보다 길다. 항목 스냅샷은 구성의 저장이 아니라 증적(EXC-DATA-02) | `accountinterlockhub#470`·`#469` | 🚧 |
| 2026-07-25 12:28 | spec ⓒ | (공통 반영) 정책 도메인 신규 작성 — `docs/specs/policies/` 정책 17건·규칙 86건·EX 코드 12종(암호화 연동 규약·복호화 판정 4단계·결과 구분 4종·추적 레코드 생성 시점·보관·무저장·동의 증적·인증 부재·수용 리스크) — common.md | `accountinterlockhub#470`·`#469` | ℹ️ |
| 2026-07-25 11:25 | directing ⓒ | (공통 반영) 값 확정 대기 잠정값 설정 — 동의 증적 보존 기간 `<CONSENT_PROOF_RETENTION_MONTHS>` 잠정 60개월 유지 확인, 법령·계약 확인 후 배포 전 확정 — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
| 2026-07-25 10:05 | directing ⓒ | (공통 반영) 리셋 방향 담당자 확정분 반영 — 동의 증적 보존 **신설**(담당자 확정). 추적 키·동의 일시·동의 항목 버전+내용 스냅샷·개인정보 미포함·보존 기간 상수 분리(잠정 60개월) — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
