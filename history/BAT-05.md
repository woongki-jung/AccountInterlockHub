# IA: BAT-05 — 동의 증적 기록

> 본 IA 노드와 연관된 작업의 시계열 이력(최신순). 정책: ai/strategies/ia-history.md

| 일시 (KST) | 단계 | 산출물·결과 | 관련 일감 | 상태 |
|---|---|---|---|---|
| 2026-07-26 19:40 | spec ⓒ | (공통 반영) `function_FN-012-013.md` FN-012 **시그니처에서 `at` 제거** + INSERT 를 `RETURNING consent_proof_id, consented_at` 으로 바꿈(E7-2) — `consented_at` 을 `ENT-002` 기본값 `now()`(DB 시계)로 기록해야 `PROC-103` `B6` 표지 검사가 시계 정합에 기대지 않는다. `data_ENT-002.md` §구현 가이드에 같은 요구 명문화 · `service_SVC-015.md` **BR-022 정본 술어 통일**(E7-6) · `spec-services-codes.md` BR-022 판정 축 병기 — common.md | `accountinterlockhub#486`·`#493`·`#489`·`#469` | ℹ️ |
| 2026-07-26 18:45 | spec ⓒ | (공통 반영) `f4e515b`·`964e8d0` 하류 정합 — ① `OPS-004` 를 `process_PROC-302.md` §관련 사양 코드 정책 행에 반영(`OPS-004-01` `B4` 질의 실행 · `OPS-004-03` 상위 PROC-103 `B6` 경계 참여 · `OPS-004-04` 로그) ② `spec-process.md` §트랜잭션 경계·멱등성 요약의 PROC-302 멱등 수단을 "행 잠금으로 중복 증적 차단" → **"행 잠금 + 기존 증적(전달 시도 표지) 확인"**으로 정정(잠금만으로는 뒤선 요청이 멈추지 않으며 증적 테이블에 유일 제약을 두지 않으므로 존재 검사가 불변식이다). PROC-302 자체의 "전달 미수행" 서술은 **발생 지점 한정이라 참** — 무변경 유지 — common.md | `accountinterlockhub#489`·`#494`·`#469` | ℹ️ |
| 2026-07-26 18:16 | spec | `docs/specs/datas/data_ENT-002.md`·`docs/specs/functions/function_FN-012-013.md`(FN-012) — build P09(`#486`)가 상신한 **사양 결함 5 회귀의 자산 소유 노드 반영**(주 처리는 `PROC-103`/`USR-04` 이력 참조). 두 문서가 공통으로 주장하던 *"같은 추적 키의 동시 승인은 추적 레코드 행을 잠근 상태에서 처리해 **중복 증적을 막는다**"*가 **거짓**임이 build tester 의 실측(증적 2행 · 수신처 이중 전달 · Postgres statement log 로 인터리빙 확인)으로 확정돼 정정했다 — **잠금은 두 요청의 순서만 세울 뿐 뒤선 요청을 멈추지 않는다.** 실제 불변식은 **행 잠금 + 이번 레코드 범위의 기존 증적 확인**(PROC-103 `B6`)으로만 성립한다. `ENT-002` §구현 가이드: 위 문장을 분리·정정하고 **조회 범위 한정(`consented_at >= 대상 추적 레코드의 created_at`)** 근거를 추가 — `tracking_key` 만으로 세면 §관계 정의·EXC-BIZ-04 가 정상으로 규정한 **앞선 보관 주기의 증적**에 걸려 삭제 후 재수신된 정상 연동이 승인되지 않는다. 유일 제약으로 대신할 수 **없는** 이유(같은 키 증적의 공존이 정상 — 제약 자체가 성립하지 않고 `DATA-002-02` 가 깨진다)도 명시했다. `FN-012` §구현 가이드: **"중복 증적을 막는 것은 본 기능이 아니라 호출측"**·**"여기에 존재 검사를 옮겨 넣지 않는다 — 검사는 잠금을 쥔 자리에서 해야 성립하고 본 기능은 경계를 열지 않아 그 자리가 아니다"**로 소재를 확정했고, §시그니처에 `exec` 를 그대로 넘기지 않으면 `FOR UPDATE` 뿐 아니라 **같은 경계에서 수행한 표지 검사까지 무의미해진다**(검사와 INSERT 가 다른 경계에 놓여 그 사이에 상대 요청이 끼어든다)는 결과를 덧붙였다. **🔴 미결 1건 — 인덱스**: §인덱스 정의의 *"`tracking_key` 조회 PROC **0건**"*이 이 변경으로 사실이 아니게 돼 1건(PROC-103 `B6`)으로 정정했다. 이 조회는 **추적 레코드 행 잠금을 쥔 채** 수행되므로 순차 스캔이면 행이 쌓일수록 잠금 점유가 길어진다 — **정확성은 인덱스에 의존하지 않으므로**(결과가 달라지지 않는다) 신설은 성능 판단이며, `(tracking_key, consented_at)` 복합 인덱스를 후보로 두고 **데이터 도메인 판단 대기**로 남겼다(신설 기준 "사용 PROC 1개 이상"은 이제 충족). FN-012 의사코드 5단계·시그니처·에러 계약(`EX-BIZ-003`), ENT-002 속성·스냅샷 구조·버전 식별자 산출 규칙·생명주기는 **무변경**. | `accountinterlockhub#486`·`#469` | 🚧 |
| 2026-07-26 13:52 | spec | `docs/specs/processes/process_PROC-302.md`(입력/출력 정의·실행 제약사항·`C1` 호출 계약·`B4` 주석·단계 통합 흐름 표) · `docs/specs/functions/function_FN-012-013.md` `FN-012` §구현 가이드 **+1 bullet** — **`exec` 전달 경로 표기 + 호출 계약에 인자 명시**(`b4fe224` 소유권 확정의 결손 보완 — 배경·왜 지금인지는 USR-04.md 항목). **이력·산출물 동일 commit**(작성 시점 미커밋 — 8파일 일괄). **[① `exec` 는 항상 필수]** 입력표에 **`exec`(`TxExecutor`) 행을 필수로** 신설했다 — 호출 지점이 `PROC-103` `B6` **하나뿐이고 그 자리가 늘 경계를 열므로 예외가 없다**. §실행 제약사항의 "호출측 트랜잭션에 참여한다"에 **"참여는 호출측이 넘긴 `exec`(같은 커넥션·실행자) 위에서만 성립한다"** 를 붙였고(본 프로세스는 `BEGIN`·`COMMIT`·`ROLLBACK` 을 수행하지 않고 커넥션도 스스로 얻지 않으며 받은 `exec` 를 `FN-012` 에 그대로 넘긴다), `exec` 없이 불리면 **증적만 따로 커밋**되고 요청 하나가 커넥션을 둘 점유함을 명시했다. `C1` 호출 계약의 전달 값을 `{ trackingKey, submission, consent, at, exec }` 로 고치고 **"넘기지 않으면 별도 커넥션에서 실행돼 참여가 성립하지 않고 행 잠금과도 갈린다"** 를 못박았으며, `B4` 의사코드 주석 2줄(받은 실행자 위에서 수행·`FN-012` 에 그대로 전달)과 단계 통합 흐름 표 1행(`C1` 처리 요지 → "상수 구성·`exec` 전달")을 동반 정합했다. **[② P06 구현 무영향 — 실측 확인]** 이 노드는 build **P06(`#483`)이 13:18 🟢 Resolved**(회귀 없이 1회차 통과)된 범위라, 오케스트레이터가 구현을 **직접 읽어** 판정했다 — `apps/backend/src/records/consent-proof-record.service.ts:34` 의 `recordConsentProof(executor: QueryExecutor, input)` 이 **executor 를 첫 필수 인자**로 받아 위 "항상 필수"와 **이미 일치**한다. 즉 **사양이 구현을 뒤늦게 문서화한 것**이라 **P06 재검증은 불요**이며 이 회차로 build 에 되돌아갈 일이 없다(다음 사람이 이력만 보고 판단할 수 있도록 근거를 남긴다). **[③]** `FN-012` 은 §구현 가이드에 전달 경로 한 줄(`PROC-103` `B6` → `PROC-302` → 본 기능, **받은 `exec` 를 그대로**)을 신설했다 — 상세는 USR-04.md 항목. **증적 기록 로직·스냅샷 순서(재정렬 없음)·유일 제약 부재·TC 는 무접촉** | `accountinterlockhub#475`·`#473`·`#469` | 🚧 |
| 2026-07-26 12:51 | build | `apps/backend/src/entities/consent-proof.entity.ts` — **P06 `ConsentSnapshotItem`↔`ConsentItemConfig` 구조 중복 정리**(`#483` journal 3417 §3, P02 코드리뷰 인계 사항 해소). 종전 `ConsentSnapshotItem`(entities, P02)이 `ConsentItemConfig`(config/interlock-config.types.ts, P01·MDL-008)와 4속성(`code`·`label`·`required`·`description`)을 **완전히 독립 재정의**하고 있어, `<CONSENT_ITEMS>` 상수표에 속성이 추가돼도 컴파일 타임 신호 없이 두 정의가 어긋날 수 있었다(현재는 `MDL-008` 정본과 정확히 일치해 정합이지만 data_ENT-002.md §구현 가이드가 경고하는 잠재 결함). **`export type ConsentSnapshotItem = Readonly<ConsentItemConfig>`** 로 교체(entities → config 방향 1방향 참조 — 순환 의존 없음, grep 으로 config 쪽이 entities 를 참조하지 않음을 실측 확인) — 구조를 재정의하지 않고 재사용해 상수표 형태가 바뀌면 `ConsentItemConfig` 갱신만으로 자동 추종한다(추가 코드 변경 불요). `ConsentSnapshot.items: readonly ConsentSnapshotItem[]` 타입 시그니처·`consent-proof-record.service.ts` 의 `items: consent.items` 대입 지점은 구조적으로 호환돼 **무변경**. `consent-proof-record.service.ts`·`metric-counter.service.ts` 에는 이 두 메서드가 각각 **PROC-302·PROC-303 전체 구현**임을 확인하는 문서 주석만 추가(로직 무변경 — P06 이 새로 만든 코드는 PROC-301 kind 디스패처 하나뿐이며 상세는 BAT-04.md 항목). **실측(실 DB — 호출측이 추적 레코드 행을 `FOR UPDATE` 로 잠근 트랜잭션 안에서 `recordConsentProof()` 를 호출하는 PROC-103 `B6` 실제 계약을 흉내냄)**: ① 증적 1건 기록 성공 ② **스냅샷 순서 = 입력 그대로(재정렬 없음) 실측** — 항목을 의도적으로 코드포인트 역순(`ITEM_C`·`ITEM_B`·`ITEM_A`)으로 넘겨 반환된 `consentSnapshot.items` 코드 순서가 **역순 그대로 보존**됨을 확인(재정렬이 남아 있었다면 정렬돼 돌아왔을 것이므로 이 결과가 "정렬 주체는 PROC-901 B4 로더 하나" 확정을 직접 증명) ③ 같은 추적 키로 증적 2건 연속 생성 → 유일 제약 없이 공존(카운트 정확히 2) ④ 승인측 트랜잭션이 의도적으로 실패하면 증적도 함께 ROLLBACK(0행 — `BIZ-003-04` "증적 없는 승인·승인 없는 증적 금지" 확인). **로케일 무관성 재확인(관측 요건 포함)** — `works/accountinterlockhub-483/verify-locale.js` 가 `LANG`/`LC_ALL`=`C`·`ko_KR.UTF-8`·`de_DE.UTF-8`·`tr_TR.UTF-8` 4개 자식 프로세스에서 `sortConsentItemsByCode()`·`computeConsentVersion()` 을 각각 실행 — **4개 로케일 전부 정렬 결과·해시 동일**, 관측 요건(`['1','2','9','A','B','ITEM10','ITEM_2','Z','a','b','z']` — ASCII 숫자→대문자→소문자, `ITEM10` 이 `ITEM_2` 보다 앞섬)과도 일치. 이 정렬·해시 함수 자체는 P01(`#478`)·P04(`#481`) 가 이미 완결했고 이번 Phase 는 **재확인만**(무변경, git log 로 `b4fe224` 이후 파일 미변경 실측). 데이터 격리·정리·기타 실측 방법론은 BAT-04.md 항목 참고. `npm --prefix apps/backend run build` 에러 0 | `accountinterlockhub#483`·`#469` | 🚧 |
| 2026-07-26 10:42 | spec | `docs/specs/processes/process_PROC-302.md` · `docs/specs/functions/function_FN-012-013.md` `FN-012` — 동의 증적 기록의 **스냅샷 재정렬 주체 중복 제거 + 트랜잭션 소유권 표기 통일**(commit `1491e4f` SD-3·SD-1 의 프로세스·기능 확산분). **근거 commit `b4fe224`** — 본 entry 작성 시점엔 미커밋이었고, 오케스트레이터가 산출물과 이 이력을 같은 commit 에 함께 넣었다(정책대로 동시 반영). **[① 재정렬 주체 중복]** `PROC-302` `B3` 와 `FN-012` 단계 3 이 **둘 다** `snapshot.items = SORT_BY_CODE_ASC(consent.items)` 로 스스로 다시 정렬하고 있어, 정렬 주체를 `MDL-008` 하나로 모은 이번 확정(`MDL-002` = `MDL-008` 결과를 **그대로 복사**·자체 정렬 금지)과 정본이 셋으로 갈리던 것을 `snapshot.items = consent.items`(재정렬하지 않는다)로 정정했다. **두 정렬 기준이 갈리는 순간 스냅샷 순서와 해시 입력 순서가 어긋난다**는 근거와 기준의 정본(`data_ENT-002.md` §버전 식별자 산출 규칙 3)을 양쪽 의사코드 주석에 남겼고, `PROC-302` 데이터 변환 흐름 표(`도메인→도메인` `B3`)·단계 통합 흐름 표(4행) 두 칸과 `FN-012` §구현 가이드·입출력 정의(`consent.items` 는 `MDL-008` 이 이미 정렬한 상태)를 동반 정합했다. **정렬 로직 신설 0 · 항목 순서 결과 자체는 무변경**. **[② 소유권]** `FN-012` 시그니처에 `exec: TxExecutor` 필수 인자 추가(모든 문을 이 실행자 위에서 실행 — 스스로 커넥션을 얻으면 호출측 경계 **밖**의 별도 커넥션을 잡는다), 단계 4·5 라벨을 **"(호출측 트랜잭션에 참여 — 새로 열지 않는다)"·"(커밋하지 않는다 — 경계를 닫는 것은 호출측이다)"** 로 교체, **경계를 여는 자리를 `PROC-103` `B6` 으로 특정**(경계 없이 호출되면 증적 기록이 승인 확정과 따로 커밋돼 **증적 없는 승인·승인 없는 증적**이 생긴다 — `BIZ-003-04`), 오류 처리표·§구현 가이드의 되돌림 주체를 호출측으로 정정. 판단 근거 전체는 BAT-04.md 항목 참고 | `accountinterlockhub#475`·`#473`·`#469` | 🚧 |
| 2026-07-26 10:26 | spec ⓒ | (공통 반영) 사양 결함 7건(SD-1~SD-7) 정정 — **동의 항목 정렬 기준(콜레이션) 확정**(SD-3). "항목 코드 오름차순"만 있고 무엇을 기준으로 한 오름차순인지가 없어, 정렬 결과가 곧 해시 입력 순서인데 로케일 인식 비교를 쓰면 **같은 배포가 환경마다 다른 `consent_version`** 을 내던 결함이다(개행 정규화와 같은 뿌리). `data_ENT-002.md` §버전 식별자 산출 규칙 3 을 정본으로 **UTF-8 바이트열을 앞에서부터 부호 없는 바이트로 견주는 사전식 순서(= 코드 포인트 오름차순)**·접두면 짧은 쪽이 앞섬으로 고정하고, 검증 가능하도록 **관측 요건**("ASCII 범위에서 숫자 → 대문자 → 소문자")과 왜 코드 포인트 비교인지(결정적 산출 요구 `PROC-901` §실행 제약사항 · 항목 코드는 사람이 읽는 문구가 아니라 식별자)를 함께 적었다. §스냅샷 구조의 `items` 정렬도 같은 기준임을 명시하고, `MDL-002` `consentSnapshot` 은 **`MDL-008` 의 정렬 결과를 그대로 복사하며 스스로 정렬하지 않는다**로 바꿔(엔터티 매핑 칸 동반 정정) **정렬 주체를 하나로** 모았다. 근거 commit `1491e4f`(이력 누락 보정) — common.md | `accountinterlockhub#473`·`#472`·`#470`·`#469` | ℹ️ |
| 2026-07-26 08:45 | build | `apps/backend/src/records/consent-proof-record.service.ts` — P04(`#481`) 회귀 1회차 [I-2] 🔴 시정. 동의 항목 스냅샷 정렬이 `.sort((a,b)=>a.code.localeCompare(b.code))`(ICU 콜레이션, 인자 없는 호출은 런타임 기본 로케일 의존)를 쓰고 있어 정본 정렬 함수(`config/interlock-config.validators.ts` `sortConsentItemsByCode` — 코드 유닛 `<`/`>` 비교, 버전 식별자 해시 산출·화면 노출이 쓰는 것과 같은 기준)와 **다른 순서**를 낼 수 있었다(리뷰어 격리 DB 실측 — `["ITEM_2","ITEM10"]` vs `["ITEM10","ITEM_2"]` 불일치 확인). 동의 항목이 현재 1건이라 잠복 상태였고, 항목 추가는 코드 변경 신호 없이 배포 상수 교체만으로 일어난다. 자체 비교자를 버리고 `sortConsentItemsByCode()` 를 그대로 재사용하도록 교체(재정렬 로직 신설 0 — 정본 1곳만 재사용). **실측**: `works/accountinterlockhub-481/verify.js` 에 `ITEM10`/`ITEM_2` 처럼 두 정렬 방식이 실제로 갈리는 케이스를 추가해 반환값·DB 저장값 모두 정본 순서(`ITEM10` 먼저)로 일치함을 확인하고, 같은 입력에서 `localeCompare` 는 실제로 반대 순서를 낸다는 대조까지 재확인(회귀 재발 방지). `recordConsentProof()` 가 **자기 트랜잭션을 열지 않고 호출측(승인 확정, `FOR UPDATE` 잠금 트랜잭션) 에 참여**하는 설계 자체는 이번 회귀로 바뀌지 않았다. `secure()`·`fixResult()` 등 트랜잭션 소유권 판단 근거·동시성 재실측 상세는 BAT-04.md 항목 참고 **[사양 델타 정합 시정 — SD-3 정렬 주체 일원화 후속(`b4fe224`), 2026-07-26]** `PROC-302` `B3`·`FN-012` 단계 3 이 `snapshot.items = consent.items`(재정렬 금지 — "정렬 주체는 `MDL-008`(로더, `PROC-901` `B4`) 하나뿐")로 확정된 뒤(`b4fe224`, 본 파일 10:42 entry) 재확인. **발견**: `consent-proof-record.service.ts` 가 회귀 1회차([I-2])에서 정본 함수 `sortConsentItemsByCode()` 를 채택했으나 그 함수를 **다시 호출**하는 형태라, 정렬 실행 지점이 로더(`interlock-config.loader.ts:91`)와 **두 곳**으로 남아 있었다 — 결과 값은 같은 함수를 두 번 호출해 우연히 같았지만 "재정렬하지 않는다"는 확정 문언과 어긋났다(구조적 미충족, 결과 값 미충족은 아니다). **시정**: 재정렬 호출과 `sortConsentItemsByCode` import 를 제거, `snapshot.items = consent.items` 로 그대로 통과시키도록 교체(사양 의사코드와 1:1). **실측(실 DB, 어드버서리얼 케이스)**: 미정렬 `CONSENT_ITEMS` 를 `loadInterlockConfig()` 에 주입 → 로더가 코드포인트 오름차순으로 정렬해 반환(단일 정렬 지점 확인) → 그 결과를 **의도적으로 역순으로 뒤집어** `recordConsentProof()` 에 전달 → 반환된 `consentSnapshot.items`·DB 저장값(JSONB) 모두 **뒤집은 순서 그대로 보존**(재정렬이 남아 있었다면 코드포인트 순으로 되돌아왔을 것이므로, 이 결과가 재정렬 제거를 직접 증명). SD-3 관측 요건도 재확인: `sortConsentItemsByCode(['b','B','2','a','A','1','Z','z','9'])` → `['1','2','9','A','B','Z','a','b','z']`(숫자→대문자→소문자) · `ITEM_2`/`ITEM10` 코드포인트 순서 `['ITEM10','ITEM_2']` vs `localeCompare` 순서 `['ITEM_2','ITEM10']` **불일치 재확인**(정본은 전자) · `LANG`/`LC_ALL`=`C`·`ko_KR.UTF-8`·`de_DE.UTF-8`·`tr_TR.UTF-8` 4개 자식 프로세스에서 `sortConsentItemsByCode()`·`computeConsentVersion()` 동일 결과(해시 동일) 확인 — 로케일 무관성. `interlock-config.loader.ts`·`consent-version.ts`(P01, `#478`)는 이미 단일 정렬 지점·코드포인트 비교·개행 정규화를 정확히 구현하고 있어 **무변경(충족 확인)**. 검증 방법론·전체 실측 상세는 BAT-04.md 항목 참고 | `accountinterlockhub#481`·`#478`·`#469` | 🚧 |
| 2026-07-26 06:19 | spec ⓒ | (공통 반영) 횡단 국소 회귀(5회차) — `model_MDL-002.md` `consentSnapshot.notice` 유효성 규칙 칸 정합(**가공·검증하지 않는다 · 화면이 노출한 원문을 그대로 보존한다 `DATA-003-05` · 개행 정규화는 해시 입력 전용이라 스냅샷에 적용하지 않는다 · 허용 형태 규정의 정본은 `MDL-022` `consentNotice`**). 4회차 §7-3 해소이며 이로써 `<CONSENT_NOTICE>` 규정이 **데이터 3모델에서 통일**됐다 — common.md | `accountinterlockhub#473`·`#470`·`#472`·`#469` | ℹ️ |
| 2026-07-26 04:57 | spec ⓒ | (공통 반영) 검증 TC 국소 회귀 — `tc_BAT-05.md` **개정 2 · 신설 0**(`BAT-05_006` 검증 대상을 8단계 재번호 정본의 **2~7**(정규화 2~6 · 구분자 결합 7)로 정정하고 `SEED-CONST-WS` **4 → 5벌**로 **개행 코드 CRLF↔LF 변형**을 추가 — 항목 설명 값·안내 문구·양쪽 **3갈래**를 각각 확인 / `BAT-05_003` 에 **스냅샷은 CRLF 원문 그대로 보존**을 한 구절 보강 — 정규화는 해시 입력에만). TC 12건 유지 — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-26 04:30 | spec | `data_ENT-002.md` §버전 식별자 산출 규칙 **국소 회귀 — 개행 코드 정규화 편입**(build P01 실측 발견 🟡). 종전 규칙에는 개행 정규화가 없어 **눈에 완전히 같은 문구도 개행 코드 차이만으로 다른 `consent_version`** 이 산출됐는데, **같은 절의 근거 문단은 이미 "같은 내용에 다른 식별자가 붙는 것은 반대 방향의 정합 위반"** 이라고 적고 있어 규칙과 근거가 서로 다른 결론을 내던 상태였다. **[확정]** 절차 통제가 아니라 **산출 규칙에 정규화를 포함**한다 — 단계 2를 신설해 `<CONSENT_ITEMS>` **파싱 결과의 모든 문자열 속성 값**과 `<CONSENT_NOTICE>` **원문** 양쪽에서 `\r\n`·`\r` 을 `\n`(LF) 하나로 통일하고, 7단계 → **8단계**로 재번호했다(자기 참조 동반 정정 — `4의 결과`·`5의 결과` → `5의 결과`·`6의 결과` · 근거 제목 `왜 정규화(2~5)가 필요한가` → `(2~6)` · §구현 가이드 `산출 규칙 3의 순서 정의` → `4`). **[근거 셋]** ① 같은 절의 근거 문단이 이미 정합(같은 내용 → 같은 식별자)을 요구하므로 규칙을 근거에 맞춘다 ② 절차 통제(배포 시 개행 고정)는 사람·편집 도구·배포 파이프라인에 의존하고 **위반해도 애플리케이션이 감지하지 못한다** — `PROC-901` `B3` 는 형태만 볼 뿐 개행 코드를 판별하지 않아 증적의 사후 입증력이 절차 준수 여부에 매달린다 ③ 개행 코드는 화면에 그려지는 문자가 아니므로 정규화가 **표시 내용을 바꾸지 않아** `DATA-003-03` 을 약화시키지 않는다. **[항목 쪽 포함 근거]** JSON 문자열 안의 개행은 **재직렬화해도 이스케이프로 살아남아** 공백 없는 JSON 화(단계 5)가 값 안의 개행을 걷어내지 못한다 — 안내 문구만 정규화하면 설명이 여러 줄인 항목에서 문제가 그대로 남는다. **[무변경]** `consent_snapshot` 은 **원문 그대로 보존**(§스냅샷 구조·`DATA-003-05` 무변경) — "정규화는 해시 입력에만 적용하고 스냅샷은 원문을 유지하며 개행 코드는 표시 내용이 아니라 둘이 어긋나지 않는다"를 같은 절에 명시해 교차검증 오독을 차단. 신설 코드 **0**(ENT·MDL·SVC·PROC·EX·BR·정책 규칙 전건) · 118줄. **[미수정 — 후속 필요]** ① `qa/BAT/tc_BAT-05.md` `BAT-05_006` 검증 대상 `산출 규칙 2~6` → `2~7` ② `apps/backend/src/config/interlock-config.validators.ts:91` 주석 `산출 규칙 2` → `3`(build) ③ `process_PROC-901.md` `B4` 요약 괄호에 개행 정규화 미기재(프로세스 doer 소관 — 참조만 하고 복제하지 않는 원칙 유지) | `accountinterlockhub#472`·`#469` | 🚧 |
| 2026-07-26 02:10 | build | `apps/backend/migrations/0001_create_storage_tables.{up,down}.sql`(ENT-002 DDL 포함)·`apps/backend/src/entities/consent-proof.{entity,model}.ts`(MDL-002) 신규 — P02(같은 커밋, BAT-04.md 항목 참고 — DB 접속 공통 계층은 3테이블 공유). `tbl_consent_proof` 정확히 6컬럼: 대리 키 PK `consent_proof_id UUID DEFAULT gen_random_uuid()`(PostgreSQL 13+ 기본 제공이라 확장 불요)·CHECK `ck_consent_proof_consent_version_format`(소문자 16진수 64자 정규식)·`ck_consent_proof_snapshot_is_object`·`ck_consent_proof_agreed_items_is_array`(JSONB 타입 검사)·`ck_consent_proof_tracking_key_length`. 인덱스 1 = `idx_consent_proof_consented_at`(`tracking_key` 인덱스는 사용 PROC 0건이라 미신설, data_ENT-002.md 그대로). MDL-002 는 전 필드 직접 매핑(파생 없음·생성 후 불변). `consent_snapshot`·`agreed_item_codes` 는 구조화 타입(`ConsentSnapshot`·`ConsentSnapshotItem`)으로 정의했고, PROC-302(P06) 실구현 시 pg 파라미터 바인딩에서 배열이 Postgres 배열 리터럴로 오변환되는 함정(JSON.stringify 필요)을 코드 주석으로 남겨 인계했다. **실측**: 실접속으로 `consent_version` CHECK 경계(64자 소문자 허용/대문자·63자 거부)·같은 `tracking_key` 증적 2건 공존(유일 제약 없음) 확인. ⚠️ 저장 3종 원칙 발견사항(구 스키마 잔존)은 BAT-04.md 항목 참고 — 본 Phase 범위 밖. **회귀 1회차**(독립 코드리뷰 Important 1건 시정 — 기능검증 12/12 🟢 통과 유지, DB 거동 무변경): `ConsentSnapshotItem`·`ConsentSnapshot`(entity.ts)·`ConsentProofRow`(entity.ts)·`ConsentProofModel`(model.ts) **4개 인터페이스 전 속성에 `readonly` 부여**(배열 속성은 `readonly T[]`) — 종전엔 4개 인터페이스 전 속성이 가변(`readonly` 0개)이었다. model_MDL-002.md §구현 가이드 "스냅샷과 동의 목록을 읽기 전용 구조로 둔다"·"생성 후 불변이라는 성질을 타입으로 강제하면 갱신 코드가 생기지 않는다" 미이행 지적 시정 — 근거는 data_ENT-002.md "수정 조건: 없다"(U 수행 PROC 0건, 개인정보 보호법 제17조 입증 수단 성립 근거). `toConsentProofModel`·`toConsentProofRow`(model.ts) 매핑 함수는 객체 리터럴 반환이라 무변경 확인. **실측**: `npx tsc --noEmit`(backend) exit 0(readonly 부여가 기존 매핑 코드를 깨지 않음 확인) · 루트 `npm run build` 에러 0건. 자체보완 2건(DATE 파서 주석 정정·`migration:revert` 프록시)은 BAT-04.md·BAT-06.md 항목 참고 | `accountinterlockhub#479`·`#469` | 🚧 |
| 2026-07-26 00:45 | spec ⓒ | (공통 반영) 요구사항 회귀 — 증적 미생성 계기의 표현 교체. `service_SVC-015.md`·`data_ENT-002.md`·`process_PROC-302.md` — 계기 목록에서 "거부"가 사라지고 **미동의 제출·미동의 이탈(창 닫기)** 로 바뀌었다(`DATA-003-04`·`BIZ-003-03`). **증적은 승인 확정 계기에서만 1건**이라는 `BR-022` 규칙은 그대로이고, `BR-004` 는 슬롯 내용이 **필수 동의 충족 판정(화면 게이팅)** 으로 교체돼 미충족 분기가 이 서비스를 발화시키지 않는다. TC `tc_BAT-05.md` — 거부 제출 단계를 **화면 게이팅으로 요청이 발신되지 않는 단계**로 교체(TC 신설·결번 0) — common.md | `accountinterlockhub#469`·`#470`·`#471`·`#472`·`#475`·`#476` | ℹ️ |
| 2026-07-25 16:19 | spec ⓒ | (공통 반영) 검증 TC 도메인 신규 작성 — `docs/specs/qa/` **TC 232건**(커버리지 SVC 18/18 · PROC 18/18 · BR 24/24 · EX 13/13 · 정책 17/17 · 접점 11/11 — 미검증 0)·**횡단 시나리오 `SCEN_001`~`SCEN_023`**(여정 6 · 규약 대칭 왕복 3 · 기동·상수 검증 5 · 무저장 원칙 2 · 설계된 부재·수용 리스크 7)·**AUTH 치환 12건**·모의 수신처·C# 하네스·시드 카탈로그 37항·미해소 1건(성공률 정의 이월) — common.md | `accountinterlockhub#476`·`#469` | ℹ️ |
| 2026-07-25 16:05 | spec | `docs/specs/qa/BAT/tc_BAT-05.md` 신규 — **동의 증적 기록 TC 12건**(API·서버 — 증적 조회 API 가 없어 DB 직접 확인). 승인 확정 시 1건 INSERT(6컬럼)·**승인 외 6 계기 전건 증적 0**(거부·본인확인 실패·진입 실패·필수 미충족·재복호화 실패·자가진단)·**스냅샷 = 화면 노출 내용 그대로·`code` 오름차순**·`consent_version` 은 기동 시 산출값 재사용(재계산 금지)·**항목/문구 1자 변경 시 식별자 변경**·**정규화 경계**(속성 순서·공백·배열 순서·끝 개행만 다르면 같은 식별자 / 문구 중간 공백은 다른 식별자)·CHECK 소문자 16진수 64자·`agreed_item_codes` ⊆ 스냅샷 코드·**개인정보 컬럼 0**·생성 후 불변(U 수행 PROC 0)·**같은 추적 키 증적 2건 공존**(대리 키 근거)·INSERT 실패 시 전달 0건·되돌림. 검증 BR-004·BR-022 · EX `EX-BIZ-003` | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec | `process_PROC-302.md` 신규 — **PROC-302 동의 증적 기록**(EVT·호출측 트랜잭션 참여·**PROC-103 `B6` 이 유일한 호출 지점**). `B1` 승인 계기·항목 코드 실재 재검증 → `B2` **기동 시 산출한 버전 식별자를 그대로 사용**(기록 시점 재계산 금지) → `B3` 스냅샷(코드 오름차순·문구 무가공) → `B4` INSERT 1건. 상위가 추적 레코드 행을 잠근 상태에서 호출해 중복 증적 차단·증적 테이블 유일 제약 부재 근거 명시. **생성 후 불변**(갱신 경로 없음) | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec ⓒ | (공통 반영) 프로세스 도메인 신규 작성 — `docs/specs/processes/` **PROC 18종 확정**(정책·서비스 예약분을 같은 코드·같은 의미로 — 통합·폐기·추가·재배치 0)·**단계 라벨 체계 확정**(`F` FE·`B` BE·`C` 호출측·`L` 라이브러리 내부, 레이어별 1부터 시간 순, 분기는 접미 문자)·선행 도메인 단계명 전건 라벨 매핑·의존관계 순환 0(호출 깊이 3층)·트랜잭션 경계와 멱등 수단 총괄·정책 규칙 86건 매핑(런타임 80 + 게이트·절차 6) — common.md | `accountinterlockhub#475`·`#469` | ℹ️ |
| 2026-07-25 14:40 | spec ⓒ | (공통 반영) 화면 도메인 신규 작성 — `docs/specs/screens/` **화면 4건**(SCR-001~004 — 서비스 예약 대비 통합·폐기·추가·재배치 0)·**단일 경로 4단계 모델**(리다이렉트 없음·화면별 URL 없음)·**디자인 시스템 1벌**(표면이 하나라 토큰 분기·접두어 없음 · 색 23종 **실측 대비비 표** · WCAG 2.1 AA · 모바일 우선 2 브레이크포인트 · 히트 영역 44x44)·공통 컴포넌트 11종·**화면 없는 IA 노드 12건 근거 기재** — common.md | `accountinterlockhub#474`·`#469` | ℹ️ |
| 2026-07-25 14:10 | spec | `function_FN-012-013.md` §FN-012 — 동의 증적 기록 기능 확정. **승인 확정 계기에서만 1건 INSERT**(거부·본인확인 실패·진입 실패는 미생성 — 계기가 승인이 아니면 500 `EX-BIZ-003`). `agreedItemCodes` 의 각 코드가 스냅샷 항목 코드에 있는지 **응용 계층에서 정합 검증**(JSONB 간 참조를 DB 제약으로 강제하지 않는 데이터 도메인 결정과 정합). 스냅샷은 `notice` + `items`(코드 오름차순)로 **화면 노출과 같은 구성 모델(`MDL-008`)에서** 만들어 증적과 실제 노출이 어긋나지 않게 한다. 버전 식별자는 **기동 시 산출값을 그대로 사용**(기록 시점 재계산 금지). 증적 기록 실패 시 결과 미확정·수신처 전달 미수행·재시도 가능 | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-25 14:10 | spec ⓒ | (공통 반영) 기능·API 도메인 신규 작성 — `docs/specs/functions/` **공통 기능 15건**(FN-001~015 · POL 5·DAT 8·CRS 2·**EXT 0**)·**인터페이스 11접점**(사용자 진입 표면 3 · 서버 대면 API 4 · 수신처 전달 1 · 명령 진입점 1 · 라이브러리 공개 진입점 2)·오류 응답 엔벨로프 `{code,message,details?}`·인증 0/요청 제한 0·**신규 EX `EX-OPS-002` 1건**을 정책 카탈로그에 등록 — common.md | `accountinterlockhub#473`·`#469` | ℹ️ |
| 2026-07-25 13:35 | spec | `data_ENT-002.md`·`model_MDL-002.md` 신규 — **ENT-002 동의 증적**(`tbl_consent_proof`, 이력·생성 후 불변). **대리 키 PK 채택**(`consent_proof_id UUID`) — 보관 기간이 지나 추적 레코드가 삭제된 뒤 같은 키가 재수신되면(EXC-BIZ-04) 새 증적이 생기는데 앞선 증적은 아직 보존 기간 안이라 **같은 추적 키의 증적이 시간차로 공존**한다. 따라서 `tracking_key` 는 키도 유일 제약도 아니다. 컬럼 6 = `consent_proof_id`·`tracking_key`·`consented_at`(동의 일시 = 생성 시각, 별도 `created_at` 미도입)·`consent_version`·`consent_snapshot`(JSONB — `notice` + `items`)·`agreed_item_codes`(JSONB 배열 — 선택 항목 동의 여부 포함). **[위임 확정] 동의 항목 버전 식별자 = 상수 값의 결정적 SHA-256 해시**(소문자 16진수 64자, `VARCHAR(64)` + 정규식 CHECK). 산출 7단계(파싱→항목 코드 오름차순 정렬→속성 순서 고정→공백 없는 JSON 재직렬화→안내 문구 우측 트림→`0x1F` 구분자 결합→SHA-256). **수동 채번 대신 해시를 택한 근거** = `DATA-003-03`(항목·문구 변경 시 식별자 동반 변경)이 자동으로 지켜지며, 수동 채번은 갱신 누락 시 같은 식별자가 다른 내용을 가리키는 위반이 조용히 발생한다. 정규화가 필요한 근거(공백·순서 차이로 같은 내용에 다른 식별자가 붙는 역방향 위반 방지)도 명시. 인덱스 2 = PK + `consented_at`(PROC-304) — **`tracking_key` 인덱스 미신설(사용 PROC 0건)**. UPDATE PROC 0건(불변)·하드 삭제·**개인정보 보호법 제17조** 명시. 스냅샷과 동의 목록을 별도 컬럼으로 나눈 근거(상수 유래 값과 사용자 입력 값의 출처 분리) 기재 | `accountinterlockhub#472`·`#469` | 🚧 |
| 2026-07-25 13:35 | spec ⓒ | (공통 반영) 데이터 도메인 신규 작성 — `docs/specs/datas/` **엔터티 3건**(ENT-001~003 = 저장 대상 셋)·**데이터 모델 22종**(MDL-001~022 확정 — 예약 대비 통합·추가·재배치 0)·인덱스 5·물리 FK 0·소프트 삭제 미도입 — common.md | `accountinterlockhub#472`·`#469` | ℹ️ |
| 2026-07-25 13:05 | spec | `service_SVC-015.md` 신규 — **SVC-015 동의 증적 기록**(매핑 PROC-302). Happy Path 5단계(계기 수신→버전 식별자 확보→스냅샷 구성→증적 기록→결과 반환), 입력 MDL-007+MDL-008 → 출력 MDL-002. BR-022 = 생성 조건(**승인 확정에만** 1건 — 거부·본인확인 실패·진입 실패는 미생성), BR-004 인용. 예외 `EX-BIZ-003`(증적 실패 시 연동 미진행). 개인정보 미포함(추적 키·동의 일시·버전 식별자·항목 내용 스냅샷만)·승인 1건당 1건 상한·추적 레코드보다 긴 보존(`<CONSENT_PROOF_RETENTION_MONTHS>` 상수 참조·수치 미복제). 선택 항목의 동의 여부도 기록 대상임을 명시 | `accountinterlockhub#471`·`#469` | 🚧 |
| 2026-07-25 13:05 | spec ⓒ | (공통 반영) 서비스 도메인 신규 작성 — `docs/specs/services/` **서비스 18건**(SVC-001~018)·사용자 역할 4종·분기 `BR-001`~`BR-024`·MDL 22종/SCR 4종 예약 채번·`PROC-403`·`PROC-404` 추가 채번 — common.md | `accountinterlockhub#471`·`#469` | ℹ️ |
| 2026-07-25 12:28 | spec | `policy_DATA.md` `DATA-003` 동의 증적 + `DATA-002-02` 신설 — 증적은 **개인정보를 담지 않는다**(연동 추적 키·동의 일시·동의 항목 **버전 식별자**+그 시점 항목 내용 **스냅샷**만). **동의 항목(`<CONSENT_ITEMS>`)·안내 문구(`<CONSENT_NOTICE>`)가 바뀌면 버전 식별자도 바뀐다**(같은 식별자가 다른 내용을 가리키는 상태 금지) — 상수 값에서 결정적으로 산출하면 자동 충족(산출 방식은 데이터 도메인 확정). 승인 확정 시점에만 1건 생성(거부·본인확인 실패·진입 실패는 미생성). 보존은 동의 일시 기산 `<CONSENT_PROOF_RETENTION_MONTHS>` 로 추적 레코드보다 길다. 항목 스냅샷은 구성의 저장이 아니라 증적(EXC-DATA-02) | `accountinterlockhub#470`·`#469` | 🚧 |
| 2026-07-25 12:28 | spec ⓒ | (공통 반영) 정책 도메인 신규 작성 — `docs/specs/policies/` 정책 17건·규칙 86건·EX 코드 12종(암호화 연동 규약·복호화 판정 4단계·결과 구분 4종·추적 레코드 생성 시점·보관·무저장·동의 증적·인증 부재·수용 리스크) — common.md | `accountinterlockhub#470`·`#469` | ℹ️ |
| 2026-07-25 11:25 | directing ⓒ | (공통 반영) 값 확정 대기 잠정값 설정 — 동의 증적 보존 기간 `<CONSENT_PROOF_RETENTION_MONTHS>` 잠정 60개월 유지 확인, 법령·계약 확인 후 배포 전 확정 — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
| 2026-07-25 10:05 | directing ⓒ | (공통 반영) 리셋 방향 담당자 확정분 반영 — 동의 증적 보존 **신설**(담당자 확정). 추적 키·동의 일시·동의 항목 버전+내용 스냅샷·개인정보 미포함·보존 기간 상수 분리(잠정 60개월) — common.md | `accountinterlockhub#468`·`#467` | ℹ️ |
