# IA: 공통 — 횡단 사양 이력

> 본 문서는 특정 IA 노드에 매이지 않는 횡단(공통) 사양의 시계열 이력(최신순)이다. 영향받는 IA 노드는 영향 IA 열에 나열하고, 각 노드 파일에는 `spec ⓒ` 역링크 entry(ℹ️)를 둔다. 정책: ai/strategies/ia-history.md

| 일시 (KST) | 단계 | 산출물·결과 | 영향 IA | 관련 일감 | 상태 |
|---|---|---|---|---|---|
| 2026-07-26 12:20 | spec | `docs/specs/screens/design-system.md`·`design-system-components.md` + `docs/specs/qa/spec-qa.md`·`spec-qa-mapping.md` — **제목의 프로그램 포커스 표시 미규정 시정**(본체) + 검증 색인 정합. **이력·산출물 동일 commit**(작성 시점 미커밋 — 오케스트레이터가 함께 넣는다). **[① 🔴 사양 공백 — 프로그램 이동으로 받은 포커스에 무엇이 서는지가 없었다]** §접근성 기준은 "단계가 바뀌면 새 제목(`tabindex="-1"`)으로 포커스를 옮긴다"까지만 정하고 **그 제목에 어떤 표시가 서는지**를 정하지 않았다. 포커스 표시 규칙의 조건이 **`:focus-visible` 하나**에 걸려 있어, 규칙이 이 제목까지 닿지 않으면 실제로 그려지는 것은 사양의 링(2px 안쪽 여백 + 2px `--color-focus`)이 아니라 **브라우저 기본 링(1px)**(Chrome 실측)이고, 휴리스틱이 매치되지 않는 브라우저에서는 **아무 표시도 서지 않는다**. **공통 링으로 통일**하는 것으로 확정했다 — **예외를 두지 않는다**. 근거 셋 — ⓐ 기준 칸의 문면이 이미 **"항상 보이는 포커스 표시"** 이고 조작 요소로 좁혀져 있지 않다 ⓑ 이 제목은 `tabindex="-1"` 이라 탭 순서에 없어 **포커스가 닿는 경로가 단계 전환의 프로그램 이동 하나뿐**이며, 표시가 없으면 **다음 `Tab` 의 출발점**을 잃는다(특히 `SCR-003` 은 조작 요소가 0 이라 대신할 단서가 아예 없다) ⓒ 기본 링은 브라우저·테마마다 굵기·색이 달라 §색 대비 검증이 **실측으로 보장한 `--color-focus` 대비**(카드 위 **7.78:1** · 페이지 위 **7.19:1**)가 그 자리에서만 무너진다. 문면은 **조건이 아니라 결과**로 못박았다 — 요구는 "`:focus-visible` 을 걸어라"가 아니라 **"제목이 포커스를 갖는 동안 그 링이 실제로 보일 것"** 이며, 그래서 `:focus-visible` 만 건 구현도 휴리스틱이 매치되지 않는 브라우저도 **둘 다 사양 위반**이 된다(수단은 build 가 정한다 — 문면의 `:focus` 언급은 **선택자 예시**다). 제목이 조작 요소로 오독될 우려보다 **포커스 위치를 잃는 손실**이 크다고 판단했고(링의 뜻은 '누를 수 있다'가 아니라 **'지금 포커스가 여기'**), 밑줄·커서 변화 같은 조작 단서는 두지 않는다. **컴포넌트 카탈로그도 같은 회차에 정합**했다 — `design-system-components.md` §StageTitle §접근성이 "교체 시 `tabindex="-1"` 인 제목으로 포커스를 옮긴다"까지만 적어 **`design-system.md` 와 똑같은 공백을 복제**하고 있었으므로, **옮겨 간 그 제목에도 §접근성 기준의 링이 그대로 선다**를 1줄 덧붙이고 **링 값도 그 성립 수단도 그 절을 정본으로 따르게** 했다(값 복제 0 — `de482d3` 이 `screen_SCR-003.md` 의 `aria-busy` 값 복제를 지우고 정본을 가리키게 한 것과 같은 방향). **`de482d3` 의 F-3(`aria-busy` 해제 시점 미규정)과 같은 부류** — 규정이 한쪽 끝만 정하고 반대쪽을 비워 둔 자리이며, **P16(`#493`)이 `SCR-003` 을 구현하며 같은 자리에서 갈리기 전에 차단**하는 것이 목적이다. **새 토큰·새 색·새 컴포넌트 0**(§색 토큰 23종·§색 대비 검증 27행 표 무변경). **[② 검증 색인 정합]** `spec-qa.md` — TC 파일의 `###` 헤딩을 기계적으로 세면 **244** 가 나온다는 사실을 각주 1줄로 명시했다(결번 2건이 `### USR-04_008 (결번)`·`### USR-05_002 (결번)` **플레이스홀더 헤딩**으로 남아 있기 때문). **수치·총계(242)·결번 정의는 무변경**이며 목적은 도구 오독 방지다. `spec-qa-mapping.md` — §화면(SCR) → TC 의 `SCR-001`·`SCR-003` **검증 항목 서술만 보강**(수치 칸 없음). **[무변화 — 실측]** TC **신설 0** · 재채번 0 · 결번 2건 유지 · **총 242건 불변**(19개 TC 파일 `###` 헤딩 재계수 = 실 TC 242 + 결번 플레이스홀더 2 = 244, 3축·파일별 합 전건 정합) · `screen_SCR-00*.md`·`mockup/` **전부 무접촉**이고 `design-system-components.md` 는 **§StageTitle §접근성 1줄뿐**(§ProgressPanel 의 `aria-busy` 해제 규칙 등 나머지 11종 컴포넌트 정의 무변경). **[TC 2건 본체는 노드 소유]** 같은 회차의 검증 공백 2건(`USR-03_015` 제출 중 `aria-busy` 관측 · `USR-04_017` 라이브 리전 관측)은 각 노드가 소유한 산출물이라 USR-03.md·USR-04.md 항목이 본체다 | `USR-01`·`USR-02`·`USR-03`·`USR-04`·`USR-05`·`공통` | `accountinterlockhub#474`·`#476`·`#469` | 🚧 |
| 2026-07-26 11:32 | spec | `docs/specs/screens/` **2 파일**(`design-system-components.md`·`screen_SCR-003.md`) + `mockup/` **4 파일**(`SCR-001.html`~`SCR-004.html`) — 목업 전수 훑기의 **잔여 접근성 3건(F-1·F-3·F-6) 마감**. **이력·산출물 동일 commit**(앞선 `9e6eb90`·`316bf77`·`36aeb39` 3건과 달리 누락 보정이 아니다). **새 상태·새 컴포넌트·새 토큰·새 ARIA 패턴 0건**. **[F-3 🔴 사양 공백 — `aria-busy` 의 해제 시점이 없었다]** `ProgressPanel` 진행 영역에 **`aria-busy="true"` 를 부여**하라는 규정만 있고 **언제 푸는지가 없었다**. 그래서 `Unconfirmed`(응답 미수신 **또는** 응답으로 받은 500 `EX-OPS-002`)에 이르면 **기다림은 끝났는데 `aria-busy` 가 남은 채** InlineAlert 를 띄우게 된다 — `aria-busy="true"` 는 라이브 리전에 **"아직 갱신 중이니 지금 읽지 말라"** 는 신호라 보조기술이 **재진입 안내를 미루거나 건너뛸 수 있고**, 이 안내는 사용자가 **반드시 읽어야** 결과를 볼 수 있는 문구라 영향이 실질적이다. 정본인 `design-system-components.md` §ProgressPanel §접근성에 **해제 규칙을 세웠다** — 대기가 끝나면 `false` 로 바꾸거나 속성을 지우며, **결과가 도착해 카드 내용이 다른 화면으로 바뀔 때도, 이 화면에 잔류하는 `Unconfirmed` 로 들어갈 때도 같다**. **`role="status"`·`aria-live="polite"` 는 그대로 둔다** — 알림을 그 자리에서 읽혀야 하므로 영역을 라이브 리전에서 내리지 않는다(푸는 것은 `aria-busy` **하나뿐**). `screen_SCR-003.md` 는 **값을 복제하지 않고 정본을 가리키도록** 기존 1줄을 정합했다(`Unconfirmed` 도 **대기가 끝난 상태**라 그 해제 규칙을 그대로 받는다 · 전환 시 제목 포커스 이동 문구는 유지). 목업 `SCR-003.html` 도 상태표에 `busy` 축을 더해 `waiting` 은 세우고 `unconfirmed` 는 지우도록 맞췄다. **목적** — P16(`#493`)이 `SCR-003` 을 구현할 때 **같은 자리에서 갈리는 것을 막는 것**이라 사양을 먼저 세우고 목업을 거기에 맞췄다. **[F-1 목업 `SCR-001` 제출 중 잠금 표기 누락]** `submitting` 의 입력 필드에 `aria-busy="true"` 가 없었다(`design-system.md` §상태 표현 — 로딩(제출 중) 행이 "입력 필드 잠금(`aria-busy="true"`)"을 요구). 잠금 상태를 갖는 것은 `submitting` **하나뿐**이라 **그 상태에서만 세우고 나머지 상태에서는 지우도록** 했다(F-3 의 해제 원리와 같다). **[F-6 목업 4종 제목 `h1` 의 `tabindex="-1"` 누락]** `design-system-components.md` §StageTitle §접근성·`design-system.md` §접근성 기준이 **단계 전환 시 새 제목으로 포커스 이동**을 요구하는데 그 포커스를 받을 자리가 없었다. **목업은 화면당 1파일이라 실제 단계 전환이 없어 동작 영향은 없고 정적 구조 계약 표기**이며, `SCR-004` 의 결과 제목도 `StageTitle(결과 변형)` 합성이라 함께 포함했다. **포커스를 실제로 옮기는 스크립트는 만들지 않았다.** **[성격]** F-3 은 **사양 공백 시정**(사양 + 목업), F-1·F-6 은 **사양·구현이 맞고 목업이 틀린 건**으로 목업 `hidden` 무력화(`9e6eb90`)·`SCR-001` 재입력 오류 표현(`316bf77`)·`SCR-004` 복귀 안내 기본 노출(`36aeb39`)과 **같은 계열·같은 기준**이다 | `USR-01`·`USR-02`·`USR-03`·`USR-04`·`USR-05`·`공통` | `accountinterlockhub#474`·`#469`·`#467` | 🚧 |
| 2026-07-26 10:26 | spec | `docs/specs/` **7 파일**(`data_ENT-002.md`·`model_MDL-002.md`·`model_MDL-007-010.md`·`model_MDL-019-022.md`·`function_FN-007-008.md`·`function_FN-014-015.md`·`spec-functions-api.md`) — build 가 구현 중 올린 **사양 결함 7건(SD-1~SD-7)** 정정. **⚠️ 이력 누락 보정 — 산출물은 commit `1491e4f` 로 이미 커밋·origin push 돼 amend 할 수 없으므로(같은 commit 에 이력을 넣지 못했다) 본 entry 가 그 근거 해시로 추적을 잇는다.** 셋은 **코드가 실제로 실패하거나 환경마다 다른 값을 내던** 자리다. **[SD-1 🔴 트랜잭션 소유권 표기 충돌 — 소유권은 호출측이다]** `FN-008` 이 단계 4·6 을 "트랜잭션 시작"·"트랜잭션 종료"로 적어 **스스로 경계를 소유**하는 것으로 읽혔는데, `PROC-301` 은 "호출측 트랜잭션에 참여한다·새 트랜잭션을 열지 않는다"를 명시한다 — **build 에서 커넥션 풀 교착으로 실현된 건**이다. `PROC-301` 을 정본으로 삼아 `FN-008` 표기를 통일하고 **경계를 실제로 여는 자리가 `PROC-102` `B6`·`PROC-103` `B3`** 임을 밝혔다. 참여의 성립 조건인 **실행 문맥(`exec`)** 이 시그니처에 없던 것을 `FN-008` **필수 인자**로 명시(`FN-007` 은 조회 전용 접점의 기존 호출을 깨지 않도록 **선택 인자**) — 스스로 커넥션을 얻으면 호출측 경계 **밖**의 별도 커넥션을 잡아 같은 경계의 변경이 보이지 않고 요청 하나가 커넥션을 둘 점유해 풀이 마른다. **[SD-2 🔴 `FN-008` 의사코드가 PostgreSQL 에서 실행 불가]** "INSERT 시도 → 기본 키 충돌 catch → 같은 트랜잭션에서 재조회"는 **실행되지 않는다** — 문 하나가 오류나면 트랜잭션이 **중단 상태**가 되어 되감기 전까지 어떤 조회도 못 한다. **`ON CONFLICT (tracking_key) DO NOTHING RETURNING`** 으로 정정(오류를 만들지 않아 경계가 살아 있고 **반환 행의 유무가 곧 `isCreated`**). `SAVEPOINT`(경계가 하나 더 생긴다)·트랜잭션 밖 재조회(원자성 파괴 — `SVC-014` F-008) **미채택 근거를 구현 가이드에 남겨** 되돌리지 않게 했고, 충돌 재조회는 **`READ COMMITTED` 를 전제**로 성립하므로 격리 수준을 그 위로 올리지 않는다. **[SD-3 🔴 동의 항목 정렬 기준(콜레이션) 미정의]** "항목 코드 오름차순"만 있고 무엇을 기준으로 한 오름차순인지가 없었다 — 정렬 결과가 곧 해시 입력 순서라 로케일 인식 비교를 쓰면 **같은 배포가 환경마다 다른 `consent_version`** 을 낸다(개행 정규화와 **같은 뿌리**). `data_ENT-002.md` §버전 식별자 산출 규칙 3 을 정본으로 **UTF-8 바이트열의 사전식 순서(= 코드 포인트 오름차순)**·접두면 짧은 쪽 우선으로 고정하고, 검증 가능하도록 **관측 요건**("ASCII 범위에서 숫자 → 대문자 → 소문자")을 병기했다. 같은 문구가 흩어져 있던 `MDL-008`(정본 참조)·`MDL-002`(스냅샷은 `MDL-008` 결과를 **그대로 복사**하며 스스로 정렬하지 않는다)·§스냅샷 구조를 맞춰 **정렬 주체를 하나로** 모았다. **[SD-4 규약 테스트 벡터의 개행 취급 미명문]** `MDL-019` 를 **파일 층·값 층 둘로 나눠** 규정 — ① **벡터 파일 개행 = LF 고정**(파일 바이트열이 체크섬 대조 대상이라 CRLF 가 섞이면 같은 내용의 패키지가 **다른 체크섬**을 낸다) ② **값 안 개행 = `\n` 하나로 통일**(직렬화 바이트열이 갈리면 기대 출력이 갈라진다). **대조는 파일 통째가 아니라 파싱 후 값 단위**로 한다고 못박았다. **[SD-5 동봉 구성의 내부 참조 금지 조항 부재]** `usageGuide` 한정이던 제약을 `MDL-020` **구성 전체에 거는 제약**으로 올렸다(구성을 여섯으로 늘린 것이 아니다). 열거형 금지 목록은 오탐을 만들므로(`CONSENT_NOTICE` 형식 검증을 검증 대상에서 뺀 선례와 같은 함정) **판정 질문 하나**로 정의 — "발송처가 수령분만으로 해석·도달할 수 있는 표기인가". 자가진단 경로(`SEC-003-02`)는 이 판정과 무관하게 **더 강한 금지**로 그대로 선다. **[SD-6 오류 메시지 기본값의 내부 개념어 — 바꾸지 않는다(확정)]** "규약"·"전달 정보"가 사용자에게 설명력이 낮은 것은 사실이나 ⑴ `FN-014` 의 이 표는 **응답 본문 `message` 의 기본값**이고 사용자 문구의 정본은 화면 도메인이며 ⑵ 두 문구는 **행동 안내를 담지 않고**(사용자가 할 일은 `SCR-004` 의 다음 안내가 전담) ⑶ 문자열이 9곳에 인용돼 있어 바꾸면 기능·화면·검증 사양이 함께 흔들린다 — **판정을 문서로 남겨** 다음에 다시 열리지 않게 했다(문구 변경 0). `EX-DATA-001`·`EX-DATA-002` 의 "연동 추적 키"는 **서버 대면 API 3종 전용**이라 사용자 표면에 도달하지 않음을 실측 확인. **[SD-7 분류 계층 미명시]** `spec-functions-api.md` §경로·메서드 규약의 "추적 키 입력 3종"·"사용자 표면 → 각 접점의 입력 검증 코드"가 **어느 접점인지 문서에 없던 것**을 카탈로그 실측으로 접점명·귀결 코드까지 명시했다 — 추적 키 입력 3종(처리상태 확인·연동 완료 확인·완료 콜백)→`EX-DATA-002` · 연동 규약 자가진단→`EX-SEC-003` · 본인확인 제출→`EX-AUTH-001` · **동의·승인 제출→`EX-BIZ-001`**(`EX-AUTH-001` 이 아니다) · 연동 요청 진입은 `GET` 이라 요청 본문이 없어 해당하지 않는다. **[신설 0]** EX 코드·정책 규칙·MDL·FN 무신설 · 상수 실값 복제 0. **[연쇄 — 별건 진행 중]** 같은 실행 불가 의사코드가 남아 있던 `PROC-301` `B3` 와 경계를 여는 자리(`PROC-102` `B6`·`PROC-103` `B3`)·`FN-009`~`FN-013` 소유권 표기·`PROC-302`·`FN-012` 재정렬 주체는 **후속 묶음(commit `b4fe224`)** 으로 닫았다(각 IA 파일의 10:42 entry 참고) | `BAT-04`·`BAT-05`·`USR-03`·`USR-04`·`LIB-03`·`LIB-04`·`공통` | `accountinterlockhub#473`·`#472`·`#470`·`#469` | 🚧 |
| 2026-07-26 08:45 | spec | `docs/specs/screens/` **3 파일**(`design-system-components.md`·`screen_SCR-003.md`·`screen_SCR-004.md`) + `mockup/` **4 파일** — build 가 봉합하지 않고 미해소로 올린 **간격 규칙 국소 회귀(6회차)**. **새 토큰·새 컴포넌트·새 화면 신설 0 · 기존 `--space-*` 조합만 사용**. **[① 🔴 `StageTitle` 아래 여백과 패널 간격이 합산돼 어느 사양에도 없는 값이 나옴]** CSS 에서 컨테이너 간격과 자식 여백은 **합산**되므로 두 곳이 각자 값을 가지면 제목 다음 간격만 커지고 나머지와 어긋난다. `StageTitle` 의 여백(제목→보조 설명 `--space-2xs` · 블록 아래 `--space-lg`)을 **카드 직계 자식일 때의 값**으로 한정하고(`design-system.md` §레이아웃 3 의 "①화면 제목 ②보조 설명 → ③본문 영역" 블록 경계 — `SCR-001`·`SCR-002` 가 그 자리, **거동 무변경**), **`ProgressPanel`·`ResultPanel` 안에 합성될 때는 자기 여백을 하나도 갖지 않고 패널의 간격 모델이 전적으로 지배**하도록 확정(두 패널 동일 규칙). 제목+보조 설명을 감싸는 **wrapper 금지**를 함께 명문화해 재발 경로를 막았다. **[② 애초에 사양에 없던 두 패널의 간격 모델 신설]** `ProgressPanel` = 요소 간 **`--space-md` 균일**(Spinner ↔ 제목 ↔ 보조 문구 ↔ 조건부 알림) / `ResultPanel` = **균일 간격을 쓰지 않고** 요소별(아이콘→제목 `--space-md` · 제목→배지 **`--space-sm`**(제목 바로 아래 — §확정 결과 재안내의 배지 위치와 양립) · 배지(없으면 제목)→설명 `--space-md` · 설명→다음 안내 `--space-md` · 다음 안내→복귀 영역 `--space-md`) + **안쪽 여백 `--space-lg`**. §복귀 안내 영역의 중복 "위 여백" 서술은 §간격으로 일원화. **[③ `SCR-003` 알림 영역 소속 확정]** `InlineAlert` 를 `ProgressPanel` 의 요소(패널 마지막)로 못박아 균일 간격에 포함 — `mockup/SCR-003.html` 의 `.alert{margin-top:var(--space-sm)}` 1속성 제거로 **컨테이너 `gap`(16px) + 자식 `margin`(12px) = 28px** 이라는 **어떤 `--space-*` 토큰으로도 표현되지 않던 값**을 없앴다(`InlineAlert` §위치 "본문 아래·액션 위" 규칙은 불변 — 소속만 확정). **[④ 목업 `hidden` 무력화 결함 시정(목업 4파일)]** 저자 `display` 선언이 UA `[hidden]{display:none}` 을 원산지 우선순위로 이겨 `SCR-001`·`SCR-002`·`SCR-003` 의 알림과 `SCR-004` 의 `재안내` 배지가 **조건과 무관하게 항상 노출**되던 결함(사양은 정확했고 렌더가 배반). 각 파일에 `[hidden]{display:none !important}` 를 넣고 `SCR-004` 의 국소 패치 `#reason-row[hidden]` 은 흡수·제거. **격리 프로필 헤드리스 Chrome(CDP 직구사) 실렌더 검증** — 조건부 요소 6건 전부 초기 `display:none`, 양성대조(`hidden` 제거 시 `flex`/`inline-flex`/`block` 복귀) 통과, 정상 표시 상태의 전체 요소 기하·display **4파일 0 차이**(회전 스피너 bbox 0.01px 애니메이션 노이즈 제외). **[영향 범위]** 4개 화면 전부에서 규칙 성립 확인 · **화면 사양에 간격 값 복제 0**(정본이 카탈로그임을 가리키는 문장만 추가) · `SCR-001`·`SCR-002` 사양 파일 **무수정·거동 무변경** · 목업 재작성 0. **[이월]** `ResultPanel` 모서리 `--radius-md` 미기재 → 다음 회귀 / 복귀 안내 영역 내부 2px 하드코딩(토큰 미준수) → 담당자 판단. | `USR-01`·`USR-02`·`USR-03`·`USR-04`·`USR-05`·`공통` | `accountinterlockhub#474`·`#469` | 🚧 |
| 2026-07-26 08:24 | build | `apps/backend/src/common/`(신규 15파일)·`apps/backend/src/app.module.ts`(`CommonModule` 배선)·`apps/backend/src/main.ts`(캐시 금지 헤더·405 판정 미들웨어 배선) — 횡단 공통 기능 `FN-014`(오류 응답 엔벨로프)·`FN-015`(민감값 제거·기록 통제) 구현(P05, 일감 `#482`). **[구현]** `FN-014`(`errors/error-envelope.ts` `buildErrorEnvelope(code, details?)` → `{ httpStatus, body }` — `body` 는 `{code,message,details?}` 뿐이고 `httpStatus` 는 절대 JSON 본문에 섞이지 않게 반환 형을 분리) — 코드 유효성 판정(카탈로그 밖 값은 `EX-OPS-002` 로 대체) · 메시지 선택(`errors/ex-catalog.ts` `EX_CODE_CATALOG` — spec-policies.md §예외(EX) 코드 카탈로그의 HTTP·문구 12행을 그대로 옮긴 단일 출처) · `details` 화이트리스트 재구성(`field`·`reason` 두 속성만 남기는 화이트리스트 — 필드 "이름 라벨"은 남기되 값·길이·그 밖 속성은 버림, 빈 `field`·허용 밖 `reason`(`REQUIRED`\|`FORMAT`\|`LENGTH` 아님)은 항목째 드랍, 빈 배열이면 `details` 속성 자체를 생략) 후 `FN-015` 동기 호출(의존 기능표 그대로 반영, 2차 방어). `FN-015`(`security/sanitize-value.ts` `sanitizeValue(value)`) — 금지 항목 중 **이름으로 안전히 식별되는 5종**(`encX`·`encY`·`birthDate`·`senderKey`·`stack`)만 깊은 사본에서 속성째 제거(부분 마스킹 없음). 순환 참조는 **조상-경로 Set**(진입 시 add·`finally` 에서 delete)으로 판정해 무한 재귀 없이 `undefined` 로 안전 실패시키되, 순환이 아닌 **DAG 형태의 공유 참조**(같은 객체를 형제 두 곳에서 참조)는 정상 보존되도록 구분(전역 `WeakSet` 한 번 방식이면 이 구분이 깨져 오탐한다는 점을 자가 실측으로 확인 후 조상-경로 방식으로 확정). `Buffer`/`TypedArray` 는 순회 시 바이트가 인덱스 속성(`{0:12,1:34,...}`)으로 그대로 드러나는 위험이 있어 `ArrayBuffer.isView()` 로 판정해 통째로 불투명 제거. `trackingKey`·`returnUrl` 은 금지 목록에 없어 그대로 통과(예외 조항 반영, 별도 보존 로직 불요). **복호화 원문 X 의 개별 업무 필드는 의도적으로 다루지 않음** — 발송처가 자유 정의하는 열린 스키마라 안전한 키 이름을 고정할 수 없고, 휴리스틱 구조 탐지("trackingKey 와 다른 필드가 함께 있으면 X 로 간주")는 `MDL-012`/`MDL-013`(처리상태·콜백 응답이 정확히 그 모양)을 오삭제할 위험이 커 채택하지 않음(FN-015 §구현 가이드의 "1차 방어는 담는 모델에 애초에 속성을 두지 않는 것" 그대로 위임, 코드 주석에 근거 명문화). **[전역 예외 필터]** `filters/global-exception.filter.ts` `GlobalExceptionFilter`(`@Catch()`, `common.module.ts` 가 `APP_FILTER` 로 등록) — ① `exCode` 를 가진 예외(`errors/http-mapped.error.ts` `isHttpMappedError` 덕 타이핑 — 기존 크립토·레코드 예외 클래스는 이미 이 형태를 구조적으로 만족해 무수정) → `FN-014` 엔벨로프, HTTP 상태는 **캐치한 예외 자신의 `httpStatus` 를 신뢰하지 않고 항상 카탈로그로 재계산**(단일 출처) ② 그 밖의 `NotFoundException` → 본문 없는 일반 404(EX 코드 없음) ③ 그 밖 전부 → `EX-OPS-002`/500(마지막 방어). `@nestjs/core` 소스 직접 확인(`router/routes-resolver.js` `registerNotFoundHandler()` 가 라우트 매치 실패 시 `throw new NotFoundException(...)` 하고 이것이 `RouterExceptionFilters.create()` → `helpers/context-creator.js` `createContext()` 경로로 **전역 필터 목록과 병합되어 전달**됨을 확인)로 "Nest 라우팅 자신의 404 도 전역 필터가 받는다"는 설계 전제를 사전 검증 후 구현 — 별도 Express 404 핸들러 불요. 500 대(`httpStatus>=500`)만 `Logger` 로 `"{method} {path} -> {status} ({예외 클래스명})"` 1줄 남기고 `message`·`stack`·쿼리스트링은 담지 않음(4xx 는 일상적 처리 결과라 로그 대상 아님). **[404/405/캐시 헤더]** `http/known-routes.ts` `buildKnownRoutes(interlockEntryPath)` — spec-functions-api.md §인터페이스 카탈로그의 고정 경로 6개(진입 GET·`/verify`·`/approve` POST·서버 대면 API 3종 POST)를 표로 옮김, **자가진단 경로는 의도적으로 제외**(넣으면 메서드 불일치 시 405 로 응답해 "경로가 존재한다"는 사실이 새어 나간다 — `SEC-003-02`). `http/route-guard.middleware.ts` `createRouteGuardMiddleware()` — 표에 있는 경로인데 메서드가 다르면 즉시 405(본문 없음), 그 밖은 `next()`(경로 자체가 없음은 이 미들웨어가 응답하지 않고 전역 필터의 일반 404 로 위임 — 책임 분리). 이 표는 "실제 등록된 Nest 라우트"가 아니라 "사양이 정의한 경로 목록"이라 **컨트롤러가 아직 없는 지금도, 후속 Phase 가 컨트롤러를 붙인 뒤에도** 등록 순서·타이밍 없이 그대로 성립(실측으로 확인 — 아래). `http/cache-control.ts` `cacheControlMiddleware`/`applyNoStoreHeaders()` — `Cache-Control: no-store, no-cache, must-revalidate`+`Pragma`+`Expires` 를 단일 지점에서 구성, `main.ts` 가 Nest 라우팅보다 먼저 `app.use()` 로 걸어 성공·404·405·오류 엔벨로프 **전 응답 경로**를 한 미들웨어로 커버(전역 필터에도 멱등 재적용해 이중 방어). `errors/http-mapped.error.ts` `HttpMappedException` — 향후 Phase 가 `details` 를 실어 던질 때 쓸 재사용 클래스(기존 클래스에 상속 강제 없음). **[실측 — 컴파일]** `npm --prefix apps/backend run build` 0에러(단독 2회) + 루트 `npm run build`(frontend 포함) 1회 — 총 3회 재확인. **[실측 — 순수 함수 전건 대조, HTTP 무관]** `works/accountinterlockhub-482/verify-ex-catalog.js`(컴파일된 `dist/common/errors` 직접 require) — spec-policies.md §예외(EX) 코드 카탈로그 **원문 13행 전체**를 코드에 옮겨 `buildErrorEnvelope()` 실제 반환과 대조, **67/67 통과**(`EX-OPS-001` 은 런타임 카탈로그 대상이 아님을 별도 확인·카탈로그 밖 임의 코드는 `EX-OPS-002` 로 수렴·`details` 화이트리스트가 값(`999999`)·길이는 버리고 필드 이름 라벨 3건만 남김을 직렬화 전수 검색으로 확인). `works/accountinterlockhub-482/verify-sanitize.js`(`dist/common/security` 직접 require) — **25/25 통과**(중첩 3단 제거·`trackingKey`/`returnUrl` 보존·원본 불변·순환 참조 안전 실패·DAG 공유 참조 정상 보존·`Buffer` 불투명 제거·직렬화 결과에 원본 값 문자열 잔존 0). **[실측 — 실제 HTTP, 합성 env]** 실 `.env`·`CLAUDE.local.md` 무열람 원칙에 따라 `works/accountinterlockhub-482/test.env`(합성 상수·미사용 더미 DB 자격, `DOTENV_CONFIG_PATH` 로 주입 — P01 선례와 같은 방식)로 **실제 `AppModule` 을 포트 48731 에 기동**(`pg.Pool` 이 지연 연결이라 DB 미도달 상태에서도 정상 기동 확인) 후 curl 12케이스 전부 기대값과 일치: 미정의 경로 GET·POST → **404 본문 없음**(`Content-Length: 0`) · 진입/`/verify`/`/approve`/서버 API 3종에 오메서드(POST·GET·DELETE·PUT 등) → **405 본문 없음** 6/6 · 자가진단 경로에 **오메서드·정메서드 둘 다 404**(구분 불가 — 405 로 새지 않음, `SEC-003-02` 실측 확인) · 진입 경로 **정메서드인데 컨트롤러 미배선** → 404(405 아님 — route-guard 오탐 없음 확인) · 끝 슬래시 정규화(`/interlock/entry/` POST → 405) · **전 응답에 `Cache-Control: no-store, no-cache, must-revalidate`+`Pragma: no-cache`+`Expires: 0` 존재**. 별도로 `works/accountinterlockhub-482/http-harness.js`(데코레이터를 문법이 아니라 **함수로 직접 호출**해 적용 — `Controller('demo')(Class)`·`Get(path)(proto,key,descriptor)` — ts-node·경로별칭 설정 없이 이 Phase 의 컴파일 산출물을 그대로 배선하기 위함, 커밋 대상 아님)로 포트 48732 에 최소 데모 컨트롤러 4개를 배선해 실제 HTTP 왕복으로 추가 확인: **성공 응답 미포장**(`GET /demo/success` → 200, 본문이 `{ok,stage,trackingKey,returnUrl}` **그대로**, 엔벨로프 없음) · **분류된 예외**(`HttpMappedException('EX-BIZ-001', 던진메시지, details)` → 400 + `{code:"EX-BIZ-001", message:카탈로그 기본값(던진 메시지 아님), details:[{field,reason} 2건]}`, 던진 메시지·값 어디에도 응답에 없음) · **미분류 예외**(`throw new Error('UNCLASSIFIED-SECRET-MARKER...')` → 500 + `{code:"EX-OPS-002", message:카탈로그 기본값}`, 원본 메시지 문자열이 응답에 전혀 없음 + 로그에 `"GET /demo/throw-unclassified -> 500 (Error)"` 1줄만 — 마커 문자열·스택 없음) · **대조군**(Nest 내장 `NotFoundException` 을 컨트롤러가 직접 던져도 `exCode` 가 없어 일반 404 로 수렴 — 아래 확인 필요 참고). 테스트 서버 2개는 확인 직후 `Stop-Process` 로 종료·포트 재확인(연결 거부)까지 실측. **[제약 준수]** `git status --short` 로 매 단계 확인 — `config`·`crypto`·`database`·`entities`·`models`·`records` 무접촉(읽기만 소비) · `apps/frontend`·`apps/sender-sdk`·`history/` 의 `common.md` 외 파일 무접촉 · `apps/backend/.env` 무열람·무수정(합성 스크래치 env 로만 실측) · 401·403 무도입 · `details.reason` 값 셋(`REQUIRED`\|`FORMAT`\|`LENGTH`) 고정·값 자체 미포함(자가 실측으로 재확인) · 결과 경로 3종·`decision`/`REJECT` 인터페이스 무접촉(이 Phase 는 결과 경로 로직 자체를 다루지 않음) · 폐지 IA 코드(`ADM`·`BAT-01`·`BAT-03`) 미사용(grep 재확인) · 잠정값 하드코딩 0(`interlockEntryPath` 만 주입값 경유, 나머지 5경로는 사양이 직접 고정한 리터럴이라 상수 대상 아님을 근거 주석에 명문화) · `main.ts`/`app.module.ts` 는 Edit 로 국소 치환(편집 직전 재확인) · Redmine 일감 무기재(오케스트레이터 위임). **[확인 필요 — 후속 Phase 조율]** ① **컨트롤러는 반드시 `exCode` 를 가진 예외만 던져야 한다** — Nest 내장 `HttpException`(예: `NotFoundException`)을 exCode 없이 직접 던지면 전역 필터가 "라우팅 자체가 못 찾음"과 구분하지 못해 **본문 없는 404 로 수렴**하거나(그 예외가 `NotFoundException` 인 경우) 그 밖의 내장 `HttpException` 은 원래 상태 코드가 무시되고 **`EX-OPS-002`/500 으로 수렴**한다(실측으로 대조군 확인) — 기존 크립토·레코드 예외 클래스 관례(모두 `exCode` 보유)를 그대로 따르면 문제 없음. ② **요청 본문 JSON 파싱 실패**(Express body-parser `SyntaxError` → Nest 가 `exCode` 없는 `BadRequestException` 으로 매핑, `@nestjs/core` `routes-resolver.js` `mapExternalException()` 확인)도 ①과 같은 이유로 지금 이 계층까지 흘러오면 500 으로 수렴한다 — 그러나 사양은 접점별로 다른 코드(추적 키 3종 `EX-DATA-002`·자가진단 `EX-SEC-003`·사용자 표면 각 접점 코드)를 요구하므로(spec-functions-api.md §경로·메서드 규약), **후속 컨트롤러 Phase 가 본문 파싱을 직접 검증해 적합한 `exCode` 로 먼저 분류한 뒤에만** 이 전역 필터에 도달해야 한다(이 Phase 범위 밖 — 컨트롤러 부재). ③ `known-routes.ts` 의 6경로 표는 **현재 사양의 닫힌 집합**을 그대로 옮긴 것이라, 향후 인터페이스 카탈로그에 새 HTTP 표면이 추가되면 이 표도 함께 갱신해야 한다(유지보수 결합 — 문서화만, 이 Phase 가 임의로 확장하지 않음). ④ `Allow` 응답 헤더(RFC 7231 §6.5.5 권고)는 **의도적으로 붙이지 않음** — 사양 원문이 "405(본문 없음)" 만 명시해 문구 밖 추가를 보수적으로 피함(필요시 후속 변경 용이). ⑤ `HEAD` 요청은 Express 의 기본 GET-암묵-HEAD 관례 대신 route-guard 표에 없는 메서드로 취급돼 405 가 난다(Nest 라우팅보다 앞서 걸리는 미들웨어라 그 관례를 가로챔) — 사양이 `GET`/`POST` 외 메서드를 언급하지 않아 의도적으로 특례를 두지 않았다. 사양 결함은 발견되지 않았다 **[회귀 1회차 — 코드리뷰 Important 4·Suggestion 7 전건 시정, 재검증 대기]** ①**I-1**(🔴 FN-015 가 성공 응답 경로에 미배선 — 리뷰가 `{encX,encY,birthDate}` 를 그대로 반환하는 컨트롤러의 200 응답에 값이 그대로 나가는 것을 실측) — `common/interceptors/sanitize-response.interceptor.ts` `SanitizeResponseInterceptor`(`APP_INTERCEPTOR`, `common.module.ts`) 신설 — `next.handle().pipe(map(sanitizeValue))` 로 컨트롤러 반환값에도 FN-015 를 적용(엔벨로프 미부여 — 형태 불변·내용만 정제). 재실측: `{encX,encY,birthDate,trackingKey,nested:{senderKey,keep}}` 반환 컨트롤러의 실제 200 응답이 `{"trackingKey":"...","nested":{"keep":"..."}}` 로 정확히 걸러짐. `error-envelope.ts` 의 기존 `sanitizeValue` 호출(FN-014 §의존 기능표 반영용)은 유지하되 실효는 이 인터셉터가 갖는다. ②**I-4**(🔴 성능 — 리뷰 실측 재현 깊이16:15ms·20:202ms·22:744ms, depth 당 약 2배) — `sanitize-value.ts` 에 방문결과 메모 `Map` 추가(조상 `Set`=순환 판정과 역할 분리 — 순환은 진입시add·이탈시delete, 메모는 계산완료 후 add·불변 유지, 하나로 합치면 다이아몬드 재방문이 순환으로 오판정됨을 확인 후 분리 확정). 깊이 30 다이아몬드(메모 없으면 약 2분 추정)가 재실측 0ms. ③**S-1**(🔴 `toJSON` 우회 — 리뷰가 `{toJSON:()=>({encX:'VIA-TOJSON'})}` 를 `JSON.stringify` 하면 값이 부활함을 실측) — 함수 값(`toJSON` 포함)을 `Buffer` 와 같은 취급으로 속성째 제거(최상위·중첩 공통 가드, `typeof value==='function'→undefined`). 재현 케이스 재확인 — 우회 완전 차단. ④**I-2**(🔴 근거 역전 — `selfcheckPath` 는 허브가 `InterlockConfigService.selfcheckPath` 로 실제 보유하는데도 "값의 출처로만 식별" 이라며 잘못 제외) — `FORBIDDEN_KEYS` 에 `selfcheckPath`·`SELFCHECK_PATH` 추가, 주석을 "이름으로 식별 가능한 군"으로 재정렬(리뷰가 지적한 `senderKey` 와의 기준 불일치 해소 — 둘 다 "이름이 등장하면 무해히 제거"라는 하나의 기준으로 통일). 나머지 3항목(X 개별필드·정규화키/IV·URL전체) 제외 근거는 리뷰가 타당하다고 확인해 그대로 유지. ⑤**I-3**(🔴 본문 파싱 실패 — 리뷰가 실제 컨트롤러를 붙여도 깨진 JSON 은 컨트롤러가 아예 실행되지 않고 500 으로 수렴함을 실측, Express 본문파서가 Nest 라우터보다 먼저 걸려 `next(err)` 로 라우팅 자체를 건너뛰기 때문 — `mapExternalException()` 확인) — `http/body-parse-failure.ts` `classifyBodyParseFailure()` 신설(경로·메서드로 판정: 추적키3종+POST→`EX-DATA-002` · 자가진단+POST→`EX-SEC-003`·**자가진단은 메서드가 달라도 항상 404**(본문이 깨졌다는 이유로 존재은닉이 깨지지 않도록 명시적으로 메서드 게이팅) · 미정의경로→404 · **사용자 진입 표면(진입·verify·approve)은 의도적으로 `UNCLASSIFIED`**(사양이 "각 접점의 입력 검증 코드"로 접점별 상이를 요구하는데 그 구체값은 각 접점의 실제 검증 순서(컨트롤러 부재)에 달려 있어 이 횡단 계층이 대신 단정하지 않음 — 아래 §미해소 참고). `GlobalExceptionFilter` 가 `InterlockConfigService` 를 생성자 주입받도록 확장(`@Injectable()` 추가). 4가지 실 HTTP 케이스(추적키3종·자가진단 정/오메서드·미정의경로) 전부 실측 일치. ⑥**S-2** — `buildErrorEnvelope` 에 `Array.isArray(details)` 가드 추가(비배열 5종 입력 무예외 재확인, "throws 없음" 계약 실제 이행). ⑦**S-3**(🔴 대소문자 — 리뷰가 실제 컨트롤러로 `POST /API/INTERLOCK/STATUS` 가 매치됨을 실측) — **함정 2단을 자가 실측으로 직접 재발견 후 해결**: ⓐ `NestFactory.create()` 뒤에 `.set('case sensitive routing', true)` 하면 이미 컨트롤러 라우트가 등록된 뒤라 무효 ⓑ `new ExpressAdapter()`(인자 없음) 뒤 `.getInstance().set()` 도 무효 — 그 생성자 자신이 내부에서 `this.instance.use(...)` 를 호출해 Express 의 지연 라우터(`app.router` getter, 첫 접근 시 1회만 생성)를 그 시점에 이미 기본값(대소문자 무시)으로 굳혀 버림(처음 이 방식으로 시도했다가 재현 실패로 발견). **최종 해법**: `express()` 를 직접 만들어 `case sensitive routing` 을 먼저 설정한 뒤 `new ExpressAdapter(그 인스턴스)` 로 감싸 `NestFactory.create()` 에 넘김(`main.ts`) — 대문자 변형 요청이 정확히 404 로 걸러지고 정상 소문자 요청은 그대로 라우팅됨을 실측 확인. `express` 를 `apps/backend/package.json` **직접** 의존성으로 승격(`@nestjs/platform-express` 의 전이 의존으로 이미 설치돼 있던 5.2.1 그대로 — 코드가 직접 `import` 하게 됐으니 정직하게 선언, `npm install --package-lock-only` 로 락파일만 갱신, 두 파일 각 1행 diff 로 범위 확인 완료·새 패키지 설치·버전 변경 없음). ⑧**S-4** — 405 응답에 `Allow` 헤더 추가(RFC 9110 §15.5.6). ⑨**S-5** — `known-routes.ts` `buildMethodsByPath()` 로 route-guard·`classifyBodyParseFailure` 가 표를 공유하도록 정리하며 `GET` 등록 경로에 `HEAD` 자동 허용을 추가(실측: 진입 경로 `HEAD` 가 405→404 로 바뀜 — 컨트롤러가 없어 404 인 것이지 405 가 아니라는 점이 핵심). ⑩**S-6** — `sanitizeValue` 반환형을 `T\|undefined` 로 정직화(안전 실패가 실제로 `undefined` 를 낳는다는 계약을 타입에 드러냄), 호출측(`error-envelope.ts`) `?? []` 보강. **[실측]** `npm --prefix apps/backend run build` 0에러(`dist` 삭제 후 클린 재빌드 포함) + 루트 `npm run build` 0에러. 순수함수 스크립트 확장 재실행 — `verify-ex-catalog.js` 67→**77/77**(S-2 5케이스 추가) · `verify-sanitize.js` 25→**39/39**(I-2·I-4·S-1 신규 섹션 3개 추가, 다이아몬드 성능 케이스 포함). 실 HTTP — 원 12케이스 + 신규 11케이스(본문파싱 재분류 7·`HEAD` 2·헤더 2) 도합 **23/23 자동 대조 스크립트로 일치**(합성 env 로 재기동, 기존 `works/accountinterlockhub-482/test.env` 재사용) + 별도 하네스(`http-harness.js`, S-3 대조용 실제 컨트롤러 1개 추가)로 I-1·S-3 실제 HTTP 왕복 재확인. **[건드린 파일]** 수정 11(`package.json`·`package-lock.json`·`common.module.ts`·`errors/error-envelope.ts`·`filters/global-exception.filter.ts`·`http/index.ts`·`http/known-routes.ts`·`http/route-guard.middleware.ts`·`common/index.ts`·`security/sanitize-value.ts`·`main.ts`) + 신규 3(`http/body-parse-failure.ts`·`interceptors/sanitize-response.interceptor.ts`·`interceptors/index.ts`). **[제약 준수 재확인]** `config`·`crypto`·`database`·`entities`·`models`·`records`·`apps/frontend`·`apps/sender-sdk` 무접촉(`git status` 재확인) · `app.module.ts` 이번 회차 무변경 · `main.ts`·`common.module.ts` 는 Edit 국소 치환(편집 직전 재독) · `.env`·`CLAUDE.local.md` 무열람. **[미해소 — 리뷰 재판정 대기]** I-3 의 사용자 진입 표면(진입·verify·approve) 본문 파싱 실패는 여전히 `EX-OPS-002`/500 으로 수렴한다 — 사양의 "각 접점의 입력 검증 코드"가 요구하는 접점별 구체값은 그 접점의 실제 검증 순서(컨트롤러 부재)에 달려 있어 이 Phase 가 단정하지 않기로 한 선택이며, P07(컨트롤러 구현 Phase)이 자기 접점의 검증 순서를 확정하는 시점에 함께 닫아야 한다 **[회귀 2회차 — 코드리뷰 재판정 Important 2건 시정, 재검증 대기]** 대상 파일 2개(`http/body-parse-failure.ts`·`filters/global-exception.filter.ts`), 그 밖 무접촉. **[I-A]** 사용자 진입 표면 2접점의 본문 파싱 실패 미해소분 — `body-parse-failure.ts` `USER_SUBMIT_EX_BY_SUFFIX`(`/verify`→`EX-AUTH-001`·`/approve`→`EX-BIZ-001`) 신설, `classifyBodyParseFailure` 에 `entryPath` 인자를 추가해 추적키3종 검사와 나란히 판정한다. 근거는 리뷰 인용을 그대로 신뢰하지 않고 직접 재도출했다 — `spec-functions-api.md` §경로·메서드 규약 원문("본인확인 제출 → `EX-AUTH-001`, 동의·승인 제출 → `EX-BIZ-001`, 연동 요청 진입은 GET 이라 해당 없음")과 `docs/specs/qa/USR/tc_USR-04.md` `USR-04_005` ④ 원문("본문 자체를 해석할 수 없게 보낸다 → 400 `EX-BIZ-001`")을 직접 열어 대조했다. 진입 GET 은 이 표에 없어 계속 `UNCLASSIFIED`→`EX-OPS-002`/500 으로 수렴한다(의도됨 — `#484`(P07) S-7 진입 200 폴백 소관, 이번엔 만들지 않음). **[I-B]** `SyntaxError` 이외의 본문 파서 오류(`PayloadTooLargeError`·gzip 해제 실패 등)가 미정의 경로에서도 EX 코드 본문을 내던 미해소분 — `global-exception.filter.ts` 에 엔벨로프 구성 직전 **경로 기준 불변식**을 추가했다(`isKnownRequestPath()`: 알려진 6경로 표 + 자가진단 경로에 없으면 `!mapped` 예외는 오류 클래스와 무관하게 전부 본문 없는 404). 오류 클래스를 나열하는 대신 경로만 판정해 현재·미래의 모든 파서 오류 클래스를 함께 덮는다. 생성자에서 `methodsByPath` 를 `route-guard.middleware.ts` 와 같은 이유로 1회만 사전계산한다(요청마다 재계산하지 않음). **[자체보완 S-e]** `express` 를 `package.json`·`package-lock.json` 양쪽에서 `^5.2.1`→`5.2.1` 정확 핀(`npm install --package-lock-only` 로 락파일만 갱신 — diff 각 1행). `@nestjs/platform-express` 자신의 express 의존 선언도 `package-lock.json` 상에서 `5.2.1` 정확 핀임을 실측 대조해 재확인(두 벌 설치 위험 해소). **[실측]** `npm --prefix apps/backend run build` — `dist` 삭제 후 클린 재빌드 포함 2회 0에러. 실 HTTP 왕복(합성 env, `works/accountinterlockhub-482/r2-*` — 종료 후 전량 삭제): Server A(포트 49401, 실제 `main.js`, 컨트롤러 0개, DB 미접속 상태로 정상 기동) + Server B(포트 49402, 데모 컨트롤러 부착 하네스 — round1 `http-harness.js` 와 같은 "데코레이터를 함수로 직접 호출" 방식). 자동 대조 스크립트 **28/28 통과**: [I-A] `<ENTRY>/verify`·`/approve` 깨진 JSON → 400 `EX-AUTH-001`/`EX-BIZ-001` 정확 확인 · 추적키3종·자가진단은 무변경(`EX-DATA-002`/`EX-SEC-003`) · 진입 GET 잔여 500 무변경(의도) 확인. [I-B] 리뷰가 실측한 두 시나리오를 그대로 재현(미정의 경로+200KB 본문 · 미정의 경로+깨진 gzip) → 둘 다 404 본문 없음으로 전환 확인 + round1 시정분(미정의 경로+깨진 JSON)이 여전히 무변화임을 재확인. **과잉적용 아님** — 알려진 경로·자가진단 경로는 200KB 본문에도 기존 500 `EX-OPS-002` 그대로(무변경) 확인. 그 밖 회귀 부작용 금지 목록 재확인 — 자가진단 은닉 GET/HEAD/OPTIONS/PUT 4종 전부 디코이와 상태·본문·헤더(Date 제외) 완전 동일 · 대소문자 라우팅 무변경(`POST /API/INTERLOCK/STATUS`→404, `main.ts` 무접촉) · 401/403 부재(Authorization 헤더 유무 무관 동일 응답) · 캐시 금지 헤더 400/404/405/500 전 응답 부착 · 일반 404 `Content-Length: 0` · 405 `Allow` 헤더. Server B 대조군 — 알려진 경로에 실제로 컨트롤러를 붙여 미분류 `Error` 를 던져도 여전히 500 `EX-OPS-002`(내부 마커 문자열 비유출 — 과잉적용 아님 재확인) · 매핑 예외(`EX-BIZ-001`) 엔벨로프·카탈로그 문구 무변경 · **진입 접점의 `text/html` 문자열 응답이 인터셉터(`SanitizeResponseInterceptor`, 이번 회차 무접촉)를 거쳐도 무손상**(작업 지시가 명시한 회귀 검증 항목). 서버 stdout 로그 전문 검사 — 두 서버 합산 500 응답 로그 **9건 전부** `METHOD path -> status (ExceptionName)` 1줄뿐, 200KB 본문 패딩·깨진 JSON 조각·gzip 마커 문자열·`Authorization` 값·쿼리스트링 **유출 0건**. **[실측 vs 추정 — 명확히 구분]** `security/sanitize-value.ts`·`interceptors/sanitize-response.interceptor.ts`·`errors/ex-catalog.ts`·`errors/error-envelope.ts`·`http/route-guard.middleware.ts`·`http/known-routes.ts`·`main.ts`·`app.module.ts` 는 이번 회차 **무접촉**(diff 0, `git status` 로 실측) — 1회차 재판정에서 이미 확정된 그 실측 결과를 그대로 승계할 뿐 이번 회차가 다시 실측하지 않았다(단, 이번 왕복의 부수 결과로 text/html 무손상·EX-BIZ-001 엔벨로프 2건은 다시 실측됐다). 이번 회차가 직접 재실측한 것은 위 28건 자동 대조 + 로그 전문 검사뿐이다. **[제약 준수]** `works/accountinterlockhub-482/r2-*`(env·하네스·검증스크립트·로그) 전량 삭제, 두 서버(PID 를 `CommandLine` 전문 대조 후 `Stop-Process`)를 종료하고 포트 49401·49402 리스너 0 확인 · 원본 `.env` 무열람(`.env.example` 기준 합성값만 사용) · `apps/backend/src/common/**`·`package.json`·`package-lock.json` 밖 무접촉 — 동시 세션의 `apps/frontend/**` 5파일·`docs/specs/functions/function_FN-009-011.md`·`docs/specs/processes/process_PROC-10{2,3}.md`·`process_PROC-30{1,2}.md`·(이후 정리된) `mockup/SCR-001.html` 을 `git status` 로 식별해 좁은 pathspec 커밋으로 배제 · 잠정값 하드코딩 0(`entryPath` 만 주입 경유, 그 외 리터럴은 기존 근거 그대로 유지) · 폐지 IA 코드 무사용. **[미해소·후속 유지]** S-a(5xx 로그 자가진단 경로 값)는 spec 이관 그대로(무변경) · S-b(`Buffer`/`StreamableFile` 인터셉터 훼손)·S-7(진입 접점 200 폴백)·"컨트롤러는 `exCode` 보유 예외만 던진다"는 `#484`(P07) 인계 그대로 유지(이번 Phase 서 만들지 않음) · S-c(`toJSON` 주석)·S-d(금지 키 대소문자 무시)는 이번 범위 밖으로 유지(미래 위험) — 독립 재판정 대기 **[회귀 3회차 — 코드리뷰 재판정 Critical 1·Important 1 시정, 상한 3회 중 마지막]** 대상 파일 2개(`filters/global-exception.filter.ts`·`http/body-parse-failure.ts`), 그 밖 무접촉. **[C-1]** 🔴 자가진단 경로 존재 오라클 — 회귀 2회차의 `isKnownRequestPath()` 가 자가진단 경로를 메서드와 무관하게 무조건 "알려진 경로"로 쳐, exCode 없는 예외(대형 본문·깨진 gzip·미지원 encoding)가 자가진단 경로에 오면 `EX-OPS-002`/500(디코이는 404)로 응답해 인증 없는 요청 1회로 경로 존재를 확증시켰다(코드리뷰 실측 — `PUT`·`GET`·`DELETE`·`HEAD`·`OPTIONS` 전 메서드 재현, `5ff4ca0`↔`5a47542` A/B 대조로 이번 commit 이 만든 회귀임을 확정). `SEC-003-02`·`spec-functions-api.md:52-53` 위반. **시정** — `isKnownRequestPath()` 에서 자가진단 경로 예외를 완전히 제거(`return this.methodsByPath.has(path);` 로 단순화 — 리뷰가 제시한 "POST 만 known" 안(메서드 반영판)을 그대로 베끼지 않고 사양으로 재도출한 결과다: `POST` 만 남기면 `POST`+대형본문/깨진gzip 이 여전히 디코이(404)와 갈리는 잔여 오라클이 남는다는 것을 회귀 2회차 tester 재판정이 같은 근거로 이미 재확인해 두었다 — 완전 제거만이 §검증 요건의 "전 메서드·전 오류 클래스 완전 수렴"을 만족한다). `POST`+깨진JSON(`EX-SEC-003`, `SEC-003-04`)은 `classifyBodyParseFailure()` 가 이 함수보다 먼저 처리해 끝내는 별도 경로라 영향받지 않는다(자가진단 자신의 정상 동작 보존). **[I-1]** 🟠 그 불변식이 마지막 방어(`EX-OPS-002`)·5xx 로그까지 함께 삼킨다 — 표 밖 경로의 컨트롤러 예외가 404·본문 0·로그 0줄로 사라짐(`OPS-003-05`·`OPS-003-03` 이탈, 컨트롤러 0개인 현재는 잠재). **시정** — "경로가 표에 있는가"(`isKnownRequestPath`) 판정에 "라우팅에 아예 닿지 못한 실패인가"(신설 `isRoutingUnreachedFailure` — 예외가 `.status`(숫자)+`.expose`(불리언)를 모두 가지면 Express 본문 파서(`body-parser`/`raw-body`, `http-errors` `createError()`) 기원으로 판정, 둘 다 소스 직접 확인) 조건을 **AND** 로 더해 두 조건이 **모두** 참일 때만 조용한 404 로 수렴시킨다 — 실제로 코드가 실행되어 던진 예외(Nest `HttpException` 계열은 `.status` 는 있어도 `.expose` 를 절대 만들지 않음, 소스 확인)는 표 상태와 무관하게 계속 마지막 방어(500+로그)로 흘러간다. C-1 을 "표에서 완전 제거"만으로 닫았다면 향후 `known-routes.ts` 갱신 누락 시 자가진단(또는 임의 미등재 경로)의 진짜 5xx 까지 로그 없이 삼켜지는 새 문제가 났을 것 — 두 항목을 한 메커니즘으로 함께 닫았다. **[I-4-guard 반려 재확인]** 회귀 2회차 반려(journal 3520)를 리뷰가 "결론은 타당하나 근거 2건은 부정확"으로 재판정했고 오케스트레이터가 spec 이관·build 완료 기준 미해당으로 최종 확정 — 이번 회차 코드 변경 대상이 아니라 무변경 유지. **[Suggestion 6건 처리]** S-1(자체보완) `classifyBodyParseFailure` 가 매 호출마다 `buildMethodsByPath()` 를 재생성하던 것을 제거 — 시그니처를 `knownRoutes` 배열 대신 `GlobalExceptionFilter` 가 이미 캐시한 `methodsByPath` 맵을 직접 받도록 변경(그 필드 자체도 `KnownRoute[]` 를 필드로 남기지 않고 생성자에서 즉시 맵으로 변환). S-2(자체보완) `USER_SUBMIT_EX_BY_SUFFIX` 선형 탐색(`for...of`)을 `TRACKING_KEY_INPUT_PATHS.has(path)` 와 대칭인 `Map.get(path.slice(entryPath.length))` 단일 조회로 축소(이 지점 도달 시 `path` 는 이미 6경로 중 하나로 좁혀져 있어 접두사 검사 없이도 안전함을 코드 흐름으로 확인). S-6(자체보완, C-1 리팩터에 자연 포함) `[I-B]` 주석이 "예외 클래스와 무관"이라 적었던 부분을 실제 적용 범위(`!mapped` 미분류 예외 한정)에 맞춰 정정. S-3·S-4(spec 이관/후속 Phase 인계 유지, 무변경) — 5xx 로그의 자가진단 경로 값 잔존은 기존 spec 이관 상태 그대로, `Buffer`/`StreamableFile` 인터셉터 훼손은 `#484`(P07) 인계 그대로. S-5(결함 아님, 기록만) — `details.field` 에 금지 키 이름이 그대로 실리는 것은 사양이 의도한 필드명 식별자라 결함이 아님. **[실측]** `npm --prefix apps/backend run build` — `dist` 삭제 후 클린 재빌드 포함 2회 0에러. **A/B 대조**(직전 commit `5a47542` 를 별도 워크트리 `_aih-wt/p05-r3-base` 에 `--detach` 로 빌드) + **은닉 수렴 표**(FIXED 서버, 자가진단 vs 형태만 비슷한 디코이 2종) — `GET`·`POST`·`PUT`·`DELETE`·`HEAD`·`OPTIONS` 전 메서드 × 정상·깨진JSON·대형본문(300KB)·깨진gzip·미지원encoding(`Content-Encoding: compress`) 전 오류클래스 = 30 조합을 자동 대조 스크립트로 실측: **수렴 위반 0 · 사양상 정상 차이 1건**(POST+깨진JSON, self=400 `EX-SEC-003` vs decoy=404, `SEC-003-04` 그대로). baseline 대비 A/B — 자가진단 경로 18개 조합(6메서드×largeBody/brokenGzip/unsupportedEncoding)이 500→404 로 **의도된 변경**, 그 밖 알려진 경로·진입 경로·디코이 2종 등 나머지 전 조합(150건 중 132건) **예상 밖 회귀 0건**(상태·본문·헤더(Date 제외) 완전 일치, 응답 헤더 상세 대조로 `etag`·`content-type` 잔존 여부까지 확인). 자가진단 `POST`+깨진JSON 은 baseline·fixed 동일(`EX-SEC-003` 무변경) 재확인. `/verify`·`/approve` 접점 분류(S-1/S-2 리팩터 대상)도 baseline·fixed 완전 동일 재확인. 응답시간 축 — 인터리브 N=35 반복 표본(largeBody·brokenGzip·정상 3종), self·decoy median 차 전부 0.02ms 이하로 판별 신호 아님(단발 표본에서 관측된 최대 1ms 차는 초기 워밍업 잡음, 반복 표본에서 소멸 확인). **[I-1] 별도 검증**(격리 워크트리 `_aih-wt/p05-r3-verify` — fixed 코드 사본 + 임시 데모 컨트롤러 2개, 커밋 대상 아님) — ⓐ 자가진단 실경로에 등록된 컨트롤러가 미분류 `Error`·`ForbiddenException` 을 던지면 **500 `EX-OPS-002`+로그 1줄**(디코이·매핑 예외·`NotFoundException`(기존 한계 무변경) 대조군 포함) ⓑ **`known-routes.ts` 표에 전혀 없는 임의 경로**(`/r3verify/forgotten-route`, I-1 이 우려하는 "표 갱신 누락" 시나리오 그대로 재현)에 컨트롤러를 붙이고 미분류 `Error` 를 던져도 **500+로그 1줄**로 정상 수렴(표에 없다고 침묵하지 않음). **[실측 vs 추정]** 전 항목 실측 — A/B 대조·은닉 수렴 표·타이밍 표본·I-1 데모 컨트롤러 왕복 전부 실 HTTP 재현이며 추정치는 없다. **[제약 준수]** 워크트리 2개(`p05-r3-base`=baseline 빌드, `p05-r3-verify`=fixed+임시 데모 컨트롤러)를 `git worktree remove --force` 로 제거 후 디렉터리·`node_modules` 정션까지 정리 확인(정션은 대상이 아니라 링크만 삭제 — 원본 `apps/backend/node_modules` 무변경 재확인) · 서버 3개(포트 49401~49403) `CommandLine` 전문 대조 후 종료·포트 해제 확인 · `works/accountinterlockhub-482/r3-*`(하네스·로그) 전량 삭제, 그 폴더의 이전 회차 잔존 파일은 무접촉 · 실 `.env`/`CLAUDE.local.md` 무열람(env 값은 전부 합성, `MSYS_NO_PATHCONV=1` 로 Git Bash 의 경로 자동변환이 `/` 로 시작하는 값을 훼손하는 것을 방지) · `apps/backend/src/common/**` 밖 무접촉(동시 세션의 `docs/specs/qa/USR/tc_USR-02.md`·`mockup/INDEX.md`·`history/` 의 다른 항목 등은 `git status` 로 식별해 배제). **[반영 판단]** 지시가 제시한 코드 스니펫(메서드 반영판)을 그대로 쓰지 않고 완전 제거로 재도출한 것이 유일한 편차이며 근거는 위 [C-1] 절에 명시했다. | 공통 | `accountinterlockhub#482`(부모 `#469`) | 🚧 |
| 2026-07-26 06:19 | spec | `docs/specs/` **3 파일**(functions·policies·datas) — build 가 올린 사양 결함의 **횡단 국소 회귀(5회차)**. 표 행·칸 단위 정합이며 **거동 변경 0 · 신설 0**. **[① 🟡 접점 계약 ↔ 프로세스 분기표 불일치]** `spec-functions-api-user.md` 의 **§본인확인 제출·§동의·승인 제출** 두 §에러 처리 표에 `EX-OPS-002` 행이 없어(진입 접점 표에는 200 매핑 특례 근거까지 있음) `process_PROC-102.md`·`process_PROC-103.md` 분기표(**둘 다 500**)와 어긋나던 것을 **500 행 1개씩 신설**해 해소 — **이미 성립하는 사실을 계약에 적은 것**이다. **두 접점의 화면 처리가 다르다(비대칭이 핵심)**: `verify()` 는 **`PROC-102` `F3` 미매핑 catch-all → 결과 경로 ②**(이 접점은 **어떤 결과도 확정하지 않아** 경로 ② 로 흘려도 **뒤집을 결과가 없다** — `SVC-005` F-003) / `approve()` 는 **`SCR-003` `Unconfirmed`(재진입 안내) · 결과 패널을 그리지 않는다**(`B7` 전달 실행 → 결과 확정 → `B8` 결과 안내 구성 순서라 **결과가 확정된 뒤에도** 이 코드가 날 수 있고, 정의상 **분류되지 않은** 실패라 결과 미확정을 보장할 수 없다 — 그 상태에서 결과를 그리면 **화면이 결과 구분을 추측해 만들어 내는 것** `BIZ-001-02`). 발생 지점이 전달 **이전**으로 특정돼 "결과 미확정·재시도 가능"이 보장되는 `EX-BIZ-003` 과의 **대비**는 표 아래 불릿 **1개**로만 남겼다(근거 정본은 프로세스·화면 사양 — 이 문서는 참조만). 진입 접점의 **200 매핑 특례가 `GET` 한정**임을 기존 불릿에 명문화(제출 접점 `POST` 2종에는 특례 미적용 — 카탈로그대로 500 + 오류 응답 엔벨로프). 사용자 메시지는 `function_FN-014-015.md` §오류 메시지 기본값 문구 **그대로**(새 문구 0) · 내부 사유·스택 미포함(`OPS-003-05`). **[② 연쇄 — EX 카탈로그 전역 단정]** `spec-policies.md` EX 카탈로그 `EX-OPS-002` 행의 **"결과 미확정" 전역 단정 제거** → **"발생 지점이 정의상 분류되지 않아 결과 확정 여부는 접점에 따라 다르다"**(카탈로그는 단정하지 않고, 접점별 귀결은 인터페이스·프로세스·화면 사양이 정본). ① 의 `approve()` 확정과 **정면 충돌하던 뿌리**를 해소했다. 발생 지점이 특정돼 단정이 참인 다른 행(`EX-BIZ-003` 등)은 **무변경**. **[③ 4회차 §7-3 해소]** `model_MDL-002.md` `consentSnapshot.notice` 유효성 규칙 칸을 **"가공·검증하지 않는다 — 화면이 노출한 원문을 그대로 보존한다(`DATA-003-05`) · 개행 정규화는 해시 입력 전용이라 스냅샷에 적용하지 않는다 · 허용 형태 규정의 정본은 `MDL-022` `consentNotice`"** 로 정합 — 이로써 `<CONSENT_NOTICE>` 규정이 **데이터 3모델에서 통일**됐다. **[신설 0]** 코드 체계·EX 코드·정책 규칙·절 전건 무신설 · 결과 경로 **3종 불변**(① 연동 완료 · ② 링크·복호화 오류 · ③ 수신처 전달 실패) · 기존 표 행 무수정 · 상수 실값 복제 0. **[실측]** `spec-functions-api-user.md` **249 → 252행**(표 행 2 · 불릿 1 · 기존 불릿 1칸 in-place — 300줄 제한 이내, 여유 48) · 직전 회귀 commit `2b9b29e` 기준 · `etc/` 미참조 · git 조작 0 · 다른 세션이 점유 중인 `apps/` 무접촉. **[미수정 — 후속 필요]** **TC 공백 1건** — `approve()` 의 `EX-OPS-002` **응답 수신** → `Unconfirmed` 를 검증하는 TC 가 없다(`USR-02_015` 는 응답 **미수신** 기준 · `USR-05_003` 은 검증 PROC 가 `PROC-105`·`PROC-101`·`PROC-102` 라 verify 한정이어서 이번 비대칭과 **어긋나지 않음** — 다만 approve 까지 확대 해석하면 충돌하므로 범위 명시 유지 필요) · **`process_PROC-103.md` `F5` `onError` 에 catch-all `else` 부재**(`PROC-102` `F3` 에는 있음 — 이 결함의 뿌리 중 하나, 프로세스 도메인 소관. `else → SCR-003 Unconfirmed` 이며 **`F3` 의 경로 ② 를 그대로 복사하면 이번 확정과 정면 충돌**) · **`screen_SCR-003.md` `Unconfirmed` 진입 조건에 500 `EX-OPS-002` 수신 미기재**(현재 "응답을 받지 못함(전송 계층 단절·창 이탈 후 복귀)"만 — 화면 도메인 소관이며 **이동 경로 표가 아니라 상태 전이 표**에 더해야 한다, `Unconfirmed` 는 이동이 아니라 잔류) · `spec-functions-api-server.md` 의 `EX-OPS-002` 4행이 근거를 `SEC-002-05` 로 인용(사용자 접점 3표는 `OPS-003-05`)하고 발생 조건 표기가 3종 혼재 — **거동 차이 없음, 표현 통일 검토** | `USR-01`·`USR-02`·`USR-03`·`USR-04`·`USR-05`·`BAT-05`·`공통` | `accountinterlockhub#473`·`#470`·`#472`·`#469` | 🚧 |
| 2026-07-26 04:57 | spec | `docs/specs/qa/` **7 파일** — build 가 올린 사양 결함 6건의 **검증 TC 국소 회귀**(TC 전면 재작성·재채번 없음 — 기존 채번·결번 체계 보존. **신설 3 · 개정 4 · 총 238 → 241건**). **[③ 개행 정규화]** `BAT-05_006` 검증 대상을 8단계로 재번호된 정본 범위 **2~7**(정규화 2~6 · 구분자 결합 7)로 정정하고, `SEED-CONST-WS` 를 **4벌 → 5벌**로 늘려 **⑤ 개행 코드만 CRLF↔LF 로 다른 변형**을 더했다 — 단계는 **항목 설명 값의 개행 · 안내 문구의 개행 · 양쪽**을 세 갈래로 나눠 각각 같은 식별자가 나오는지 확인한다(항목 쪽 개행은 재직렬화해도 이스케이프로 살아남아 안내 문구와 **별개의 관측 지점**이다). **스냅샷은 원문 보존이라 무효화되지 않음**을 `BAT-05_003` 단계 ⑥ **한 구절**로만 보강(정규화는 해시 입력에만). **[④ 🔴 초기 상태 주입 형식]** `USR-01_001` 단계 ②를 확정 형식(`<script id="__INTERLOCK_INITIAL_STATE__" type="application/json">` **정확히 하나** · 초기 상태 JSON 1건 · UTF-8 · **`id` 로 찾기**)으로 구체화. **`USR-01_014` 신설** — 부재 판정 4조건(요소 없음 · 같은 `id` 둘 이상 · `JSON.parse` 실패 · `stage` 두 값 밖) **전건 경로 ②**, **`IDENTITY` 폴백 금지**(정상 링크 진입인데도 본인확인을 열지 않는다), 폴백은 `reasonCode` 를 만들지 않으므로 **SCR-004 미상 코드 → `EX-OPS-002` 문구 수렴**까지 확인. **정적 서빙 배선**(미리 빌드된 문서가 값 없이 서빙되면 정상 진입까지 경로 ② 로 끝난다)은 ① 케이스에 **한 구절로 흡수**했다(별도 TC 미신설 — 기존 커버 TC 0건 실측). **`USR-01_015` 신설** — 주입 측 5문자 무조건 치환을 **문서 바이트**에서 확인 + `JSON.parse` 후 **값 불변** + **`</script>` 가 값에 섞여도 문서 무파손**(핵심 관측 지점). 값 주입 하네스 전제·미구축 시 🟠 Block. **[⑤ 응답 미수신]** **`USR-03_017` 신설** — 본인확인 제출의 **전송 계층 단절** → `SCR-001` 유지 · **`Retryable`**(InlineAlert + 버튼 재활성) · 문구는 `EX-BIZ-003` **기본 문구 그대로**(사유를 구분해 알리지 않는다 — `SEC-002-05`) · 화면 자체 타임아웃 없음 · 복구 후 **같은 값 재제출로 정상 진행**(결과 미확정이라 안전 — `BIZ-002-03` ②) · 승인 제출의 `Unconfirmed`(`USR-02_015`)와 **다른 상태**. `USR-03_014` 는 응답 기준이라 **무변경**. **[① `B3` 형식 기준]** `SCEN_012` 에 **복귀 주소가 절대 URL 형태를 벗어난 6번째 변형**을 더하고(시드 `SEED-CONST-BADFORMAT` **5벌 → 6벌**) 단계 ③의 형식 검증 대상 나열을 개정된 `B3` 와 일치시켰다(**필수 8종 × 4갈래 = 빠짐 0**). **[② `<CONSENT_NOTICE>` 미검증]** 같은 TC 단계 ④에 **역방향 확인을 흡수**(**TC 신설 0** — 신설하지 않는 쪽을 택했다) — 태그처럼 보이는 문자열·400자·3단락 초과에도 **기동 성공 · 종료 코드 0 · 미충족 상수명 출력 없음**, 부재·빈 값도 빈 문자열(EXC-BIZ-07), 화면은 **평문 그대로** 렌더(`DATA-003-05`·EXC-DATA-08). 시드 `SEED-CONST-NOTICE-OVER` 1벌 신설. **[⑥ 제목 글꼴 크기]** **TC 영향 0 — 실측 재확인**: `docs/specs/qa/` 전체에 타이포그래피 토큰(`--font-size-*`·`StageTitle`·글꼴 크기) 검증 TC **0건**(전문 검색 무매치) → **무수정**. **정합** — `spec-qa-mapping.md` 양방향 추적 갱신(SVC-001 15·SVC-002 17 / PROC-101 25·PROC-102 30 / BR-001 20 / `EX-BIZ-003` 9·`EX-OPS-002` 5 / 접점 15·17 / SCR-001·SCR-004 행에 신설 TC 등재 / 정책 BIZ-001 28·BIZ-002 27·SEC-002 28), `spec-qa.md` 총계(**241** · 계층 INTG 99·E2E 23 · 유형 Negative 70·시스템예외 17 · 자동 207·반자동 30). **커버리지 미매핑 0 유지**(신설 3건 전부 검증 PROC·분기/예외·IA 기재) · **사양 문서 무변경**(`docs/specs/` 중 `qa/` 밖 diff 0 — 실측) | `USR-01`·`USR-03`·`BAT-05`·`공통` | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-26 04:40 | spec | `docs/specs/processes/` **3 파일** — build 가 올린 사양 결함의 **프로세스 연쇄 국소 회귀**(상위 도메인 확정분을 의사코드에 정렬. PROC 전면 재작성 아님). **[① 🟡 `PROC-901` `B3` 형식 기준이 `MDL-022` 보다 좁음]** "URL 형태(**수신처 전달 주소**)" 괄호가 `completionRedirectUrl` 을 배제해 읽히던 것을 **"URL 형태(수신처 전달 주소·복귀 주소)"** 로 넓혔고, 절대 URL 조건은 **정본 참조**(`MDL-022`·`BIZ-001-06`)로만 달아 복제하지 않았다(주소 도달 가능 여부는 보지 않는다 — `SVC-018` F-002). **대조 실측 — `MDL-022` 필수 8종**(`interlockEntryPath`·`selfcheckPath`·`receiverDeliveryUrl`·`consentItems`·`retentionMonths`·`retentionMaxMonths`·`consentProofRetentionMonths`·`completionRedirectUrl`) **× `B3` 4갈래 = 빠짐 0**. `SVC-018` F-002 는 괄호 한정 없이 "URL 형태"로 이미 포괄 — **무수정**(실측 확인만). 같은 파일 `C1` 의 **배포 주입 대상 나열에도 복귀 주소가 빠져 있어**(배포 절차 7종 vs 기동 요구 8종 — 절차대로 해도 기동 실패) 한 항목 추가로 문서 내부 정합. **[② 🟡 `<CONSENT_NOTICE>` 판정 기준 부재]** 오케스트레이터 확정대로 **기동 시 형식 검증 대상 제외**를 `B3` 에 명문화 — 반복 대상은 **필수 상수**이고 `consentNotice` 는 **선택 상수라 미충족 판정 대상이 아니며** 부재·빈 값은 **빈 문자열**(`MDL-022` 기본값 · `EXC-BIZ-07`), 허용 형태(400자·3단락·평문)도 검증하지 않는다(근거 ① 화면이 평문으로만 출력해 실행·해석 위험 없음 `DATA-003-05` ② 판정 기준이 한국어 산문의 정상 문자 `<`·`>` 를 오탐해 기동을 막음 — 통제 지점은 상수 작성·배포 절차 `EXC-DATA-08`, `OPS-001-03`·EXC-OPS-01 과 동형). §구현 가이드 불릿 1개 추가. **[③ 연쇄 — 개행 정규화]** `data_ENT-002.md` 가 8단계로 재번호되며 신설된 **개행 정규화**가 `B4` 요약 인용에서 빠져 있던 것을 정본 순서대로 재정렬(**파싱 → 개행 정규화 → 정렬 → 속성 순서 재구성 → 공백 없는 JSON → 끝 공백 제거한 문구 → `0x1F` 결합 → SHA-256 → 소문자 16진수 64자**). **단계 번호는 요약에 박지 않았다**(번호가 또 밀리면 다시 어긋난다) · 규칙 복제 금지 원칙 유지 · **정규화는 해시 입력에만** 적용된다는 한 줄 추가(노출 문구·증적 스냅샷은 원문 그대로). 데이터 변환 흐름 표 한 칸 동반 정합. **[④ 🔴 연쇄 — 초기 상태 주입 형식]** `PROC-101` `F1` 검증이 부재 판정 **2종만** 쓰던 것을 확정된 **4조건 전부**(요소 없음 · 같은 `id` 둘 이상 · `JSON.parse` 실패 · `stage` 두 값 밖 → `stage=RESULT`·경로 ②)로 넓히고 **`IDENTITY` 폴백 = 사양 위반**(`BIZ-001-02`)을 못박았다. 한 문서 안에서 **둘로 갈리던 `initial` 형상 표기**를 §입력/출력 정의 쪽(`{ stage, resultPath?, reasonCode?, isReAnnouncement?, returnUrl? }`)으로 통일. `B7` 에 **담는 형식**(`<script id="__INTERLOCK_INITIAL_STATE__" type="application/json">` 정확히 하나 · JSON 직렬화 1건 · UTF-8 · 전역 변수 주입/별도 API 금지) 인용과 **이스케이프의 주입 측(서버) 의무**(`<`·`>`·`&`·U+2028·U+2029 를 값의 출처와 무관하게 무조건 치환 · 값 불변 · 화면 역이스케이프 없음)를 명시. §구현 가이드의 정적 자산 라우팅 순서 불릿에 **왜**(미리 빌드된 문서가 값 없이 서빙되면 부재 판정이 발동해 **정상 진입까지 경로 ② 로 끝난다**)를 덧붙였고, 폴백이 **`reasonCode` 를 만들지 않는다**는 사실과 그 상태를 `SCR-004` §결과 3경로의 **미상 코드 → `EX-OPS-002` 문구 수렴**·미매핑 경로 ② 안내(`FN-014`·`SVC-005` F-003)가 받는다는 이음을 남겼다 — **새 EX 코드 무신설 · `reasonCode` 강제 충전 없음 · 분기·예외 흐름 표 행 무추가**(부재 판정은 새 EX 가 아니라 기존 경로 ②). 단계 통합 흐름 8행 한 칸 정합. **[⑤ 연쇄 — `verify()` 응답 미수신]** `PROC-102` FE `F3` 에 **응답 미수신(전송 계층 단절)** 갈래 한 줄을 더해 `EX-BIZ-003` 과 **같은 재시도 안내·같은 문구**로 보냈다(사유를 구분해 알리지 않는다 — `SEC-002-05` · `SCR-001` `Retryable` 확장분 반영). §구현 가이드에 **비대칭 근거 불릿 1개** — `verify()` 는 어떤 결과도 확정하지 않아 재제출이 이어쓰기로 수렴하고(`BIZ-002-03` ②), `approve()` 는 결과가 확정됐을 수 있어 `SCR-003` `Unconfirmed` 로 간다. **화면 쪽 요청 타임아웃 무신설**. **[신설 0]** PROC·SVC·FN·MDL·ENT·EX·BR·정책 규칙·단계 라벨(`B1`~`B7`·`F1`~`F3`) 전건 무신설 · 결과 경로 3종 불변(사용자 거부는 결과 경로 아님) · 상수 실값 복제 0 · 폐지 IA 코드 사용 0. **[실측]** 3 파일 **+48/-12행** · 전 파일 300줄 이내(901=248 · 101=256 · 102=283) · `docs/specs/processes/` 밖 무접촉(다른 세션이 `apps/` 점유 중) · git 조작 0. **[build 재작업 3건]** ① 기동 검증에서 `<CONSENT_NOTICE>` 형식 검사 제거(`apps/backend/src/config/interlock-config.validators.ts` `isValidConsentNotice`·`MAX_CONSENT_NOTICE_LENGTH` — `MDL-022` 회귀가 보고한 것과 같은 건) ② 버전 식별자 산출에 **개행 정규화** 반영(`\r\n`·`\r`→`\n` · 항목 문자열 속성 + 안내 문구 양쪽 · **해시 입력 한정**) ③ FE 초기 상태 수화의 부재 판정을 **4조건**으로 확장(`IDENTITY` 폴백 금지) + 주입 측 이스케이프 5문자 무조건 치환 점검. **[TC 영향 — 미수정·식별만]** `SCEN_012`(`SEED-CONST-BADFORMAT` 5변형에 **복귀 주소 비-절대 URL 변형 없음**) · `BAT-05_006`(`SEED-CONST-WS` 4변형에 **CRLF↔LF 변형 없음** — 신설된 개행 정규화 미커버) · `USR-01` 전 13건(**주입 형식·부재 판정 4조건·서버 이스케이프를 검증하는 TC 0건**) · `USR-03`(**응답 미수신 → `Retryable` 미커버** — `#474` 보고와 같은 건) | `USR-01`·`USR-03` | `accountinterlockhub#475`(부모 `#469`) | 🚧 |
| 2026-07-26 04:30 | spec | `docs/specs/screens/` **5 파일** — build 가 올린 **사양 결함 🟠 2건 국소 회귀**. **[⑤ `verify()` 응답 미수신의 상태 부재]** 본인확인 제출의 응답 미수신(전송 계층 단절)에 대응하는 상태가 어느 화면에도 없어 build 가 방어적으로 `SCR-001` 재시도 안내로 라우팅했던 처리를, **새 상태를 만들지 않고 기존 `Retryable` 의 진입 조건을 넓혀** 명문화했다(`screen_SCR-001.md` §화면 상태 전이·§조건부 표시). 근거는 **두 접점의 비대칭** — 본인확인 제출은 **어떤 결과도 확정하지 않아**(같은 추적 키 재제출은 같은 레코드를 이어 쓴다 · `BIZ-002-03` ②) 옳은 안내가 "그대로 다시 시도"이고, 승인 제출은 **서버가 이미 결과를 확정했을 수 있어** 재진입 안내(`SCR-003` `Unconfirmed`)가 옳다 → 두 응답 미수신을 **한 상태로 합치지 않는다**. 문구는 `EX-BIZ-003` 기본 문구("처리 중 문제가 발생했습니다. 잠시 후 다시 시도해 주세요.") 하나를 그대로 쓰고 사유를 구분해 알리지 않는다(`SEC-002-05`). **화면 쪽 요청 타임아웃은 신설하지 않았다**(`SCR-003` §구현 가이드 유지 — 전송 계층 단절만 다룬다). `screen_SCR-003.md` 에 "`Unconfirmed` 는 승인 제출 전용" 한 줄, `spec-screens.md` §확정값의 "응답을 받지 못한 경우" 행을 **두 접점으로 가르도록** 정정(정본 위치 2곳 병기). **[⑥ 제목 글꼴 상충]** 카탈로그 §ProgressPanel 이 자체 제목을 `--font-size-lg`(토큰 정본상 용도 = 강조 문단)로 규정해 화면 사양의 StageTitle(`--font-size-xl` = 화면 제목)과 상충하던 것을 **화면 사양 정본**으로 확정 — `design-system-components.md` §StageTitle 에 **크기 변형 2종**(기본 `--font-size-xl` · 결과 `--font-size-2xl`, **그 밖의 크기 금지**·`lg` 변형 금지)을 명시하고 §ProgressPanel 구조를 **StageTitle(기본) 합성**, §ResultPanel 제목을 **StageTitle(결과 변형)** 표기로 바꿔 두 패널이 따로 정의하던 제목 계약(유일 `<h1>`·전환 시 포커스·`document.title` 일치)을 한 곳으로 모았다. `screen_SCR-003.md`(§레이아웃 2·3)·`screen_SCR-004.md`(§레이아웃 2)는 **변형 표기 한 줄씩만** 다듬었다(문구·구조 무변경). **[신설 0]** SCR·PROC·SVC·MDL·EX·BR·정책 규칙·컴포넌트(11종 유지)·디자인 토큰 전건 무신설 · `design-system.md` **무변경**. **[build 재작업 1건]** ProgressPanel 제목 `--font-size-lg` → **StageTitle 기본(`--font-size-xl`)** (ResultPanel 은 값이 같아 재작업 없음). **[TC]** 무효화 0건(`USR-02_015` 는 승인 제출 기준이라 그대로 유효) · **미커버 1건 — 본인확인 제출의 응답 미수신 → `Retryable` 을 검증하는 TC 없음**(신설 제안, 본 작업 범위 밖) | `USR-01`·`USR-02`·`USR-03`·`USR-05` | `accountinterlockhub#474`(부모 `#469`) | 🚧 |
| 2026-07-26 04:30 | spec | `model_MDL-019-022.md` `MDL-022` `consentNotice` **국소 회귀 — 판정 기준 부재 해소**(build P01 발견 🟡). `<CONSENT_NOTICE>` 의 "서식·링크·HTML 태그를 담지 않는다" 제약에 **무엇을 위반으로 볼지가 어디에도 정의돼 있지 않아** 기동 시 상수 형식 검증(`PROC-901` `B3`)이 판정할 근거가 없었다. **[확정]** 판정 기준을 새로 만들지 않고 **기동 시 형식 검증 대상에서 제외**한다 — 유효성 규칙 칸을 `허용 형태는 상수표가 정본` → **"기동 시 형식 검증 대상이 아니다 · 허용 형태(최대 400자·최대 3단락·평문)는 상수 **작성 지침**이며 정본은 상수표 · 부재·빈 값은 미충족이 아니라 **빈 문자열**"** 로 교체하고 표 아래 근거 불릿 2개를 더했다. **[근거]** ① 화면이 안내 문구를 **평문으로만 출력**하고 서식·링크·HTML 을 해석하지 않으므로(`DATA-003-05` · `design-system.md` §안내 영역 레이아웃 전제 3 · `design-system-components.md` §NoticeBlock) 태그가 담겨도 문자 그대로 보일 뿐 — **막을 실행·해석 위험이 없고 안전은 렌더 규칙이 이미 보장한다** ② 통제 지점은 **상수 작성·배포 절차**이며 허용 형태를 벗어나는 문구가 필요해지면 상수와 화면 사양을 함께 고친다(`EXC-DATA-08` · 같은 문서 전제 5) — 애플리케이션이 판별하지 않는 구조라는 점에서 잠정값 미판별(`OPS-001-03`·EXC-OPS-01)과 같다. 400자·3단락도 같은 이유로 검증하지 않는다(초과해도 렌더는 되며 처리는 절차가 맡는다). 기동이 이 상수에 대해 하는 일은 **문자열로 읽어 들이는 것뿐**이다(`EXC-BIZ-07` 안내 영역 미표시). 신설 코드 **0** · 205줄. **[미수정 — 후속 필요]** ① `PROC-901` `B3` 쪽 명문화는 프로세스 doer 소관 ② **build 현행 구현이 본 확정과 상충** — `apps/backend/src/config/interlock-config.validators.ts` `isValidConsentNotice`(HTML 태그·링크 정규식 + 400자·3단락 검사, `MAX_CONSENT_NOTICE_LENGTH` 등)가 기동 검증에 걸려 있어 build 회귀로 제거 필요 | 공통 | `accountinterlockhub#472`·`#469` | 🚧 |
| 2026-07-26 02:41 | build | `apps/backend/src/crypto/`(FN-001~FN-006 신규 8파일)·`apps/backend/src/models/`(MDL-004~006 신규 4파일) 암호 규약 공통 기능 구현(Phase 3/18, P17 연동 라이브러리 C#·03776cd 의 반대 방향). **[구현]** `FN-001` 키 32바이트 정규화·IV 도출(`key-normalizer.ts` — 패딩바이트 사전충전 후 원본 바이트로 덮어쓰는 1절차로 32B 초과(절단)/미만(패딩잔존)/정확(무패딩) 3경계를 분기 없이 동시 처리, `iv` 는 `key` 의 **복사본**(subarray view 아님) — 한쪽 변경이 다른 쪽을 오염시키지 않게) · `FN-002` Base64URL 인코딩·디코딩(`base64url.ts` — Node 내장 `Buffer#toString/from('base64url')` 은 표준 Base64 문자(`+`·`/`·`=`)까지 관대히 허용해 규약(`SEC-001-08` "관대하게 받지 않는다")보다 느슨해 **미채택**, 의사코드(문자집합검사→길이판정→패딩복원·역치환→디코드)를 수동 구현) · `FN-003` 암호값 구조 판정(`cipher-pair.ts`) · `FN-004` 복호화 판정 4단계(`decryption-judgment.ts` — 본인확인·연동실행·자가진단 3접점이 공유할 **단일 진입점**으로 설계(`SEC-002-01`), AES-256-CBC 복호화는 Node 기본 패딩검증 유지(`setAutoPadding` 미변경 — 끄지 않음), UTF-8 엄격 디코딩은 `TextDecoder(fatal:true)`로 구현(`Buffer#toString('utf8')` 의 무음 `U+FFFD` 치환으로는 의사코드의 "UTF-8 해석 실패" 분기가 성립하지 않아 대체)) · `FN-005` 생년월일 형식(`birth-date.validator.ts` — `/^\d{6}$/` **m 플래그 미부여**, P17 개행함정 JS 미재현을 자가점검으로 실측 확인) · `FN-006` 추적 키 형식(`tracking-key.validator.ts` — 길이는 **코드포인트 기준**(`[...str].length`, `string.length` 아님) — Postgres `character_length` 와 단위 일치) · `MDL-004`~`006` 모델(`models/` — 매핑 엔터티 없음, `EncPair`·`TransferPayload`·`IdentityCheckInput`). 예외 클래스 4종(`ProtocolFormatError`=`EX-SEC-001`·`IdentityMismatchError`=`EX-AUTH-002`·`ProtocolViolationError`=`EX-SEC-002`·`BirthDateFormatError`=`EX-AUTH-001`)은 정책 카탈로그 값 그대로 사용, 새 코드 0건. 컨트롤러 미배선(접점은 이 Phase 범위 밖 — `app.module.ts` 미접촉, P07 이후 위임). 상수(키/IV 길이·패딩바이트·알고리즘·추적키 상한 255)는 `SEC-001` §규약 확정값이라 판단해 P01 설정 서비스 주입 대상에서 제외하고 모듈 내 상수로 둠(`CLAUDE.env.md` §연동 구성 상수 표에 미등재 — 배포로 안 바뀌는 프로토콜 고정값, `MAX_CONSENT_NOTICE_LENGTH` 등 기존 선례와 동일 처리). **[자가점검 — C# 하네스 대조, 실측]** `powershell -File apps/sender-sdk/build.ps1` 로 SDK 재빌드(소스 무수정·읽기 전용 참조) 후 스크래치 도구(`works/accountinterlockhub-480/DumpVectors.cs` — csc.exe 로 SDK dll 참조 컴파일, **PowerShell 도구로만 호출·Bash 미사용**)로 8케이스 실제 `encX`/`encY` 생성 → `works/.../verify.js`(컴파일된 `dist/crypto` 직접 require)로 왕복 복호화 대조. **결과 40/40 PASS**(exit 0): 키 원문 경계 3종(32B 미만·정확·초과, senderKey 길이로 유발 + `normalizeKey` 직접 단위점검 중복 확인) · base64url 치환문자(전 8케이스 `encX`/`encY` 전량에서 `-`/`_` 실측 확인 + encode/decode 왕복·관대하지 않음 별도 점검 4건) · X평문 1024B 정확 경계(`pad` 980자 왕복 일치) · 한글 다국어(119 UTF-8바이트 vs 79 UTF-16 문자, divergence 확인) · 이모지 추적키(코드포인트 11 ≠ UTF-16 길이 12, `FN-006` 판정 통과 확인) · 오류 경로(추적키 부재→`EX-SEC-002`·JSON파싱실패→`EX-SEC-002`·오생년월일→`EX-AUTH-002`) · `FN-005` P17 개행함정 재현 시도 2건(앞/뒤 개행 전부 거부 확인) · `FN-006` 255/256 경계. **[실측 발견·즉시수정]** 자가점검 1회차 실행에서 `Object.freeze(buffer)` 가 Node(v26) 에서 `TypeError: Cannot freeze array buffer views with elements` 로 즉시 크래시함을 발견(원소 있는 TypedArray freeze 시 Node 가 명시적으로 던짐 — 문서화되지 않은 채 관례적으로 쓰기 쉬운 함정) → `key-normalizer.ts` 를 "wrapper 객체만 freeze, `key`/`iv` 버퍼 자체는 freeze 하지 않음(내용 불변은 호출측이 변형하지 않는다는 구현 규약으로 대체)"으로 즉시 수정 후 재검증 40/40 통과(수정 전 최초 실행은 1번째 케이스에서 크래시로 중단). **[실측]** 루트 `npm run build` 0 에러(frontend+backend, 자가점검 전후 총 3회 재확인). **[제약 준수]** `apps/frontend`·`apps/sender-sdk`(읽기만)·`config`·`database`·`entities`·`migrations`·`.env*`·루트 `package.json`·`.gitignore` 미접촉(동시 작업 중이던 P14 프론트엔드·P01 회귀 워킹트리 변경분을 매 단계 `git status`로 확인하며 무접촉 유지) · 커밋은 명시 pathspec. **[WARN/확인 필요]** `FN-004` 는 `FN-005` 를 내부 호출하지 않는다(`function_FN-004.md` 의존기능표에 `FN-005` 부재 확인 — 호출측이 `FN-004` 호출 전 `FN-005` 로 형식을 먼저 통과시켜야 하며, P07 배선 시 호출 순서 준수 필요, 누락돼도 크래시하지 않고 대부분 `EX-AUTH-002` 로 귀결되어 발견이 늦어질 수 있음) · `Object.freeze(Buffer)` 크래시는 이 코드베이스 다른 위치에서도 재현 가능한 일반 Node 함정이라 후속 참고용으로 기록 | 공통 | `accountinterlockhub#480`(부모 `#469`) | 🚧 |
| 2026-07-26 01:35 | build | `apps/backend`(NestJS 11·TS)·`apps/frontend`(React 19+Vite·TS) 신규 스캐폴드(근-그린필드, 기존 `apps/` 부재) + `PROC-901` 애플리케이션 기동·상수 검증 본구현(`SVC-018`·`MDL-022`). **[PROC-901 구현]** `apps/backend/src/config/`(loader·validators·consent-version·service·module, 순수함수부는 NestJS 비의존) — 기동 시 `process.env` 9개 상수(`INTERLOCK_ENTRY_PATH`·`SELFCHECK_PATH`·`RECEIVER_DELIVERY_URL`·`CONSENT_ITEMS`·`CONSENT_NOTICE`(선택)·`RETENTION_MONTHS`·`RETENTION_MAX_MONTHS`·`CONSENT_PROOF_RETENTION_MONTHS`·`COMPLETION_REDIRECT_URL`)의 존재·형식을 **전량 수집 후 일괄 판정**(`OPS-001-02`), 미충족 시 `NestFactory.create()` 이전(표면 개방 전)에 표준출력으로 **상수명만**(FN-015 취지) 남기고 `process.exitCode=1`로 중단(`EX-OPS-001`, HTTP 응답 없음). 전량 충족 시 `data_ENT-002.md` 규칙대로 동의 항목 버전 식별자 산출(정렬→속성순서 재구성→공백없는 JSON→문구 끝공백만 제거→`0x1F`결합→SHA-256 소문자 64자)해 `InterlockConfigService`(`@Global` 모듈)로 전역 보유 후 `app.listen()`. **[실측]** `npm run build`(루트) 0 에러(컴파일 1건 수정 — `emitDecoratorMetadata`+`isolatedModules` 조합에서 순수 인터페이스는 `import type` 필수, TS1272) · 런타임 부트 3케이스 재현(무상수 8개 전량 보고+exit 1 / 형식위반 8필드 전량 보고+exit 1 / 전량 유효값 기동 성공+포트 리슨+curl 404 확인(컨트롤러 미등록이라 정상, 리스닝 증거)+로그 버전해시가 별도 단위테스트 해시와 일치) · 동의 문구 400자/3단락/HTML태그/링크 검증기·항목 JSON 구조(중복코드·누락속성) 검증기는 단위 함수 호출로 개별 실측. **[제약 준수]** 관리자·DB·인증 미구현(P02·P05 이후 소관 — 저장 3종 원칙상 이 Phase 는 DB 테이블 0) · 상수 실값 코드·커밋 무기재(런타임 검증은 임시 `.env` 로만, 테스트 후 삭제·미스테이지) · `<COMPLETION_REDIRECT_URL>` 기동 필수 상수로 반영. **[WARN/확인 필요]** `migration:run` 은 P02 착수 전까지 안내만 출력하는 자리표시자 · `SELFCHECK_PATH` 실제 라우팅 배선(`B5`)은 컨트롤러 부재로 값 제공까지만 하고 실배선은 `PROC-204`(P12) 시점 판단 위임(Nest `@Controller` 는 컴파일타임 상수 경로만 허용해 런타임 값 기반 마운트 방식 별도 설계 필요) · **작업 중 저장소에서 미커밋 `apps/sender-sdk`(C# 연동 라이브러리, P17 추정) 발견 — 동시 작업 정황, 본 작업은 접촉·스테이징하지 않음**(오케스트레이터 확인 요청) · `<CONSENT_NOTICE>` '서식' 금지의 구체 판정은 HTML 태그·링크 정규식으로 구현(사양 원문에 예시가 없어 해석이 필요했음, tester 확인 권장). **[회귀 1회차]** 독립 코드리뷰 Important 1건 시정 — `.env.example` `INTERLOCK_ENTRY_PATH` 주석이 확정 실값(`/interlock/entry`)을 "예시"로 복제해 `OPS-001-04` 위반(형태 설명으로 정정, 파일 전체·P02 추가 DB 접속 키 재점검 결과 잔여 실값·비밀값 유출 0건 재확인) · 자체보완 2건 — `isValidConsentNotice` 단락분리 정규식이 `\r\n\r\n`(CRLF 빈 줄)을 놓쳐 4단락 이상 문구를 1단락으로 오판정하던 결함 수정(실행 검증으로 CRLF 4단락 거부·CRLF 3단락 통과 확인)·루트 `package.json` 댕글링 `dummy-test` 스크립트 제거. 의존성은 로컬 npm 레지스트리 실측 최신판 고정(Nest CLI 11.0.24·create-vite 9.1.1 스캐폴드 기준, TypeScript 는 backend `^5.7.3`/frontend `~6.0.2` — 앱별 독립 버전) **[재작업 — P01(#478) 사양 역전파 3건, 2026-07-26]** spec 회귀(`#472`·`#475`·`#476`)가 지목한 build 시정 3건 반영. **③ 개행 정규화 신설(data_ENT-002.md §버전 식별자 산출 규칙 7→8단계)** — `consent-version.ts` 에 `\r\n`·`\r`→`\n` 정규화 단계를 파싱 결과 항목의 모든 문자열 속성(코드·항목명·설명)과 안내 문구 원문 양쪽에 추가(정렬보다 앞선 단계로 배치해 정본 순서 준수), **해시 입력에만** 적용하고 호출측이 보유하는 원본 items·notice 는 변경하지 않아 `consent_snapshot` 원문 보존과 분리 유지. `interlock-config.validators.ts` 의 재번호 인용("산출 규칙 2" → "3")도 정정. **② `<CONSENT_NOTICE>` 기동 형식 검증 폐지(MDL-022)** — `isValidConsentNotice`·`MAX_CONSENT_NOTICE_LENGTH`·`MAX_CONSENT_NOTICE_PARAGRAPHS`·`HTML_TAG_PATTERN`·`LINK_PATTERN`(1회차 자체보완 CRLF 단락판정 정규식 포함) 전량 삭제, `loader.ts` 는 값을 그대로 읽기만 함, `MissingConstantKey` 유니온에서 `CONSENT_NOTICE` 제거(필수 8종과 정확히 일치하도록 축소) — 최초 구현 로그의 "구체 판정은 HTML 태그·링크 정규식으로 구현했다"는 WARN 은 이번 제거로 해소. **① 필수 8종·복귀 주소 절대 URL(확인만)** — 기존 `isUrlFormat()`(`/^https?:\/\//` 선검사 + `new URL()`)이 이미 `completionRedirectUrl` 에 적용돼 있어 코드 변경 없음(`interlock-config.validators.ts` 17~26행 · `interlock-config.loader.ts` 58~61행) — 사양의 "필수 8종"과 `MissingConstantKey`(8개) 일치를 함께 확인. **[실측]** 루트 `npm run build` 0 에러(frontend+backend) · 컴파일 산출물(`apps/backend/dist/config/*.js`) 직접 require 하는 격리 스크립트(scratchpad, 저장소 밖)로 재현 — 같은 내용의 LF·CRLF·단독 CR 세 변형이 **동일 버전 해시**, 내용이 다르면 **다른 해시**, 회귀 전 로직이면 거부됐을 HTML 태그+525자+4단락 안내 문구가 새 loader 에서는 `missing` 에 `CONSENT_NOTICE` 없이 통과, 빈 env 기준 `missing` 정확히 **8개**(`CONSENT_NOTICE` 불포함), 상대경로·스킴없음 복귀주소는 거부·정상 https 는 통과 — 전 판정 기대값과 일치. 추가로 `DOTENV_CONFIG_PATH` 격리 fixture(scratchpad, 실 `.env` 무접촉)로 `node dist/main.js` 8초 기동 재현 — HTML 태그+400자 초과 안내 문구를 포함한 채 Nest 앱 정상 기동·포트 리슨·버전 해시 로그 출력까지 확인(회귀 전이면 기동 중단됐을 값). **[건드린 파일]** `apps/backend/src/config/consent-version.ts`·`interlock-config.validators.ts`·`interlock-config.loader.ts`·`interlock-config.types.ts`·`apps/backend/.env.example`(주석 정합, 실값 무기재) — `entities`·`database`·`frontend` 등 타 Phase 점유 경로 무접촉(매 단계 `git status` 로 확인). **[확인 필요]** 없음(사양 결함 미발견 — 3건 전부 원문과 현행 구현이 정합). **[미해소]** 이 설정 로직을 고정하는 자동화 테스트가 저장소에 없어(backend 테스트 러너 미구성) 향후 회귀는 `npm run build` 컴파일 통과만으로는 못 잡음 — TC 커버리지는 `docs/specs/qa/` 소관(`BAT-05_006` 등)이며 본 작업은 코드만 시정 | 공통 | `accountinterlockhub#478`(부모 `#469`) | 🚧 |
| 2026-07-26 00:45 | spec | `docs/specs/` 7 도메인 + `mockup/` **77 파일** — **담당자 요구사항 회귀 반영**(commit `162c793`·`d433fea` 방향분, 담당자 지시 `#467` journal 3277·3285). directing 이 확정한 요구 delta 2건을 spec 전 도메인으로 내렸다. **[delta ① 복귀 이동]** **연동 완료 결과에만** 복귀 이동을 둔다 — 결과를 3초 보여 준 뒤 **요청 처리 응답이 준 복귀 주소로 자동 이동**하고, 완료가 아닌 결과는 **주소를 받지 못하므로**(허브가 싣지 않는다) 결과 화면에 잔류한다. **[delta ② 사용자 거부 결과 경로 폐지]** **전 항목 동의가 진행 조건**이 되어 거부를 제출하는 인터페이스가 사라졌고, 미동의 제출은 **화면 유효성 안내로 차단**돼 접점에 닿지 않는다. 동의하지 않으려는 사용자의 종료 수단은 **창을 닫는 것**이며 그 연동은 **결과 미확정**으로 남아 보관 기한에 삭제된다 — 거부라는 확정 결과를 쓰지 않는다. **[확정 사항]** 결과 경로 **4→3 재번호**(① 연동 완료 / ② 링크·복호화 오류 / ③ 수신처 전달 실패 — **결번 없이 당겨 표기**) · 결과 구분 **4종→3종**(`USER_DENIED` 폐지) · **응답 필드 `returnUrl` 확정**(선택 · **경로 ① 한정** · `https://`·`http://` 절대 URL · **부재의 표현은 속성 부재**(빈 문자열·`null` 로 채우지 않는다) · 동봉 판정은 `PROC-105` `B3` **한 곳** · **화면은 필드 존재 여부만** 보고 이동하며 경로 번호로 다시 판정하지 않는다) · 승인 요청 `decision` 필드 **폐지**(값이 하나뿐이라 분기가 성립하지 않는다) · `ENT-001.result_code` CHECK **3종** · **`ENT-003.user_denied_count` 컬럼 삭제**(카운터 5→**4개** = 요청 수 1 + 결과 구분 3, 두 성공률 정의 산출 가능성은 유지) · `PROC-103` `F4`·`B5a` **결번**(**뒤 라벨을 당기지 않는다**) · **`BR-004` 슬롯 내용 교체**(승인·거부 선택 → **필수 동의 충족 판정 = 화면 게이팅 1차 방어**) · **복귀 이동 대기 시간 3초의 정본 = [`screen_SCR-004.md`](../docs/specs/screens/screen_SCR-004.md)**(다른 도메인은 값이 아니라 이름으로 참조). **[신설 코드 3건]** 정책 규칙 `BIZ-001-06`(복귀 주소는 연동 완료 결과에만) · 예외 `EXC-BIZ-15`(적용 경로 = ① 하나로 담당자 확정 종결) · 분기 `BR-025`(복귀 주소 동봉 판정) — **그 외 신설 0**(SVC·PROC·SCR·FN·MDL·ENT·EX 전건). **[실측]** 문서 **77 파일**(`docs/specs/` 72 + `mockup/` 5) · TC **232→238**(신설 8 = `USR-04_016`·`USR-05_011`~`_017` / 결번 2 = `USR-04_008`·`USR-05_002`) · 커버리지 게이트 전건 충족(SVC 18/18 · PROC 18/18 · BR 25/25 · EX 13/13 · 정책 규칙 88/88 · 접점 11/11 · MDL 22/22 · ENT 3/3 · SCR 4/4 · FN 15/15 — **미검증 0**) · 상대 링크 깨짐 **0**(`docs/`·`mockup/` 실측) · 300줄 초과 **0**(최대 294 — `process_PROC-103.md`) · 폐지 IA 코드 사용 0 · 상수 실값 복제 0(명문화된 TC·목업 예외 제외). **[교차검증]** 재검증 **🔴 0 · 🟠 0**(🟠 3 해소) · **🟡 5 전건 자체보완** — ① `SVC-004` 참여 PROC 색인의 `PROC-105` 를 **간접 호출**(`PROC-103` `B8` 경유)로 표기 ② SCR-002 에 성립하지 않던 **제출 중 스피너·폼 잠금** 서술을 SCR-001 한정으로 정합(`design-system.md` §상태 표현 · `design-system-components.md` §Button) ③ `API-02_004` 가 쓰는 시드 3행의 "사용 TC" 누락 보완 ④ **검증 TC 의 상수 실값 인용 예외 명문화**(`spec-qa.md` — 인용일 뿐 정본 아님 · **비공개 값(`<SELFCHECK_PATH>`)은 예외 대상 아님**) ⑤ 목업의 상수 실값 예외를 `<COMPLETION_REDIRECT_URL>` 하나에서 **`<CONSENT_NOTICE>`·`<CONSENT_ITEMS>` 항목명·설명까지 확장** | `USR-01`·`USR-02`·`USR-03`·`USR-04`·`USR-05`·`API-01`·`API-02`·`API-04`·`BAT-04`·`BAT-05`·`BAT-06`·`LIB-02` | `accountinterlockhub#469`·`#470`~`#477` | 🚧 |
| 2026-07-25 18:39 | spec | `mockup/`(SCR-002.html·SCR-004.html·INDEX.md 갱신 + Redmine 첨부 `index.html` 보완) **사양 확정분 목업 동기화** — spec 오케스트레이터가 처리한 사양 보완 후보 3건(`accountinterlockhub#477` 후속 라운드, 사양 commit `1c098b5`·`c9a48e5`)을 목업에 반영했다. **[SCR-004]** 경로 ③ 은 확정 결과 재안내 대상이 아님(`EXC-BIZ-14`)이 확정돼 경로③ 선택 시 재안내 토글을 `disabled`+자동 해제하고 안내 문구로 대체 — 이전에 띄운 노란 경고 배너 제거(사양 확정으로 해소). **[SCR-002]** ConsentList 잠금(제출 중) 시각 처리가 비활성 토큰 쌍(`--color-disabled-bg`/`--color-disabled-fg`, 4.82:1 재사용)으로 확정돼 `opacity:.6` 을 제거하고 토큰 색상으로 교체 + 목록에 `aria-busy="true"` 추가(체크박스 `disabled`·`pointer-events:none`·`필수` 배지는 유지). **[SCR-001]** 알림 위치는 목업 구현(입력 영역 아래 단일 위치)이 그대로 사양으로 확정돼 **변경 불요**. `mockup/INDEX.md` §사양 보완 후보 3건 전건을 "해소"로 갱신(확정 내용·근거·목업 반영 결과 기재, 기존 행 상태는 유지). Redmine `#477` 첨부를 5건→**6건**으로 보완 — 누락됐던 `index.html` 신규 첨부 + 내용 변경 3건(`SCR-002.html`·`SCR-004.html`·`INDEX.md`) 구 버전 삭제 후 최신본 재첨부(파일당 최신 1건 유지), 무변경 2건(`SCR-001.html`·`SCR-003.html`)은 그대로 둠 | `USR-04`·`USR-05` | `accountinterlockhub#477` | 🚧 |
| 2026-07-25 18:32 | spec | `docs/specs/`(policy_BIZ · data_ENT-001 · process_PROC-105 · screen_SCR-004 · qa/spec-qa-seed — 5 파일) **목업 리뷰 발견 M-1 최종 처리 — 구조적 한계 명문화(동작 무변경)**. 직전 회귀의 Option A(승인 시점 재복호화 실패를 레코드 `result_code = DECRYPT_FAILED` 로 확정)가 **구현 불가로 반려·오케스트레이터 수용**됨에 따라, 사양 동작은 그대로 두고 **암묵적이던 사실을 4지점에 드러냈다**(후행 build·qa 가 도달 불가 분기를 결함으로 오인하거나 도달 가능한 것처럼 구현하지 않게 하는 것이 목적). **[정책]** `BIZ-001` 예외에 **EXC-BIZ-14 신설**(이번 작업의 유일한 신설 코드) — `DECRYPT_FAILED` 는 결과 구분 4종 값 체계의 일원이나 **추적 레코드의 `result_code` 로 확정되는 경로가 없다**: 근거 ① 추적 키는 X 안의 필드값이라 복호화 판정 **3·4단계 통과로만** 얻어지는데 이 값을 만드는 실패가 바로 그 단계 실패다(`function_FN-004.md` 단계 5·6·`SEC-002-03`) ② 세션 부재로 앞 요청의 추적 키를 가져올 수단이 없다(`AUTH-001`·`BIZ-002-06`·`DATA-001-02`) ③ `FN-004` 는 (`encX`·`encY`·생년월일)의 **순수 결정적 함수**라 3·4단계가 실패하는 조합으로 만들어진 레코드가 애초에 없다(승인 시점 재복호화 실패 포함). 구현 가이드 3항 동반 — **열거형 4종 유지**(값 체계 공유 `BIZ-001-03`)·도달 불가 분기를 결함으로 다루지 않음 / **발송처 인지 수단 3종**(지표 집계 `decrypt_failed_count` 집계 수준 · 사용자 화면 경로 ③ · **통합 시점 자가진단 API `API-04`** = PRD §제공 가치 "통합 시점의 규약 확인"의 설계 의도) / **개발사양 간극 명시** — `devspec/database.md` §연동 추적 레코드의 "복호화 실패를 서로 구별한다" 요구가 **레코드 단위로는 성립하지 않는다**는 한계를 적고 세 수단으로 대체된다는 근거를 남겼다(충족되지 않는 것은 개별 연동 1건을 사후에 지목해 복호화 실패로 읽는 것뿐이며, 그러려면 실패한 바로 그 복호화로만 얻어지는 추적 키가 필요하다). **[데이터]** `ENT-001` — `result_code` CHECK 열거형 4종 **무변경**, 기록 경로 부재·값 체계 공유 근거를 속성 주석에 명시하고 §데이터 생명주기 수정 조건에 "복호화 실패는 결과 확정 계기 목록에 없다"를 병기. **[프로세스]** `PROC-105` — `RECORD` 출처가 실어 오는 값은 `SUCCESS`·`USER_DENIED`·`DELIVERY_FAILED` 셋뿐임을 진입 조건에 명시하고, `B1` 주석·`B2` `case 'DECRYPT_FAILED'` 를 **방어적 분기(현 설계에서 도달 불가)** 로 표기(**분기 삭제 0** — 열거형 전건 처리 유지) + 구현 가이드에 3출처 근거와 "`RECORD` 에 `reasonCode` 가 없어도 무해" 근거 기재. **[화면]** `SCR-004` §확정 결과 재안내 — **경로 ③ 은 재안내 대상이 아니다**(레코드에 `DECRYPT_FAILED` 가 없고, 진입 단계 실패는 레코드 자체가 없어 재진입해도 매번 새 판정) · **배지가 붙는 경로는 ①·②·④** 를 대상 경로 행·상태 전이 `ReAnnounced`·§조건부 표시에 반영. **연쇄 정정** `spec-qa-seed.md` — `SEED-TRK-FIXED-DECRYPT` 는 런타임으로 만들어질 수 없는 상태를 SQL 로 직접 만든 **값 체계 검증 전용 시드**(재진입·재안내 흐름 미사용)임을 시드 표 서문·비고에 명시. **신설 코드 EXC 1건 외 0**(정책 규칙·EX·MDL·SVC·PROC·SCR·FN·ENT·TC 전부) · **TC 232 유지** · 결과 구분 **4종**·화면 **4경로** 유지 · 300줄 초과 0(최대 297) · 상대 링크 깨짐 0(1,175) | `USR-05`·`BAT-04`·`API-01` | `accountinterlockhub#477`·`#470`·`#472`·`#474`·`#475` | 🚧 |
| 2026-07-25 18:16 | spec | `docs/specs/screens/`(design-system · design-system-components — 2 파일) **목업 리뷰 발견 사항 반영(회귀 3회차)** — mockup-builder 가 `mockup/INDEX.md` §사양 보완 후보로 올린 3건 중 **M-2·M-3 해소 · M-1 미반영(구현 불가 판단 → 차단 보고)**. **[M-2 알림 위치 상충 해소]** 디자인시스템 §상태 표현의 "오류(처리) = **카드 상단**" 서술을 폐기하고, 인라인 알림은 **화면 사양이 고정한 알림 영역 한 곳**(본문 영역 아래 · 액션 영역이 있으면 그 위)에만 나타나며 **오류 유형(입력·처리)에 따라 자리를 옮기지 않는다**는 규칙으로 통일 — 컴포넌트 카탈로그 §InlineAlert 의 2분기 서술("필드 아래 / 카드 제목 아래")도 같은 규칙으로 교체하고 `SCR-001`·`SCR-002`·`SCR-003` 레이아웃을 역참조했다(화면 사양 3건은 이미 이 규칙과 일치해 **무변경**이며 목업 구현과도 일치). **연쇄 정정** — §목록의 InlineAlert 사용 화면에 **SCR-003 추가**(SCR-003 §레이아웃 구성 4 가 이미 알림 영역을 갖는데 카탈로그만 누락돼 있었다). **[M-3 ConsentList 잠금(제출 중) 시각 처리 정의]** 잠금 상태를 **버튼 비활성과 같은 토큰 쌍**(행 바탕 `--color-disabled-bg` · 항목명·설명·체크 표시 글자 `--color-disabled-fg`)으로 확정 — **실측 대비비 4.82:1 을 그대로 재사용해 새 측정이 필요 없다**. 목업이 임의 적용한 `opacity:.6` 은 **금지**로 명문화(전경·배경을 함께 옅게 만들어 실측 대비비를 무효로 한다)했고, 체크 상태·구분선·행 높이·`필수` 배지는 유지, 접근성은 목록 `aria-busy="true"` + 체크박스 `disabled`(Button 이 `aria-disabled` 를 쓰는 이유가 여기엔 성립하지 않음을 근거로 명시). §색 대비 검증 마지막 행 "쓰이는 자리"에 잠금 목록 글자 병기. **[M-1 미반영 — 차단 보고]** `DECRYPT_FAILED` 레코드 확정 경로 복원(Option A)은 **지정된 반영 지점(`PROC-104` `B2`)에서 구현 불가**로 판단해 정책·데이터·기능·프로세스·화면·TC **6도메인 전부 무변경** — 근거 ① 추적 키는 X 안의 필드값이라 `FN-004` 판정 3·4단계가 실패하면 값 자체가 존재하지 않는다 ② 허브는 세션이 없어(`AUTH-001`·`BIZ-002-06`) 본인확인 때 얻은 추적 키를 승인 요청으로 가져오지 않는다(`spec-functions-api-user.md` — "승인 단계는 추적 키를 다시 복호화로 얻는다") ③ `FN-004` 는 (`encX`·`encY`·생년월일) 의 **순수 결정적 함수**라 3·4단계가 실패하는 입력 조합은 본인확인에서도 동일하게 실패해 **그 조합으로 레코드가 만들어진 적이 없다**. 오케스트레이터 재판단 요청(대안 = 구조적 도달 불가를 사양에 명시하는 안). 신설 **0**(토큰·컴포넌트·정책 규칙·TC 전부) · TC **232 유지** · 결과 구분 4종·화면 4경로 유지 · 300줄 초과 0(최대 297) · 깨진 링크 0 | `USR-01`·`USR-02`·`USR-03`·`USR-04` | `accountinterlockhub#477`·`#474` | 🚧 |
| 2026-07-25 17:36 | spec | `docs/specs/` 11 파일 **교차검증 2회차 경고 5건 보완(회귀 2회차 — 이월 종결)** — 🔴 0·🟠 0(완료 게이트 기충족) 상태에서 🟡 N-1~N-5 전건 자체보완. **정책** `OPS-003-05` 적용 PROC 을 `전 PROC` → **FN-014 호출 9개**(`PROC-101`~`PROC-105`·`PROC-201`~`PROC-204`)로 축소하고 HTTP 응답 표면이 없는 배치·라이브러리·기동 PROC 제외 근거를 구현 가이드에 명시(`spec-process-policy-map.md` 같은 행 동반 정정 — 별칭 색인은 이미 정확해 무변경). **검증 TC** `EX-OPS-002` TC 4건(`USR-01_013`·`API-02_008`·`API-03_010`·`API-04_015`) §검증 대상에 `OPS-003-05` 병기 — 런타임 매핑 81건 중 유일하던 TC 미인용 규칙 해소(**TC 신설 0**). **화면** `SCR-004` 경로 ③ 사유 코드 목록에 `EX-OPS-002` 문구 확정 + 미등재 코드 폴백 규칙 명시(목업이 그대로 소비) — 같은 사실을 인용하는 `spec-screens.md`·`PROC-105` 3지점·`SVC-005` 예외 흐름까지 연쇄 정정해 기능·API 정본(200 화면 + 경로 ③)과 5개 문서 일치. **서비스** `SVC-013` F-002·Happy Path 2단계의 "형식" 을 **생년월일 한정**으로 축소(암호값 구조 위반은 판정 결과 — §예외 흐름 경계 문구와 일치). **IA 이력** 검증 TC 도메인 5개 시각대를 실제 commit 시각으로 정정(16:05→15:48 · 16:25→15:53 · 16:45→15:59 · 17:05→16:05 · 17:35→16:19 · 18파일, 행 추가·삭제 0) — 이번 루프 구간 최신순 역전 0. 신설 코드 **0**(EX·MDL·SVC·PROC·SCR·FN·ENT·정책 규칙·TC 전부) · 정책 규칙 **87 유지** · TC **232 유지** · 300줄 초과 0(최대 297) · 깨진 링크 0(1,224) | `USR-01`·`USR-05`·`API-02`·`API-03`·`API-04` | `accountinterlockhub#470`·`#471`·`#474`·`#475`·`#476` | 🚧 |
| 2026-07-25 17:01 | spec | `docs/specs/` 18 파일 **교차검증 1회차 지적 보완(회귀 1회차)** — 🟠 2건(E-1 MDL 소비처 역참조 4지점 · E-2 예외 코드 개수 표기)·🟡 7건(W-1~W-7) 전건 자체보완. **데이터** `MDL-013` 완료 콜백 재사용 반영(`SVC-012`·`PROC-203` · §모델을 두지 않는 것 1 갱신)·`MDL-005`·`MDL-006` 자가진단 소비 반영(`SVC-013`·`PROC-204` — 문서 내 자기모순 해소)·예외 코드 **개수 표기 제거**(정책 카탈로그 단일 출처 참조로 전환). **정책** `SEC-003-04` 를 **입력 부재 한정**으로 좁히고 형식 위반은 `AUTH-002-02`·`SEC-002-02` 로 위임(`EX-SEC-003` 의미 정정)·**`OPS-003-05` 신설**(`EX-OPS-002` 정의 규칙 이관 — 정책 규칙 86→**87**)·`SEC-001-12` 적용 PROC 을 `PROC-403`·`PROC-404` 로 갱신. **프로세스** 단계명 색인의 과장 주장 정정 + **별칭 색인 26건 신설**(정책 적용 단계 표기 87/87 추적 가능)·`PROC-304` `batchSize` 출처 명시. **기능·API** 진입 접점 `EX-OPS-002` = 200 화면 + 결과 경로 ③ 명시·`FN-013` 계수 지점에 `PROC-102` 추가. **서비스·화면** 단계명 3건을 확정명으로 정정. 신설 코드 0(정책 규칙 `OPS-003-05` 1건 제외) · 300줄 초과 0(최대 297) · 깨진 링크 0(1,157) · 미정의 코드 참조 0 | `USR-01`·`USR-02`·`USR-03`·`USR-04`·`API-02`·`API-03`·`API-04`·`BAT-02`·`BAT-04`·`BAT-06`·`LIB-01`·`LIB-03`·`LIB-04` | `accountinterlockhub#470`·`#471`·`#472`·`#473`·`#474`·`#475` | 🚧 |
| 2026-07-25 16:19 | spec | `docs/specs/qa/`(spec-qa · spec-qa-seed · spec-qa-mapping · USR/tc_USR-01·02·03·04·05 · LIB/tc_LIB-01~04 · API/tc_API-01~04 · BAT/tc_BAT-02·04·05·06 · common/tc_SCEN-journey · tc_SCEN-platform — 22 파일) **검증 TC 도메인 신규 작성**(`#469` 제품 루프 spec 레그 도메인 7순위 · **구 TC 미승계** — git 이력의 구 TC 를 근거로 삼지 않았다). **TC 232건**(기능단위 209 + 횡단 `SCEN_` 23) · 중복 채번 0. **[커버리지 게이트 전건 충족]** SVC **18/18** · PROC **18/18** · BR **24/24** · EX **13/13** · 정책 **17/17**(전건 3건 이상) · 접점 **11/11**(2xx+4xx+5xx 최소 3케이스) · SCR **4/4** · ENT **3/3** — 미검증 0. **[유형 분포]** Positive 80(34.5%) · Negative 64(27.6%) · **Boundary 33(14.2% — 20% 상한 충족)** · 권한/인증 12(5.2%) · 상태전이 20(8.6%) · 시스템예외 15(6.5%) · 조합경계값 8(3.4%). 계층은 INTG 96 · DATA 53 · POL 32 · UNIT 20 · E2E 19 · UI 12, 자동화는 자동 203 · 반자동 25 · 수동 4. **[AUTH 치환 12건]** 인증이 설계상 없으므로(`AUTH-001`) 토큰 검사 TC 를 두지 않고 — 서버 대면 API 4종 **인증 없이 호출 가능**(설계된 부재의 Positive 검증) · 자격이 담긴 요청도 검증 없이 처리(401·403 미정의) · **관리자 경로·화면·구성 API 부재** · **계정·세션 부재**(새로고침 = 처음부터) · **본인확인이 유일한 신원 확인 수단** · **자가진단 비공개 경로 한정 응답**(추측 경로·메서드 불일치 전건 일반 404) · 요청 제한 부재·조회로 인한 보관 기산 개시 수용 리스크 확인. **[범위 밖 TC 미생성 7종 + 근거 명시]** 토큰·권한 검사 · 레이트 리밋 · N회 실패 잠금 · 개인정보 마스킹 · **성과 지표 목표치 판정**(잠정값 — 대신 카운터 누적 정확성 13건) · **데이터 이관 `MIG_`**(이관 범위 없음 — 근거 명시) · 다섯 번째 경로(부재 검증으로 대체). **[환경·모킹]** `ENV-WEB`·`ENV-API`·`ENV-CLI`·`ENV-LIB`·`ENV-DB`(PostgreSQL 16) · **모의 수신처 `MOCK-RCV` 필수**(`<RECEIVER_DELIVERY_URL>` 잠정값이 문서용 예약 도메인이라 실 호스트 미연결 — 응답 모드 6종) · **C# 콘솔 호출 하네스** · fault 주입·동시 요청 하네스 미구축 시 해당 TC 는 🟠 Block 로 등재. **시드 카탈로그 5군 37항**(암호값·전달 데이터 · 추적 레코드 · 증적·지표 · 구성 상수 · 점검 절차)을 **"확인 방법" 전건 기재**(검증 쿼리 조건 또는 화면 확인 절차). **[미해소 1건]** QA-OPEN-01 연동 성공률 분모·분자 정의(담당자 이월) — **TC 를 Block 시키지 않는다**(`BAT-06_011` 은 두 정의 산출 가능성만 검증). 잠정값 6건·성과 지표 목표치는 **미해소 항목이 아니며**(담당자 잠정 확정) 배포 게이트 TC(`SCEN_022`·`LIB-03_008`)로 대신 확인. **[정합 실측]** 신설 코드 0(SVC·FN·MDL·ENT·SCR·PROC·정책 규칙 ID·EX·BR 인용만) · 횡단 식별자는 `SCEN_` 하나 · 상대 링크 깨짐 **0건** · 잠정값·성과 지표·재시도 수치·보관 개월 수 literal 복제 **0건**(플레이스홀더 참조만) · 자가진단 경로 값 기재 0건 · 폐지 IA 코드 채번 **0건** · 전 문서 300줄 이하(최대 192줄). 템플릿 항목은 300줄 제한 안에 **누락 없이 압축**했고 대응표를 `spec-qa.md` §TC 문서의 항목 대응에 남겼다 | USR-01, USR-02, USR-03, USR-04, USR-05, LIB-01, LIB-02, LIB-03, LIB-04, API-01, API-02, API-03, API-04, BAT-02, BAT-04, BAT-05, BAT-06 | `accountinterlockhub#476`·`#469` | 🚧 |
| 2026-07-25 15:24 | spec | 프로세스 도메인 신규 작성 — `docs/specs/processes/` **PROC 18종**(101~105·201~204·301~304·401~404·901 — 선행 도메인 예약분을 같은 코드·같은 의미로 확정, 추가 채번 0)·목록/매핑/정책맵 3문서. **단계 라벨 체계 확정**(`F` FE·`B` BE·`C` 호출측·`L` 라이브러리 내부 — 사용자 화면이 없는 PROC 도 FE 섹션을 비우지 않고 호출측 처리를 적는다)·선행 도메인 단계명 **전건** 라벨 매핑·의존관계 **순환 0**(호출 깊이 3층: 901 / 접점 / 하위)·트랜잭션 경계·멱등 수단 총괄·**보상 트랜잭션 부재 근거** 명시. 커버리지 SVC 18/18·서버 호출 화면 액션 3/3·FN 15/15·MDL 22/22·정책 규칙 86/86(런타임 80 + 사양·구현 게이트·배포 절차 6). `PROC-901` 기동·상수 검증(미충족 전량 일괄 보고·부분 기동 금지·버전 식별자 기동 시 1회 산출) | `USR-01`·`USR-02`·`USR-03`·`USR-04`·`USR-05`·`LIB-01`~`LIB-04`·`API-01`~`API-04`·`BAT-02`·`BAT-04`·`BAT-05`·`BAT-06` | `accountinterlockhub#475`·`#469` | 🚧 |
| 2026-07-25 14:40 | spec | `docs/specs/screens/`(spec-screens · design-system · design-system-components · screen_SCR-001~004 — 7 파일) **화면 도메인 신규 작성**(`#469` 제품 루프 spec 레그 도메인 5순위 — **구 사양·구 목업 미승계**, git 이력의 구 화면 사양·구 디자인 토큰을 근거로 삼지 않았다). **[SCR 채번]** 화면 **4건**(`SCR-001` 본인확인 · `SCR-002` 동의·승인 · `SCR-003` 연동 진행 안내 · `SCR-004` 연동 결과 안내) — 서비스 도메인 예약 4종을 **의미 변경 없이 전건 확정**(통합 0 · 폐기 0 · 추가 0 · 재배치 0). 예약이 열어 둔 두 선택지는 ① `SCR-003` 통합안 **미채택**(독립 화면 유지) ② `SCR-004` 경로별 분리 **미채택**(한 화면 네 상태)로 판단했고, **진입(`USR-01`) 전용 화면도 신설하지 않았다**(진입 응답이 곧 첫 화면). **[단일 경로 4단계 모델]** 화면 넷은 `<INTERLOCK_ENTRY_PATH>` **하나의 경로 위 네 단계**이며 화면별 URL 이 없다(기능·API 의 리다이렉트 없음 계약 소비). 단계 값은 응답 `stage` 와 1:1 이고 `PROCESSING` 만 화면 소유. 전환은 전부 응답이 만들고 **되돌아가는 경로 2개**(`EX-BIZ-001`·`EX-BIZ-003` → SCR-002 / `EX-AUTH-001`·`EX-AUTH-002` → SCR-001)도 버튼이 아니라 응답이 만든다. 새로고침 = 진입부터. **[디자인 시스템]** 표면이 하나라 **토큰 분기·접두어 체계 미도입**. 색 **23종**을 역할 이름으로 정의하고 **실측 대비비 표 24행**으로 AA 충족을 증명(본문 4.5:1 · 비텍스트 3:1) — 본문 16.37:1 · 보조 6.38:1 · 경계 4.05:1 · 주 버튼 흰 글자 6.42:1 · 포커스 링 7.78:1 · 결과 4경로 텍스트 5.92~8.61:1, **기준 미달 0건**(약한 바탕 1.1:1 은 의미를 혼자 지지 않아 1.4.11 대상 아님을 명시, 비활성은 1.4.3 예외이나 4.82:1 확보). **모바일 우선** 단일 컬럼·브레이크포인트 2개(480·768)·본문 최대 폭 480px·320px 폭 가로 스크롤 0·히트 영역 **44x44** 전 조작 요소. 타이포는 시스템 글꼴(웹 글꼴 0)·입력 필드 16px 하한(모바일 자동 확대 방지). 상태 표현 7종(초기·로딩·입력 오류·처리 오류·비활성·빈 상태·결과)·**감소 모션 대응**·아이콘은 인라인 SVG(아이콘 글꼴·외부 이미지 0 — 외부 자원 요청 0). 공통 컴포넌트 **11종**. **[결과 4경로]** 색+아이콘+문구 3중 구분으로 **색각 접근성 확보**, **거부를 오류로 그리지 않음**(중립 계열). 다섯 번째 경로·경로별 URL 미생성. **[화면 없는 IA 12건 근거 기재]** `LIB-01`~`04`(발송처 프로세스 내 함수 호출·파일 산출물) · `API-01`~`04`(서버 대면 접점) · `BAT-02`(명령 진입점) · `BAT-04`~`06`(내부 기록) — 커버리지 판정 시 누락으로 오인되지 않게 목록 문서에 명시. **[관리자 표면 0]** 관리자 화면·인증·구성 화면·전역 네비게이션·로그인·모달 **전부 미생성**(`AUTH-001-01`·`AUTH-001-02`), 역할별 노출 제어 없음(런타임에 역할 개념이 없다). **[마스킹]** 화면에 **표시 대상 개인정보 0** — 허브가 보관하지 않아 되돌려 그릴 값이 없고, 화면이 다루는 값은 사용자 입력 생년월일(필드 안에서만)·URL 에서 읽은 암호값(미표시)·상수 유래 동의 문구 셋뿐. **[정합 실측]** 인용 정책 규칙 ID 35종·EX 8종·EXC 3종·MDL 5종·SVC 5종·PROC 8종·BR 1종 **깨진 참조 0건**(신설 코드 0 — SCR 외 채번 없음) · 상대 링크 전건 해석 성공 · 잠정값 literal 유출 **0건**(플레이스홀더 8종 참조만) · 성과 지표·재시도 수치 복제 **0건** · 폐지 IA 코드 사용 0건(미사용 선언 1줄 제외) · 전 문서 300줄 이하(최대 218줄). 후행 도메인(processes·qa) 미작성 — PROC 는 예약 채번 인용이며 실재 확인은 교차검증 시점 | USR-01, USR-02, USR-03, USR-04, USR-05, LIB-01, LIB-02, LIB-03, LIB-04, API-01, API-02, API-03, API-04, BAT-02, BAT-04, BAT-05, BAT-06 | `accountinterlockhub#474`·`#469` | 🚧 |
| 2026-07-25 14:10 | spec | `docs/specs/functions/`(spec-functions · spec-functions-api · spec-functions-api-user · spec-functions-api-server · spec-functions-lib · function_FN-001-003 · FN-004 · FN-005-006 · FN-007-008 · FN-009-011 · FN-012-013 · FN-014-015 — 12 파일) **기능·API 도메인 신규 작성**(`#469` 제품 루프 spec 레그 도메인 4순위 — **구 사양 미승계**, git 이력의 구 API·구 FN 을 근거로 삼지 않았다). **[FN 채번]** 공통 기능 **15건**(`FN-001`~`FN-015`) — 분류 **POL 5 · DAT 8 · CRS 2 · EXT 0**. **2개 이상 SVC 재사용 기준을 전건 충족**하며, 단일 SVC 전용 로직 6종(결과 경로 선택·필수 동의 재검증·수신처 전달 호출·기동 상수 검증·보관 삭제 산정·라이브러리 진입점)은 **의도적으로 FN 으로 만들지 않고** 근거를 목록 문서에 남겼다. **EXT 0건 근거** = 허브의 유일한 외부 호출(수신처 전달)이 `SVC-004` 하나만 쓰므로 기준 미충족이며, 그 호출의 프로토콜·타임아웃·재시도·성공 판정은 전달 접점 계약으로 정의. **[의존관계]** 순환 0 · 호출 깊이 3층(0층 8 · 1층 6 · 2층 `FN-004`), `FN-008`·`FN-009` → `FN-013` 은 동일 트랜잭션 경계. **[POL→FN 매핑]** 정책 17건 중 **12건이 FN 으로 구현**되고 나머지 5건(`AUTH-001`·`BIZ-004`·`OPS-001`·`OPS-002`·일부 `BIZ-001`/`BIZ-003`)은 설계 제약·배포 절차·기동 처리·단일 서비스 로직이 담당함을 매핑표로 명시 — 미구현 0. 인용 정책 규칙 ID **80종 깨진 참조 0건**. **[인터페이스 11접점]** 사용자 진입 표면 3(진입 GET·`/verify`·`/approve`) · 서버 대면 API 4(`/api/interlock/status`·`/completion`·`/callback`·`<SELFCHECK_PATH>` — **전부 POST**, 근거 3항 명시) · 수신처 전달 1 · 명령 진입점 1 · 라이브러리 공개 진입점 2. **[위임 확정 17건]** 진입 파라미터 이름 `encX`·`encY` · 리다이렉트 없는 페이지 응답 + 초기 상태 주입 · **승인 요청이 생년월일을 다시 실어 오는 재수신 규약**(서버 무보관 — 화면 도메인이 그대로 소비) · `decision`=`APPROVE`/`REJECT` · 오류 엔벨로프 `{code,message,details?}`(성공은 무래핑) · 완료 콜백 응답 = `MDL-013` 재사용 · 자가진단 **사유 코드 = 정책 EX 값 그대로**(라이브러리와 동일 체계·부적합도 200·원문 절대 미포함) · **전달 성공 판정 = HTTP 200~299**(본문 미사용·3xx 미추종) · **결과 확인 표시 = 응답 `resultCode` 가 null 이 아닐 때만 최초 1회** · 라이브러리 공개 API 2진입점 + 불변 결과 타입 + 전용 예외 + 규약 버전 상수 · 오류 3층 · MAJOR 불일치=비호환 · 배포 패키지 5종 + `SHA256SUMS.txt` · **🟡 G-04 해소 = 사용 안내 §4 환경별 기준 URL 기재란** · 테스트 벡터 JSON 1파일(파일 수준 `hubBaseUrl` 로 `MDL-019` 무변경·경계 6종) · **보관 배치 CLI `node dist/cli/retention.js`**(인자 없음·표준 출력 요약 JSON 1줄·종료 코드 0/0 이외). **[채번 정합]** 신규 EX **1건**(`EX-OPS-002` 500 = 다른 EX 로 분류되지 않는 내부 처리 실패)을 **정책 카탈로그에 등록**(EX 단일 출처가 정책이므로 `docs/specs/policies/spec-policies.md` 동반 수정) — `EX-FN-nnn` 류 새 그룹 미생성. **추가 PROC 0건**(예약 18종 인용만) · MDL 신설 0건 · ENT 신설 0건. **[제약 준수 실측]** 인증 0(401·403 미정의) · 요청 제한 0(전 접점 Rate Limiting 칸 = "미적용 — 수용 리스크" `OPS-002` 인용, 임의 수치 0) · 잠정값 literal 유출 **0건** · 성과 지표 수치 복제 **0건** · `BIZ-004-02` 재시도 수치 복제 **0건**(인용만) · 추상 표기("API 호출"·"데이터 조회"·"응답 처리") **0건** · 폐지 IA 코드 사용 0건(미사용 선언 1줄 제외) · 전 문서 300줄 이하(최대 256줄) · 상대 링크 전건 해석 성공. **[담당자 확인 대기 2건 기본안 적용]** 전달 실패 후 재시도 경로 ⑦ · 자가진단 운영 노출 ⑧ 을 사양 본문에 명시. **[후속 보완 2건 범위 밖]** 링크 TTL·1회성·서명 검증·요청 제한 / Delphi — 대응 파라미터·헤더·응답 필드 0. 후행 도메인(screens·processes·qa) 미작성 — PROC 는 예약 채번이며 실재 확인은 교차검증 시점 | USR-01, USR-02, USR-03, USR-04, USR-05, LIB-01, LIB-02, LIB-03, LIB-04, API-01, API-02, API-03, API-04, BAT-02, BAT-04, BAT-05, BAT-06 | `accountinterlockhub#473`·`#469` | 🚧 |
| 2026-07-25 13:35 | spec | `docs/specs/datas/`(spec-datas·data_ENT-001~003·spec-models·model_MDL-001~003·model_MDL-004-006·007-010·011-015·016-018·019-022 — 13 파일) **데이터 도메인 신규 작성**(`#469` 제품 루프 spec 레그 도메인 3순위 — **구 스키마 미승계**, `devspec/database.md` "이전 구성을 승계하지 않고 세 저장 대상 기준으로 새로 설계" 준수). **엔터티 3건 · 데이터 모델 22종(저장 3 · 비저장 19).** **[저장 범위 보증]** 저장 대상은 정확히 셋 — ENT-001 연동 추적 레코드(`BAT-04`)·ENT-002 동의 증적(`BAT-05`)·ENT-003 연동 지표 집계(`BAT-06`). **세션·사용자·감사 로그·보안 이벤트·배치 실행 이력·연동 구성 테이블을 하나도 만들지 않았다**(`DATA-001-01`·`DATA-001-05`·`OPS-003-01`·`OPS-003-02`·`OPS-003-04`). 금지 값(`encX`·`encY`·복호화 원문 X·생년월일·발송처키)은 **스키마에 담을 자리 자체가 없다** — 예외는 추적 키뿐. **[횡단 설계 원칙 8]** 하드 삭제만(소프트 삭제·`deleted_at` 미도입 — 보관 정책의 목적이 삭제) · 생성자·수정자 컬럼 미도입(`AUTH-001` 인증 부재로 식별할 행위자 없음) · `updated_at` 미도입(사건별 일시 컬럼이 대체) · 파생값 미저장(처리 성공 여부·결과 확인 여부·콜백 수신 여부·성공률) · 3NF(의도적 비정규화는 동의 증적 스냅샷 1건, 성능이 아니라 사후 입증 목적) · PostgreSQL 16 기준(ORM 미강제) · 물리 테이블·컬럼 snake_case. **[관계]** **물리 FK 0건** — ENT-001↔ENT-002 는 `tracking_key` 값 수준 논리 참조만 둔다(보존 기간 상이 + 같은 키 증적의 시간차 공존 + 지표는 추적 키 미포함). 삭제 순서 제약 없음. **[인덱스 5]** ENT-001 PK(추적 키 단건 조회 5 PROC)·`result_confirmed_at` 부분·`created_at` / ENT-002 PK·`consented_at` / ENT-003 PK. **인덱스 신설 기준(사용 PROC ≥1) 전건 충족**, 반례로 **ENT-002 `tracking_key` 인덱스는 사용 PROC 0건이라 미신설**. 조회 패턴은 추적 키 단건·보관 경과 범위 둘뿐이며 파티셔닝·특수 인덱스 미도입. **[MDL 채번 정합]** 서비스 도메인 예약 `MDL-001`~`MDL-022` 를 **의미 변경 없이 전건 확정** — 통합 0·폐기 0·추가 0·재배치 0. 분류 COM 7 · SVC 3 · EXT 12(EXT 우선 규칙 = 외부 계약이 내부 공유 여부보다 강한 제약). 저장 3종만 `도메인→ENT`·`ENT→도메인` 변환 지점을 갖고 나머지 19종은 그 두 지점이 존재하지 않음. **[위임 확정 4건]** ① 동의 항목 버전 식별자 = 상수 값의 결정적 SHA-256(64자·정규화 7단계) ② 지표 일자 경계 = `Asia/Seoul` 고정 ③ 추적 키 컬럼 길이 = `VARCHAR(255)`(정책 값과 동일 → 정책 개정 불요, EXC-DATA-10) ④ 진입 게이트 🟡 **G-05 = 카운터 5개 분리 저장**(두 성공률 정의 모두 산출 가능·정의 확정은 담당자 이월). **[모델 미신설 4종 명시]** 완료 콜백 응답·오류 응답 엔벨로프(기능·API 소관) · 세션/구성/감사 모델(저장 대상 없음) · 파생 지표 모델(정의 이월). **[정합 실측]** 인용 정책 규칙 ID 64종·EX 코드 10종·EXC 15종·BR 11종·PROC 18종·SVC 18종 **깨진 참조 0건**(EX 신설 0 — 정책 카탈로그 참조만) · 상대 링크 전건 해석 성공 · 잠정값·성과 지표 목표치 **본문 literal 유출 0건**(상수 플레이스홀더 참조만) · 폐지 IA 코드 사용 0건 · 전 문서 300줄 이하(최대 237줄). 후행 도메인(functions·screens·processes·qa) 미작성 — PROC 는 예약 채번이며 실재 확인은 교차검증 시점 | USR-01, USR-02, USR-03, USR-04, USR-05, LIB-01, LIB-02, LIB-03, LIB-04, API-01, API-02, API-03, API-04, BAT-02, BAT-04, BAT-05, BAT-06 | `accountinterlockhub#472`·`#469` | 🚧 |
| 2026-07-25 13:05 | spec | `docs/specs/services/`(spec-services·spec-services-codes·service_SVC-001~018) **서비스 도메인 신규 작성**(`#469` 제품 루프 spec 레그 도메인 2순위 — 구 사양 미승계 near-greenfield). **서비스 18건**으로 **IA leaf 17 노드 전량 + 횡단 1건** 커버(미커버 0). **[사용자 정의]** 역할 4종 — 발송처·수신처·사용자(이용 대상 3) + **허브 운영자(배포 주체 — 이용 대상 아님·화면 없음)**. 상하위·위임 관계 없음, **인증이 없으므로 시스템은 역할을 식별·검증하지 않고 접근 표면으로만 구분**함을 명문화(관리자 화면·관리자 역할 미생성). **[SVC-018 횡단]** 연동 구성 상수 검증(PROC-901) — 기동 시 상수 로드·필수 존재/형식 검증·**미충족 항목을 모아 한 번에 보고 후 기동 중단**(`EX-OPS-001`)·동의 항목 파싱과 **버전 식별자 산출**·자가진단 경로 라우팅 값 확보. BR-019(검증 충족 여부)·BR-014(경로 라우팅). 허브 기준 URL 은 발송처 호출 인자라 필수 상수 검증 대상 아님·**잠정값 여부는 애플리케이션이 판별하지 않음**(배포 절차 통제). **[채번 확립]** 분기 `BR-001`~`BR-024` **신규 24건**(프로세스가 동일 코드 인용) · 예외 `EX-*` **신규 0건**(정책 카탈로그 12종만 참조 — 정책이 정본이며 후행 도메인 신설 금지 원칙 준수, EX 코드를 갖지 않는 실패 3종은 '일반 404·배포 절차 통제·종료 코드'로 명시) · 데이터 모델 `MDL-001`~`MDL-022` **예약 채번**(저장 3 = 추적 레코드·동의 증적·지표 집계 / 비저장 19) · 화면 `SCR-001`~`SCR-004` **예약 채번**(본인확인·동의승인·진행 안내·결과 안내) · 프로세스는 정책 예약 16종 사용 + **`PROC-403`(배포 패키지 구성·무결성 확인)·`PROC-404`(규약 테스트 벡터 생성·대조) 서비스 도메인 추가 2건**. 기능 번호 `F-nnn` 은 **SVC 내 지역 번호**(인용 시 `SVC-004 F-002` 형태) 규약 확정. **[의존관계]** 사용자 본체 선후 5단, 라이브러리 제공·소비 체인, API↔기록 근거 관계, `SVC-018` = 전 서비스 선행 조건. **[확정 사항]** PRD 미결 ⑦·⑧ **기본안 적용 사실을 사양 본문에 명시** · 잠정값 6종은 상수 플레이스홀더 참조만(본문 값 복제 0건) · 성과 지표 목표치 수치 미복제(PRD 참조) · 폐지 IA 코드(`ADM`·`ADM-01`~`03`·`BAT-01`·`BAT-03`) 사용 0건. **[정합 실측]** 인용 정책 규칙 ID **깨진 참조 0건**이며 정책 규칙 86건 전량이 하나 이상의 SVC 에서 인용됨 · EX 코드 사용 12종 = 정책 카탈로그 12종 일치 · BR/MDL/SCR/PROC 카탈로그↔개별 문서 상호 참조 일치 · 전 문서 300줄 이하(최대 128줄). 후행 도메인(datas·functions·screens·processes·qa) 미작성 — MDL·SCR·PROC 는 예약 채번이며 실재 확인은 교차검증 시점 | USR-01, USR-02, USR-03, USR-04, USR-05, LIB-01, LIB-02, LIB-03, LIB-04, API-01, API-02, API-03, API-04, BAT-02, BAT-04, BAT-05, BAT-06 | `accountinterlockhub#471`·`#469` | 🚧 |
| 2026-07-25 12:28 | spec | `docs/specs/policies/`(spec-policies·policy_AUTH·policy_DATA·policy_BIZ·policy_SEC·**policy_SEC-crypto 분기**·policy_OPS) **정책 도메인 신규 작성**(`#469` 제품 루프 spec 레그 도메인 1순위 — 구 사양 미승계 near-greenfield). **정책 17건·규칙 86건·EX 코드 12종·예외(EXC) 49건.** **[SEC]** `SEC-001` 암호화 연동 규약 = AES-256-CBC+PKCS#7·UTF-8·키 32바이트 정규화(`_`(0x5F) 우패딩·초과분 앞 32바이트 절단)·**IV = 정규화 키의 앞 16바이트**·`encX` 키 = 발송처키+생년월일(`yyMMdd`) **구분자 없이 연결 후 정규화**·`encY` 키 = 생년월일 정규화·**`encY` 평문 = 정규화된 32바이트 키 바이트열 그대로(허브 재정규화 금지)**·Base64URL(RFC 4648 §5)·X 평문 1,024B·요청 URL 2,000자·규약 버전 `1.0`(**MAJOR 불일치=비호환**)·규약 개정 시 라이브러리·허브 동시 배포. 결정적 IV 한계는 **수용 리스크로 명시**(EXC-SEC-01 — 결함 아님). `SEC-002` 복호화 판정 4단계와 **실패 분류 경계 확정** — 생년월일 입력 이전 구조 위반 = `EX-SEC-001`(레코드 미생성) / 판정 **1·2단계 실패 = 본인확인 실패(재입력·결과 미확정, `EX-AUTH-002`)** / **3·4단계 실패 = `DECRYPT_FAILED`(`EX-SEC-002`)**. `SEC-003` 자가진단 경로 = **운영 값을 git 상수표에 적지 않고 배포 시 주입되는 비밀 값으로 취급**(진입 게이트 🟡 **G-03 자체보완**)·비경로 요청 일반 404·유출 시 교체·재배포·운영 노출은 PRD 미결 ⑧ **기본안 적용**. **[BIZ]** `BIZ-001` 결과 구분 **4종**(`SUCCESS`·`USER_DENIED`·`DECRYPT_FAILED`·`DELIVERY_FAILED`)과 화면 **4경로** 1:1 매핑 단일 열거형·다섯째 값/경로 신설 금지·**생년월일 불일치는 결과가 아님(재입력 루프·횟수 제한 없음)**. `BIZ-002` **추적 레코드 생성 시점 = 본인확인 최초 성공**으로 확정(근거 EXC-BIZ-03 — 추적 키는 복호화 전 미확보)·중복 수신 3분기·확정 결과 재안내(요청 수 미계수·레코드 미갱신·보관 기산 불변)·X 는 추적 키만 취해 즉시 폐기 후 승인 시 재복호화. `BIZ-003` 동의·승인 게이팅(서버 재검증·거부 `USER_DENIED`·승인 시 증적 1건). `BIZ-004` 수신처 전달 서버 간 호출·즉시 재시도 최초 1+2회(간격 1·2초·회당 5초·총 20초 상한)·PRD 미결 ⑦ **기본안 적용**. `BIZ-005` 지표 계수 시점·재안내 미계수·**성공률 정의는 담당자 이월**(G-05 대응 카운터 분리). **[DATA]** `DATA-001` 저장 대상 셋 한정·`encX`/`encY`/X/생년월일/발송처키 전량 미저장·복호화는 메모리 전용·로그/응답 미포함·구성은 DB 미저장. `DATA-002` `<RETENTION_MONTHS>`·`<RETENTION_MAX_MONTHS>` 중 먼저 도달·`<CONSENT_PROOF_RETENTION_MONTHS>`·**지표 집계 미삭제**·배치 멱등. `DATA-003` 개인정보 제3자 제공 동의(**개인정보 보호법 제17조**)·`<CONSENT_ITEMS>` 정본·증적 개인정보 미포함·**항목/문구 변경 시 버전 식별자 동반 변경**. `DATA-004` 추적 키 불투명·유일성 발송처 책임·형식 1~255자. **[AUTH]** `AUTH-001` **관리자·사용자 계정·서버 대면 API 인증 부재 선언**(401·403 미정의). `AUTH-002` 본인확인이 유일한 신원 확인 수단·`yyMMdd` 6자리·**재입력 횟수 제한 없음**. **[OPS]** `OPS-001` 기동 시 필수 상수 존재·형식 검증 실패 → **기동 중단**·**잠정값 운영 배포 금지(절차 통제 — 애플리케이션이 잠정 여부를 판별하지 않음)**·상수 값 본문 복제 금지. `OPS-002` **접근 통제 없음 — 수용 리스크 4종**(기밀성·무결성/출처·반복 시도·조회로 인한 보관 기산 개시) 명시, **"요청 제한 미수립"이 누락이 아니라 의도된 결정**임을 명문화. `OPS-003` 감사 로그 저장소 미도입(→ 보존 기간 부재)·보안 이벤트 미기록·배치 관측은 표준 출력+종료 코드. 잠정값 7종은 **상수 플레이스홀더로만 참조**(수치 미복제)·성과 지표 목표치는 PRD §성과 지표 참조. 후행 도메인(services·datas·functions·screens·processes·qa) 미작성 — PROC-101~105·201~204·301~304·401·402·901 **예약 채번**(실재 확인은 교차검증 시점) | USR-01, USR-02, USR-03, USR-04, USR-05, LIB-01, LIB-02, LIB-03, LIB-04, API-01, API-02, API-03, API-04, BAT-02, BAT-04, BAT-05, BAT-06 | `accountinterlockhub#470`·`#469` | 🚧 |
| 2026-07-25 11:25 | directing | **값 확정 대기 6건 전건 잠정값 설정 · directing 완료 판정분 main 병합**(담당자 지시 `#467` journal 3157 — "값 확정 대기항목들을 현시점에서 우선 적절한 임의 값으로 설정한 후 main 병합") — `CLAUDE.env.md` §연동 구성 상수의 잔여 `TBD` **4건을 실제 값과 같은 형식의 잠정값으로 교체**: `<RECEIVER_DELIVERY_URL>`(문서용 예약 도메인 기반 — 교체를 잊고 배포해도 외부로 데이터가 나가지 않고 전달만 실패) · `<HUB_BASE_URL_PROD>`(App Service 기본 도메인 형태) · `<HUB_BASE_URL_DEV>`(`http://localhost:3000`) · `<SELFCHECK_PATH>`(비공개 경로 형식 + **git 기록분은 비공개가 아니므로 운영 값은 배포 시점 재지정** 조건 명시). 기존 잠정값 2건(`<CONSENT_NOTICE>` 문구 · `<CONSENT_PROOF_RETENTION_MONTHS>` 60개월) 유지·표기 정합. **성과 지표 목표치 잠정 설정**(월 1,000건 · 성공률 95% 이상 · 발송처 통합 소요 5영업일 이내) — 판정 기준 부재로 사양·검증이 멈추지 않게 하는 용도이며 확정 전 미달은 결함으로 다루지 않음. `(**잠정**)` 표기 규약 + "잠정값 운영 배포 금지"·"확정 시 상수 값만 교체(사양·코드 개정 불요)" 원칙을 `CLAUDE.env.md`·`PRD.md` §성과 지표/§미결·`infra.md` §연동 구성 상수 주입·`external-apis.md` §7 에 일관 반영. **활성 문서 잔여 `TBD` 0건.** directing 게이트(prd-reviewer 3회차 🔴0·🟠0) 통과분과 함께 `directing/467-project-reset` → `main` 병합 | USR-02, USR-04, API-04, LIB-02, BAT-05, BAT-06 | `accountinterlockhub#468`·`#467` | ✅ |
| 2026-07-25 10:05 | directing | 리셋 방향 **담당자 확정분 일괄 반영**(`#467` journal 3143·3151) + prd-reviewer 1회차 세분화 반영 — **[보안 방향 확정]** 서버 대면 API 인증 없음·요청 제한 없음·발송처 위조 리스크 수용을 `PRD.md` §시스템 제약사항의 **수용 리스크 3종**으로 확정 기재(강화는 후속), 보안 이벤트 별도 기록 미도입 확정. **[동의 증적]** 보존 확정 → `IA.md` `BAT-05` 신설·`database.md` §동의 증적(추적 키·동의 일시·항목 버전+내용 스냅샷·개인정보 미포함)·보존 기간 상수 `<CONSENT_PROOF_RETENTION_MONTHS>`(잠정 60개월) 분리. **[지표 집계]** 고도화 제안 ③ 채택 → `BAT-06` 신설(일자별 요청·결과 구분 건수·추적 키 미포함·삭제 대상 아님)로 보관 삭제 후 성과 지표 산출 근거 확보. **[자가진단]** API 테스트 엔드포인트 채택 → `API-04` 신설·`external-apis.md` §7(무전달·무기록·원문 미반환)·비공개 경로 상수 `<SELFCHECK_PATH>`. **[라이브러리]** C# 단독 확정 → 네이티브 C ABI 기본안 폐기, **.NET Framework 4.8 클래스 라이브러리**로 전면 교체(`<LIB_SRC_DIR>`·`<LIB_BIN>`·`<LIB_TARGET_FRAMEWORK>` 확정), qa 케이스 **CLI·배치(C# 콘솔 호출 하네스)** 로 확정(미확정 해소)·Delphi 직접 호출은 범위 외 명시. `LIB-03`(배포 패키지)·`LIB-04`(규약 테스트 벡터) 신설. **[추적 키]** 유일성 = 발송처 책임 + 허브 중복 수신 3분기 확정(미존재=신규 / 진행 중=이어쓰기 / **결과 확정분 재수신 = 새 연동으로 받지 않고 확정 결과를 그대로 재안내**). **[동의 항목]** `<CONSENT_ITEMS>` 1건(`THIRD_PARTY_PROVISION` 개인정보 제3자 제공 동의·필수) JSON 주입 형식 확정, `<CONSENT_NOTICE>` 잠정 문구(교체 지점 명시). **[IA 세분화]** `USR-01`(링크 진입)·`USR-02`(연동 실행) 범위 축소 + `USR-03`(본인확인)·`USR-04`(동의·승인)·`USR-05`(결과 안내 4경로) 신규 발급(코드 재사용 아님). **[구 산출물 정리]** 재구축 결정에 따라 `docs/specs/`(87)·`apps/`(162)·`mockup/`(10) 삭제 — 이력은 git(`main` `0d83d60`)에 보존. 용어사전 `wiki/WIKI.md` 신설. **[2회차 평가 반영]** `prd-reviewer` 2회차 = 🔴 0·🟠 2·🟡 9·🟢 6, 1회차 29건 중 **해소 25·부분 4·미해소 0**, IA 세분화 **수렴**(신규 분할 0건). 차단 2건 해소 — ① 확정 결과 재진입은 `USR-05` 4경로 중 해당 경로 **재안내**(5번째 경로 없음·지표 요청 수 미계수·레코드 미갱신) ② `<LIB_SRC_DIR>` 를 `apps/sender-sdk/` 로 정정(모든 코드는 `apps/` 하위 — `doc-structure.md` 정합). 🟡 정정 10건(목표 서술·무보관 예외 표기·프로그램 귀속 표기·자가진단 입력·조회로 인한 보관 기산 리스크·안내 문구 확정 시점·허용 형태·검증 하네스 산출물·WIKI 링크 규약·배치 수동 진입점 **요구 고정**) + 담당자 확인 대기 2건(전달 실패 재시도 경로·자가진단 운영 노출)은 기본안 적용. 잔여 미결 = 값 확정 대기 6·담당자 확인 2·spec 위임 4·후속 보완 2(제품 루프 진입 차단 없음) | USR-01, USR-02, USR-03, USR-04, USR-05, LIB-01, LIB-02, LIB-03, LIB-04, API-01, API-02, API-03, API-04, BAT-02, BAT-04, BAT-05, BAT-06, ADM-01, ADM-02, ADM-03, BAT-01, BAT-03 | `accountinterlockhub#468`·`#467` | ✅ |
| 2026-07-25 02:52 | directing | 전체 프로젝트 리셋 방향 정의(`#467` 4대 요구 — 관리자 제거·구성 상수화·암호화 라이브러리 신설·데이터 최적화) — `docs/prd/PRD.md` 전면 개정 · `docs/prd/ia/IA.md` 재편(`ADM`·`ADM-01~03`·`BAT-01`·`BAT-03` 코드 미사용 등재(재사용 금지), `LIB`·`LIB-01`·`LIB-02` 영역 신설, `BAT-04` 연동 추적 기록 통합 신설) · `devspec/external-apis.md`(외부 접점 6종·라이브러리 계약 신설)·`database.md`(단일 추적 레코드)·`infra.md`(관리자 경로 제거·상수 주입·라이브러리 별도 배포) · `CLAUDE.env.md` §연동 구성 상수·§연동 라이브러리 식별자 신설. **prd-reviewer 평가·담당자 확인 전 초안** — 노드별 역링크 entry 는 방향 확정 후 반영 | USR-01, USR-02, LIB-01, LIB-02, API-01, API-02, API-03, BAT-04, BAT-02 | `accountinterlockhub#468`·`#467` | ✅ |
| 2026-07-18 23:16 | qa ⓒ | `#416`(Wave 3/최종) SCEN_001~006 E2E 여정 재검증(`c7b0773`, 사용자 페이지 SCR-005·006 시각 폴리시 변경 후) → **6/6 Pass**(🟢3·🔵3, Fail 0·Block 0) — 정상 전여정(001)·거부(002)·복호화실패 재입력→성공(003)·전달실패 복구(004)·관리자 세션만료 복구(005)·보관 생명주기(006). SCR-005→SCR-006 전이·재입력 잔류·재시도 복귀 등 여정 연속성 전 구간 유지 확인. SCEN_005 로 "관리자 렌더 불변"(Wave0 결론)이 여정 수준에서도 재확인됨(만료 Banner 계산색이 전역 `--color-warning-weak` 유지, `-u-` 미침투). SCEN_003 은 기지식 결함 `#465`(경계색 CSS 특이도 충돌) 가 여정 문맥에서도 재현되나 여정 차단 없음(신규 오류 미생성, `#465` 에 누적 노트만 추가). SCEN_006 배치는 `config_code`·`tracking_key` 로 격리한 시드만 백데이트해 실행 — 생존대조군(Wave2 실증적키)·전역 delta(2/2) 실측으로 선행 Wave 증적 데이터 무손실 확인. 검증 일감 `#453~458` 상태 동기화(해결 6건, GET 실측 확인). | USR-01, USR-02, ADM-01, ADM-02, ADM-03, API-01, API-02, API-03, BAT-01, BAT-02, BAT-03 | `accountinterlockhub#416`(검증 `#453~458`) | ✅ |
| 2026-07-12 23:55 | build ⓒ | (공통 반영) #237 처리상태 응답 trackingKey 원문 에코 확정(FN-010 정본) — 횡단 `qa/common/tc_SCEN.md`(SCEN_001 step10)·`services/spec-services.md`(MDL-302) 정합. 개별 델타는 API-01 이력 | API-01 | `accountinterlockhub#237`·`#236` | ℹ️ |
| 2026-07-12 22:00 | qa ⓒ | SCEN_001~006 E2E 여정 검증 → 6/6 Pass(happy·거부·복호화실패 재입력→성공·전달실패 복구·세션만료·보관생명주기). | `USR-01`·`USR-02`·`API-01`·`API-02`·`API-03`·`BAT-01`·`BAT-02`·`BAT-03` | `accountinterlockhub#236` | ℹ️ |
| 2026-07-12 10:30 | spec | `mockup/`(INDEX·SCR-003·004·005·006·index.html) **#215 목업 리뷰 5건 보완 반영**(횡단) — ① 동의 대상 설명 문구(consentNotice) SCR-003 입력(textarea)·SCR-004 상세 표시·SCR-005 상단 Notice. ② 복호화 실패 문구 "사용자 정보가 일치하지 않습니다." 고정(BD_ERR_DECRYPT·FE 형식 안내와 구분). ③ 전달 실패 SCR-006 결과 유형 제거→SCR-005 재시도 배너·상태 시연 버튼 신설. ④ 거부 배지 info(청록) 확정(변경 없음). ⑤ 접근 주소 고유 ID 관리자 직접 입력 힌트(자동 생성 문구 제거). §사양 보완 후보 5건 전부 ✅ 확정·개정 이력 추가. 개별 화면 사양 델타는 각 leaf(ADM-01/02·USR-01/02) 이력 참조 | ADM-01, ADM-02, USR-01, USR-02 | `accountinterlockhub#223`·`#215` | 🚧 |
| 2026-07-11 23:30 | spec | `docs/specs/qa/`(spec-qa·tc_ADM-01~03·USR-01/02·API-01~03·BAT-01~03·common/tc_SCEN) **#214 검증 TC 사양 개정**(횡단) — 발송처 링크 진입→생년월일 본인확인→동의→승인→허브 복호화(FN-020)→추적 키 추출→연동이력 생성(복호화 후)→수신처 B 서버-서버 전달→완료 흐름으로 TC 재정의. **[신규 BR/EX 커버리지]** BR-204(복호화 성공/실패)=USR-02_001·002·003 / EX-SEC-006(복호화 실패 재입력)=USR-02_003·USR-01_013 / EX-SEC-007(암호 파라미터 Base64URL 형식)=USR-02_004·USR-01_014 / EX-BIZ-008(X 파싱·추적 키 필드 누락)=USR-02_005·USR-01_015 각각 대응 TC 추가·BR-401/402 fallback two-pass(90/180일) 재정의. **[결번 제거]** BR-102/107/203·EX-BIZ-007 대응 TC 제거(ADM-01 파라미터/키값 지정 6건·BAT-03 미지정/값 누락). **[키 단일화]** API-01/02/03·BAT-01/03 조회·통지·이력 키를 요청 키값(UUID)→연동 추적 키(tracking_key)·surrogate PK·재사용 최신 1건. **[시드]** encX/encY 생성 헬퍼(규약대로 유효/무효 쌍·확인 방법)·mock 수신처 B(서버-서버 200/5xx/타임아웃)·추적 키 처리상태/연동이력 시드 신설(서비스 A 진입 목·요청 키값·전달 파라미터 시드 폐기). **[레코드 미생성]** 거부·복호화 실패·복호화 이전 종료는 추적 키 없어 ENT-004·007 미생성(감사만) 검증. **[SCEN]** 정상 전여정(복호화·mock B 수신·완료 콜백·완료 확인)·복호화 실패 재입력→성공·전달 실패 복구·거부·세션 만료·보관 fallback 6여정. 총 156 TC(제거 7·추가 7)·유형 분포 Boundary 12.8%(≤20%)·PROC↔TC 양방향 추적·SVC/POL/API 최소 수량 충족. Block: #214 SPEC-DECISIONS 로 BLK-Q2/Q4/05/10/13/14/15/17/18 해소·잔여 기본안(Q1·Q3·06~09·11·12·16)+build(BLK-19 링크 URL·BLK-20 재시도 액션). 개별 leaf 델타는 각 leaf 이력 참조. 선행/타 도메인 파일 무수정(qa 도메인만) | ADM-01, ADM-02, ADM-03, USR-01, USR-02, API-01, API-02, API-03, BAT-01, BAT-02, BAT-03 | `accountinterlockhub#222`·`#214` | 🚧 |
| 2026-07-11 22:30 | spec | `docs/specs/processes/`(spec-process·process_PROC-101~403) **#214 프로세스 사양 개정**(횡단) — 사용자 흐름을 발송처 링크 진입(접근 주소=발송처 판별)→생년월일 입력→동의→승인→(허브 복호화 FN-020·연동이력 생성 FN-016·수신처 B 서버-서버 전달 FN-012·상태 저장 FN-009)→완료/실패로 개정. **[승인·복호화 오케스트레이션]** PROC-202 승인 게이팅(FN-008)→PROC-203 FN-020 복호화(생년월일→encY→키→encX→X·추적 키 추출)→PROC-403 연동이력 생성(복호화 후·전달에 앞서 BIZ-004-07)→FN-012 서버-서버 POST(재시도 2회 SEC-007·브라우저 미경유)→PROC-401 처리상태 저장. **[키 단일화]** 조회·통지·이력 키를 요청 키값(UUID)에서 **연동 추적 키**(X 내부 필드·불투명)로 전환 — PROC-301(형식 검증 FN-007·비공백/255)·PROC-302(완료 판정 tracking_key 최신 1건)·PROC-303(완료 콜백 MDL-305 trackingKey 단독). ENT-004·007 surrogate uuid PK + tracking_key 비유니크 인덱스. **[보관]** PROC-402 fallback two-pass(확인/수신+90d 또는 생성 created_at+180d 중 먼저·미확인/미수신 180d 절대상한, IX_*_RETENTION_CREATED 활용·구 PENDING 인덱스 폐기). **[무저장]** encX·encY·생년월일·복호화 원문 X·회원 키 전량 미기록(SEC-005-06)·추적 키만 저장·조회. **[관리자]** PROC-101/102/105/106 전달 파라미터·exactly-one·service_a_entry_url·개인정보 파라미터 경고 폐기(수신처 B 주소·동의 항목·고유 ID). **[BR]** BR-204(복호화 성공/실패) 신규·BR-401/402 재정의(90/180 fallback)·BR-102/107/203 결번. **[EX]** EX-SEC-006/007·EX-BIZ-008 신규(정책 SEC-006-05·BIZ-004-08 인용)·EX-BIZ-007 폐기 결번·EX-DATA-002 추적 키 형식. 6축 정합(PROC↔SVC/FN/MDL/ENT/POL/SCR) 깨진 참조 0·순환 의존 0(202→203→{403,401}·301→401·303→403 단방향). 개별 PROC 델타는 각 leaf 이력 참조 | ADM-01, ADM-02, USR-01, USR-02, API-01, API-02, API-03, BAT-01, BAT-02, BAT-03 | `accountinterlockhub#221`·`#214` | 🚧 |
| 2026-07-11 21:30 | spec | `docs/specs/screens/`(spec-screens·design-system·screen_SCR-002~006) **#214 화면 사양 개정**(횡단) — 사용자 흐름을 발송처 링크 진입(접근 주소=발송처 판별)→생년월일 입력→동의→승인→(허브 복호화·수신처 전달)→완료/실패로 개정. [design-system] `TextField(birthdate)` 변형(6자리 numeric·민감값 미노출)·§사용자 연동 실행 상태 표현(복호화 실패 재입력·전달 실패·링크 오류·완료·거부·Fallback)·Badge 결과 유형·RepeatableRows 동의 항목 한정(전달 파라미터 폐기)·마스킹 규칙 encX/encY/생년월일 전량 미노출(SEC-005-06·SEC-006-06)·완료 페이지 원문 미포함(SEC-007-02). [spec-screens] 화면 목록·경로(SCR-005 `/interlock/entry/:accessAddressId`+encX·encY·SCR-006 `/interlock/result`)·인터랙션→PROC(승인 PROC-202 내부 203)·네비게이션·역할 매트릭스(발송처)·데이터/마스킹·확정 대기(발송처 링크 URL 형태 EXC-SEC-08·생년월일·복호화 실패 재입력·result client-state) 갱신. 폐기: 요청 키값(UUID) 경로·사용자 키값 파라미터 지정(`#33`) UI. 개별 SCR 델타는 각 leaf 이력 참조. 후행 도메인(processes/qa) v0.1.0·트리거 PROC 예약 채번(실재 확인 교차검증 시점) | ADM-01, ADM-02, ADM-03, USR-01, USR-02 | `accountinterlockhub#220`·`#214` | 🚧 |
| 2026-07-11 20:30 | spec | `docs/specs/functions/`(spec-functions·function_FN-001~020) **#214 기능·API 사양 개정**(횡단) — **FN-020 허브 복호화·연동 추적 키 추출 신규**(생년월일→encY→키→encX→X, normalize32/iv16 `_`(0x5F) 우패딩·AES-256-CBC·PKCS#7, 추적 키 X 필드 추출·완결성 검증, encX·encY·생년월일·keyXstr·원문 메모리 전용·전량 미기록 SEC-005-06). [개정] FN-004 주체 분리 인증(expectedActor SERVICE_A/B·SEC-003-03)·SVC-006/008/009 확장·FN-012 회원키 직전달→복호화 원문 X 서버-서버 POST(SEC-007·재시도 2회·상태 저장)·FN-006 파라미터/exactly-one/진입URL/PII경고 폐기(수신처B주소·동의항목·고유ID 고유성)·FN-007 요청키값 발급→연동 추적 키 형식 검증(불투명·발급/컨텍스트 저장 폐기)·FN-008 동의 accessAddressId 기반·승인 게이팅·거부 시 상태/이력 미생성(감사만)·FN-009 requestKey→trackingKey(surrogate PK·최신1건 조회)·FN-016 복호화 후 생성·userKey 폐기·5항목·FN-017/018/019 완료 판정·콜백 특정 스코프 {구성+키값}→**연동 추적 키 단독**·MDL-305 trackingKey 단독·FN-010 추적 키 마스킹·암호값 미기록·FN-011 보관 fallback(확인/수신+90d 또는 생성+180d 중 먼저·미확인/미수신 180d two-pass)·FN-013 복호화·전달·콜백 이벤트·FN-014 완료 확인/콜백 확장·FN-015 EX-SEC-006/007·EX-BIZ-008 엔벨로프. [EX] 신규 EX-SEC-006(복호화 실패·재입력)·EX-SEC-007(암호 파라미터 Base64URL 형식)·EX-BIZ-008(X 파싱 실패·추적키 필드 누락) 정책(SEC-006-05·BIZ-004-08) 인용 채번·EX-BIZ-007 폐기 결번. 정책 25종 전량 FN 매핑·의존 순환 0. **[보류]** 오케스트레이터 의사코드 'X JSON 파싱 실패→EX-SEC-007' 을 EX 재정의 금지 원칙에 따라 EX-BIZ-008 로 귀속(선행 SVC-005·BIZ-003-08 정합)·오케스트레이터 확인 대상. 개별 FN 델타는 각 leaf 이력 참조. 후행 도메인(screens/processes/qa) v0.1.0·PROC 예약 채번(실재 확인 교차검증 시점) | ADM-01, USR-01, USR-02, API-01, API-02, API-03, BAT-01, BAT-02, BAT-03 | `accountinterlockhub#219`·`#214` | 🚧 |
| 2026-07-11 19:30 | spec | `docs/specs/datas/`(spec-datas·spec-models·data_ENT-001/002/004/005/006/007·model_admin/user/api/common) **#214 데이터 사양 개정**(무저장 강화 횡단) — 저장 대상을 **① 발송처 접근 주소 구성(ENT-001·002) ② 처리상태(ENT-004) ③ 연동이력(ENT-007) + 관리자계정(ENT-005)·감사로그(ENT-006)** 로 한정. **비영속(무저장) 모델**: encX·encY·생년월일(MDL-201 접근 컨텍스트)·복호화 원문 X·회원 키(MDL-204)는 ENT 매핑 없이 메모리 전용·전량 미기록(SEC-005-06). **연동 추적 키**(MDL-202)만 유일한 X 파생 저장값 — 불투명 원문 그대로 ENT-004·007.tracking_key(varchar(255)·비유니크). ENT-004·007 은 추적 키 재사용 방어 위해 **내부 surrogate uuid PK + tracking_key 조회 인덱스**, **생성일시(created_at) 컬럼 필수**·보관 fallback(결과확인/수신+90d 또는 생성+180d 중 먼저·미확인/미수신 180d 절대상한, 삭제 인덱스=결과확인일시·수신일시 부분 + 생성일시). 연동이력 생성 시점=**복호화 성공 후**(SVC-005). **폐기**: ENT-003(전달 파라미터·결번)·ENT-001.user_key_param_id·순환 FK·사용자 키값 exactly-one(`#33`)·요청 키값 UUID·지정 사용자 키값 원문 저장(구 EXC-DATA-07 재정의). 서비스 대면 API 인증 자격은 운영 구성값(비 ENT). 개별 ENT/MDL 델타는 각 leaf 이력 참조. 후행 도메인(functions/screens/processes/qa) v0.1.0·PROC 예약 채번(실재 확인 교차검증 시점) | ADM-01, ADM-02, USR-01, USR-02, API-01, API-02, API-03, BAT-01, BAT-02, BAT-03 | `accountinterlockhub#218`·`#214` | 🚧 |
| 2026-07-11 18:30 | spec | `docs/specs/services/spec-services.md` **#214 서비스 목록문서 개정**(횡단) — 사용자 정의 4역할 재편(발송처·수신처·사용자·연동 관리자 + 상태 보관 배치)·역할 관계·시나리오 요약·의존관계(SVC-005=연동이력·처리상태 생성원) 갱신. [BR] BR-204(복호화 성공/실패) 신규·BR-102/107/203 결번. [EX] EX-SEC-006(복호화 실패)·EX-SEC-007(암호 파라미터 형식)·EX-BIZ-008(X 추적키 필드 누락) 신규·EX-BIZ-007 폐기 결번. [MDL] MDL-201 접근 컨텍스트(encX/encY/생년월일 무저장)·MDL-202 요청키값 UUID→연동 추적 키·MDL-204 전달 페이로드=복호화 원문 X. POL 매핑 정책 25종 전량(+SEC-006~008·AUTH-004). 개별 SVC 델타는 각 leaf 이력 참조. 후행 도메인(datas/functions/screens/processes/qa) v0.1.0·PROC/MDL 예약 채번(실재 확인 교차검증 시점) | ADM-01, ADM-02, ADM-03, USR-01, USR-02, API-01, API-02, API-03, BAT-01, BAT-02, BAT-03 | `accountinterlockhub#217`·`#214` | 🚧 |
| 2026-07-11 16:20 | spec | `docs/specs/policies/`(spec-policies·AUTH·DATA·BIZ·SEC·**SEC-crypto 신규 분기**·OPS) **#214 암호화 연동 플로우 정책 개정** — 입력을 단일 암호화 JSON(encX/encY)으로 전환, 허브 복호화(생년월일→encY→키→encX→X)·수신처 서버-서버 전달, **연동 추적 키**(X 내부 필드)를 조회·통지·이력 단일 키로 채택. [신규 정책] AUTH-004(생년월일=비인증 복호화 요소·재입력 재시도·시도 감사)·SEC-006(AES-256-CBC+PKCS#7·키32B/IV16B `_`정규화·encY평문=encX키문자열·Base64URL·복호화순서·미기록)·SEC-007(복호화 원문 브라우저 미경유 서버-서버 POST·완료페이지 원문 미포함)·SEC-008(정적IV·생년월일 저강도 알려진 제약 수용·TTL/1회성/서명검증 후속). [개정] DATA-001 무저장 강화(encX/encY/X/생년월일/발송처키 미저장)·DATA-002 요청키값 UUID→연동 추적 키 취급·불투명·DATA-003/005 키 필드 추적키 전환·DATA-004/006 보관 fallback(결과확인+90d 또는 생성+180d 중 먼저)·BIZ-001 접근주소 구성(수신처B주소·동의항목·고유ID=발송처식별자)·BIZ-002 승인 게이팅(필수동의·약관선택·거부시 복호화미수행 최소기록)·BIZ-003 복호화·서버전달·상태전이(진입→생년월일→동의→승인→복호화→전달→완료)·BIZ-004 완료판정 추적키 단독(최신1건 tiebreaker)·SEC-002 발송처키·X신뢰위임·SEC-003 인증(추적키 불충분·위조통지 차단)·SEC-005 마스킹/미기록 확대·OPS-002 복호화·전달 감사. [폐기] #33 사용자 키값 파라미터 exactly-one 지정 체계(BIZ-001-07·BIZ-004-05·요청키값 UUID·지정 사용자 키값 저장 예외 EXC-DATA-07 재정의). EX 신규 EX-SEC-006(복호화 실패)·EX-SEC-007(암호 파라미터 형식)·EX-BIZ-008(X 추적키 필드 누락), EX-BIZ-007(지정 파라미터 값 누락) 폐기. 규칙 리비전 상향(ID 재사용 금지)·SEC 300줄 초과로 SEC-006~008 를 `policy_SEC-crypto.md` 로 분기. 후행 도메인(services/datas/functions/screens/processes/qa) 미개정 v0.1.0 — PROC/SVC 등 예약 채번, 교차검증 시점 실재 확인 | ADM-01, ADM-02, ADM-03, USR-01, USR-02, API-01, API-02, API-03, BAT-01, BAT-02, BAT-03 | `accountinterlockhub#216`·`#214` | 🚧 |
| 2026-07-09 09:20 | spec | `docs/specs/`(policies·functions·processes·services·screens·qa) **SEC-004-01 바인딩 단독 방어 역전파**(담당자 결정 (b), qa #54). policy_SEC SEC-004-01 을 '길이·형식·허용 문자 재검증→400'에서 '길이·형식(타입)·필수만 재검증; 허용 문자·주입 패턴 재검증 미요구, 주입은 SEC-004-02 파라미터 바인딩·출력 이스케이프 단독 방어'로 개정. 연동 문구 정합(FN-005·PROC-101/102·SVC-001/002·SCR-002)·TC 개정(tc_ADM-01_009·tc_ADM-02_005 Negative 400→Positive 200 · tc_USR-01_007 주입 200/형식·크기 400 전송검증 유지). 오류 #211·#212 = 제품결함 아님(spec-sync 재분류). 축약 재검증 클린(200 실측·테이블 무손상·바인딩 방어·저장형 XSS 는 React 기본 이스케이프로 무력화, `dangerouslySetInnerHTML` 미사용 확인). 서비스 API(PROC-301/302/303·SVC-006/008/009·tc_API) SEC-004-01 동기화는 후속(스코프 404 안전종결, QA 기분류) | ADM-01, ADM-02, USR-01 | `accountinterlockhub#211`·`#212`·`#54` | ✅ |
| 2026-07-09 03:35 | qa | 공통 SCEN E2E 5 TC 전수 독립검증(배포산출물 v0.1.0/688fec2 · 실 round-trip localhost:3100 + 배치 CLI + 서비스B 목3155 + 세션만료 별포트3151) → 🟢3(002·004·005)·🔵2(001·003)·🟣0·🔴0·🟠0. **SCEN_001 Happy**: 관리자 로그인→구성등록(동의항목2·파라미터3·키값 exactly-one)→진입 `POST /interlock/entry` requestKey UUIDv4·회원키(top-level) 무저장(DB 0건)→동의화면 구성소속 항목만→AGREE 200→서비스B목 200 수신(회원키 무변형·deliver_to_b 2건만·내부메모 제외·payload{memberKey,parameters,configCode,requestKey})→ENT-004 is_success=true·미확인→status HMAC-A 4항목·is_result_confirmed 최초 1갱신 — 전달레그 목의존 🔵. **SCEN_002 거부**: REJECT 200(非EX)·CONSENT_REJECT 감사·B 미전달(목 0회)·ENT-004 is_success=false·컨텍스트 폐기 재제출 400 EX-DATA-002·status is_success=false — 실BE+DB 🟢. **SCEN_003 B실패복구**: 목500 AGREE→최초1+재시도2=3회 실패→502 EX-BIZ-004·DELIVERY_FAIL 감사·ENT-004 is_success=false 저장·status false → 목200 복구 신규진입 1회 성공 — 🔵. **SCEN_004 세션만료복구**: 별포트3151(idle 0.05분)·유휴초과 PUT 401 EX-AUTH-002(구성 무변형)→재로그인 200→PUT 재시도 200 영속 — 실BE+DB 🟢·3151 PID 타겟종료. **SCEN_005 보관생명주기**: 실플로우 완료+확인행(RK5A) aging now-91d·미확인행(RK5B) 시드 now-91d→배치 real-now·RETENTION_DAYS=90 statusDeletedCount=2 확정(하드)삭제·BATCH_RUN SUCCESS 감사·status 404 EX-DATA-003·재실행 멱등 deletedCount=0 — 실배치+DB 🟢. 진입 POST=확정결정 후보①(일부 GET표기 spec-sync·非결함). 오류일감 0. evidence: `works/accountinterlockhub-54/evidence/SQA/` | ADM-01, ADM-02, ADM-03, USR-01, USR-02, BAT-01, BAT-02, BAT-03, API-01 | `accountinterlockhub#54` (검증 #206~#210) | ✅ |
| 2026-07-08 17:03 | build | `apps/backend/src/api/common`(신규) — 서비스 대면 API 인증·요청제한 공통 가드(API-P1). FN-004 인증 = 사전 발급 API 키(X-Api-Key 주체 식별) + HMAC-SHA256 서명(X-Signature=hex(HMAC(canonical, secret)), canonical=`METHOD\n path\n timestamp\n sha256(rawBody)`)·시간창 ±300s(X-Timestamp)·주체 분리(SERVICE_A/B, SEC-003-03)·상수시간 비교(timingSafeEqual)·실패 시 API_AUTH_FAIL 감사(마스킹) 후 401 EX-SEC-003. FN-014 요청제한 = 인증 주체 기준 `scope:callerId` 분당 60회 초과 시 RATE_LIMIT_EXCEEDED 감사(BLOCKED) 후 429 EX-OPS-001, 고정윈도우+sweep(unref·OnModuleDestroy) 카운터. ServiceApiGuard(CanActivate, 인증→주체→요청제한) + @ServiceApi({actor,scope}) 데코레이터(Reflector)로 P2~P4 가 소비. 자격 = 운영 구성값(SERVICE_A/B_API_KEY·SECRET, 미설정 fail-closed) — `.env.example` 스킴 주석 추가. main.ts body parser verify 로 req.rawBody 캡처(HMAC 대상)·Express Request 타입 보강. app.module 에 ApiCommonModule 등록. 빌드 컴파일 0 에러(가드 미부착 — P2~P4 소비 예정). 3책임 통과(리뷰 PASS·안티패턴 0·tester 실 HTTP round-trip 🟢 10/10+H: 정상통과·서명변조·헤더누락·주체분리(SEC-003-03)·시간창·본문서명정합·요청제한 61회 429·fail-closed, 감사 마스킹 확인) | API-01, API-02, API-03 | `accountinterlockhub#49` | ✅ |
| 2026-07-08 00:35 | build | `apps/backend/src/common`(envelope·validation·audit) — 백엔드 공통 기반(F3). FN-015 성공 인터셉터+전역 예외 필터+EX 코드→메시지 맵(미매핑 EX-FN-999·스택 비노출)·FN-005 전역 ValidationPipe(whitelist·transform, 실패 400 EX-SEC-004 details)+본문 1MB 상한(413 EX-SEC-005)·FN-013 AuditService(ENT-006 append-only INSERT, best-effort, maskToken 앞2뒤2). 빌드 컴파일 0 에러. **회귀 수정(80525f9)**: serve-static 제외 경로(/api/*·/interlock/*)에서 본문 파서 에러가 AllExceptionsFilter 도달 전 404 EX-FN-999 로 재작성되던 문제 해소 — 파서 등록 직후 4-arg 에러 미들웨어로 413 EX-SEC-005·malformed JSON 400 EX-SEC-004 를 선종결(엔벨로프 형상 FN-015 동일) | ADM-01, ADM-02, ADM-03, USR-01, USR-02, API-01, API-02, API-03, BAT-01, BAT-02, BAT-03 | `accountinterlockhub#37` | 🚧 |
| 2026-07-08 00:20 | build | `apps/backend/src/database` — 로컬 PostgreSQL 연결·마이그레이션 기반·초기 스키마(F2). TypeORM DataSource+마이그레이션(raw SQL DDL)으로 ENT-001~007 7개 테이블 생성 — uuid PK gen_random_uuid()·timestamptz(3)·부분 유니크(UQ_CONFIG_CODE)·보존 부분 인덱스·CHECK(URL/enum/정합)·FK ON DELETE 이원화(CASCADE/NO ACTION/RESTRICT)·순환 FK(ALTER 후행)·autovacuum 0.05. 로컬 DB 적용·round-trip 검증 완료 | ADM-01, ADM-02, ADM-03, USR-01, USR-02, API-01, API-02, API-03, BAT-01, BAT-02, BAT-03 | `accountinterlockhub#36` | 🚧 |
| 2026-07-08 00:05 | build | `apps/backend`(NestJS)·`apps/frontend`(React+Vite) 스캐폴딩 — 단일 App Service 부트스트랩(F1). NestJS 정적 서빙(`@nestjs/serve-static`, `/api/**`·`/interlock/**` 제외→SPA 폴백)·관리자 IP 제한 미들웨어 스켈레톤(전량 통과, 본구현 refs #38)·루트 빌드/기동 스크립트. 프런트·백엔드 빌드 컴파일 0 에러 | ADM-01, ADM-02, ADM-03, USR-01, USR-02, API-01, API-02, API-03, BAT-01, BAT-02, BAT-03 | `accountinterlockhub#35` | 🚧 |
| 2026-07-07 06:20 | spec | `docs/specs/policies/` 키값 용어 정의·무저장 예외 확정 — spec-policies.md §용어 정의 신설(W3: PRD "회원 고유 키" = ADM-01 "지정 사용자 키값" = API-03 "전달받은 키값" 동일 값·요청 키값과 구별)·DATA-001-01 개정(연동이력 기록 목적 경유 허용)·EXC-DATA-07(연동이력의 지정 사용자 키값 저장 = PRD §제외 확정 예외, DATA-005 상한 내 한정) | ADM-01, USR-01, USR-02, API-01, API-02, API-03, BAT-01, BAT-03 | `accountinterlockhub#25` | 🚧 |
| 2026-07-06 22:01 | spec | `docs/specs/datas/` PostgreSQL 물리 설계 spec 확정(build 이관 반려 재반영) — UUID v4 PK 유지(uuidv7 미채택)·ENT-004 시간 파티셔닝 미채택(전역 유니크 강제 불가·이원 삭제 기준)→청크 DELETE 5,000행+autovacuum scale_factor 0.05·fillfactor 기본 유지(HOT 불가)·ENT-006 힙 순서 의존 서술 제거(ORDER BY 논리 보장, 보존 삭제 시 BRIN/월별 파티셔닝 방향 확정)·spec-datas.md §PostgreSQL 물리 설계·운영 전제 신설(INCLUDE·BRIN 미채택 근거 포함). 의미 층위(컬럼·제약·정책) 불변. 후속 도메인 반영(FN-011·013, PROC-402, qa TC)도 본 entry 가 포괄 | ADM-01, ADM-02, API-01, BAT-01, BAT-02 | `accountinterlockhub#24` | ✅ |
| 2026-07-06 17:10 | spec | `docs/specs/`(datas·functions·processes·qa) MSSQL→PostgreSQL 표기 정합화 — 타입(uuid·varchar·text·timestamptz·boolean·bigint)·기본값(gen_random_uuid()·now()·GENERATED ALWAYS AS IDENTITY)·부분 인덱스(partial)·boolean 비교(true/false)·트랜잭션(BEGIN)·DATEADD→INTERVAL 등 36개 파일 정합. 사양 의미 불변(#24 노트 이행) | ADM-01, ADM-02, ADM-03, USR-01, USR-02, API-01, BAT-01, BAT-02 | `accountinterlockhub#24` | 🚧 |
| 2026-07-06 16:56 | directing | DB 요구사항 MSSQL → PostgreSQL 전환(담당자 지시) — `docs/prd/PRD.md` §시스템 제약사항·`devspec/database.md`·`devspec/infra.md`·`CLAUDE.env.md` 갱신. `docs/specs/`(datas·process·qa)의 MSSQL 기준 표기는 spec 재정렬 대상(#24 노트) | ADM-01, ADM-02, ADM-03, USR-01, USR-02, API-01, BAT-01, BAT-02 | `accountinterlockhub#24` | ✅ |
| 2026-07-06 03:20 | spec | `docs/specs/` 횡단 사양 — DATA-001 무저장·SEC-002 신뢰위임·FN-013/014/015 감사·요청제한·응답엔벨로프·design-system.md·qa SCEN 시나리오 TC(#25·#28·#29·#31) | ADM-01, ADM-02, ADM-03, USR-01, USR-02, API-01, BAT-01, BAT-02 | `accountinterlockhub#24` | 🚧 |
