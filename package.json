{
  "name": "accountinterlockhub",
  "version": "0.1.0",
  "private": true,
  "description": "에이전트 기반 업무 수행 워크스페이스. ai-pm 이 Redmine 작업세션 이슈로 담당자와 협업하며 업무를 오케스트레이션하고, 작업은 Redmine 이슈로 추적한다.",
  "scripts": {
    "install:apps": "npm --prefix apps/backend install && npm --prefix apps/frontend install",
    "build:frontend": "npm --prefix apps/frontend run build",
    "build:backend": "npm --prefix apps/backend run build",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "npm --prefix apps/backend run start:prod",
    "start:dev": "npm --prefix apps/backend run start:dev",
    "migration:run": "npm --prefix apps/backend run migration:run",
    "dummy-test": "npm --prefix apps/backend run dummy-test --"
  }
}
