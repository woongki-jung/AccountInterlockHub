# 동의·승인 기능 정의

정본 목록은 [`spec-services.md`](spec-services.md). 용어는 [`../../../wiki/WIKI.md`](../../../wiki/WIKI.md).

## 개요

- **서비스 목적**: 사용자가 **자기 정보가 누구에게 어떤 항목으로 제공되는지 확인한 뒤 스스로 승인·거부를 결정**하게 한다. 개인정보 제3자 제공 동의가 성립해야만 연동이 진행되며, 동의한 사실은 개인정보를 담지 않는 증적으로 남는다.
- **관련 PRD 요구사항**:
	- [`../../prd/PRD.md`](../../prd/PRD.md) §제공 가치 — "**동의 기반 투명성**: 사용자가 어떤 항목에 동의하는지 화면에서 확인하고 스스로 승인·거부를 결정한다. 동의 사실은 개인정보를 담지 않는 **동의 증적**으로 남는다."
	- [`../../prd/PRD.md`](../../prd/PRD.md) §수행 범위 4 — "… 상수로 고정된 동의 항목 노출·동의 후 승인 또는 거부."

---

## SVC-003 동의·승인

### 기본 정보

| 항목 | 내용 |
|------|------|
| 기능명 | 동의·승인 |
| 분류 | 사용자 연동 본체 |
| 주 사용자 | 사용자 |
| 우선순위 | 높음 |
| 선행 기능 | `SVC-002`(본인확인 성공·추적 레코드 확보) · `SVC-018`(동의 항목 상수 파싱·버전 식별자 산출) |
| 매핑 PROC | PROC-103 · PROC-302 |
| 관련 IA 항목 | `USR-04` |

### 서비스 기능

| 기능 번호 | 기능 내용 | 입력 (MDL 코드) | 출력 (MDL 코드) | 비고 |
|-----------|-----------|----------------|----------------|------|
| F-001 | 상수 `<CONSENT_ITEMS>` 가 정한 항목명·설명·필수 여부와 안내 문구 `<CONSENT_NOTICE>` 를 **그대로** 노출한다 | MDL-008 | 동의 화면 구성 | `DATA-003-01`·`DATA-003-05` |
| F-002 | 사용자의 항목별 동의 여부와 승인·거부 의사를 제출받는다 | (사용자 입력) | MDL-007 | 항목이 식별 가능한 형태여야 재검증·증적이 성립 |
| F-003 | 서버가 화면 판단을 신뢰하지 않고 **필수 항목 동의 충족 여부를 다시 검증**한다 | MDL-007 · MDL-008 | 검증 결과 | `BIZ-003-02` |
| F-004 | 승인이 확정되면 동의 증적 1건을 만든다. 증적 생성이 실패하면 연동을 진행하지 않는다 | MDL-007 · MDL-008 | MDL-002 | `BIZ-003-04` |
| F-005 | 거부 제출이면 결과 구분을 `USER_DENIED` 로 확정하고 복호화·수신처 전달을 수행하지 않으며 동의 증적도 만들지 않는다 | MDL-007 | MDL-001 | `BIZ-003-03` |
| F-006 | 승인이 확정되면 연동 실행 단계로 넘긴다 | 추적 키 | 연동 실행 진입 | `SVC-004` 로 이관 |

### 사용 시나리오

#### Happy Path

| 단계 | 단계명 | 주체 | 행동/처리 | 입력 | 출력 | 매핑 PROC 단계 |
|------|--------|------|----------|------|------|---------------|
| 1 | 동의 항목 노출 | 시스템 | 상수가 정한 항목과 안내 문구를 동의·승인 화면(SCR-002)에 그대로 싣는다 | MDL-008 | 동의 화면 구성 | PROC-103 동의 항목 구성 |
| 2 | 동의 선택 | 사용자 | 항목별로 동의 여부를 고른다 | (사용자 입력) | MDL-007 | PROC-103 동의 항목 구성 |
| 3 | 승인 제출 | 사용자 | 필수 항목을 모두 동의한 상태로 승인을 제출한다 | MDL-007 | 승인 요청 | PROC-103 승인 요청 재검증 |
| 4 | 서버 재검증 | 시스템 | 필수 동의 충족 여부를 서버에서 다시 확인한다 | MDL-007 · MDL-008 | 검증 결과 | PROC-103 승인 요청 재검증 |
| 5 | 동의 증적 기록 | 시스템 | 추적 키·동의 일시·항목 버전 식별자·항목 내용 스냅샷으로 증적 1건을 만든다 | MDL-007 · MDL-008 | MDL-002 | PROC-302 동의 증적 기록 |
| 6 | 연동 실행 이관 | 시스템 | 승인 확정을 알리고 연동 실행으로 넘긴다 | 추적 키 | 연동 실행 진입 | PROC-103 연동 실행 이관 |

#### 분기 흐름

| 코드 | 발생 조건 | 분기 처리 | 결과 |
|------|----------|----------|------|
| BR-004 | 3단계 제출 의사 | 승인 → 4단계로 진행 / 거부 → 4·5단계를 건너뛰고 결과 구분 `USER_DENIED` 확정 후 `SVC-005` 결과 경로 ②로 이동 | 승인은 연동 실행, 거부는 결과 안내(증적 미생성·전달 미수행) |
| BR-005 | 4단계 필수 동의 충족 여부 | 충족 → 5단계로 진행 / 미충족 → 결과를 확정하지 않고 2단계로 되돌린다 | 미충족 시 동의 화면 유지 |

#### 예외 흐름

| 코드 | 발생 조건 | 처리 방식 | 사용자 응답 |
|------|----------|----------|-------------|
| `EX-BIZ-001` | 필수 항목을 모두 동의하지 않은 상태의 승인 제출 | 결과를 확정하지 않고 동의 화면으로 되돌린다 | 동의 화면 유지 · 필수 항목 안내 |
| `EX-BIZ-003` | 동의 증적 기록 실패 | 결과를 확정하지 않고 수신처 전달도 수행하지 않는다. 사용자는 다시 시도할 수 있다 | 재시도 가능 안내 |

### 사용자 정의

- **사용자** — 승인·거부를 결정하는 유일한 주체다. 어떤 역할도 사용자를 대신해 동의할 수 없다.
- **발송처·수신처** — 동의 항목의 내용에 관여하지 않는다. 항목은 허브의 배포 상수가 정본이다.
- **허브 운영자** — 상수를 바꿔 항목·문구를 갱신할 수 있으나 화면 조작 수단은 없다. 항목·문구가 바뀌면 버전 식별자도 함께 바뀐다.

### 관련 서비스

- `SVC-002` — 본인확인을 통과한 연동을 넘겨준다.
- `SVC-004` — 승인 확정을 이어받아 전달을 수행한다.
- `SVC-005` — 거부 결과를 경로 ②로 받는다.
- `SVC-015` — 동의 증적을 기록한다.
- `SVC-014` — 거부 시 결과 구분을 레코드에 이어 쓴다.
- `SVC-018` — 동의 항목 상수를 파싱하고 버전 식별자를 산출한다.

### 연관 정책 (policy)

| 정책 코드 | 적용 단계 | 적용 위치 | 위반 시 처리 |
|-----------|----------|----------|--------------|
| `BIZ-003-01` | 단계 1·3 | 검증 | 필수 미충족 상태의 제출을 화면이 막지 않으면 1차 방어 누락 |
| `BIZ-003-02` | 단계 4 | 검증 | 400 `EX-BIZ-001` · 결과 미확정 |
| `BIZ-003-03` | 분기 BR-004 | 검증 | 거부에 전달이 일어나면 정책 위반으로 반려 |
| `BIZ-003-04` | 단계 5 | 검증 | 500 `EX-BIZ-003` · 결과 미확정 |
| `DATA-003-01` | 단계 1 | 검증 | 상수 밖 항목 정의는 반려 |
| `DATA-003-02` | 단계 5 | 검증 | 개인정보가 포함되면 차단 결함 |
| `DATA-003-04` | 단계 5 | 검증 | 승인 1건당 증적 1건을 넘기면 반려 |
| `DATA-003-05` | 단계 1 | 결과 마스킹 | 문구를 가공한 화면 사양은 반려 |
| `BIZ-001-01`·`BIZ-001-04` | 분기 BR-004 | 검증 | 목록 밖 결과 구분·결과 덮어쓰기는 반려 |
| `DATA-001-05` | 단계 1 | 검증 | 동의 항목을 데이터베이스에 두면 반려 |

### 예외 사항

- **선택 항목은 승인을 막지 않는다.** 현재 상수의 항목은 필수 1건이지만, 상수가 바뀔 수 있으므로 필수·선택 구분을 전제로 판정한다.
- **안내 문구가 비어 있어도 승인은 가능하다.** 문구 부재는 안내 영역 미표시일 뿐 게이팅 조건이 아니다.
- **거부는 최종이다.** 거부로 결과가 확정되면 같은 링크의 재진입은 확정 결과 재안내로 처리되고, 재연동은 발송처가 새 추적 키로 새 요청을 발행해 수행한다.
- 동의 안내 문구는 잠정값이며 실문구 확정은 담당자 소관이다(필요 시 법무 확인). 사양은 상수를 참조하므로 문구 확정으로 개정되지 않는다.

### 구현 가이드

- 동의 항목은 기동 시 파싱한 상수를 그대로 쓴다. 항목을 데이터베이스에 두지 않는다.
- 승인 요청에는 사용자가 동의한 항목이 **식별 가능한 형태**로 담겨야 서버 재검증과 증적 기록이 함께 성립한다. 구체 필드 형태는 기능·API 도메인이 정한다.
- 화면 검증은 사용자 편의이고 판정 근거는 서버 검증이다. 화면 검증만 두면 `BIZ-003-02` 위반이다.
- 증적 기록과 승인 확정을 같은 처리 경계에 두어, 증적이 없는 승인이나 승인 없는 증적이 생기지 않게 한다.
