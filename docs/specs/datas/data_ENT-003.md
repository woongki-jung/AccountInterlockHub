# 연동 구성 전달 파라미터 데이터 정의

## 개요

- **데이터 목적**: 연동 구성 1건에 종속되는 전달 파라미터 정의를 보관한다. 서비스 A 진입 시 원천 키를 매핑하고, 동의 완료 후 서비스 B 전달 페이로드 구성에 사용한다. 정의(메타)만 저장하며 실제 값(회원 정보 포함)은 저장하지 않는다 — 단 **사용자 키값 파라미터로 지정된 행**(ENT-001.user_key_param_id 참조)의 값은 진입 시 원문 추출되어 연동이력(ENT-007.user_key)에 저장된다(PRD 확정 예외, EXC-DATA-07).
- **관련 PRD 요구사항**: [`../../prd/PRD.md`](../../prd/PRD.md) §수행 범위 "서비스 A의 호출 주소·전달 파라미터"·"전달 파라미터 중 **사용자 키값 파라미터를 명시적으로 지정**" / [`../../prd/devspec/external-apis.md`](../../prd/devspec/external-apis.md) §1·2 접점 / IA [`../../prd/ia/IA.md`](../../prd/ia/IA.md) `ADM-01`·`USR-02`.

---

## ENT-003 연동 구성 전달 파라미터

### 기본 정보

| 항목 | 내용 |
|------|------|
| 엔터티명 | 연동 구성 전달 파라미터 |
| 물리 테이블명 | TBL_INTERLOCK_PARAMETER |
| 분류 | 마스터(자식, 1:N) |
| 관련 서비스 | SVC-001, SVC-002, SVC-004, SVC-005 |
| 보존 정책 | 부모 구성(ENT-001)에 종속. 개인정보 아님(정의 메타만) |
| 개인정보 여부 | 비해당 (파라미터 정의 메타 — 실제 파라미터 값·회원 키는 무저장, DATA-001·EXC-DATA-01) |
| CRUD 수행 PROC | C: PROC-101 / R: PROC-102·PROC-201·PROC-203 / U: PROC-101 / D: PROC-101(편집 교체)·ENT-001 CASCADE |
| 관련 IA 항목 | ADM-01, USR-01, USR-02 |

### 속성 정의

| 속성명 | 데이터 타입 | 길이/precision | NULL | 기본값 | CHECK 제약 | 키 | 설명 |
|--------|-----------|----------------|------|--------|-----------|----|------|
| id | uuid | - | NOT NULL | gen_random_uuid() | - | PK | 파라미터 정의 고유 식별자 |
| config_id | uuid | - | NOT NULL | - | - | FK | 소속 연동 구성(ENT-001.id) |
| param_name | varchar | 100 | NOT NULL | - | length(param_name) > 0 | - | 파라미터명(허브·서비스 B 기준 키명) |
| source_key_a | varchar | 100 | NOT NULL | - | length(source_key_a) > 0 | - | 서비스 A 진입 원천 키명(매핑 원본) |
| deliver_to_b | boolean | - | NOT NULL | true | - | - | 서비스 B 전달 여부(true=전달) |
| is_required | boolean | - | NOT NULL | false | - | - | 진입 시 필수 여부 |
| display_order | INT | - | NOT NULL | 0 | display_order >= 0 | - | 표시·처리 순서 |

> 본 테이블은 파라미터의 "정의(이름·매핑·전달 여부)"만 보관한다. 진입 시 수신하는 실제 값(회원 키 포함 가능)은 어떤 컬럼에도 저장하지 않고 메모리에서 서비스 B 전달에만 사용한다(DATA-001-01).

### 관계 정의

| 대상 엔터티 | 관계 유형 | FK 컬럼 | ON DELETE | ON UPDATE | 설명 |
|-------------|-----------|---------|-----------|-----------|------|
| ENT-001 연동 구성 | N:1 | config_id | CASCADE | NO ACTION | 부모 구성 삭제 시 자식 파라미터 정의 연쇄 정리(안전망) |
| ENT-001 연동 구성(지정 역참조) | 0..1:1 | ENT-001.user_key_param_id(본 엔터티 피참조) | RESTRICT(피참조) | NO ACTION | 사용자 키값 파라미터로 지정된 행은 ENT-001 이 참조한다 — 지정 유지 상태의 행 삭제는 DB 가 차단(BIZ-001-07 안전망). 지정 해제·변경은 부모 편집으로만 수행 |

### 인덱스 정의

| 인덱스명 | 대상 컬럼 | 유형 | 카디널리티 추정 | 조회 패턴 (인용 PROC) |
|----------|-----------|------|-----------------|----------------------|
| PK_INTERLOCK_PARAMETER | id | PK | 높음 | 내부 식별 |
| IX_PARAM_CONFIG | config_id, display_order | BTREE | 중간 | PROC-201 진입 파라미터 매핑(WHERE config_id=? ORDER BY display_order), PROC-203 전달 페이로드 구성, PROC-101 편집 조회, PROC-102 상세 |

### 데이터 생명주기

- **생성 조건**: PROC-101 등록·편집 · "DB 접근·출력 생성" 단계에서 부모 구성과 동일 트랜잭션 INSERT.
- **수정 조건**: PROC-101 편집 시 param_name·source_key_a·deliver_to_b·필수·순서 UPDATE 또는 교체.
- **삭제/보관 조건**: PROC-101 편집 교체 시 하드 삭제 후 재삽입, 또는 부모 구성 물리 삭제 시 ENT-001 CASCADE. 단 사용자 키값 파라미터로 지정된 행(ENT-001.user_key_param_id 피참조)은 지정 해제 없이 삭제할 수 없다(RESTRICT — BIZ-001-07 "지정 유지 상태의 해당 파라미터 삭제 제출 거부" 안전망).

### 연관 정책 (policy)

| 정책 코드 | 적용 컬럼/제약 | 적용 위치 (DB 무결성 / 응용 검증 / 마스킹) |
|-----------|----------------|------------------------------------------|
| BIZ-001-01 | 전달 파라미터 정의 필수 | 응용 검증(PROC-101) |
| BIZ-001-05 | 개인정보 직접 수신 파라미터 경고(비차단) | 응용 감사(PROC-101 audit, EXC-BIZ-01) |
| BIZ-001-07 | 지정 파라미터 실재·삭제 제출 거부 | 응용 검증(PROC-101) + ENT-001 FK(RESTRICT 피참조) |
| BIZ-004-01·DATA-005-03 | 지정 행의 진입 값 → 연동이력 user_key 원문 추출 | 응용 처리(PROC-201, 내부 PROC-403 — 정의만 참조, 본 테이블 무저장 유지) |
| DATA-001-01 | 파라미터 실제 값 무저장 | 스키마 설계(정의 메타만) |
| SEC-004-01 | param_name·source_key_a 형식 | 응용 검증(DTO) + NOT NULL·length |
| SEC-002 | 회원 키 값 무변형 전달 | 응용 처리(PROC-203, 정의만 참조) |

### 구현 가이드

- 전달 페이로드 구성(PROC-203)은 deliver_to_b=true 인 파라미터만 서비스 B 로 실어 보낸다. source_key_a 는 진입 원천 키를 param_name 으로 리매핑하는 기준이다.
- 개인정보 직접 수신 파라미터가 정의에 포함되면 저장은 차단하지 않고 경고를 감사 로그에 남긴다(BIZ-001-05). 값 자체는 저장하지 않으므로 무저장 원칙과 양립한다.
- **사용자 키값 파라미터 지정은 본 테이블에 컬럼을 두지 않고** ENT-001.user_key_param_id 가 본 테이블 행을 참조하는 방식이다(지정 소재의 단일화 — 구성당 최대 1개 구조 보장). 진입 처리(PROC-201)는 그 참조 행의 source_key_a 로 진입 값을 원문 추출해 연동이력(ENT-007)에 기록한다. 편집 교체(하드 삭제 후 재삽입) 시 지정 참조 정합 순서는 [`data_ENT-001.md`](data_ENT-001.md) §구현 가이드를 따른다.
