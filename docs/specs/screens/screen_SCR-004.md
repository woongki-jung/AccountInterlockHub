# 연동 구성 상세 화면 정의

## 개요

- **화면 목적**: 연동 관리자가 특정 연동 구성 1건의 전체 설정(기본 정보·서비스 A/B 주소·전달 파라미터·동의 항목)을 확인하고, 활성/비활성 전환·삭제로 운영하며 편집 화면으로 이동한다.
- **관련 PRD 요구사항**: [`../../prd/PRD.md`](../../prd/PRD.md) §수행 범위 "관리자 — 연동 구성 관리" / IA `ADM-02` "등록된 연동 구성 확인·관리". 서비스 근거 [SVC-002](../services/service_SVC-002.md).
- **디자인**: [`design-system.md`](design-system.md) 관리자 셸·`Card`·`Badge`·`Toggle`·`Modal`·`Toast` 인용.

---

## SCR-004 연동 구성 상세

### 기본 정보

| 항목 | 내용 |
|------|------|
| 화면명 | 연동 구성 상세 |
| 화면 경로 | `/admin/configs/:id` |
| 접근 권한 | IP 허용([SEC-001](../policies/policy_SEC.md#sec-001-관리자-경로-ip-접근-제한)) + 인증 세션([AUTH-001](../policies/policy_AUTH.md#auth-001-관리자-로그인-인증)·[AUTH-002](../policies/policy_AUTH.md#auth-002-관리자-세션-관리)) |
| 관련 서비스 | [SVC-002](../services/service_SVC-002.md) 연동 구성 조회·목록·활성/삭제 |
| 트리거 PROC | PROC-102(상세 조회) · PROC-105(활성 전환) · PROC-106(삭제) |
| 선행 화면 | SCR-002(행 클릭) · SCR-003(저장 성공 후) |
| 관련 IA 항목 | ADM-02 |

### 레이아웃 구성

- 관리자 셸 + 본문 컨테이너(최대 폭 1120px). 상단 제목(구성명) + 활성 `Badge` + 우측 액션(편집·활성 전환·삭제).
- 본문 `Card` 묶음: (1) 기본 정보(구성 코드·구성명·활성·생성 일시) → (2) 서비스 A 진입(호출 주소) → (3) 서비스 B 전달(전달 주소·HTTP 메서드) → (4) 전달 파라미터 목록(테이블) → (5) 동의 항목 목록(테이블).
- 삭제 확인 `Modal`, 결과 `Toast`.
- 반응형: mobile 은 `Card` 세로 스택, 파라미터·동의 항목 테이블은 가로 스크롤 컨테이너.

### 데이터 표시

| 표시 항목 | 데이터 모델(MDL) | 속성 | 표시 형식 | 마스킹 규칙 |
|-----------|-----------------|------|-----------|-------------|
| 구성 코드 | [MDL-101](../datas/model_admin.md) | configCode | 텍스트 | 설정 데이터, 마스킹 없음(EXC-SEC-05) |
| 구성명 | MDL-101 | configName | 텍스트 | 마스킹 없음 |
| 서비스 A 호출 주소 | MDL-101 | serviceAEntryUrl | 링크/텍스트 | 마스킹 없음(EXC-SEC-05) |
| 서비스 B 전달 주소 | MDL-101 | serviceBDeliveryUrl | 링크/텍스트 | 마스킹 없음(EXC-SEC-05) |
| 전달 방식 | MDL-101 | serviceBHttpMethod | 텍스트(GET/POST/PUT/PATCH) | 마스킹 없음 |
| 활성 여부 | MDL-101 | isActive | `Badge`+`Toggle` | 마스킹 없음 |
| 전달 파라미터 | MDL-101 | parameters[] {name·sourceKeyA·deliverToB·required·order} | 테이블 | 마스킹 없음(정의 메타) |
| 동의 항목 | MDL-101 | consentItems[] {label·description·required·order} | 테이블 | 마스킹 없음 |

> 상세 응답은 설정 데이터(MDL-101)만 담으며 회원 키·처리 상태를 포함하지 않는다. 서비스 A/B 주소는 마스킹 대상이 아니다(EXC-SEC-05).

### 사용자 인터랙션

| 인터랙션 | 대상 요소 | 동작 | 호출 API(FN) | 트리거 PROC | 결과 |
|----------|-----------|------|-------------|-------------|------|
| 페이지 mount | (자동) | 구성 상세 조회 | GET /api/admin/configs/:id([FN-001](../functions/function_FN-001.md)·FN-003·FN-010) | PROC-102 | `Card` 렌더링 / 대상 없음 / Error |
| 클릭 | 활성 `Toggle` | 활성/비활성 전환 | PATCH /api/admin/configs/:id/active(FN-001·FN-003·FN-013) | PROC-105 | 성공 `Toast` + `Badge` 갱신 |
| 클릭 | 삭제 버튼 | 삭제 확인 `Modal` → 확정 | DELETE /api/admin/configs/:id(FN-001·FN-003·FN-013) | PROC-106 | 성공 `Toast` + SCR-002 이동 |
| 클릭 | 편집 버튼 | 편집 화면 이동 | (없음) | (네비게이션) | SCR-003(편집) 이동 |
| 클릭 | 목록 버튼 | 목록 이동 | (없음) | (네비게이션) | SCR-002 이동 |

> 활성 전환·삭제는 모두 SVC-002 범위(활성 전환=PROC-105·BR-103, 삭제=PROC-106·BR-104). 확인 `Modal` 열기 자체는 클라이언트 UI 이며 확정 클릭이 PROC-106 을 트리거한다.

### 화면 상태 전이

| 상태 | 진입 조건 | PROC 단계 매핑 | 표시 내용 |
|------|----------|---------------|-----------|
| Initial | 페이지 mount | (트리거 전) | `Card` 골격 Skeleton |
| Loading | 상세 요청 대기 | PROC-102 상세 조회 진입 직후 | Skeleton 유지 |
| Loaded | 200 응답 | PROC-102 응답 DTO 변환 | 상세 `Card` 렌더링 |
| Empty(대상 없음) | 대상 없음/이미 삭제됨 | PROC-102 응답 분기 | "대상 구성을 찾을 수 없습니다" + 목록 이동 |
| Error | 4xx/5xx | PROC-102 onError | `Banner`(error) + 재시도 |
| 세션 만료 | 401 EX-AUTH-002 | PROC-102 진입 세션 검사 | SCR-001(`?expired=1`) 리다이렉트 |

### 입력 폼 정의

해당 없음 — 상세 조회 화면으로 입력 폼이 없다. 활성 전환·삭제는 컨트롤 액션이며 편집 입력은 SCR-003 에서 처리한다.

### 조건부 표시

| 대상 요소 | 표시 조건 | 관련 정책 |
|-----------|-----------|-----------|
| 편집·삭제·활성 전환 버튼 | 인증 세션 유효(관리자 권한) | AUTH-001 |
| 활성 `Badge`(active/inactive) | `config.isActive` 값 | (표시 규칙) |
| 삭제 확인 `Modal` | 삭제 버튼 클릭 시 | OPS-002(삭제 감사 대상) |

### 이동 경로 (Navigation)

| 이동 대상 | 트리거 | 조건 | 트리거 PROC |
|-----------|--------|------|-------------|
| SCR-003 편집 폼 | 편집 클릭 | 대상 구성 존재 | (네비게이션, 편집 진입 로드는 SCR-003 mount 시 PROC-102) |
| SCR-002 목록 | 목록 클릭 / 삭제 성공 | 없음 / DELETE 200 | (네비게이션) / PROC-106(삭제) |

### 구현 가이드

- 상세는 목록(MDL-102 요약)보다 넓은 MDL-101 전체를 조회한다. 존재하지 않는 구성은 오류가 아닌 "대상 없음"으로 처리한다(SVC-002).
- 삭제는 되돌릴 수 없으므로 확인 `Modal` 을 강제하고, 삭제 성공 시 목록으로 복귀한다. 삭제·활성 전환은 감사 로그 대상(OPS-002)이다.
