# 연동 링크 진입 테스트 케이스 정의

정본 목록은 [`../spec-qa.md`](../spec-qa.md). 용어는 [`../../../../wiki/WIKI.md`](../../../../wiki/WIKI.md).

## 개요

- **검증 목적**: 발송처가 만든 연동 요청 URL 로 들어온 요청이 **생년월일 입력 이전에 판정할 수 있는 구조 위반**을 정확히 걸러 내는지, 그리고 판정 결과와 무관하게 **언제나 화면 문서**로 응답하는지 확인한다. 이 단계의 실패는 추적 키를 알 수 없으므로 레코드를 만들지 않고 지표에만 계수되어야 한다.
- **관련 PRD 요구사항**: [`../../../prd/PRD.md`](../../../prd/PRD.md) §수행 범위 4 — "발송처 링크로 고정 진입 경로에 진입, 생년월일 입력, …" · [`../../../prd/ia/IA.md`](../../../prd/ia/IA.md) `USR-01` — "발송처가 만든 링크로 고정 진입 경로에 진입해 `encX`·`encY`를 받고 형식을 판정(무효 링크는 결과 안내로 분기)".
- **관련 IA 항목**: `USR-01` (본 파일의 전 TC 공통)
- **검증 방법(qa 케이스)**: **웹·SPA UI** (진입 응답의 HTTP 계약 확인은 API·서버 절차를 병용)
- **공통 전제**: `ENV-WEB` 기동 · `ENV-DB` 시드 투입 완료 · `SEED-CONST-OK` 상수 주입([`../spec-qa.md`](../spec-qa.md) §테스트 환경 · §시드 데이터 카탈로그)
- **공통 판정 기준**: **Pass** = 모든 단계의 기대 결과 일치 / **Fail** = 하나 이상 불일치(불일치 항목 기록) / **Block** = 전제 조건 미충족으로 실행 불가(사유 기록). 실행 단계의 세분 판정 5종은 [`../../../../ai/strategies/qa-execution.md`](../../../../ai/strategies/qa-execution.md) §공통 원칙.
- **매핑 PROC 단계**: [`../../processes/process_PROC-101.md`](../../processes/process_PROC-101.md) 의 라벨(`B1`~`B7` · `F1`~`F3`)을 그대로 인용한다.

---

### USR-01_001 정상 링크 진입 — 구조 판정 통과 후 본인확인 단계

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| E2E · Positive | SVC-001 · FN-002 · FN-003 · SCR-001 · `SEC-001-08` · 인터페이스 §초기 상태 주입 형식 1 | PROC-101 | BR-001(통과) | 높음 | 자동 |

- **전제**: `SEED-VEC-01` 의 기대 요청 URL(파일)
- **단계**: ① 벡터 01 의 요청 URL 로 진입 `B1`~`B4` → HTTP 200 · `Content-Type: text/html; charset=utf-8` ② 초기 상태 판독 `B6`·`B7`·`F1` → 문서에 `<script id="__INTERLOCK_INITIAL_STATE__" type="application/json">` 요소가 **정확히 하나** 있고, 그 텍스트가 초기 상태 JSON **1건**(`{"stage":"IDENTITY"}` · `resultPath` 부재)이며 문서와 같은 UTF-8 로 인코딩돼 있다. 화면은 그 요소를 **`id` 로 찾아** 읽는다(문서 안 위치·순서에 의존하지 않는다) ③ 화면 확인 `F2`·`F3` → SCR-001 렌더(제목 "본인 확인" · 생년월일 필드 · 확인 버튼 활성)
- **데이터 검증**: `tbl_interlock_tracking` 에 어떤 행도 추가되지 않는다(`BIZ-002-02`) · `tbl_interlock_metric_daily` 무변화
- **판정**: 공통 판정 기준

### USR-01_002 진입 응답의 형식·캐시 계약

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| INTG · Positive | SVC-001 F-005 · 인터페이스 공통 규약(§공통 응답 포맷·캐시) | PROC-101 | BR-001(통과) | 보통 | 자동 |

- **전제**: `SEED-VEC-01`(파일)
- **단계**: ① 정상 요청 URL 로 진입 `B7` → 200 · `text/html; charset=utf-8` ② 응답 헤더 확인 `B7` → 캐시 금지 지시가 지정되어 있다(중간 캐시 개입 차단) ③ 구조 위반 URL 로 진입 `B4`→`B6`→`B7` → **여전히 200 + 화면 문서**이며 4xx/5xx 본문이 아니다
- **판정**: 공통 판정 기준

### USR-01_003 초기 상태에 금지 값이 실리지 않음

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| POL · Positive | `DATA-001-04` · FN-015 · SCR-001 §데이터 표시 | PROC-101 | BR-001(통과) | 높음 | 자동 |

- **전제**: `SEED-VEC-01`(파일)
- **단계**: ① 정상 진입 `B6` → 초기 상태 JSON 에 `encX`·`encY`·`trackingKey`·생년월일·복호화 원문이 **하나도 없다** ② 화면 DOM 확인 `F1`·`F2` → 암호값이 어떤 요소에도 그려지지 않는다(URL 쿼리에서 읽어 메모리로만 보유) ③ 서버 로그 확인 → 금지 항목이 기록되지 않는다
- **판정**: 공통 판정 기준

### USR-01_004 `encX` 파라미터 부재 → 결과 경로 ②

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| E2E · Negative | SVC-001 F-003 · FN-003 · `SEC-002-02` | PROC-101 | BR-001(위반) · `EX-SEC-001` | 높음 | 자동 |

- **전제**: `SEED-URL-NOENCX`(파일 — `encY` 만 실린 URL)
- **단계**: ① `encY` 만 붙여 진입 `B3`·`B4` → 구조 판정 실패 ② 초기 상태 판독 `B6`·`F1` → `{"stage":"RESULT","resultPath":2,"reasonCode":"EX-SEC-001","isReAnnouncement":false}` ③ 화면 확인 `F3` → SCR-004 경로 ② 패널, 설명 "연동 링크가 올바르지 않습니다." ④ 초기 상태에 `returnUrl` 속성이 **없음**을 확인(진입 실패는 항상 경로 ② 라 동봉 조건이 성립하지 않는다 — `BIZ-001-06`)
- **데이터 검증**: `tbl_interlock_tracking` 행 추가 0건 · 당일 `request_count` +1 · `decrypt_failed_count` +1(`BIZ-005-02` ②)
- **판정**: 공통 판정 기준

### USR-01_005 `encY` 빈 값 → 결과 경로 ②

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| E2E · Negative | SVC-001 F-003 · FN-003 · `SEC-002-02` | PROC-101 | BR-001(위반) · `EX-SEC-001` | 높음 | 자동 |

- **전제**: `SEED-URL-EMPTYENCY`(파일)
- **단계**: ① `?encX=<정상>&encY=` 로 진입 `B3` → 빈 값을 부재와 같이 다룬다 ② 초기 상태 `B6` → `resultPath = 2` · `reasonCode = EX-SEC-001` ③ 복호화 시도 여부 확인 → 복호화를 **시도하지 않는다**(`SEC-002-02`)
- **판정**: 공통 판정 기준

### USR-01_006 Base64URL 디코드 실패 → 결과 경로 ②

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| INTG · Negative | FN-002 · FN-003 · `SEC-001-08` · `SEC-002-02` | PROC-101 | BR-001(위반) · `EX-SEC-001` | 높음 | 자동 |

- **전제**: `SEED-URL-B64BAD`(파일 — 표준 Base64 의 `+`·`/`·`=` 와 알파벳 밖 문자를 각각 섞은 3 변형)
- **단계**: ① 변형 3종으로 각각 진입 `B3` → 전건 디코드 실패 ② 판정 결과 `B4`·`B6` → 전건 `resultPath = 2` · `reasonCode = EX-SEC-001` ③ 퍼센트 인코딩을 겹쳐 적용한 값도 같은 결과인지 확인
- **판정**: 공통 판정 기준

### USR-01_007 암호문 길이가 16의 배수가 아님 — 경계 판정

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| INTG · Boundary | FN-003 · `SEC-002-02` · `SEC-001-01`(CBC 블록) | PROC-101 | BR-001(위반) · `EX-SEC-001` | 높음 | 자동 |

- **전제**: `SEED-URL-BLOCK`(파일 — 디코드 결과가 각각 15·16·17 바이트가 되는 `encY` 3종)
- **단계**: ① 15바이트 값으로 진입 `B4` → 실패 · `EX-SEC-001` · 경로 ② ② 16바이트 값으로 진입 `B4` → **구조 판정 통과**(`stage = IDENTITY`) ③ 17바이트 값으로 진입 `B4` → 실패 · `EX-SEC-001` · 경로 ②
- **판정**: 공통 판정 기준

### USR-01_008 같은 이름의 파라미터가 둘 이상 → 구조 위반

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| INTG · Negative | 인터페이스 공통 규약 §진입 파라미터 이름 · FN-003 | PROC-101 | BR-001(위반) · `EX-SEC-001` | 보통 | 자동 |

- **전제**: `SEED-URL-DUP`(파일)
- **단계**: ① `?encX=A&encX=B&encY=C` 로 진입 `B3` → 중복을 구조 위반으로 다룬다(먼저 온 값·나중 값 어느 쪽도 채택하지 않는다) ② 초기 상태 `B6` → `resultPath = 2` · `reasonCode = EX-SEC-001` ③ 대소문자가 다른 이름(`EncX`)만 실린 요청 → 파라미터 부재와 같은 처리(대소문자 구분)
- **판정**: 공통 판정 기준

### USR-01_009 요청 URL 전체 길이 상한 경계

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| INTG · Boundary | `SEC-001-10` · `SEC-002-02` · SVC-001 F-002 | PROC-101 | BR-001(위반) · `EX-SEC-004` | 높음 | 자동 |

- **전제**: `SEED-URL-LEN`(파일 — 전체 길이가 규약 상한 −1 / 상한 / 상한 +1 인 URL 3종. 상한 값은 정책 `SEC-001` §규약 확정값 참조)
- **단계**: ① 상한 −1 URL 로 진입 `B2` → 길이 판정 통과 후 구조 판정으로 진행 ② 상한 URL 로 진입 `B2` → 통과(상한은 허용값) ③ 상한 +1 URL 로 진입 `B2` → `EX-SEC-004` · `resultPath = 2` · 설명 "전달 정보가 허용 크기를 넘었습니다."
- **데이터 검증**: ③ 에서 `request_count` +1 · `decrypt_failed_count` +1 · 추적 레코드 0건
- **판정**: 공통 판정 기준

### USR-01_010 정의되지 않은 파라미터는 읽지 않고 무시

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| INTG · Positive | 인터페이스 공통 규약 §진입 파라미터 이름 · `OPS-002-05` | PROC-101 | BR-001(통과) | 보통 | 자동 |

- **전제**: `SEED-VEC-01`(파일)
- **단계**: ① 정상 URL 에 `&ttl=60&once=1&sig=abc` 를 덧붙여 진입 `B3` → 구조 판정 통과 · `stage = IDENTITY` ② 초기 상태·화면 확인 `B6`·`F3` → 링크 만료·1회성·서명 관련 표시가 **어디에도 없다** ③ 이후 본인확인 제출이 정상 진행되는지 확인
- **판정**: 공통 판정 기준

### USR-01_011 진입 경로가 아닌 요청 → 본문 없는 일반 404

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| INTG · Negative | 인터페이스 공통 규약 §경로·메서드 규약 · `SEC-003-02` | PROC-101 | BR-001(진입 전) | 보통 | 자동 |

- **전제**: `ENV-WEB` 기동
- **단계**: ① 존재하지 않는 경로로 GET → 404 · **본문 없음** · EX 코드를 담지 않는다 ② 진입 경로에 POST → 405(본문 없음) ③ 응답에 자가진단 경로·내부 경로 힌트가 없음을 확인
- **판정**: 공통 판정 기준

### USR-01_012 판정 실패는 레코드를 만들지 않고 지표에만 계수

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| DATA · Positive | `BIZ-002-02` · `BIZ-005-02` ② · FN-013 · ENT-001 · ENT-003 | PROC-101 · PROC-303 | BR-001(위반) · BR-018(추적 키 미확보) | 높음 | 자동 |

- **전제**: `SEED-URL-NOENCX`(파일) · 당일 `tbl_interlock_metric_daily` 행 초기값 기록
- **단계**: ① 실행 전 당일 카운터 4종을 기록 `B5` ② 구조 위반 URL 로 3회 연속 진입 `B4`·`B5` ③ 실행 후 카운터 재조회 `PROC-303 B3`
- **데이터 검증**: `tbl_interlock_tracking` 행 수 변화 0 · `request_count` +3 · `decrypt_failed_count` +3(추적 키를 알 수 없어 시도마다 계수 — `BIZ-005-05` 수용 한계) · `success_count`·`delivery_failed_count` 무변화
- **판정**: 공통 판정 기준

### USR-01_013 진입 처리 중 내부 오류 → 화면으로 경로 ② 안내

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| INTG · 시스템예외 | `EX-OPS-002` 정의 규칙(`OPS-003-05`) · FN-014 · FN-015 · `SEC-002-05` · `OPS-003-03` | PROC-101 | `EX-OPS-002` | 보통 | 반자동 |

- **전제**: `ENV-WEB` 기동 · 데이터베이스 연결 차단으로 내부 실패를 주입(fault 주입 하네스)
- **단계**: ① 데이터베이스 연결을 끊은 상태에서 구조 위반 URL 로 진입(지표 계수 실패 유도) `B5` → 화면 문서 200 응답 유지 ② 초기 상태 `B6` → `resultPath = 2` ③ 응답·화면 확인 `F1` → 내부 사유·스택·중간 값이 담기지 않는다
- **판정**: 공통 판정 기준 (Block 사유: fault 주입 하네스 미구축)

### USR-01_014 초기 상태 부재 판정 4조건 → 결과 경로 ②(`IDENTITY` 폴백 금지)

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| E2E · Negative | 인터페이스 §초기 상태 주입 형식 1·3·4 · `BIZ-001-02` · SVC-005 F-003 · SCR-004 §결과 3경로 | PROC-101 | BR-001(통과) · `EX-OPS-002`(문구 수렴 — 사유 코드 미생성) | 높음 | 반자동 |

- **전제**: `SEED-VEC-01`(파일) · `MOCK-RESP`(응답 변조 프록시 — 진입 문서의 초기 상태 블록을 조작해 화면에 전달)
- **단계**: ① **요소 없음** — **정상 링크로 진입**한 문서에서 스크립트 요소를 제거해 전달 `F1` → 본인확인이 아니라 SCR-004 **경로 ②** 가 그려진다(화면이 서버 판정을 추측해 본인확인을 열지 않는다). **배선 순서 요구**(정적 자산 라우팅보다 진입 경로 처리를 앞에 둔다 — PROC-101 §구현 가이드 · 인터페이스 §초기 상태 주입 형식 4)는 **구현 배치의 문제**라 본 TC 가 재현하지 않는다 — 배선이 뒤집히면 값 없이 서빙된 문서가 이 부재 판정을 발동시켜 `stage = IDENTITY` 를 요구하는 **정상 진입 TC 가 전건 실패**하므로(`USR-01_001` ②·③ · 시드 `SEED-VEC-01` 의 확인 방법) 별도 하네스 없이 드러난다 ② **같은 `id` 요소가 둘 이상** — 정상 블록 뒤에 같은 `id` 스크립트 블록을 하나 더 넣어 전달 `F1` → 경로 ②. **첫 요소만 읽어 `IDENTITY` 로 진행하면 Fail** 이다 — 개수를 세지 않는 판독 수단을 쓴 것이며, 검출 수단 정본은 인터페이스 §초기 상태 주입 형식 1) 담는 자리 2 다 ③ **`JSON.parse` 실패**(깨진 JSON·빈 텍스트) → 경로 ② ④ **`stage` 가 `IDENTITY`·`RESULT` 밖**(`CONSENT`·빈 문자열·숫자·`null`) → 경로 ② ⑤ 네 경우 모두 **`IDENTITY` 로 폴백하지 않음**을 확인(생년월일 폼이 어디에도 그려지지 않는다) ⑥ 네 경우 모두 `reasonCode` 가 만들어지지 않으므로 SCR-004 가 **미상 코드 수렴 문구**(`EX-OPS-002` 문구 "처리 중 문제가 발생했습니다. 잠시 후 다시 시도해 주세요.")로 안내하고, 새 EX 코드·네 번째 패널·빈 화면이 생기지 않음을 확인
- **데이터 검증**: 네 경우 모두 화면 쪽 판정이므로 `tbl_interlock_tracking` 행 추가 0건 · 지표 카운터 무변화(서버는 정상 진입으로 처리했다)
- **판정**: 공통 판정 기준 (Block 사유: 응답 변조 프록시 미구축)

### USR-01_015 초기 상태 주입 이스케이프 — 값이 문서를 깨뜨리지 않음

| 계층·유형 | 검증 대상 | 검증 PROC | 검증 분기/예외 | 우선순위 | 자동화 |
|---|---|---|---|---|---|
| INTG · Negative | SVC-001 · 인터페이스 §초기 상태 주입 형식 2 · PROC-101 `B7`(이스케이프 의무) | PROC-101 | BR-001(통과) | 높음 | 반자동 |

- **전제**: `SEED-VEC-01`(파일) · **초기 상태 값 주입 하네스**(서버가 만드는 초기 상태 값에 `<`·`>`·`&`·`U+2028`·`U+2029` 와 `</script>` 문자열을 담는 테스트 전용 수단 — [`../spec-qa-seed.md`](../spec-qa-seed.md) §모킹 전략)
- **단계**: ① 표식 값을 담아 진입 문서를 만든다 `B7` → 응답 **바이트**에서 스크립트 블록 텍스트를 확인: `<`·`>`·`&`·`U+2028`·`U+2029` 가 **원 문자로 하나도 남지 않고** 각각 `\u003c`·`\u003e`·`\u0026`·`\u2028`·`\u2029` 로 치환돼 있다 ② 값에 `</script>` 문자열이 섞인 경우 → **문서가 깨지지 않는다**(스크립트 블록이 그 자리에서 끊기지 않고 하나로 유지되며 뒤 내용이 마크업으로 해석되지 않는다 — `<` 가 남지 않아 종료 시퀀스도 주석 개시 `<!--` 도 성립하지 않는다) ③ 화면의 `JSON.parse` 결과 `F1` → **값이 원래 문자 그대로 복원**된다(다섯 표기가 모두 유효한 JSON 문자열 이스케이프라 값이 바뀌지 않는다) ④ 화면이 **역이스케이프를 따로 하지 않음**을 확인 ⑤ 치환이 값의 출처·내용을 따지지 않고 **주입 경로 전체에 무조건** 적용됨을 확인(출처를 따지는 조건부 분기가 없다) ⑥ 주입 값이 **스크립트로 평가되지 않음**을 확인(`type="application/json"` 데이터 블록이며 전역 변수 주입 형태를 쓰지 않는다)
- **판정**: 공통 판정 기준 (Block 사유: 초기 상태 값 주입 하네스 미구축)
