# 테스트 환경·시드 데이터 카탈로그

정본 목록은 [`spec-qa.md`](spec-qa.md). 용어는 [`../../../wiki/WIKI.md`](../../../wiki/WIKI.md).

본 문서는 TC 가 요구하는 **실행 환경**과 **사전 데이터**를 정의한다. 실제 환경 구성·실행은 qa 단계 소관이며, 본 문서는 **무엇이 어떤 상태로 준비돼야 하는지**의 사양이다.

> **민감 정보 미기재** — 데이터베이스 접속 비밀번호·키, 자가진단 경로의 운영 값, 발송처키 실값을 본 문서군에 직접 적지 않는다. 상수는 플레이스홀더로만 참조하고 값의 단일 출처는 [`../../../CLAUDE.env.md`](../../../CLAUDE.env.md) §연동 구성 상수다.

## 실행 환경

| 환경 ID | 대상 프로그램 | 준비 절차 | 관측 수단 |
|---|---|---|---|
| `ENV-WEB` | 사용자 웹 | 배포 산출물 기동(React 정적 빌드를 NestJS 가 서빙 — 단일 배포 단위). 브라우저 자동화 도구 연결 | DOM·네트워크 로그·스크린샷 |
| `ENV-API` | 백엔드 API 서버 | `ENV-WEB` 과 같은 기동 인스턴스 | HTTP 요청·응답(상태 코드·헤더·본문) |
| `ENV-CLI` | 보관 배치 | 같은 배포 산출물 안의 명령 진입점 실행([`../functions/spec-functions-api-server.md`](../functions/spec-functions-api-server.md) §명령 진입점) | **표준 출력 요약 JSON + 종료 코드**(표준 오류는 분리 수집) |
| `ENV-LIB` | 연동 라이브러리 | 라이브러리 빌드 산출물 + **C# 콘솔 호출 하네스**(라이브러리 산출물과 함께 만드는 이번 범위의 산출물) | **표준 출력 요약 JSON + 종료 코드** |
| `ENV-DB` | PostgreSQL 16 | ① 스키마 마이그레이션 ② 시드 투입 ③ TC 실행 ④ 검증 쿼리 ⑤ 초기화 | 검증 쿼리 결과 |
| `MOCK-RCV` | 모의 수신처 | HTTP 서버 1개를 띄우고 `<RECEIVER_DELIVERY_URL>` 을 그 주소로 주입 | 수신 요청 로그(본문 바이트·시각·횟수) |
| `MOCK-RESP` | 응답 변조 프록시 | 브라우저와 허브 사이에 프록시를 두고 **진입 초기 상태·접점 응답 본문을 조작**해 화면에 전달한다(서버 구현을 바꾸지 않는다) | 변조 전후 본문·화면 렌더 결과 |

### 데이터베이스 초기화·시드 절차

1. **스키마 생성** — 엔터티 3종([`../datas/data_ENT-001.md`](../datas/data_ENT-001.md)·[`data_ENT-002.md`](../datas/data_ENT-002.md)·[`data_ENT-003.md`](../datas/data_ENT-003.md))과 인덱스 5개를 만든다. 물리 FK 가 없어 **마이그레이션 순서 의존이 없다**.
2. **초기 상태 확인** — 세 테이블이 비어 있고, 그 밖의 테이블(세션·사용자·감사 로그·구성)이 **존재하지 않는지** 확인한다.
3. **시드 투입** — 아래 카탈로그의 SQL 시드를 넣는다. **일시 컬럼은 실행 시각을 기준으로 상수 값을 적용해 산정**한다(고정 날짜를 쓰면 보관 경계 TC 가 시간이 지나며 깨진다).
4. **TC 실행 → 검증 쿼리 → 초기화**. 초기화는 세 테이블 TRUNCATE 로 충분하다.

### 모킹 전략

| 대상 | 모킹 여부 | 근거·방식 |
|---|---|---|
| **수신처(서비스 B)** | **모킹 필수** | 실엔드포인트가 없다. `<RECEIVER_DELIVERY_URL>` 잠정값의 도메인이 **문서용 예약 도메인**이라 실 호스트로 연결되지 않는다. 전달 성공·실패를 검증하려면 모의 수신처가 있어야 한다 |
| 발송처 | 모킹하지 않는다 | 발송처의 역할은 **연동 라이브러리 호출**이며 `ENV-LIB` 하네스가 그 역할을 그대로 수행한다 |
| 사용자 | 모킹하지 않는다 | 브라우저 자동화가 실제 화면을 조작한다 |
| 시각 | 부분 모킹 | 보관 경계·일자 경계 TC 는 **시드 데이터의 일시를 조정**해 만든다. 명령 진입점이 기준 일시 인자를 받지 않으므로 시스템 시각을 바꾸는 방식은 쓰지 않는다 |
| 내부 실패 | fault 주입 | DB 연결 차단·특정 UPDATE/INSERT/UPSERT 실패·암호 기능 초기화 실패. 미구축 시 해당 TC 는 🟠 Block |
| 서버 응답(화면 방어 검증용) | 응답 변조(`MOCK-RESP`) | 화면이 **서버가 낼 수 없는 값**을 받았을 때의 방어를 확인하려면 응답을 조작해야 한다 — 범위 밖 `resultPath`(`USR-05_005`), 절대 URL 미달·다른 스킴의 `returnUrl`(`USR-05_014`). 서버는 그런 값을 만들지 않으므로(기동 검증·`B3` 판정) 실제 흐름으로는 재현되지 않는다. 미구축 시 해당 TC 는 🟠 Block |

**`MOCK-RCV` 응답 모드**(TC 전제에서 지정한다): ① 상태 코드 지정(199·200·299·300·4xx·5xx) ② 순차 응답(1회차 실패 → 2회차 성공) ③ 지연 응답(회당 대기 상한 초과) ④ 연결 거부 ⑤ 리다이렉트(3xx + 다른 호스트 `Location`) ⑥ 본문 변형(빈 값·비 JSON).

- 모의 수신처를 쓴 전달 검증은 실행 시 **🔵 Pass-Mock** 으로 분리 기록한다.

## 시드 데이터 카탈로그

**시드 수단**: `SQL`(대량 레코드) · `UI`(등록 흐름 자체가 TC 인 것) · `API`(외부 시스템 상태) · `파일`(대량 등록 양식·산출물) · `설정`(배포 시점 상수 주입).

### 암호값·전달 데이터 (파일)

| 항목 | 최소 수량 | 저장 위치 | 시드 수단 | 확인 방법 | 비고(검증 대상 TC) |
|---|--:|---|---|---|---|
| `SEED-VEC` 규약 테스트 벡터 | 6 | 배포 패키지 안 `protocol-test-vectors.json` | 파일 | 하네스를 `--vectors` 로 실행해 `{"total":6,"passed":6,"failed":0,...}` · 종료 코드 0 을 확인 | `LIB-04_001`~`_008` · `SCEN_007`·`SCEN_008` |
| `SEED-VEC-01` 대표 정상 항목 | 1 | 위 파일의 첫 항목 | 파일 | 항목의 `expected.requestUrl` 로 진입해 `stage = IDENTITY` 를 확인 | 대부분의 Positive TC |
| `SEED-VEC-B64` Base64URL 치환 문자 출현 항목 | 1 | 위 파일 | 파일 | `expected.encX`·`encY` 에 `-` 또는 `_` 가 포함돼 있고 `+`·`/`·`=` 가 없음을 확인 | `LIB-01_007`·`LIB-02_004` |
| `SEED-VEC-I18N` 다국어 X 항목 | 1 | 위 파일 | 파일 | `input.payload` 에 한글·일본어·이모지·결합 문자가 포함됐는지 확인 | `LIB-01_008`·`SCEN_008` |
| `SEED-KEY-SHORT`·`-EXACT`·`-LONG` 키 원문 경계 | 각 1 | 위 파일 | 파일 | `senderKey + birthDate` 의 UTF-8 바이트 길이가 각각 32 미만 / 정확히 32 / 32 초과인지 계산해 확인 | `LIB-01_002`~`_004`·`SCEN_008` |
| `SEED-X-SIZE` 평문 상한 경계 | 3 | 별도 입력 파일 | 파일 | X 의 UTF-8 직렬화 바이트 수가 상한 −1 / 상한 / 상한 +1 인지 계산해 확인(상한 값은 `SEC-001` §규약 확정값) | `LIB-01_009`·`API-04_005` |
| `SEED-X-NOKEY` `trackingKey` 부재 X | 1 | 별도 입력 파일 | 파일 | X 를 파싱해 `trackingKey` 키가 없음을 확인 | `USR-03_007`·`USR-02_011`·`API-04_004`·`BAT-05_002` |
| `SEED-X-NOTJSON` JSON 아닌 X | 3 | 별도 입력 파일 | 파일 | 평문·잘린 JSON·비 UTF-8 바이트열 3 변형이 각각 표준 JSON 파서로 로드되지 않음을 확인 | `USR-03_008`·`LIB-01_011` |
| `SEED-X-EXACT` 바이트 비교용 X | 1 | 별도 입력 파일 | 파일 | 필드 순서·유니코드 이스케이프·숫자 표현이 특징적인 값이며 다국어를 포함하는지 확인 | `USR-02_002`·`SCEN_007` |
| `SEED-URL-*` 진입 URL 위반 세트 | 8 | 별도 입력 파일 | 파일 | 각 변형이 의도한 위반 1종만 담는지 확인 — `NOENCX`(`encY` 만) · `EMPTYENCY`(빈 값) · `B64BAD`(3 변형) · `BLOCK`(디코드 15·16·17B) · `DUP`(같은 이름 2회) · `LEN`(상한 −1·상한·상한 +1) | `USR-01_004`~`_009`·`USR-03_009`·`SCEN_004` |
| `SEED-URLGEN-LEN` URL 생성 길이 경계 | 3 | 별도 입력 파일 | 파일 | 조립 결과 길이가 상한 −1 / 상한 / 상한 +1 자가 되도록 X 크기를 맞췄는지 실제 생성으로 확인 | `LIB-02_005`·`LIB-02_006` |
| `SEED-PKG` 배포 패키지 | 1벌 | 배포 산출물 | 파일 | 5 구성이 모두 있고 `SHA256SUMS.txt` 대조가 전건 일치하는지 확인 | `LIB-03_001`~`_008`·`SCEN_009` |

### 추적 레코드 (SQL · `tbl_interlock_tracking`)

각 시드는 **대응하는 암호값 쌍**(그 `trackingKey` 를 담은 X 로 만든 값)을 파일 시드로 함께 갖춰야 재진입 흐름을 검증할 수 있다. 예외는 `SEED-TRK-FIXED-DECRYPT` 다 — `DECRYPT_FAILED` 는 추적 레코드에 기록되는 경로가 없어(EXC-BIZ-14) 런타임으로는 만들어질 수 없는 상태를 SQL 로 직접 만든 **값 체계 검증 전용 시드**이며, 재진입·재안내 흐름에 쓰지 않는다.

| 항목 | 최소 수량 | 시드 수단 | 확인 방법 | 비고(검증 대상 TC) |
|---|--:|---|---|---|
| `SEED-TRK-OPEN` 결과 미확정 | 1 | SQL | `result_code IS NULL AND result_at IS NULL` 인 행이 대상 키로 1건 | `USR-03_010`·`API-01_002`·`API-02_004`·`API-03_004`·`BAT-04_002` |
| `SEED-TRK-FIXED-SUCCESS` | 1 | SQL | `result_code = 'SUCCESS' AND result_at IS NOT NULL AND result_confirmed_at IS NULL` | `USR-03_011`·`USR-05_006`·`USR-05_013`·`API-01_001`·`API-01_013`·`API-02_004`·`API-03_001`·`BAT-04_003` |
| `SEED-TRK-FIXED-DECRYPT` | 1 | SQL | `result_code = 'DECRYPT_FAILED'` | `API-01_013`·`API-02_004` (값 체계 검증 전용 — 재진입 흐름 미사용) |
| `SEED-TRK-FIXED-DELIVERY` | 1 | SQL | `result_code = 'DELIVERY_FAILED'` | `USR-02_012`·`USR-03_011`·`USR-04_010`·`USR-05_006`·`USR-05_013`·`API-01_013`·`API-02_004`·`API-03_003` |
| `SEED-TRK-CONFIRMED` 결과 확인 완료 | 1 | SQL | `result_confirmed_at IS NOT NULL AND callback_received_at IS NOT NULL` | `BAT-04_010` |
| `SEED-TRK-CALLBACK` 콜백 수신 | 1 | SQL | `callback_received_at IS NOT NULL` | `API-02_001` |
| `SEED-TRK-LEN` 키 길이 경계 | 2 | SQL | `char_length(tracking_key)` 가 각각 1·255 | `API-01_008` |
| `SEED-TRK-OPAQUE` 불투명 키 | 1 | SQL | 대소문자 혼합·구분 기호·다국어 문자를 포함하고, 조회 응답의 에코와 문자 단위로 같은지 대조 | `API-01_014`·`API-03_011` |
| `SEED-TRK-RETENTION-CONFIRMED` | 2 | SQL | `result_confirmed_at` 이 실행 시각 − `<RETENTION_MONTHS>` 보다 과거인 행 1건 · 이후인 행 1건 | `BAT-02_004` |
| `SEED-TRK-RETENTION-CREATED` | 2 | SQL | `result_confirmed_at IS NULL` 이고 `created_at` 이 실행 시각 − `<RETENTION_MAX_MONTHS>` 보다 과거인 행 1건 · 이후인 행 1건 | `BAT-02_005` |
| `SEED-TRK-RETENTION-MIX` 두 기준 조합 | 4 | SQL | 결과 확인 기산만 경과 / 생성 기산만 경과 / 둘 다 경과 / 둘 다 미경과 각 1건 | `BAT-02_006` |
| `SEED-TRK-BOUNDARY` 경계 3종 | 3 | SQL | 각 기준 일시의 1초 전 / 정확히 / 1초 후에 해당하는 행 | `BAT-02_007` |
| `SEED-TRK-RETENTION` 삭제 대상 일반 | 3 | SQL | 위 기준 중 하나 이상을 넘긴 행. 배치 실행 후 0건이 되는지로 재확인 | `BAT-02_001`·`_010`·`_014`·`API-01_011` |
| `SEED-PAIR-RETENTION` 레코드+증적 쌍 | 1쌍 | SQL | 같은 `tracking_key` 로 추적 레코드는 삭제 대상, 증적은 보존 기간 안인지 각각 확인 | `BAT-02_008` |

### 동의 증적·지표 집계 (SQL)

| 항목 | 최소 수량 | 저장 위치 | 시드 수단 | 확인 방법 | 비고(검증 대상 TC) |
|---|--:|---|---|---|---|
| `SEED-CSP-OLD` 보존 경과 증적 | 1 | `tbl_consent_proof` | SQL | `consented_at` 이 실행 시각 − `<CONSENT_PROOF_RETENTION_MONTHS>` 보다 과거인 행 1건 | `BAT-02_001`·`_010`·`BAT-05_011` |
| `SEED-CSP-BOUNDARY` 경계 3종 | 3 | `tbl_consent_proof` | SQL | 기준 일시의 1초 전 / 정확히 / 1초 후 | `BAT-02_007` |
| `SEED-MET` 지표 집계 행 | 3일치 | `tbl_interlock_metric_daily` | SQL | 알려진 값으로 채워 두 성공률 계산 결과가 **서로 다른** 값이 나오는지 확인(**결과 구분 3종의 합이 요청 수보다 작은 일자**를 포함해야 정의 차이가 드러난다 — 결과 미확정으로 끝난 요청) | `BAT-06_011` |
| `SEED-MET-OLD` 오래된 일자 행 | 1 | `tbl_interlock_metric_daily` | SQL | 어떤 보관 기간보다도 과거인 `metric_date` 행이 배치 실행 후에도 남는지 확인 | `BAT-02_009`·`BAT-06_013` |

### 연동 구성 상수 (설정)

애플리케이션 설정으로 주입하는 구성 세트다. 값 자체는 상수 단일 출처를 따르고 본 표는 **어떤 상태의 세트가 필요한지**만 적는다.

| 항목 | 최소 수량 | 시드 수단 | 확인 방법 | 비고(검증 대상 TC) |
|---|--:|---|---|---|
| `SEED-CONST-OK` 필수 상수 전량 | 1벌 | 설정 | 기동이 성공하고 사용자 진입 경로·서버 대면 API 4종이 응답하는지 확인 | 대부분의 TC 공통 전제 |
| `SEED-CONST-RETURNURL` 복귀 주소 2벌 | 2벌 | 설정 | `<COMPLETION_REDIRECT_URL>` 에 **서로 다른 절대 URL** 을 주입해 각각 기동하고, 경로 ① 응답의 `returnUrl` 이 주입값과 문자 단위로 같은지 확인(값 자체는 본 문서군에 적지 않는다 — `OPS-001-04`) | `USR-05_011`·`USR-05_017` |
| `SEED-CONST-MISSING1`·`MISSING3` 필수 누락 | 2벌 | 설정 | 기동이 중단되고 표준 출력에 **미충족 상수명이 전량** 나오는지 확인 | `SCEN_011`·`BAT-02_012` |
| `SEED-CONST-BADFORMAT` 형식 위반 | 5벌 | 설정 | 다섯 변형 각각에서 기동이 중단되고 해당 항목명이 출력되는지 확인 | `SCEN_012` |
| `SEED-CONST-NONOTICE` 안내 문구 빈 값 | 1벌 | 설정 | SCR-002 에 안내 영역이 렌더되지 않고 승인이 정상 진행되는지 확인 | `USR-04_013` |
| `SEED-CONST-NOTICE-MAX` 문구 상한 | 1벌 | 설정 | 400자·3단락 문구가 접기 없이 렌더되고 가로 스크롤이 없는지 확인 | `USR-04_013` |
| `SEED-CONST-OPTIONAL` 필수+선택 항목 | 1벌 | 설정 | 필수 1건 + 선택 1건 구성에서 필수만 체크한 승인이 통과하는지 확인 | `USR-04_007`·`BAT-05_008` |
| `SEED-CONST-MULTI` 다항목·비정렬 | 1벌 | 설정 | 코드 오름차순이 아닌 순서로 정의한 3건이 화면·스냅샷에서는 오름차순으로 나오는지 확인 | `BAT-05_003` |
| `SEED-CONST-ITEMCHANGED`·`NOTICECHANGED` | 2벌 | 설정 | 기준 구성과 버전 식별자가 **다른 값**으로 산출되는지 확인 | `BAT-05_005` |
| `SEED-CONST-WS` 정규화 변형 | 4벌 | 설정 | 네 변형의 버전 식별자가 기준 구성과 **모두 같은지** 확인 | `BAT-05_006` |
| 자가진단 경로 2벌 | 2벌 | 설정 | 각 값으로 기동했을 때 그 경로에서만 응답하고 다른 경로는 일반 404 인지 확인 | `SCEN_013`·`API-04_009` |
| 규약 버전 변형 3벌 | 3벌 | 설정 | 동일 / MINOR 상이 / MAJOR 상이 구성에서 호환 판정 결과가 각각 호환·호환·비호환인지 확인 | `LIB-03_006`·`LIB-03_007`·`API-04_014` |

### 점검 절차 시드

| 항목 | 시드 수단 | 확인 방법 | 비고(검증 대상 TC) |
|---|---|---|---|
| `CHK-NOSTORE` 저장소 전수 점검 절차 | 절차 | 데이터베이스 전 테이블·파일 시스템·캐시·세션 저장소·브라우저 저장소를 훑어 **표식 값**(시드에 심은 고유 문자열)이 0건인지 확인하는 절차 | `USR-03_013`·`USR-02_010`·`SCEN_015`·`SCEN_016` |
| 표식 값 시드 | 파일 | 발송처키·생년월일·X 필드값에 검색 가능한 고유 문자열을 심고, 로그·응답·화면·저장소에서 그 문자열을 검색해 0건인지 확인 | `SCEN_016` |
| `CHK-NAVBLOCK` 복귀 이동 차단 관측 절차 | 절차 | 브라우저 자동화로 **복귀 주소로의 네비게이션을 차단**한 뒤, 결과 화면이 그대로 남고 수동 이동 링크가 유지되며 새 EX 코드·알림이 생기지 않는지 확인하는 절차. 이동 성공 여부를 애플리케이션이 판정하지 않으므로 **관측은 브라우저 쪽에서만** 성립한다 | `USR-05_015` |
