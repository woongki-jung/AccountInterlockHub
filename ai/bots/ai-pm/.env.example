# ai-pm Redmine 자격 (선택) — .env 로 복사해 채우면 세션 래퍼가 자식 Redmine MCP 에 주입한다.
# 미지정 시 Redmine MCP 서버가 D:\redmine\.env 의 기본 admin 키로 폴백한다(봇 = admin 동작).
# 전용 봇 계정 키로 감사 분리를 원할 때만 채운다. (.env 는 git 비관리 — .gitignore)
REDMINE_BASE_URL=http://localhost:53333
REDMINE_API_KEY=
