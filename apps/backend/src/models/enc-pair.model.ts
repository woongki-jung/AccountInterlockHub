// MDL-004 암호값 쌍 — 외부 연동(EXT) DTO(model_MDL-004-006.md §MDL-004).
// 매핑 엔터티 없음(비저장, DATA-001-02) — 어떤 저장소에도 기록하지 않는다.

/**
 * 발송처가 만든 encX·encY 쌍. 둘 다 Base64URL 문자열(패딩 `=` 없음)이며 항상 쌍으로 다룬다 —
 * 하나만 있으면 복호화 판정이 성립하지 않아 진입 단계에서 구조 위반(`EX-SEC-001`)으로
 * 종결된다(FN-003).
 *
 * 요청 처리 문맥의 지역 값으로만 다룬다 — 세션·캐시·브라우저 저장소에 넣지 않는다. 이미
 * URL 에 실을 수 있는 형태이므로 추가 인코딩을 하지 않는다(이중 인코딩은 진입 단계 디코드
 * 실패의 대표 원인이다).
 */
export interface EncPair {
  encX: string;
  encY: string;
}
