// MDL-006 본인확인 입력 — 공통(COM) 요청 모델(model_MDL-004-006.md §MDL-006).
// 매핑 엔터티 없음(비저장) — 어떤 컬럼에도 담기지 않는다(DATA-001-02).

/**
 * 사용자가 입력한 생년월일. 인증 자격이 아니라 **복호화의 나머지 키**다(`EXC-AUTH-04`) — 허브는
 * 대조할 원본을 갖고 있지 않고 복호화가 되는지로만 판단한다. 그래서 저장하지 않고 실패해도
 * 잠그지 않는다(`AUTH-002-04`).
 *
 * 화면 입력 상태로만 보유한다 — URL·로그·오류 메시지·응답 본문·화면 어디에도 남기지 않는다
 * (`DATA-001-02`·`DATA-001-04`). 승인 시점의 재복호화에도 같은 값이 필요하지만 요청 사이에
 * 보관하지 않고 사용자에게 다시 받는 경로로 성립한다(화면·기능 도메인 소관).
 */
export interface IdentityCheckInput {
  birthDate: string;
}
