# build.ps1(csc.exe 직접 호출) 산출물 — commit 하지 않는다. 재현은 build.ps1 실행으로 충분하다.
bin/
obj/
*.dll
*.exe
*.pdb

# package.ps1 산출물(LIB-03 배포 패키지 5구성 + SHA256SUMS.txt) — 빌드 산출물이라 commit
# 하지 않는다. 재현은 build.ps1 → generate-vectors.ps1 → package.ps1 실행으로 충분하다.
dist-package/
*.zip
